//! Shared test helpers included via `mod common;` in each integration test.
//!
//! Keep this file minimal — it only re-exports submodules. Each submodule is
//! owned by the phase that introduced it.

pub mod v11_fixtures;
