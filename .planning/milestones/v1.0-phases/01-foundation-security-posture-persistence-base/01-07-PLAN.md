---
phase: 01-foundation-security-posture-persistence-base
plan: 07
type: execute
wave: 5
depends_on: [01, 04, 05, 06]
files_modified:
  - .github/workflows/ci.yml
  - Dockerfile
  - .dockerignore
autonomous: true
requirements: [FOUND-07, FOUND-08, FOUND-09]
must_haves:
  truths:
    - ".github/workflows/ci.yml exists and every `run:` step invokes `just <recipe>` exclusively (no inline `cargo`/`docker`/`rustup`/`sqlx`/`npm`/`npx`)"
    - "ci.yml defines three jobs: `lint`, `test`, `image`"
    - "The `test` job has a 2-cell arch matrix with testcontainers covering both backends in every cell (no decorative `db` dimension)"
    - "The `image` job has `needs: [lint, test]`"
    - "The `image` job pushes to GHCR only on `github.ref == 'refs/heads/main'` AND `github.event_name == 'push'`"
    - "Top-level `permissions:` grants only `contents: read`; `packages: write` is scoped per-job to the `image` job only"
    - "The arm64 test cells install cross-compile targets via `run: just install-targets` (never `run: rustup target add ...` raw)"
    - "Dockerfile has a multi-stage build using `cargo-zigbuild` and produces a musl-static binary in a distroless runtime image"
    - "Dockerfile sets `USER nonroot:nonroot`"
    - "`just image` on a dev machine with buildx 0.12+ succeeds end-to-end"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GHA workflow invoking only `just` recipes in `lint` / `test` (2-cell arch matrix, testcontainers covering both backends) / `image` jobs. Top-level permissions read-only; `packages: write` scoped per-job to `image`."
    - path: "Dockerfile"
      provides: "Multi-stage build with cargo-zigbuild cross-compiling amd64+arm64 to a distroless runtime"
    - path: ".dockerignore"
      provides: "Exclude target/, .git/, .planning/, docs/ from build context"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "justfile"
      via: "every run: step calls `just <recipe>` (including `just install-targets` for arm64 cells)"
      pattern: "just (fmt-check|clippy|openssl-check|nextest|schema-diff|image|image-push|install-targets)"
    - from: "Dockerfile"
      to: "cargo-zigbuild"
      via: "RUN cargo zigbuild --release --target"
      pattern: "cargo zigbuild"
---

<objective>
Create the GitHub Actions CI workflow (`.github/workflows/ci.yml`) and the multi-stage `Dockerfile` that together deliver FOUND-07/08/09: every PR runs `fmt-check`/`clippy`/`openssl-check`/`nextest`/`schema-diff` across a 2-cell `{amd64, arm64}` arch matrix (both SQLite and Postgres exercised in every cell via testcontainers), and a multi-arch Docker image builds on every PR (load-only) and pushes to GHCR on `main`.

Purpose: Wave 5. Depends on Plans 01 (Cargo), 04 (tests exist), 05 (schema parity test exists), and 06 (justfile exists so CI can invoke recipes — including the new `just install-targets` helper that replaces raw `rustup target add`). Critically, EVERY `run:` step in ci.yml must call `just <recipe>` — no inline `cargo`/`docker`/`rustup`/`sqlx`/`npm`/`npx` commands. This is the D-10/FOUND-12 hard enforcement, verified by a grep check in the acceptance criteria.

Output: A working CI pipeline that is the gate on every PR, and a Dockerfile whose `just image` output is a ~15 MB multi-arch image.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-security-posture-persistence-base/01-CONTEXT.md
@.planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md
@.planning/phases/01-foundation-security-posture-persistence-base/01-06-PLAN.md
@justfile
@Cargo.toml
@rust-toolchain.toml
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GHCR | Pushing images to a public registry is a supply-chain trust point |
| Dockerfile → image consumer | Base image choice determines attack surface of every deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering (dev/CI drift) | ci.yml vs justfile | mitigate | CI workflow uses ONLY `just <recipe>` invocations. The Task 1 acceptance criteria run `grep -nE "^\s*run: (cargo\|docker \|rustup \|sqlx \|npm \|npx )" .github/workflows/ci.yml` and require zero matches. Plan 06's `just ci` chain is what `lint` + `test` + `image` job steps execute. The arm64 test cells use `just install-targets` (a new Plan 06 recipe) instead of a raw `rustup target add` step. |
| T-01-13 | Supply-chain | GHCR push permissions | mitigate | **Per-job** permissions scoping: top-level `permissions:` grants only `contents: read`. The `image` job alone adds a job-level `permissions:` block with `packages: write`. PR builds use `--load` (no push); push steps are gated on `github.ref == 'refs/heads/main' && github.event_name == 'push'`. The Task 1 acceptance criteria verify this with two greps: one asserting `packages: write` is NOT present under the top-level `permissions:` block, one asserting it IS present under the `image:` job body. |
| T-01-14 | Elevation | Container runtime user | mitigate | Dockerfile's runtime stage uses `gcr.io/distroless/static-debian12:nonroot` and `USER nonroot:nonroot` (UID 65532). No shell. No package manager. |
| T-01-15 | Tampering | Multi-arch cross-compile | mitigate | `cargo-zigbuild` (no QEMU) cross-compiles from amd64 runner. `cargo tree -i openssl-sys` guard (in lint job via `just openssl-check`) prevents the class of Pitfall 14 breakage where transitive openssl-sys appears and cross-compile silently succeeds with host headers. The Plan 06 `openssl-check` recipe now loops over native + amd64-musl + arm64-musl targets in a single run, so Pitfall 14 coverage is complete with one lint-job invocation. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create .github/workflows/ci.yml with lint / test (2-cell arch matrix) / image jobs — every run: step calls only `just`, `packages: write` scoped to image job only</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md (§10 GitHub Actions ci.yml Draft — source; §14 Pitfall 14 — why `openssl-check` in lint job is sufficient once the recipe loops over targets)
    - justfile (just written in Plan 06 — confirm every recipe invoked from CI exists: fmt-check, clippy, openssl-check, nextest, schema-diff, image, image-push, install-targets)
    - .planning/phases/01-foundation-security-posture-persistence-base/01-CONTEXT.md (D-10, D-17, D-18)
  </read_first>
  <behavior>
    - `lint` job runs once (no matrix) with fmt-check, clippy, openssl-check
    - `test` job runs 2 cells (amd64, arm64) with nextest + schema-diff; both SQLite and Postgres exercised in every cell via testcontainers (no decorative `db` dimension in the matrix)
    - `image` job runs after lint+test pass; on PR builds load-only, on push-to-main pushes to GHCR
    - Every `run:` step's command starts with `just ` (or is a no-op / pure GHA action invocation)
    - The arm64 test cells call `just install-targets` instead of a raw `rustup target add` step
    - Top-level `permissions:` grants only `contents: read`
    - `image` job has its own `permissions:` block with `contents: read` + `packages: write`
    - `extractions/setup-just@v2` installs `just` in every job
    - `concurrency.cancel-in-progress: true` cancels superseded runs on the same ref
  </behavior>
  <action>
Create `.github/workflows/ci.yml` with EXACTLY this body (from 01-RESEARCH.md §10, with three targeted changes: (1) 2-cell matrix instead of 4, (2) `just install-targets` instead of raw `rustup target add`, (3) per-job `packages: write` on the `image` job instead of global):

```yaml
# .github/workflows/ci.yml
# Single source of truth for Phase 1 CI. Every `run:` step invokes `just <recipe>`
# exclusively (D-10 / FOUND-12). No inline `cargo` / `docker` / `rustup` / `sqlx` /
# `npm` / `npx` commands.
name: ci

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Top-level permissions: read-only by default. `packages: write` is scoped
# per-job to the `image` job only (T-01-13).
permissions:
  contents: read

jobs:
  lint:
    name: lint (fmt + clippy + openssl-sys guard)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - uses: extractions/setup-just@v2
      - run: just fmt-check
      - run: just clippy
      # `just openssl-check` depends on `just install-targets` and loops over
      # native + amd64-musl + arm64-musl in a single run. One lint-job invocation
      # covers every target CI ships (Pitfall 14 — 01-RESEARCH.md §14).
      - run: just openssl-check

  # Test matrix: arch dimension only.
  # Both SQLite and Postgres backends are exercised in every cell via
  # testcontainers-modules::postgres (integration tests in tests/schema_parity.rs
  # and tests/db_pool_postgres.rs boot a real Postgres container). A per-cell
  # `db` dimension would be cosmetic — see ci.yml design notes in
  # .planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md §10.
  test:
    name: test ${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
    env:
      SQLX_OFFLINE: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.arch }}
      - uses: extractions/setup-just@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: nextest,cargo-zigbuild
      - name: Install cross-compile targets (arm64 cells only)
        if: matrix.arch == 'arm64'
        run: just install-targets
      - run: just nextest
      - run: just schema-diff

  image:
    name: multi-arch docker image
    runs-on: ubuntu-latest
    needs: [lint, test]
    # Per-job permissions: scoped `packages: write` for GHCR push (T-01-13).
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: extractions/setup-just@v2

      # PR path: build both platforms, load no push
      - name: just image (PR — build only)
        if: github.event_name == 'pull_request'
        run: just image

      # main path: build and push to GHCR
      - name: docker login (main only)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: just image-push (main — latest)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: just image-push "${{ github.repository_owner }}/cronduit:latest"

      - name: just image-push (main — sha)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: just image-push "${{ github.repository_owner }}/cronduit:sha-${GITHUB_SHA:0:7}"
```
  </action>
  <verify>
    <automated>test -s .github/workflows/ci.yml && grep -c "^\s*run: just " .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists and file size > 1000 bytes
    - File contains `strategy:` AND `matrix:` AND `arch: [amd64, arm64]`
    - File does NOT contain `db:   [sqlite, postgres]` or any `matrix.db` reference (the decorative dimension is removed; both backends are exercised via testcontainers in every cell)
    - File contains `needs: [lint, test]`
    - File contains `extractions/setup-just@v2` in at least 3 places
    - File contains `just fmt-check`
    - File contains `just clippy`
    - File contains `just openssl-check`
    - File contains `just nextest`
    - File contains `just schema-diff`
    - File contains `just image`
    - File contains `just image-push`
    - File contains `just install-targets` (arm64 cells use this instead of raw `rustup target add`)
    - File contains `if: github.ref == 'refs/heads/main' && github.event_name == 'push'`
    - **D-10 raw-invocation guard:** `grep -nE "^\s*run: (cargo|docker |rustup |sqlx |npm |npx )" .github/workflows/ci.yml` returns NO matches. The acceptance-criteria shell clause is: `( ! grep -nE "^\s*run: (cargo|docker |rustup |sqlx |npm |npx )" .github/workflows/ci.yml ) || (echo "D-10 violation: raw tool invocation in ci.yml" && false)`
    - `grep -c "run: just " .github/workflows/ci.yml` returns at least 8
    - **T-01-13 permissions scoping:** top-level permissions must be read-only. `grep -A 2 "^permissions:$" .github/workflows/ci.yml | grep -q "packages: write" && echo "VIOLATION: packages: write at top level" && false || true`
    - **T-01-13 per-job permissions:** `image` job must have per-job `packages: write`. `awk '/^  image:/,/^  [a-z]+:$/' .github/workflows/ci.yml | grep -q "packages: write" || (echo "MISSING: image job needs packages: write scoped" && false)`
  </acceptance_criteria>
  <done>CI workflow enforces D-10 (just-only, verified by an extended grep covering cargo/docker/rustup/sqlx/npm/npx), D-17 (lint unmatrixed + 2-cell arch test matrix with testcontainers covering both backends in every cell + image needs both), D-18 (PR build-only, main push), and T-01-13 (per-job `packages: write` on `image` only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Dockerfile (multi-stage, cargo-zigbuild → distroless-static nonroot) and .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - .planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md (§11 Multi-arch Docker + cargo-zigbuild — VERBATIM source for Dockerfile)
    - justfile (confirm `image` recipe invokes `docker buildx build --platform linux/amd64,linux/arm64`)
    - Cargo.toml (confirm package name is `cronduit` so COPY path is correct)
  </read_first>
  <behavior>
    - `docker build .` produces a working image on amd64
    - `docker buildx build --platform linux/amd64,linux/arm64 --load .` produces both arch layers (requires buildx 0.12+)
    - The runtime image contains ONLY the cronduit binary + /etc/cronduit/config.toml (from examples/) + system trust store
    - ENTRYPOINT is `/cronduit`, default CMD is `run --config /etc/cronduit/config.toml`
    - Runtime USER is `nonroot:nonroot` (UID 65532)
    - The /cronduit binary is a musl-static executable (no glibc dependency)
  </behavior>
  <action>
Create `Dockerfile` with EXACTLY this body (adapted from 01-RESEARCH.md §11 — uses `rust:1.94-slim-bookworm` builder, cargo-zigbuild cross-compile, distroless/static runtime):

```dockerfile
# syntax=docker/dockerfile:1.7
#
# Multi-stage Dockerfile for cronduit. Cross-compiles amd64 + arm64 musl-static
# via cargo-zigbuild (no QEMU), packages into gcr.io/distroless/static-debian12:nonroot.

# ---- builder ----
FROM --platform=$BUILDPLATFORM rust:1.94-slim-bookworm AS builder

ARG TARGETPLATFORM
ARG BUILDPLATFORM

RUN apt-get update && apt-get install -y --no-install-recommends \
        ca-certificates curl xz-utils pkg-config \
    && rm -rf /var/lib/apt/lists/*

# Install zig + cargo-zigbuild.
# ziglang release URL pattern: zig-linux-<arch>-<version>.tar.xz
RUN set -eux; \
    ZIG_VERSION=0.13.0; \
    ARCH="$(uname -m)"; \
    curl -sSL "https://github.com/ziglang/zig/releases/download/${ZIG_VERSION}/zig-linux-${ARCH}-${ZIG_VERSION}.tar.xz" \
        | tar -xJ -C /opt; \
    ln -s /opt/zig-linux-${ARCH}-${ZIG_VERSION}/zig /usr/local/bin/zig; \
    cargo install --locked cargo-zigbuild --version ^0.22

RUN rustup target add x86_64-unknown-linux-musl aarch64-unknown-linux-musl

WORKDIR /build
COPY . .

# Translate buildx TARGETPLATFORM → rustc target triple.
RUN set -eux; \
    case "$TARGETPLATFORM" in \
      "linux/amd64") TARGET="x86_64-unknown-linux-musl" ;; \
      "linux/arm64") TARGET="aarch64-unknown-linux-musl" ;; \
      *) echo "unsupported TARGETPLATFORM: $TARGETPLATFORM" >&2; exit 1 ;; \
    esac; \
    echo "$TARGET" > /target.txt; \
    cargo zigbuild --release --target "$TARGET"; \
    cp "target/$TARGET/release/cronduit" /cronduit

# ---- runtime ----
FROM gcr.io/distroless/static-debian12:nonroot

COPY --from=builder /cronduit /cronduit
# Migrations are embedded via `sqlx::migrate!(...)` — no filesystem copy.
COPY --from=builder /build/examples/cronduit.toml /etc/cronduit/config.toml

EXPOSE 8080
USER nonroot:nonroot

ENTRYPOINT ["/cronduit"]
CMD ["run", "--config", "/etc/cronduit/config.toml"]
```

NOTE: Plan 08 creates `examples/cronduit.toml`. This Dockerfile assumes it exists at build time. Plan 07 (this plan) is in Wave 5, Plan 08 is in Wave 2, so `examples/cronduit.toml` WILL exist when this Dockerfile runs. The executor should verify the file exists via `test -f examples/cronduit.toml` before running `docker build`.

Create `.dockerignore` to exclude non-source content from the build context (faster builds, smaller images):

```gitignore
# .dockerignore — exclude non-build content from the Docker build context.

target/
.git/
.github/
.planning/
docs/
design/
bin/
.sqlx/tmp/
*.db
*.db-wal
*.db-shm
.env
.env.*
Dockerfile
.dockerignore
README.md
THREAT_MODEL.md
```
  </action>
  <verify>
    <automated>test -s Dockerfile && test -s .dockerignore && grep -c "cargo zigbuild" Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - File `Dockerfile` exists and file size > 500 bytes
    - File `Dockerfile` contains `FROM --platform=$BUILDPLATFORM rust:1.94-slim-bookworm AS builder`
    - File `Dockerfile` contains `cargo zigbuild --release --target`
    - File `Dockerfile` contains `FROM gcr.io/distroless/static-debian12:nonroot`
    - File `Dockerfile` contains `USER nonroot:nonroot`
    - File `Dockerfile` contains `ENTRYPOINT ["/cronduit"]`
    - File `Dockerfile` contains `CMD ["run", "--config", "/etc/cronduit/config.toml"]`
    - File `Dockerfile` contains both `x86_64-unknown-linux-musl` AND `aarch64-unknown-linux-musl`
    - File `.dockerignore` exists and contains `target/` AND `.git/` AND `.planning/`
  </acceptance_criteria>
  <done>Dockerfile produces a multi-arch musl-static distroless runtime image; `.dockerignore` keeps the build context lean.</done>
</task>

</tasks>

<verification>
On a machine with buildx 0.12+ and docker running:

```bash
# Sanity-check YAML syntax without running:
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"

# Confirm no raw cargo/docker/rustup/sqlx/npm/npx in ci.yml run steps:
grep -nE "^\s*run: (cargo|docker |rustup |sqlx |npm |npx )" .github/workflows/ci.yml && echo FAIL || echo OK

# Confirm permissions scoping:
grep -A 2 "^permissions:$" .github/workflows/ci.yml | grep -q "packages: write" && echo "FAIL: global packages: write" || echo "OK: top-level read-only"
awk '/^  image:/,/^  [a-z]+:$/' .github/workflows/ci.yml | grep -q "packages: write" && echo "OK: image job has packages: write" || echo "FAIL: image job missing packages: write"

# Build the image locally:
just image   # expects buildx 0.12+ with containerd image store
```
</verification>

<success_criteria>
- FOUND-07 fmt/clippy/test enforced on every PR
- FOUND-08 2-cell arch matrix runs every PR; `test` job has a 2-cell arch matrix; both backends are exercised via testcontainers in every cell (so SQLite + Postgres coverage is per-cell, not per-dimension)
- FOUND-09 multi-arch image builds via cargo-zigbuild on every PR (load) + pushes on main (push)
- D-10/FOUND-12 strictly enforced: every CI run step calls only `just` (verified by an extended grep covering cargo/docker/rustup/sqlx/npm/npx — no raw tool invocation passes)
- T-01-06, T-01-13 (per-job `packages: write` scoping on `image` only; top-level read-only), T-01-14, T-01-15 mitigated

### Pitfall coverage trace (supersedes prior entry for Pitfall 14)

| Pitfall | Where covered in Phase 1 |
|---------|--------------------------|
| 14 — transitive `openssl-sys` only under a cross-compile target | `just openssl-check` in Plan 06 now depends on `just install-targets` and loops over native + amd64-musl + arm64-musl in a single run. The lint job invokes `just openssl-check` once and that single invocation covers every target CI ships. No per-target openssl-check step is needed in ci.yml. |
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-posture-persistence-base/01-07-SUMMARY.md` with:
- Full ci.yml body
- Full Dockerfile body
- Output of `grep -c "run: just " .github/workflows/ci.yml`
- Output of the D-10 grep guard (must be empty)
- Output of the T-01-13 permissions-scoping greps (top-level read-only; image job has `packages: write`)
- Any deviations from 01-RESEARCH.md §10/§11
</output>
</content>
</invoke>