---
phase: 01-foundation-security-posture-persistence-base
plan: 08
type: execute
wave: 2
depends_on: [01]
files_modified:
  - README.md
  - THREAT_MODEL.md
  - examples/cronduit.toml
autonomous: true
requirements: [FOUND-10, FOUND-11, CONF-07]
must_haves:
  truths:
    - "README.md exists and the first H2 header is `## Security` (within the first 50 lines of the file)"
    - "README.md references `THREAT_MODEL.md` by name at least once"
    - "THREAT_MODEL.md exists and is non-empty (size > 500 bytes)"
    - "THREAT_MODEL.md contains STRIDE section headings covering Docker socket, loopback default, no-auth-in-v1, reverse-proxy recommendation"
    - "examples/cronduit.toml exists and parses via `cronduit check examples/cronduit.toml`"
    - "examples/cronduit.toml is the file the Phase 6 docker-compose quickstart will mount read-only"
    - "No file under README.md or THREAT_MODEL.md contains ASCII box-drawing characters"
  artifacts:
    - path: "README.md"
      provides: "Project readme with SECURITY as the first H2 section, quickstart pointer, mermaid diagram of boot flow"
      contains: "## Security"
    - path: "THREAT_MODEL.md"
      provides: "Skeleton threat model with STRIDE headings covering Docker socket, loopback default, no-auth-in-v1"
      contains: "Docker socket"
    - path: "examples/cronduit.toml"
      provides: "Canonical example config used by docker-compose, `just dev`, `just check-config`, and the Dockerfile runtime"
      contains: "timezone"
  key_links:
    - from: "README.md"
      to: "THREAT_MODEL.md"
      via: "markdown link"
      pattern: "THREAT_MODEL\\.md"
    - from: "examples/cronduit.toml"
      to: "src/config/mod.rs parser"
      via: "parse_and_validate"
      pattern: "timezone"
---

<objective>
Create the project's SECURITY-forward README, the `THREAT_MODEL.md` skeleton, and the canonical `examples/cronduit.toml` example config. These three artifacts together satisfy FOUND-10 (README leads with SECURITY; THREAT_MODEL.md exists), FOUND-11 (every diagram is a mermaid code block), and CONF-07 (the example config is the file docker-compose will mount read-only in Phase 6).

Purpose: Wave 2. Docs-only; depends only on Plan 01. No code is modified. The README sets the tone for every future contributor, the docker-compose quickstart in Phase 6 depends on `examples/cronduit.toml` existing, and the Dockerfile in Plan 07 copies this file into the image runtime.

Output: A strong README top-of-file SECURITY section, a STRIDE-organized skeleton threat model, and a working example TOML that passes `cronduit check`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-security-posture-persistence-base/01-CONTEXT.md
@.planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md
@.planning/PROJECT.md
@CLAUDE.md
@docs/SPEC.md
@README.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading README to decide whether to self-host | Docs are the security interface for anyone cloning this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure / Elevation | Docker socket mount + no auth in v1 | mitigate (HIGH — docs side) | README first H2 is `## Security` explaining (1) Docker socket is root-equivalent, (2) default bind is 127.0.0.1:8080, (3) web UI unauthenticated in v1 — put behind reverse proxy. `THREAT_MODEL.md` expands on the same three points with STRIDE headings. Complements Plan 04's runtime bind warning (T-01-01 runtime side). |
| T-01-10 | Tampering (docs) | ASCII diagrams | mitigate | CLAUDE.md + user-memory require mermaid-only diagrams. All diagrams in the new files are mermaid. Task 1 and Task 2 acceptance criteria both grep for box-drawing characters. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write README.md with SECURITY as the first H2, mermaid boot-flow diagram, and links to THREAT_MODEL.md</name>
  <files>README.md</files>
  <read_first>
- README.md (current stub — 10 bytes per 01-CONTEXT.md)
- .planning/PROJECT.md (vision + core value)
- CLAUDE.md (project overview, security posture, diagrams-must-be-mermaid)
- docs/SPEC.md (feature list)
- .planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md (for diagram content)
  </read_first>
  <behavior>
- First H1 is `# Cronduit`
- First H2 is `## Security` (within the first 50 lines)
- Section explicitly names: Docker socket = root-equivalent, default bind 127.0.0.1:8080, no-auth in v1, reverse-proxy recommendation, link to THREAT_MODEL.md
- Subsequent sections: `## What It Does`, `## Quickstart (Phase 1)`, `## Architecture`, `## Building from Source`, `## Contributing`, `## License`
- Architecture diagram is a mermaid flowchart showing config -> pool -> migrate -> serve
- Footer: `*Last updated: 2026-04-10*`
- NO ASCII box drawing anywhere in the file
  </behavior>
  <action>
Overwrite `README.md` with the following body. (The README content is a single markdown document; paste it verbatim into README.md.)

Begin README.md content:

    # Cronduit

    > Self-hosted Docker-native cron scheduler with a web UI. One tool that both runs recurrent jobs reliably AND makes their state observable through a browser.

    **Status:** Early — Phase 1 (Foundation, Security and Persistence) in progress. Not yet usable as a scheduler; see the roadmap at `.planning/ROADMAP.md`.

    ---

    ## Security

    **Read this section before running Cronduit.**

    Cronduit is a single-operator tool for homelab environments. It makes three explicit security trade-offs you must understand before deploying:

    1. **Cronduit mounts the Docker socket.** That socket is root-equivalent on the host. Anything that can talk to `/var/run/docker.sock` can spawn containers, read secrets from other containers, and access the host filesystem. Only run Cronduit on a host where you already accept Docker-as-root.
    2. **The web UI ships unauthenticated in v1.** There is no login screen. Cronduit defaults `[server].bind` to `127.0.0.1:8080` for this reason. If you bind it to any non-loopback address, Cronduit emits a loud `WARN` log line at startup and sets `bind_warning: true` in the structured startup event. Put Cronduit behind a reverse proxy (Traefik, Caddy, nginx) with auth if you want to expose it beyond localhost.
    3. **Secrets live in environment variables, not in the config file.** The TOML config uses `${ENV_VAR}` references that are interpolated at parse time. The `SecretString` wrapper from the `secrecy` crate ensures credentials never appear in `Debug` output or in log lines.

    See [THREAT_MODEL.md](./THREAT_MODEL.md) for the full threat model.

    ---

    ## What It Does

    Cronduit is a small Rust binary that:

    - Runs recurrent jobs on a cron schedule (command, inline script, or ephemeral Docker container)
    - Shows every run's status, timing, and logs in a terminal-green web UI (no SPA)
    - Supports every Docker network mode, including `network = "container:<name>"` (the marquee feature — route traffic through a VPN sidecar)
    - Stores everything in SQLite by default, or PostgreSQL if you prefer
    - Ships as a single binary and a multi-arch Docker image (`linux/amd64`, `linux/arm64`)

    Phase 1 delivers the foundation: the config parser, database layer, CI matrix, security posture, and threat model skeleton. The scheduler, executors, and web UI land in Phases 2 through 6. See `.planning/ROADMAP.md` for the full plan.

    ---

    ## Quickstart (Phase 1)

    > Phase 1 does not yet run jobs. This quickstart verifies the binary builds, loads a config, and starts the placeholder web server.

    ```bash
    # Prerequisites: rustc 1.94.1 (pinned via rust-toolchain.toml), just, docker
    git clone https://github.com/SimplicityGuy/cronduit
    cd cronduit

    # Validate the example config
    just check-config examples/cronduit.toml

    # Run it against an in-memory SQLite
    cargo run -- run \
        --config examples/cronduit.toml \
        --database-url sqlite::memory: \
        --log-format text
    ```

    Visit http://127.0.0.1:8080 — you will see the Phase 1 placeholder page. Ctrl+C shuts down cleanly.

    Phases 2+ will add the scheduler loop, the real UI, and Docker-container job execution.

    ---

    ## Architecture

    Phase 1 boot flow:

    ```mermaid
    flowchart TD
        A[cronduit run] --> B[Parse CLI flags clap subcommands]
        B --> C[Load TOML config src/config/mod.rs]
        C --> D[Strict VAR interpolation secrecy SecretString]
        D --> E{Validation collect-all}
        E -->|errors| F[Print GCC-style exit 1]
        E -->|ok| G[DbPool connect SQLite split pools OR Postgres]
        G --> H[migrate idempotent]
        H --> I[Emit cronduit.startup JSON event plus bind warning]
        I --> J[axum serve with graceful shutdown]
        J --> K[SIGTERM then pool.close then exit 0]

        classDef gate fill:#0a3d0a,stroke:#00ff7f,color:#e0ffe0
        classDef fail fill:#3d0a0a,stroke:#ff7f7f,color:#ffe0e0
        class I gate
        class F fail
    ```

    More detail: `.planning/research/ARCHITECTURE.md`.

    ---

    ## Building from Source

    Every build/test/lint/image command goes through `just`:

    ```bash
    just --list              # show every recipe
    just build               # cargo build --all-targets
    just test                # cargo test --all-features
    just fmt-check           # fmt gate
    just clippy              # clippy gate
    just openssl-check       # rustls-only dependency guard (FOUND-06)
    just schema-diff         # SQLite vs Postgres schema parity test
    just image               # multi-arch docker image via cargo-zigbuild (no QEMU)
    just ci                  # full ordered chain
    ```

    Local `just ci` produces the same exit code as the GitHub Actions CI job on every PR.

    CI matrix: `linux/amd64 x linux/arm64 x SQLite x PostgreSQL`. See `.github/workflows/ci.yml`.

    ---

    ## Contributing

    1. Create a feature branch (`gsd/...` or `feat/...`)
    2. Make changes and run `just ci` locally
    3. Open a PR — direct commits to `main` are blocked by policy
    4. All diagrams in PR descriptions, commits, and docs must be mermaid code blocks (no ASCII art)

    See `CLAUDE.md` for the full project constraints.

    ---

    ## License

    MIT OR Apache-2.0. See `LICENSE` (forthcoming).

    ---

    *Last updated: 2026-04-10*

End README.md content.

NOTE: The mermaid diagram above intentionally avoids parentheses, brackets, and `->` arrows inside node labels (mermaid-unsafe characters). Keep the node-label text plain ASCII. Do NOT include any of these box-drawing characters anywhere in the file: the horizontal-line character, vertical-line character, or the four corner characters.
  </action>
  <verify>
    <automated>head -50 README.md | grep -q '^## Security' && grep -q 'THREAT_MODEL.md' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
- File `README.md` exists and file size is greater than 2000 bytes
- `head -50 README.md` output contains the literal string `## Security`
- File `README.md` contains the literal string `THREAT_MODEL.md`
- File `README.md` contains `\`\`\`mermaid` (at least one mermaid fenced block)
- File `README.md` contains `127.0.0.1:8080`
- File `README.md` contains `just check-config examples/cronduit.toml`
- File `README.md` first H1 is `# Cronduit`
- Running `grep -l` with the unicode box-drawing character set on README.md returns no matches (zero box-drawing characters in the file)
  </acceptance_criteria>
  <done>README leads with Security, references the threat model, uses a mermaid boot diagram, and points at `just` and `examples/cronduit.toml`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create THREAT_MODEL.md skeleton with STRIDE sections covering Docker socket, loopback default, and no-auth-in-v1</name>
  <files>THREAT_MODEL.md</files>
  <read_first>
- .planning/phases/01-foundation-security-posture-persistence-base/01-CONTEXT.md (D-24 and the full security posture discussion)
- .planning/research/PITFALLS.md (§1 Docker socket root-equivalent)
- CLAUDE.md (security posture subsection)
- .planning/phases/01-foundation-security-posture-persistence-base/01-RESEARCH.md (§13 Pitfall Guard Mapping)
  </read_first>
  <behavior>
- File is a STRIDE-organized skeleton with sections for S, T, R, I, D, E
- Covers the three headline Phase 1 threats explicitly: Docker socket, non-loopback bind, secrets in logs
- Labels deferred content with `TBD — Phase <N>` so Phase 6 knows what to fill in
- Footer notes: "This is a skeleton. Phase 6 release engineering fleshes it out once Phase 4 and Phase 5 expose the full attack surface."
- No ASCII box drawing
  </behavior>
  <action>
Create `THREAT_MODEL.md` with the following body. Paste verbatim into the file.

Begin THREAT_MODEL.md content:

    # Cronduit Threat Model

    > Skeleton. Phase 1 ships the Docker-socket, loopback-default, and no-auth-in-v1 analysis. Phase 6 release engineering expands this into the full STRIDE model once the Docker executor (Phase 4) and config reload loop (Phase 5) are in place.

    **Revision:** 2026-04-10 (Phase 1 skeleton)
    **Scope:** Single-node, single-operator self-hosted deployments.

    ---

    ## Assets and Trust Boundaries

    ```mermaid
    flowchart LR
        O[Operator Admin] -->|HTTP no auth in v1| W[Web UI 8080]
        O -->|edits| C[cronduit.toml config file]
        C -->|read| R[cronduit process]
        R -->|queries| D[(SQLite or Postgres)]
        R -->|Unix socket root-equivalent| S[docker.sock]
        S --> E[Ephemeral job containers]

        classDef trusted fill:#0a3d0a,stroke:#00ff7f,color:#e0ffe0
        classDef critical fill:#3d0a0a,stroke:#ff7f7f,color:#ffe0e0
        class R trusted
        class S critical
    ```

    **Trust boundaries:**

    1. **Operator to process** — CLI flags, config file edits, SIGHUP signals.
    2. **Config file to process** — TOML file on disk, mounted read-only in the Docker deployment.
    3. **Process to database** — `sqlx::Pool` connection.
    4. **Process to Docker socket** — Unix socket; the most security-sensitive boundary in the system.
    5. **Process to operator browser** — Unauthenticated HTTP in v1.

    **In scope (Phase 1):** config tampering, secret leakage through logs, non-loopback bind exposure, dependency supply-chain (rustls-only).

    **In scope (Phase 4+, deferred):** malicious Docker image execution, container escape, `container:<name>` target impersonation, log XSS, CSRF on state-changing endpoints.

    **Out of scope:** multi-tenancy, RBAC, multi-node coordination. Cronduit is a single-operator tool.

    ---

    ## Spoofing (S)

    **T-S1:** An attacker on the same LAN spoofs the operator by browsing the Cronduit web UI when it is bound to a non-loopback address.

    - **Mitigation (v1):** Default bind is `127.0.0.1:8080`. Any non-loopback bind triggers a loud WARN at startup and sets `bind_warning: true` in the structured `cronduit.startup` event. Operators are expected to put Cronduit behind a reverse proxy with their own auth layer.
    - **Status:** Mitigation in place (Plan 04). Full web-UI auth is explicitly deferred to v2 (AUTH-01 / AUTH-02).

    **T-S2:** `@random` cron field resolution could be influenced by a malicious clock source. *TBD — Phase 5 when the resolver ships.*

    ---

    ## Tampering (T)

    **T-T1:** An attacker with shell access to the host modifies `cronduit.toml` to insert malicious jobs.

    - **Mitigation (partial):** The Docker deployment mounts the config read-only (CONF-07). The host filesystem itself is the trust boundary — Cronduit cannot defend against an attacker who already has write access to its config file.
    - **Status:** Documented. Operators must secure the config file with standard Unix permissions.

    **T-T2:** A malicious dependency introduces a transitive `openssl-sys` dep, silently breaking the rustls-only invariant and exposing a new CVE surface.

    - **Mitigation:** `just openssl-check` runs `cargo tree -i openssl-sys` on every CI build (Plan 06 / FOUND-06). Any match fails the lint job.
    - **Status:** Mitigation in place.

    **T-T3:** Schema drift between SQLite and Postgres migrations leads to runtime-only failures on one backend.

    - **Mitigation:** `tests/schema_parity.rs` (Plan 05 / D-14) runs on every PR, structurally compares both backends, fails on drift.
    - **Status:** Mitigation in place.

    ---

    ## Repudiation (R)

    **T-R1:** An operator denies running a specific job.

    - **Mitigation (deferred):** `job_runs.trigger = 'manual' or 'scheduled'` column exists in the schema (Plan 04). Authenticated audit logging is deferred to v2.
    - **Status:** Schema support only; no auth layer to tie runs to individuals.

    ---

    ## Information Disclosure (I)

    **T-I1:** Database credentials leak into logs via the `cronduit.startup` event.

    - **Mitigation:** `src/db/mod.rs::strip_db_credentials` strips `username:password` from the URL before logging. Unit-tested.
    - **Status:** Mitigation in place.

    **T-I2:** Secret values from `${ENV_VAR}` interpolation appear in panics, Debug output, or error messages.

    - **Mitigation:** Every secret-bearing config field is wrapped in `secrecy::SecretString` (Plan 02 / FOUND-05 / D-20). `Debug` renders `[REDACTED]`. `Serialize` is intentionally not implemented so accidental `serde_json::to_string(&config)` will not compile. `cronduit check` tests explicitly assert secret values do not appear on stdout/stderr.
    - **Status:** Mitigation in place.

    **T-I3:** A malicious container (Phase 4) reads secrets from `/proc/self/environ` of the Cronduit process.

    - **Mitigation (deferred):** Job containers run in ephemeral namespaces with no shared PID space by default. `--security-opt no-new-privileges` and `--cap-drop=ALL` to be documented in Phase 4.
    - **Status:** TBD — Phase 4.

    ---

    ## Denial of Service (D)

    **T-D1:** SQLite writer contention under concurrent log writes collapses throughput.

    - **Mitigation:** Split read/write pools (writer `max_connections=1`, reader `max_connections=8`) plus WAL plus `busy_timeout=5000` plus `synchronous=NORMAL` (Plan 04 / DB-05 / Pitfall 7). Unit-tested via `tests/db_pool_sqlite.rs`.
    - **Status:** Mitigation in place.

    **T-D2:** A runaway job fills `job_logs` until the SQLite file consumes all free disk.

    - **Mitigation (deferred):** Bounded-channel log pipeline with tail-drop (Phase 2 / EXEC-04). Daily retention pruner (Phase 6 / DB-08).
    - **Status:** TBD — Phase 2 and Phase 6.

    **T-D3:** A graceful-shutdown bug leaves `cronduit` hung indefinitely on SIGTERM.

    - **Mitigation:** `src/shutdown.rs` installs SIGINT and SIGTERM handlers that cancel the `CancellationToken`; axum listener wired with `with_graceful_shutdown`; DB pool `.close()` called before exit. `tests/graceful_shutdown.rs` asserts SIGTERM then exit 0 within 1 s.
    - **Status:** Mitigation in place.

    ---

    ## Elevation of Privilege (E)

    **T-E1:** An attacker with access to the Cronduit web UI spawns a container as root on the host via the mounted Docker socket.

    - **Threat:** The Docker socket is root-equivalent. Any code path that reaches `/var/run/docker.sock` can pwn the host.
    - **Mitigation (v1):** Cronduit defaults to loopback bind. The web UI is unauthenticated in v1 — operators keep it behind a reverse proxy OR on the local network only. This is documented as the headline security trade-off in `README.md`.
    - **Status:** Documented plus runtime warning plus reverse-proxy recommendation. Full web-UI auth is v2 (AUTH-01).

    **T-E2:** A malicious container image uses a Docker escape CVE to break out and attack the host. *TBD — Phase 4 / Phase 6 once the executor is in place.*

    **T-E3:** A `container:<name>` job targets an unexpected container and receives network traffic meant for another workload. *TBD — Phase 4 when the pre-flight check ships (DOCKER-03).*

    ---

    ## Out-of-Band Trust Assumptions

    - Operators are expected to secure the host running Cronduit with standard Unix hygiene (file permissions, firewall, reverse proxy).
    - Operators are expected to only pull Docker images they trust (`image = "..."` is NOT sandboxed against malicious images).
    - Operators are expected to not expose Cronduit's web UI to hostile networks without a reverse-proxy plus auth layer.

    ---

    ## Changelog

    | Revision | Date | Change |
    |----------|------|--------|
    | Phase 1 skeleton | 2026-04-10 | Initial STRIDE outline with Phase 1 mitigations. Phases 4 through 6 threats marked TBD. |

    *Phase 6 release engineering will expand this document into the full threat model.*

End THREAT_MODEL.md content.
  </action>
  <verify>
    <automated>test -s THREAT_MODEL.md && grep -q "Docker socket" THREAT_MODEL.md && grep -q "Spoofing" THREAT_MODEL.md && grep -q "Tampering" THREAT_MODEL.md && grep -q "Elevation" THREAT_MODEL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
- File `THREAT_MODEL.md` exists and file size is greater than 3000 bytes
- File contains the literal string `Docker socket`
- File contains section headers for all six STRIDE categories: `Spoofing`, `Tampering`, `Repudiation`, `Information Disclosure`, `Denial of Service`, `Elevation of Privilege`
- File contains `bind_warning`
- File contains `SecretString`
- File contains `TBD — Phase`
- File contains at least one mermaid fenced block `\`\`\`mermaid`
- File does NOT contain any unicode box-drawing characters
  </acceptance_criteria>
  <done>THREAT_MODEL.md skeleton captures the three Phase 1 headline threats under STRIDE headings and clearly flags deferred Phase 4 through 6 items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create examples/cronduit.toml that parses via cronduit check and is valid for docker-compose + Dockerfile runtime</name>
  <files>examples/cronduit.toml</files>
  <read_first>
- docs/SPEC.md (example TOML at the bottom — lift structure, clean up)
- src/config/mod.rs (confirm field names required by parse_and_validate)
- Dockerfile (Plan 07 — it COPYs this file to /etc/cronduit/config.toml at build time; must be parseable)
- justfile (Plan 06 — `just dev` and `just check-config` both reference this path)
  </read_first>
  <behavior>
- `cargo run -- check examples/cronduit.toml` exits 0
- File has a valid `[server]` block with `timezone = "UTC"` (mandatory per D-19)
- File has a `[defaults]` block and at least two `[[jobs]]` entries demonstrating command, script, and image job shapes
- Secret-bearing `env` values use the `${ENV_VAR}` placeholder convention (with a comment explaining how to set them)
- File has a documentation header explaining: it is the canonical example, edited copies live at `/etc/cronduit/config.toml` in the Docker image, and it should never contain plaintext secrets
- No ASCII box drawing
  </behavior>
  <action>
Create `examples/cronduit.toml` with the following content. Paste verbatim into the file.

Begin examples/cronduit.toml content:

    # examples/cronduit.toml — canonical example config.
    #
    # This file is copied into the Docker image at /etc/cronduit/config.toml
    # by the multi-stage Dockerfile (Plan 07). `just dev` and `just check-config`
    # also reference this exact path.
    #
    # SECURITY:
    #   * Never commit plaintext secrets. Use ${ENV_VAR} references and set
    #     the variables at runtime (docker-compose `environment:` block, or
    #     a .env file mounted into the container).
    #   * Mount this file read-only (`:ro`) in docker-compose.
    #   * Default bind is loopback. Change ONLY if you put Cronduit behind
    #     a reverse proxy with auth.

    [server]
    bind = "127.0.0.1:8080"
    database_url = "sqlite:///data/cronduit.db"
    timezone = "UTC"
    shutdown_grace = "30s"
    log_retention = "90d"

    [defaults]
    image = "alpine:latest"
    network = "bridge"
    delete = true
    timeout = "5m"
    random_min_gap = "0s"

    # ------------------------------------------------------------
    # Example jobs (Phase 1 parses these; Phase 2 actually runs them)
    # ------------------------------------------------------------

    # A local command job. Runs every 15 minutes.
    [[jobs]]
    name = "health-probe"
    schedule = "*/15 * * * *"
    command = "curl -sf https://example.com/health"
    timeout = "30s"

    # An inline script job. Runs hourly.
    [[jobs]]
    name = "backup-index"
    schedule = "0 * * * *"
    script = """
    #!/bin/sh
    set -eu
    echo "building backup index at $(date -u +%FT%TZ)"
    find /data -type f -mtime -1 | wc -l
    """
    timeout = "2m"

    # An ephemeral Docker container job. Runs daily at 03:15 in the server tz.
    [[jobs]]
    name = "nightly-backup"
    schedule = "15 3 * * *"
    image = "restic/restic:latest"
    network = "container:vpn"
    volumes = [
        "/data:/data:ro",
        "/backup:/backup",
    ]
    timeout = "30m"

    [jobs.env]
    # Set RESTIC_PASSWORD in the operator environment; cronduit interpolates
    # it at parse time and wraps the value in a SecretString. If the variable
    # is unset, `cronduit check` fails with a clear error.
    RESTIC_PASSWORD = "${RESTIC_PASSWORD}"

End examples/cronduit.toml content.

After creating the file, run `cargo run --quiet -- check examples/cronduit.toml` to verify it parses. Because the last job's env references `${RESTIC_PASSWORD}`, the caller must set that var before running the check — wrap the invocation as:

    RESTIC_PASSWORD=placeholder cargo run --quiet -- check examples/cronduit.toml

The verify command below does exactly that.
  </action>
  <verify>
    <automated>RESTIC_PASSWORD=placeholder cargo run --quiet -- check examples/cronduit.toml</automated>
  </verify>
  <acceptance_criteria>
- File `examples/cronduit.toml` exists
- File contains `timezone = "UTC"`
- File contains `[defaults]`
- File contains at least three `[[jobs]]` entries
- File contains at least one `command = `, one `script = `, and one `image = ` entry
- File contains `network = "container:vpn"` to exercise the container network mode
- File contains at least one `${...}` env interpolation placeholder
- File has a comment header mentioning SECURITY and `${ENV_VAR}`
- `RESTIC_PASSWORD=placeholder cargo run --quiet -- check examples/cronduit.toml` exits 0
- File does NOT contain any unicode box-drawing characters
  </acceptance_criteria>
  <done>Example config parses end-to-end and can be mounted read-only by the Phase 6 docker-compose quickstart.</done>
</task>

</tasks>

<verification>

```bash
head -50 README.md | grep -q '^## Security' && echo OK
test -s THREAT_MODEL.md
RESTIC_PASSWORD=placeholder cargo run --quiet -- check examples/cronduit.toml
```

All three exit 0.
</verification>

<success_criteria>
- FOUND-10: README leads with Security, THREAT_MODEL.md exists and is non-empty
- FOUND-11: All diagrams in README.md and THREAT_MODEL.md are mermaid code blocks (zero ASCII box drawing)
- CONF-07 groundwork: examples/cronduit.toml is the canonical file docker-compose will mount read-only in Phase 6
- T-01-01 mitigated on the docs side
- T-01-10 mitigated via mermaid-only enforcement in acceptance criteria
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-posture-persistence-base/01-08-SUMMARY.md` with:
- README.md size and first H2
- THREAT_MODEL.md size and STRIDE section count
- examples/cronduit.toml parse output
- Any deviations from this plan
</output>
