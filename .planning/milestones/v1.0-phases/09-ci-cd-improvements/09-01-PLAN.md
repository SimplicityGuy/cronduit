---
phase: 09-ci-cd-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/cleanup-cache.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - ".github/workflows/cleanup-cache.yml exists and is valid YAML"
    - "Workflow triggers ONLY on pull_request.closed (not push, not schedule)"
    - "Workflow has least-privilege permissions: actions: write"
    - "Workflow has explicit timeout-minutes set on every job"
    - "Workflow is concurrency-grouped per PR number with cancel-in-progress: true"
    - "Workflow deletes caches whose ref matches refs/pull/<PR_NUM>/merge via gh cache list + gh cache delete"
    - "Workflow uses `set +e` around the delete loop so a missing cache does not fail the run"
  artifacts:
    - path: ".github/workflows/cleanup-cache.yml"
      provides: "PR cache cleanup on close"
      contains: "pull_request:"
  key_links:
    - from: "pull_request: closed event"
      to: "gh cache delete"
      via: "gh cache list --ref refs/pull/$PR/merge --json id --jq .[].id loop"
      pattern: "gh cache delete"
---

<objective>
Port the discogsography `cleanup-cache.yml` workflow into Cronduit verbatim (with project-name comment updates). When a PR is closed (merged or not), delete every GitHub Actions cache keyed to that PR's merge ref so the repo does not accumulate stale caches and burn out the 10 GB GHA quota.

Purpose: CONTEXT.md Plan 1. Source of inspiration is discogsography's cleanup-cache.yml — essentially correct as-is, no matrix needed (Cronduit is a single-crate repo).

Output: One new workflow file. No changes to existing workflows, no changes to runtime code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ci-cd-improvements/09-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/cleanup-cache.yml (verbatim port)</name>
  <files>.github/workflows/cleanup-cache.yml</files>
  <read_first>
    - .planning/phases/09-ci-cd-improvements/09-CONTEXT.md (Plan 1 section — LOCKED decisions)
    - .planning/ROADMAP.md (Phase 9 Success Criterion 1)
    - CLAUDE.md (no direct commits to main, mermaid-only, single binary promise)
    - .github/workflows/ci.yml (study existing workflow style: top-level permissions, concurrency groups, no emojis in step names)
    - Discogsography reference: fetch with `gh api repos/SimplicityGuy/discogsography/contents/.github/workflows/cleanup-cache.yml --jq .content | base64 -d`
  </read_first>
  <action>
Create `.github/workflows/cleanup-cache.yml` with EXACTLY the following content. Do not add or remove lines. Do not "improve" the `set +e` pattern — it is intentional per CONTEXT.md "Specifics": a cache that is already gone (race with another delete or expiry) must not fail the whole workflow.

Note on emojis: Cronduit CLAUDE.md says no emojis by default. Discogsography's reference uses `🧹 Cleanup Cache` as the step name. For THIS file, use the plain name `Cleanup Cache` (no emoji) because the CONTEXT.md emoji exception is scoped to `scripts/update-project.sh` only.

```yaml
# .github/workflows/cleanup-cache.yml
# Delete GitHub Actions caches tied to a PR's merge ref when that PR is closed
# (merged or not). Prevents the 10 GB GHA cache quota from filling with stale
# multi-arch Rust build caches on a long-lived OSS project.
#
# Source of inspiration:
#   https://github.com/SimplicityGuy/discogsography/blob/main/.github/workflows/cleanup-cache.yml
#
# The `set +e` around the delete loop is intentional: if a cache has already
# been evicted (expiry) or deleted by another run, we want the workflow to
# continue deleting the rest rather than aborting.

name: Cleanup Cache

on:
  pull_request:
    types:
      - closed

concurrency:
  group: cleanup-cache-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  cleanup:
    name: delete PR caches
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: write
    steps:
      - name: Cleanup Cache
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          BRANCH: refs/pull/${{ github.event.pull_request.number }}/merge
        run: |
          echo "Fetching list of cache keys for $BRANCH"
          cacheKeysForPR=$(gh cache list --ref "$BRANCH" --limit 100 --json id --jq ".[].id")

          # set +e on purpose: a cache already-evicted by expiry or a
          # concurrent delete must not fail the run.
          set +e
          echo "Deleting caches..."
          for cacheKey in $cacheKeysForPR
          do
              gh cache delete "$cacheKey"
          done
          echo "Done"
```

Key structural elements you MUST preserve from discogsography:
1. `on.pull_request.types: [closed]` — NOT `on.pull_request.types: [opened, closed]` and NOT just `on.pull_request:` (which fires on too many events).
2. `concurrency.group` scoped by `github.event.pull_request.number` so multiple closes of the same PR do not race.
3. `concurrency.cancel-in-progress: true`.
4. Top-level `permissions: { contents: read }` with job-level elevation to `actions: write` ONLY on the cleanup job.
5. `timeout-minutes: 10` on the job.
6. `set +e` before the delete loop with a comment explaining why.
7. `GH_TOKEN`, `GH_REPO`, `BRANCH` passed via `env:` on the step (not inline `${{ }}` in the `run:` body — this matches discogsography and also makes the script safer against shell injection if branch names ever get weird).
8. Use `refs/pull/${{ github.event.pull_request.number }}/merge` — NOT `refs/pull/.../head`. The merge ref is what `actions/checkout@v4` uses by default on PRs, so that is what gets cached.

After writing the file, do NOT run any gh CLI commands (nothing to test locally — GitHub runs this workflow, we only commit it). Verification happens post-merge per the Phase 9 plan.
  </action>
  <verify>
    <automated>test -f .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'types:' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q '- closed' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'actions: write' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'timeout-minutes: 10' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'cancel-in-progress: true' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'refs/pull/' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'set +e' .github/workflows/cleanup-cache.yml &amp;&amp; grep -q 'gh cache delete' .github/workflows/cleanup-cache.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/cleanup-cache.yml` exits 0
    - `grep -c 'types:' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c '- closed' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'actions: write' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'timeout-minutes: 10' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'cancel-in-progress: true' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'refs/pull/' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'set +e' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -c 'gh cache delete' .github/workflows/cleanup-cache.yml` returns 1
    - `grep -Ec '(on push:|schedule:)' .github/workflows/cleanup-cache.yml` returns 0 (no extra triggers)
    - File contains NO emoji characters (`LC_ALL=C grep -P '[\x80-\xff]' .github/workflows/cleanup-cache.yml` returns empty)
    - File is valid YAML: `python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/cleanup-cache.yml"))' 2>&amp;1` exits 0
  </acceptance_criteria>
  <done>
    Workflow file exists, passes YAML parse, meets every grep-verifiable criterion above, and contains the verbatim `set +e` pattern and concurrency group scoped per PR. No edits to any other file.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (human, post-merge — out of executor scope):
1. Open a draft PR against main → close it → `gh run list --workflow=cleanup-cache.yml` shows the run.
2. The run's log contains "Fetching list of cache keys for refs/pull/<N>/merge".
3. The run exits 0 even if zero caches are found (tests the `set +e` path).

This phase-level check lives in 09-04 or is executed manually after phase completion.
</verification>

<success_criteria>
- `.github/workflows/cleanup-cache.yml` exists with every must_have truth satisfied
- No other files modified by this plan
- File is valid YAML, contains no emojis, pins no third-party actions (only `gh` CLI calls — no `uses:` in the cleanup job)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ci-cd-improvements/09-01-SUMMARY.md` summarizing:
- File created (path, line count)
- All acceptance criteria results (pass/fail)
- Any deviations from CONTEXT.md Plan 1 (expected: none)
</output>
