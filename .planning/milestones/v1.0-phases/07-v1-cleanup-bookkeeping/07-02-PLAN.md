---
phase: 07-v1-cleanup-bookkeeping
plan: 02
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OPS-04, CONF-07, RAND-03]
tags: [bookkeeping, traceability, audit]

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table uses a 4-column header: `Requirement | Phase | Status | Evidence`"
    - "Every row flipped from Pending to Complete has a non-empty Evidence column citing at least one per-phase 0X-VERIFICATION.md file that contains the REQ-ID in a SATISFIED row (strict cross-check per D-03)"
    - "OPS-04 flipped to Complete citing both `06-VERIFICATION.md` (override) and `examples/docker-compose.yml` (strengthened comment) per D-06"
    - "OPS-05 stays Pending with Evidence citing Phase 8 human UAT deferral per D-07"
    - "Every flipped row in the status table ALSO has its matching bulleted checkbox in the v1 Requirements list (lines 12-126) flipped from `- [ ]` to `- [x]`"
    - "Coverage summary block (lines 267-282) is rewritten in past tense reflecting the post-flip counts, with a `**Completed (2026-04-12, Phase 7):**` note"
    - "PARTIAL/FAILED/NEEDS HUMAN entries that did not land a post-verification fix stay Pending with a footnote (D-05)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with Evidence column + flipped checkboxes + updated coverage summary"
      contains: "| Requirement | Phase | Status | Evidence |"
  key_links:
    - from: "REQUIREMENTS.md traceability table OPS-04 row"
      to: ".planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md (overrides block)"
      via: "Evidence column citation"
      pattern: "06-VERIFICATION.md.*override"
    - from: "REQUIREMENTS.md traceability table RAND-03 row"
      to: ".planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md (re_verification block)"
      via: "Evidence column citation"
      pattern: "05-VERIFICATION.md.*re_verification"
---

<objective>
Execute the strict D-03 cross-check loop against every Pending row in `REQUIREMENTS.md` (lines 178-265), flip the ones that have matching `SATISFIED` entries in per-phase `0X-VERIFICATION.md` files to `Complete`, add the Evidence column documenting the per-phase verification file path(s), and rewrite the Coverage summary block to reflect post-flip reality.

**The only real requirement closure in this plan is OPS-04** — every other flip is bookkeeping that moves the master table into sync with per-phase verification files that already exist. The flips must be **honest**: no flipping a row whose matching verification entry is PARTIAL or FAILED just to clean up the accounting.

**Why Wave 3 (depends on Plans 01 AND 03):**
- **Plan 01 must land first** so that D-06 can flip OPS-04 citing `06-VERIFICATION.md` (overrides block) + `examples/docker-compose.yml` (strengthened comment) as real, check-able evidence. Without Plan 01, neither file contains the citation target.
- **Plan 03 must land first** so that D-03's cross-check can honestly flip RAND-03 citing the `re_verification:` block in `05-VERIFICATION.md`. Without Plan 03, `05-VERIFICATION.md` still reports RAND-03 as PARTIAL, and D-03 says PARTIAL stays Pending. RESEARCH.md § D-03 "Known edge cases" explicitly flags this ordering.
- Plan 04 is NOT a dependency of this plan — it only creates a test file that is referenced from Plan 03's re_verification annotation, which in turn Plan 02 cites.

**Output:** One edited file (`REQUIREMENTS.md`) with ~80+ row flips, a new Evidence column, updated bulleted checkboxes, and a rewritten Coverage summary.

**Scope enumeration (for transparency, not for frontmatter):** This plan touches the master table rows for requirements in every category: FOUND-01..11, CONF-01..10, DB-01..07 (DB-08 already Complete), SCHED-01..08, EXEC-01..06, UI-01..13 + UI-15 (UI-14 already Complete), DOCKER-01..10, RAND-01..06, RELOAD-01..07, OPS-01, OPS-03, OPS-04, OPS-05. The `requirements:` frontmatter field lists only the three REQ-IDs whose status transition is a **judgment call** (OPS-04 after D-06 dependency, CONF-07 after researching the dual-file evidence per RESEARCH.md D-03, RAND-03 after Plan 03's re_verification annotation). The other ~80 flips are mechanical and documented in the plan body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-v1-cleanup-bookkeeping/07-CONTEXT.md
@.planning/phases/07-v1-cleanup-bookkeeping/07-RESEARCH.md

<!-- Already-landed upstream files (from Wave 1/Wave 2 plans this plan depends on): -->
@.planning/phases/01-foundation-security-posture-persistence-base/01-VERIFICATION.md
@.planning/phases/02-scheduler-core-command-script-executor/02-VERIFICATION.md
@.planning/phases/03-read-only-web-ui-health-endpoint/03-VERIFICATION.md
@.planning/phases/04-docker-executor-container-network-differentiator/04-VERIFICATION.md
@.planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md
@.planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md
@examples/docker-compose.yml

<!-- Current master table state verified in RESEARCH.md (2026-04-12): -->

- 86 total v1 requirements
- 83 currently Pending (`grep -c '| Pending |' REQUIREMENTS.md` → 83)
- 3 currently Complete: `UI-14`, `DB-08`, `OPS-02` (all from Phase 6)
- Traceability table: lines 178-265 (header line 178, separator line 179, data lines 180-265)
- Bulleted v1 requirements list with `- [ ]` checkboxes: lines 12-126
- Coverage summary: lines 267-282

**Per-phase status-column conventions (verified by reading all 6 files):**

| Phase file | Status strings used |
|------------|--------------------|
| `01-VERIFICATION.md` | `✓ SATISFIED`, `✓ SATISFIED (parse only)`, `✓ SATISFIED (struct)`, `✓ PARTIAL (groundwork)` |
| `02-VERIFICATION.md` | `SATISFIED` |
| `03-VERIFICATION.md` | `SATISFIED` |
| `04-VERIFICATION.md` | `SATISFIED` |
| `05-VERIFICATION.md` | `SATISFIED`, `PARTIAL`, `FAILED` (RAND-03 row is `PARTIAL`; fixed by Plan 03's re_verification annotation) |
| `06-VERIFICATION.md` | `SATISFIED`, `PARTIALLY SATISFIED`, `NEEDS HUMAN` |

**Grep recipe for the D-03 strict cross-check (handles both `✓ SATISFIED` and bare `SATISFIED`):**

```bash
# For a given REQ-ID + phase number, confirm the per-phase verification file has a
# SATISFIED row. Returns matching line(s), or empty if not satisfied.
check_req() {
  local req="$1" phase="$2"
  grep -E "^\| ${req} .*(SATISFIED|✓ SATISFIED)" .planning/phases/${phase}-*/${phase}-VERIFICATION.md \
    | grep -vE "(PARTIAL|PARTIALLY SATISFIED|NEEDS HUMAN|FAILED)"
}
# Example: check_req FOUND-01 01
```

**Known edge cases flagged by RESEARCH.md D-03:**

| REQ-ID | Issue | Decision for Plan 02 |
|--------|-------|---------------------|
| `RAND-03` | Original 05-VERIFICATION.md row is `PARTIAL` (do_reroll stub, pre-PR-#9). Plan 03 adds a `re_verification:` block documenting that PR #9 closed the gap. | **FLIP to Complete.** Evidence cites `05-VERIFICATION.md` with note `(re_verification block, gap_resolution 1)`. Only valid because Plan 03 is a hard dependency of this plan. |
| `CONF-07` | Phase 1 row is `✓ SATISFIED (groundwork)` — documents the SecretString + config_path plumbing but not the actual read-only mount in the example compose file. Phase 6 is where the docker-compose.yml got its `:ro` annotation. | **FLIP to Complete.** Evidence cites BOTH `01-VERIFICATION.md` (groundwork) and `examples/docker-compose.yml` (the actual `:ro` bind on the config volume, visible via `grep ':ro' examples/docker-compose.yml`). Dual-file evidence. |
| `OPS-04` | Phase 6 row is `PARTIALLY SATISFIED`. Plan 01 adds the overrides block + strengthened comment. | **FLIP to Complete per D-06.** Evidence: `06-VERIFICATION.md (override); examples/docker-compose.yml`. |
| `OPS-05` | Phase 6 row is `NEEDS HUMAN`. | **STAYS Pending per D-07.** Evidence: `(deferred to Phase 8 human UAT)`. |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Evidence column + flip every qualifying row in the traceability table (D-03, D-04, D-06, D-07)</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1-130 (the bulleted v1 requirements list — every row flipped in the traceability table needs its matching checkbox flipped too)
    - `.planning/REQUIREMENTS.md` lines 178-282 (traceability table + coverage summary — the tables being edited)
    - `.planning/phases/01-foundation-security-posture-persistence-base/01-VERIFICATION.md` lines 110-200 (Requirements Coverage table — uses `✓ SATISFIED` prefix)
    - `.planning/phases/02-scheduler-core-command-script-executor/02-VERIFICATION.md` lines 80-150 (Requirements Coverage table)
    - `.planning/phases/03-read-only-web-ui-health-endpoint/03-VERIFICATION.md` lines 110-200 (Requirements Coverage table)
    - `.planning/phases/04-docker-executor-container-network-differentiator/04-VERIFICATION.md` lines 80-150 (Requirements Coverage table)
    - `.planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md` lines 108-180 (Requirements Coverage table — includes the PARTIAL RAND-03 row, which flips only via Plan 03's re_verification block)
    - `.planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md` lines 1-50 (frontmatter — must contain the `re_verification:` block inserted by Plan 03; if it does NOT, this task fails fast and the executor must escalate to "Plan 03 did not land yet")
    - `.planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md` lines 90-170 (Requirements Coverage table + line 6 `overrides_applied: 1` which Plan 01 must have set; if it is still `0`, this task fails fast and escalates)
    - `examples/docker-compose.yml` (confirm the strengthened SECURITY comment is present — if not, Plan 01 did not land; this task fails fast; also confirm the `:ro` annotation is still on the config volume mount at line 18 for the CONF-07 dual-evidence citation)
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
  Execute the cross-check loop and rewrite the traceability table in-place. This task has 5 sub-steps; perform them in order.

  ### Sub-step 1 — Preflight: verify upstream plans landed

  Before touching `REQUIREMENTS.md`, run these check commands. If any fail, STOP and report "Plan 01/03 dependency not satisfied" — do NOT proceed.

  ```bash
  # Plan 01 landed?
  grep -q 'SECURITY: READ BEFORE DEPLOYING' examples/docker-compose.yml || echo "FAIL: Plan 01 not landed (docker-compose comment)"
  grep -qE '^overrides_applied: 1$' .planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md || echo "FAIL: Plan 01 not landed (06-VERIFICATION overrides)"

  # Plan 03 landed?
  grep -qE '^re_verification:$' .planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md || echo "FAIL: Plan 03 not landed (05-VERIFICATION re_verification block)"
  grep -q 'status: "code_complete, human_needed"' .planning/phases/05-config-reload-random-resolver/05-VERIFICATION.md || echo "FAIL: Plan 03 not landed (05-VERIFICATION status)"

  # CONF-07 dual-evidence still valid?
  grep -q ':ro' examples/docker-compose.yml || echo "FAIL: CONF-07 evidence missing (:ro bind on config volume)"
  ```

  If any of these print `FAIL:`, abort and escalate.

  ### Sub-step 2 — Run the D-03 cross-check loop and build a flip-list

  For each Pending REQ-ID (83 total), run the grep recipe below and decide its fate. Work through the categories in order: FOUND, CONF, DB, SCHED, RAND, EXEC, DOCKER, RELOAD, UI, OPS.

  ```bash
  # Helper: call for each REQ-ID + phase-number pair
  check_req() {
    local req="$1" phase="$2"
    grep -nE "^\| ${req} .*(SATISFIED|✓ SATISFIED)" .planning/phases/${phase}-*/${phase}-VERIFICATION.md \
      | grep -vE "(PARTIAL|PARTIALLY SATISFIED|NEEDS HUMAN|FAILED)"
  }
  ```

  For each REQ-ID, classify into one of:

  - **FLIP_MECHANICAL**: `check_req` returns a line containing `SATISFIED` and no PARTIAL/NEEDS HUMAN/FAILED markers. Flip to Complete. Evidence: the phase's short filename (e.g., `01-VERIFICATION.md`).
  - **FLIP_DUAL_EVIDENCE** (CONF-07 only): `check_req` returns `✓ SATISFIED (groundwork)` in `01-VERIFICATION.md`. Flip to Complete **per the RESEARCH.md recommendation**. Evidence: `01-VERIFICATION.md (groundwork); examples/docker-compose.yml (:ro bind)`.
  - **FLIP_VIA_REVERIFICATION** (RAND-03 only): `check_req` returns `PARTIAL` in `05-VERIFICATION.md`, but Plan 03's `re_verification:` block closes the gap. Flip to Complete **per the RESEARCH.md recommendation**. Evidence: `` `05-VERIFICATION.md` (re_verification block, gap_resolution 1) ``.
  - **FLIP_WITH_OVERRIDE** (OPS-04 only, per D-06): Phase 6 row is `PARTIALLY SATISFIED`. Plan 01 added an overrides block in `06-VERIFICATION.md`. Flip to Complete. Evidence: `` `06-VERIFICATION.md` (override); `examples/docker-compose.yml` ``.
  - **KEEP_PENDING_PER_D07** (OPS-05 only): Phase 6 row is `NEEDS HUMAN`. Stays Pending. Evidence: `(deferred to Phase 8 human UAT)`.
  - **KEEP_PENDING_PARTIAL** (any other REQ-ID whose phase row is PARTIAL/FAILED/NEEDS HUMAN without a closing mechanism): Stays Pending with a footnote. Evidence: `` `0X-VERIFICATION.md` (partial)[^partial-REQ-ID] ``, where the footnote points to the per-phase file and notes the condition.

  **Expected distribution after the cross-check (projection from RESEARCH.md):**
  - ~80-83 rows flip (FOUND, CONF except possibly CONF-07, DB-01..07, SCHED-01..08, EXEC, DOCKER, RELOAD, UI-01..13 + UI-15, OPS-01, OPS-03, OPS-04 via D-06, RAND via Plan 03)
  - 1 row stays Pending with known reason (OPS-05 per D-07)
  - Possibly 0-2 additional rows stay Pending if the cross-check surfaces unexpected PARTIAL/FAILED entries

  **Record the classification for every REQ-ID in a scratch note** (in the commit message or the summary file, not in the plan). If the classification count does not add up to 86, STOP and escalate.

  ### Sub-step 3 — Rewrite the traceability table with the 4-column Evidence shape

  Replace lines 178-265 of `REQUIREMENTS.md` with a 4-column table. New header:

  ```
  | Requirement | Phase | Status | Evidence |
  |-------------|-------|--------|----------|
  ```

  Each data row follows the shape:

  ```
  | <REQ-ID> | <Phase> | <Status> | <Evidence> |
  ```

  **Exact row shapes (copy verbatim, fill in the REQ-ID and Evidence column per the classification):**

  Mechanical flip:
  ```
  | FOUND-01 | Phase 1 | Complete | `01-VERIFICATION.md` |
  ```

  Dual-evidence flip (CONF-07):
  ```
  | CONF-07 | Phase 1 | Complete | `01-VERIFICATION.md` (groundwork); `examples/docker-compose.yml` (:ro bind) |
  ```

  Re-verification flip (RAND-03):
  ```
  | RAND-03 | Phase 5 | Complete | `05-VERIFICATION.md` (re_verification block, gap_resolution 1) |
  ```

  Override flip (OPS-04 — preserve the existing multi-word Phase column per CONTEXT.md established patterns line 139):
  ```
  | OPS-04 | Phase 6 → Phase 7 (gap closure) | Complete | `06-VERIFICATION.md` (override); `examples/docker-compose.yml` |
  ```

  Stays Pending per D-07 (OPS-05):
  ```
  | OPS-05 | Phase 6 → Phase 8 (gap closure) | Pending | (deferred to Phase 8 human UAT) |
  ```

  Already-Complete rows (UI-14, DB-08, OPS-02 — just add Evidence column):
  ```
  | UI-14 | Phase 6 | Complete | `06-VERIFICATION.md` |
  | DB-08 | Phase 6 | Complete | `06-VERIFICATION.md` |
  | OPS-02 | Phase 6 | Complete | `06-VERIFICATION.md` |
  ```

  **Preserve the original row order** (FOUND → CONF → DB → SCHED → RAND → EXEC → DOCKER → RELOAD → UI → OPS), matching the order in lines 180-265 of the current file.

  ### Sub-step 4 — Flip the matching bulleted checkboxes in lines 12-126

  For every row flipped to Complete in the table, find the matching `- [ ] **REQ-ID**:` line in the v1 Requirements bulleted list (lines 12-126) and change `- [ ]` to `- [x]`.

  The bullets follow the same category order (FOUND → CONF → DB → SCHED → RAND → EXEC → DOCKER → RELOAD → UI → OPS). UI-14, DB-08, and OPS-02 are already `- [x]` (line 48, line 117, line 123) — verify they stay `[x]`, no flip needed.

  OPS-05 stays `- [ ]` per D-07.

  OPS-04 flips from `- [ ]` to `- [x]` per D-06.

  Any REQ-ID kept Pending per KEEP_PENDING_PARTIAL stays `- [ ]`.

  **After this step, the count of `- [x]` in lines 12-126 must equal the count of `Complete` rows in the traceability table.**

  ### Sub-step 5 — Rewrite the Coverage summary block (lines 267-282, per D-08)

  Replace the current Coverage summary with a past-tense version reflecting the actual flip counts. Use this template, filling in the actual numbers from sub-step 2's classification tally:

  ```markdown
  **Coverage:**
  - v1 requirements: 86 total
  - Mapped to phases: 86
  - Unmapped: 0 ✓
  - **Completed (2026-04-12, Phase 7 bookkeeping flip):** <N> marked Complete with Evidence columns citing per-phase `0X-VERIFICATION.md` files (strict cross-check per Phase 7 D-03 against `SATISFIED` rows in those files). OPS-04 closed via override recorded in `06-VERIFICATION.md` (see Phase 7 Plan 01). RAND-03 closed via `re_verification:` annotation in `05-VERIFICATION.md` documenting PR #9 closure (see Phase 7 Plan 03). CONF-07 closed with dual evidence (Phase 1 groundwork + `examples/docker-compose.yml` `:ro` bind).
  - **Pending (<M> remaining):** OPS-05 (5-minute stranger quickstart) — deferred to Phase 8 human UAT<extend-here-if-any-other-REQ-IDs-stayed-Pending>.

  **Distribution by phase:**
  - Phase 1 (Foundation, Security Posture & Persistence Base): 29 requirements — <X1> Complete, <Y1> Pending
  - Phase 2 (Scheduler Core & Command/Script Executor): 13 requirements — <X2> Complete, <Y2> Pending
  - Phase 3 (Read-Only Web UI & Health Endpoint): 15 requirements — <X3> Complete, <Y3> Pending
  - Phase 4 (Docker Executor & container-network Differentiator): 11 requirements — <X4> Complete, <Y4> Pending
  - Phase 5 (Config Reload & `@random` Resolver): 13 requirements — <X5> Complete, <Y5> Pending
  - Phase 6 (Live Events, Metrics, Retention & Release Engineering): 5 requirements — <X6> Complete, <Y6> Pending
  - Phase 7 (v1.0 Cleanup & Bookkeeping): closed OPS-04 override + executed the bulk bookkeeping flip documented in this table
  - Phase 8 (v1.0 Final Human UAT Validation): closes OPS-05 + Phase 3 / Phase 6 human-needed visual items
  ```

  Fill in every `<...>` placeholder with the concrete count. The sum of all per-phase Complete + Pending must equal 86.

  Also update the trailing metadata line (after `---` around line 284) to note the 2026-04-12 Phase 7 flip:

  ```
  *Last updated: 2026-04-12 — Phase 7 bookkeeping flip: Evidence column added, <N> of 86 v1 requirements flipped to Complete with strict per-phase cross-check; OPS-05 deferred to Phase 8.*
  ```

  ### General editing rules

  - **Do not delete rows or reorder them.** The 4-column table must contain the same 86 REQ-IDs in the same order.
  - **Do not invent new Status values.** Only `Complete` and `Pending` are valid. Do not introduce `In Progress`, `Partial`, etc.
  - **Do not skip the bulleted checkboxes.** For each flipped row, the matching `- [ ]` bullet becomes `- [x]`. Mismatches between the table and the bullets are a consistency bug.
  - **Do not touch the v2 Requirements section** (lines below 128 up to line 170 or so — v2 is out of scope).
  - **Do not modify the per-phase `0X-VERIFICATION.md` files.** This plan only edits `REQUIREMENTS.md`.
  - **Evidence paths are relative filenames** (e.g., `` `01-VERIFICATION.md` ``) in backticks, not absolute paths, not full `.planning/phases/...` paths. Shorter is better for readability; the basenames are unique so the lookup is unambiguous.
  - If an unexpected PARTIAL/FAILED is discovered during the cross-check, keep the row Pending with a short footnote and document the decision in the summary file. Do NOT silently flip or silently drop.
  </action>

  <verify>
    <automated>bash -c '
set -e
REQ=.planning/REQUIREMENTS.md

# 1. Header is 4-column
grep -qE "^\| Requirement \| Phase \| Status \| Evidence \|$" $REQ || { echo "FAIL: header not 4-column"; exit 1; }

# 2. Every Complete row has a non-empty Evidence column
awk -F"|" "/\| Complete \|/ { if (length(\$5) < 3) { print \"empty evidence:\", \$0; exit 1 } }" $REQ

# 3. Count Complete vs Pending in the table
COMPLETE=$(grep -cE "^\| [A-Z]+-[0-9]+ .*\| Complete \|" $REQ)
PENDING=$(grep -cE "^\| [A-Z]+-[0-9]+ .*\| Pending" $REQ)
TOTAL=$((COMPLETE + PENDING))
[[ $TOTAL -eq 86 ]] || { echo "FAIL: total rows = $TOTAL, expected 86"; exit 1; }
[[ $COMPLETE -ge 80 ]] || { echo "FAIL: expected >= 80 Complete, got $COMPLETE"; exit 1; }

# 4. OPS-04 is Complete with dual evidence (06-VERIFICATION override + docker-compose.yml)
grep -qE "^\| OPS-04 .*\| Complete \|.*06-VERIFICATION.md.*examples/docker-compose.yml" $REQ \
  || { echo "FAIL: OPS-04 not flipped with dual evidence"; exit 1; }

# 5. OPS-05 stays Pending
grep -qE "^\| OPS-05 .*\| Pending" $REQ || { echo "FAIL: OPS-05 not Pending"; exit 1; }

# 6. RAND-03 flipped citing re_verification
grep -qE "^\| RAND-03 .*\| Complete \|.*05-VERIFICATION.md.*re_verification" $REQ \
  || { echo "FAIL: RAND-03 not flipped with re_verification evidence"; exit 1; }

# 7. CONF-07 flipped with dual evidence
grep -qE "^\| CONF-07 .*\| Complete \|.*01-VERIFICATION.md.*examples/docker-compose.yml" $REQ \
  || { echo "FAIL: CONF-07 not flipped with dual evidence"; exit 1; }

# 8. Bulleted checkbox count matches table Complete count
CHECKED=$(grep -cE "^- \[x\] \*\*[A-Z]+-[0-9]+\*\*" $REQ)
[[ $CHECKED -eq $COMPLETE ]] || { echo "FAIL: checked bullets ($CHECKED) != Complete rows ($COMPLETE)"; exit 1; }

# 9. OPS-05 bullet still unchecked
grep -qE "^- \[ \] \*\*OPS-05\*\*" $REQ || { echo "FAIL: OPS-05 bullet flipped"; exit 1; }

# 10. Strict cross-check: every flipped REQ-ID appears as SATISFIED in its phase file
for req in $(grep -oE "^\| [A-Z]+-[0-9]+ " $REQ | awk "{print \$2}" | sort -u); do
  status=$(grep -E "^\| $req " $REQ | awk -F"|" "{print \$4}" | tr -d " ")
  [[ "$status" == "Complete" ]] || continue
  # The OPS-04 row has special Phase "Phase 6 → Phase 7 (gap closure)", CONF-07 / RAND-03 have multiline evidence.
  # For all flipped rows, at least one per-phase verification file must contain the REQ-ID as SATISFIED or (groundwork) or re_verification.
  found=0
  for vf in .planning/phases/*/0?-VERIFICATION.md; do
    if grep -qE "(\| $req .*SATISFIED|re_verification.*$req|$req.*re_verification|(groundwork).*$req|$req.*(groundwork))" "$vf"; then
      found=1; break
    fi
  done
  [[ $found -eq 1 ]] || { echo "FAIL: $req marked Complete but no SATISFIED/re_verification/groundwork evidence found"; exit 1; }
done

echo "OK: $COMPLETE Complete, $PENDING Pending, total $TOTAL"
'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^\| Requirement \| Phase \| Status \| Evidence \|$' .planning/REQUIREMENTS.md` returns `1` (new 4-column header).
    - `grep -cE '^\| [A-Z]+-[0-9]+ .*\| Complete \|' .planning/REQUIREMENTS.md` returns `80` or more (expected post-flip: 80-84 Complete rows).
    - `grep -cE '^\| [A-Z]+-[0-9]+ .*\| Pending' .planning/REQUIREMENTS.md` returns `1` to `6` (OPS-05 at minimum; possibly CONF-07 if executor chose not to flip it, plus any unexpected PARTIAL surfaces).
    - `grep -cE '^\| [A-Z]+-[0-9]+' .planning/REQUIREMENTS.md` returns `86` (total row count unchanged).
    - `grep -E '^\| OPS-04' .planning/REQUIREMENTS.md` shows `Phase 6 → Phase 7 (gap closure)` in the Phase column, `Complete` in Status, and both `06-VERIFICATION.md` and `examples/docker-compose.yml` in Evidence.
    - `grep -E '^\| OPS-05' .planning/REQUIREMENTS.md` shows `Phase 6 → Phase 8 (gap closure)` in Phase, `Pending` in Status, and `Phase 8 human UAT` (or similar) in Evidence.
    - `grep -E '^\| RAND-03' .planning/REQUIREMENTS.md` shows `Complete` status and `05-VERIFICATION.md` + `re_verification` in Evidence.
    - `grep -E '^\| CONF-07' .planning/REQUIREMENTS.md` shows `Complete` status and both `01-VERIFICATION.md` and `examples/docker-compose.yml` in Evidence (dual-evidence citation).
    - `grep -cE '^- \[x\] \*\*[A-Z]+-[0-9]+\*\*' .planning/REQUIREMENTS.md` equals the Complete-row count from the table (bullet/table consistency).
    - `grep -cE '^- \[ \] \*\*OPS-05\*\*' .planning/REQUIREMENTS.md` returns `1` (OPS-05 bullet still unchecked).
    - `grep -cE '^- \[x\] \*\*OPS-04\*\*' .planning/REQUIREMENTS.md` returns `1` (OPS-04 bullet flipped).
    - `grep -c 'Completed (2026-04-12, Phase 7 bookkeeping flip)' .planning/REQUIREMENTS.md` returns `1` (updated Coverage prose).
    - `grep -cE '81 requirements are documented as SATISFIED' .planning/REQUIREMENTS.md` returns `0` (old "81 unflipped" prose removed).
    - **Strict cross-check (the automated verify command includes this loop):** every row with Status `Complete` must have its REQ-ID appear in at least one `.planning/phases/*/0?-VERIFICATION.md` file as either `SATISFIED`, `(groundwork)`, or inside a `re_verification` block.
    - The full automated verify command exits 0.
  </acceptance_criteria>

  <done>
  `REQUIREMENTS.md` traceability table is 4-column (`Requirement | Phase | Status | Evidence`), 80+ rows are flipped from Pending to Complete with honest cross-checked Evidence citations, OPS-04 is flipped with dual-file evidence per D-06, OPS-05 is still Pending per D-07, RAND-03 + CONF-07 are flipped with the special dual/re_verification evidence format, every flipped row has its matching bulleted checkbox flipped, the Coverage summary is past-tense with accurate post-flip counts, and the strict per-REQ cross-check passes against all 6 per-phase verification files.
  </done>
</task>

</tasks>

<sequencing>
**Wave 3** — this plan is the only member of its wave and depends on both Plan 01 (Wave 1) and Plan 03 (Wave 2).

- **Depends on Plan 01** because D-06 (OPS-04 flip) cites `06-VERIFICATION.md` (override block) and `examples/docker-compose.yml` (strengthened comment) as Evidence. Those citations are lies until Plan 01 lands.
- **Depends on Plan 03** because D-03's strict cross-check says `PARTIAL` stays Pending. The only way to honestly flip RAND-03 (currently `PARTIAL` in the original 05-VERIFICATION.md) is to cite Plan 03's `re_verification:` block as the closing mechanism. If Plan 03 has not landed, RAND-03 stays Pending and the projected flip count drops by 1.
- This plan does NOT depend on Plan 04 (the test file creation). Plan 04's output is referenced transitively by Plan 03's re_verification annotation, but Plan 02 only needs the annotation to exist, not the test.

Why not split this plan into 4 sub-plans (one per category)? Because every flip shares the same preflight check, the same cross-check harness, and the same Evidence column rewrite. Splitting would duplicate the preflight and waste context budget on 4 separate plans each doing the same thing for a different third of the table. A single plan also avoids a partial-state bug where half the table is 4-column and half is 3-column.
</sequencing>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor → REQUIREMENTS.md traceability table | Future auditors (including v1.1 planning) trust the flipped-Complete status. A lazy flip (one that skipped the cross-check) would pollute the v1 archive. |
| Plan 02 executor → per-phase verification files | The cross-check is a one-time read against 6 files. A stale or in-flight edit in those files would produce wrong flips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | T (Tampering) | REQUIREMENTS.md Complete rows | mitigate | The automated verify command includes a loop that, for every Complete row, greps all 6 per-phase verification files for a matching SATISFIED / (groundwork) / re_verification entry. A row flipped without real backing evidence fails the verify. |
| T-07-10 | R (Repudiation) | "Who flipped what" audit trail | mitigate | The Coverage summary block rewrites the 2026-04-12 note with a `**Completed (2026-04-12, Phase 7 bookkeeping flip):**` header naming Phase 7 as the executor. Combined with the git commit in the PR, the audit trail is traceable. |
| T-07-11 | I (Information Disclosure) | Evidence column cites internal file paths | accept | The paths (`01-VERIFICATION.md`, etc.) are already in the repo. No new secrets exposed. |
</threat_model>

<verification>
**Plan-level checks (run after the task passes):**

1. The automated verify command in Task 1 exits 0 (strict cross-check + shape checks).
2. `git diff --name-only .planning/REQUIREMENTS.md` is the only file in the diff (no accidental edits to per-phase files).
3. `git diff .planning/REQUIREMENTS.md` shows edits across three regions: the bulleted v1 list (`- [ ]` → `- [x]` flips), the traceability table (4-column rewrite), and the Coverage summary rewrite. No edits outside those regions.
4. Spot-check: pick 5 random flipped REQ-IDs from the Complete rows, manually grep each one in its cited Evidence file, confirm it appears in a SATISFIED row. This is the "reviewer manual verification" item in `07-VALIDATION.md` row 7-02-01.
5. `python3 -c "import re; lines = open('.planning/REQUIREMENTS.md').read().splitlines(); complete = [l for l in lines if re.match(r'^\| [A-Z]+-[0-9]+ .*\| Complete \|', l)]; checked = [l for l in lines if re.match(r'^- \[x\] \*\*[A-Z]+-[0-9]+\*\*', l)]; assert len(complete) == len(checked), f'{len(complete)} complete rows vs {len(checked)} checked bullets'; print(f'{len(complete)} Complete / {86 - len(complete)} Pending — consistency OK')"` exits 0.
</verification>

<success_criteria>
- [ ] Preflight checks confirmed Plans 01 and 03 landed before this plan started.
- [ ] `REQUIREMENTS.md` traceability table is 4-column with `Evidence` as the 4th column.
- [ ] 80+ rows flipped from Pending to Complete (the exact count recorded in the summary file).
- [ ] OPS-04 flipped with dual-file evidence per D-06.
- [ ] OPS-05 still Pending per D-07.
- [ ] RAND-03 flipped citing Plan 03's re_verification block.
- [ ] CONF-07 flipped with dual-file evidence (01-VERIFICATION groundwork + docker-compose.yml `:ro`).
- [ ] Every flipped row has its matching bulleted checkbox flipped (`- [ ]` → `- [x]`).
- [ ] Coverage summary block rewritten in past tense with accurate post-flip counts.
- [ ] Strict cross-check (automated verify loop) passes: every Complete row has real backing evidence in the cited file.
- [ ] No changes to any file other than `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-v1-cleanup-bookkeeping/07-02-SUMMARY.md` documenting:
- The exact flip count (N Complete / M Pending) post-flip
- The classification table from sub-step 2: for each REQ-ID, which category (FLIP_MECHANICAL / FLIP_DUAL_EVIDENCE / FLIP_VIA_REVERIFICATION / FLIP_WITH_OVERRIDE / KEEP_PENDING_PER_D07 / KEEP_PENDING_PARTIAL)
- Any unexpected PARTIAL/FAILED surfaces discovered during the cross-check that kept a row Pending
- Confirmation that the bulleted checkbox count matches the Complete-row count
- A 5-row spot-check transcript: pick 5 random flipped REQ-IDs, show the `check_req` grep output for each, confirming the SATISFIED backing row exists
</output>
