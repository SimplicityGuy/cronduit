---
phase: 12
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - tests/Dockerfile.ops08-old
  - tests/compose-override.yml
  - .github/workflows/compose-smoke.yml
autonomous: true
requirements: [OPS-07, OPS-08]
must_haves:
  truths:
    - "On every PR + push to main + tag push, GHA runs a `compose-smoke` workflow that exercises both OPS-08 reproduction (OLD wget HEALTHCHECK → unhealthy) AND the fix (NEW cronduit health HEALTHCHECK → healthy)."
    - "The workflow asserts `examples/docker-compose.yml` reports `healthy` within 90 s of `docker compose up -d` on the shipped quickstart compose file (T-V11-HEALTH-01)."
    - "The workflow asserts an operator's compose `healthcheck:` stanza in `tests/compose-override.yml` overrides the Dockerfile HEALTHCHECK (T-V11-HEALTH-02)."
    - "The workflow runs alongside `ci.yml` (NOT inside it — D-09)."
    - "If the OPS-08 OLD-state image returns `healthy` (root cause was different), the workflow logs the divergence and still passes (12-04-05); the `cronduit health` fix path is correct regardless because it removes busybox wget from the healthcheck path."
  artifacts:
    - path: "tests/Dockerfile.ops08-old"
      provides: "OLD-state fixture: cronduit:ci with the busted busybox wget HEALTHCHECK baked in"
      contains: "wget"
    - path: "tests/compose-override.yml"
      provides: "Compose-override fixture: distinguishable healthcheck: stanza different from Dockerfile defaults"
      contains: "healthcheck:"
    - path: ".github/workflows/compose-smoke.yml"
      provides: "GHA workflow with three assertions: OPS-08 before/after, compose-override smoke, shipped-compose healthy-by-default smoke"
      contains: "name: compose-smoke"
  key_links:
    - from: ".github/workflows/compose-smoke.yml"
      to: "Dockerfile + tests/Dockerfile.ops08-old + tests/compose-override.yml"
      via: "docker build / docker run / docker compose up + docker inspect"
      pattern: "docker inspect --format"
    - from: ".github/workflows/compose-smoke.yml"
      to: "/cronduit health binary"
      via: "exec inside running container via Dockerfile HEALTHCHECK"
      pattern: "cronduit:ci"
---

<objective>
Stand up a dedicated `compose-smoke` GitHub Actions workflow plus the two test fixtures it consumes. The workflow runs three independent assertions: (1) OPS-08 before/after reproduction, (2) compose-override smoke (T-V11-HEALTH-02), (3) shipped-compose healthy-by-default smoke (T-V11-HEALTH-01).

Purpose: Closes OPS-07 + OPS-08. Provides the automated proof that the Phase 12 healthcheck fix works AND reproduces the original `(unhealthy)` symptom before the fix is declared complete (per Success Criterion #4 in ROADMAP.md and per D-08).

Output:
- `tests/Dockerfile.ops08-old` — NEW fixture; layers the OLD `wget --spider` HEALTHCHECK on top of the production image (D-08).
- `tests/compose-override.yml` — NEW fixture; declares a service with a deliberately distinguishable `healthcheck:` stanza (D-09).
- `.github/workflows/compose-smoke.yml` — NEW workflow with one job that builds `cronduit:ci`, runs the three assertions, dumps diagnostics on failure, and tears down cleanly.
- All three workflow assertions pass on the feature-branch PR (CI green).
- Workflow does NOT extend `ci.yml` — distinct runner requirements (docker daemon + buildx + compose CLI) belong in their own workflow (D-09).

This plan **depends on Plan 02** (the binary must have a working `health` subcommand for the NEW-state assertion to flip to healthy) and **Plan 03** (the production Dockerfile must have the HEALTHCHECK directive baked in for `cronduit:ci` to inherit it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-CONTEXT.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-RESEARCH.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-VALIDATION.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-02-SUMMARY.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-03-SUMMARY.md

<interfaces>
<!-- Existing CI patterns the new workflow must follow. -->

From .github/workflows/ci.yml (the established `compose-smoke` job analog — same name, different workflow per D-09):
- `set -eu` at top of every multi-line `run:` block.
- `::group::` / `::endgroup::` for collapsible diagnostic output.
- `env:` block to route `${{ ... }}` values; never inline `${{ ... }}` directly into shell `run:`.
- Diagnostic-on-failure step gated by `if: failure()`.
- Tear-down step gated by `if: always()`.
- `docker/setup-buildx-action@v3` + `docker/build-push-action@v6` + `cache-from: type=gha,scope=...` for builds.

From .github/workflows/release.yml (multi-arch build mechanics):
- `docker/setup-qemu-action@v3` only needed for multi-arch; compose-smoke is amd64-only per CONTEXT.md so QEMU is unnecessary.
- `docker/login-action@v3` only needed for push; compose-smoke does NOT push.

From examples/docker-compose.yml (shipped quickstart compose file — Plan 04 does NOT edit this file; it is the target of T-V11-HEALTH-01):
```yaml
services:
  cronduit:
    image: ghcr.io/simplicityguy/cronduit:latest
    # ... etc
```

The compose-smoke workflow needs to override `image:` with the locally-built `cronduit:ci` so the test exercises the FRESH binary, not GHCR's `:latest`. Use a docker-compose override file or `--env-file` + `image:` template; the simplest path is to write a small one-line override that maps the service to `cronduit:ci`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/Dockerfile.ops08-old fixture (busted wget HEALTHCHECK over production image)</name>
  <files>tests/Dockerfile.ops08-old</files>
  <read_first>
    - Dockerfile (production) — read the runtime stage (L80-130) so the fixture inherits identical state and only the HEALTHCHECK varies.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md § "`tests/Dockerfile.ops08-old` (NEW test fixture)" — for the verbatim recommended approach (option (a): multi-stage `FROM cronduit:ci` that pulls the built image and only re-issues HEALTHCHECK).
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-CONTEXT.md § D-08 — exact OLD-state HEALTHCHECK shape.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-RESEARCH.md § "Pattern 5: Compose-override smoke test" and adjacent OPS-08 design notes.
  </read_first>
  <behavior>
    - `docker build -t cronduit:ops08-old -f tests/Dockerfile.ops08-old .` exits 0 (after `cronduit:ci` is built first by an earlier workflow step).
    - `docker inspect --format '{{json .Config.Healthcheck.Test}}' cronduit:ops08-old` outputs exactly `["CMD","wget","--spider","-q","http://localhost:8080/health"]`.
    - The fixture inherits everything else (USER, ENTRYPOINT, CMD, embedded migrations) from `cronduit:ci` — only HEALTHCHECK differs.
    - `--start-period=20s` (NOT 60s) so the test reaches the unhealthy verdict in ~50 s instead of 90+ s — production timing belongs in production, not in the test fixture.
  </behavior>
  <action>
Ensure the `tests/` directory exists at the repo root (it should already — verify with `ls tests/` before writing). If `tests/` does not exist, create it.

Create `tests/Dockerfile.ops08-old` with EXACTLY the following content:

```dockerfile
# tests/Dockerfile.ops08-old — Phase 12 D-08 fixture.
#
# Layers the OLD `wget --spider` HEALTHCHECK over the production cronduit:ci
# image to reproduce the OPS-08 (unhealthy) symptom in CI. The compose-smoke
# workflow builds cronduit:ci first (using the production Dockerfile), then
# builds this fixture as `cronduit:ops08-old` and runs both side-by-side to
# assert OLD → unhealthy and NEW → healthy.
#
# This image is for testing only — do NOT push, do NOT tag, do NOT publish.
# `--start-period=20s` (vs production's 60s) so the test reaches its verdict
# fast; the production value is for first-boot migration backfill which the
# fixture doesn't exercise.

FROM cronduit:ci

# Phase 12 D-08: deliberately broken HEALTHCHECK that reproduces the OPS-08
# (unhealthy) symptom. busybox `wget --spider` misparses axum's chunked /health
# response and exits non-zero, flipping the container to (unhealthy) after 3
# retries. ONLY for the compose-smoke before/after assertion. Do NOT use in
# production images.
HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \
    CMD ["wget", "--spider", "-q", "http://localhost:8080/health"]
```

Notes:
- `FROM cronduit:ci` requires `cronduit:ci` to exist locally (workflow step builds it first).
- Re-issuing `HEALTHCHECK` in a child image OVERRIDES the parent's `HEALTHCHECK` — Docker semantics, verified by the `docker inspect` assertion.
- DO NOT redeclare `ENTRYPOINT`, `CMD`, `USER`, or any COPY/RUN — they all inherit from `cronduit:ci`.
- DO NOT use `wget --spider --timeout=N` — the bug being reproduced is that busybox wget mishandles chunked responses; adding wget-side flags would mask it.
- DO NOT set this fixture's start-period to 60 s — the test wants the verdict fast, and 20 s is plenty (the OLD `wget --spider` exits with non-zero on the very first probe attempt, no warm-up needed).

Verify locally if docker is available:

```bash
docker build -t cronduit:ci .
docker build -t cronduit:ops08-old -f tests/Dockerfile.ops08-old .
docker inspect --format '{{json .Config.Healthcheck.Test}}' cronduit:ops08-old
# expected: ["CMD","wget","--spider","-q","http://localhost:8080/health"]
```
  </action>
  <verify>
    <automated>test -f tests/Dockerfile.ops08-old && grep -F 'FROM cronduit:ci' tests/Dockerfile.ops08-old && grep -F 'HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \' tests/Dockerfile.ops08-old && grep -F 'CMD ["wget", "--spider", "-q", "http://localhost:8080/health"]' tests/Dockerfile.ops08-old</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Dockerfile.ops08-old` exists (verifiable: `test -f tests/Dockerfile.ops08-old`).
    - Contains the literal line `FROM cronduit:ci` (verifiable: `grep -F 'FROM cronduit:ci' tests/Dockerfile.ops08-old`).
    - Contains the literal line `HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \` (verifiable: `grep -F 'HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \' tests/Dockerfile.ops08-old`).
    - Contains the literal line `    CMD ["wget", "--spider", "-q", "http://localhost:8080/health"]` (verifiable: `grep -F '    CMD ["wget", "--spider", "-q", "http://localhost:8080/health"]' tests/Dockerfile.ops08-old`).
    - Does NOT contain `ENTRYPOINT`, `CMD ["run"`, or `USER` (those inherit from cronduit:ci — verifiable: `! grep -E '^ENTRYPOINT|^CMD \\[\"run\"|^USER' tests/Dockerfile.ops08-old`).
    - Comment block references `Phase 12 D-08` (verifiable: `grep -F 'Phase 12 D-08' tests/Dockerfile.ops08-old`).
    - Contains exactly ONE HEALTHCHECK directive (verifiable: `grep -c '^HEALTHCHECK' tests/Dockerfile.ops08-old` returns 1).
  </acceptance_criteria>
  <done>
    `tests/Dockerfile.ops08-old` exists, references the production image as base, and re-declares the OLD `wget --spider` HEALTHCHECK to reproduce OPS-08.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/compose-override.yml fixture (operator healthcheck overrides Dockerfile)</name>
  <files>tests/compose-override.yml</files>
  <read_first>
    - examples/docker-compose.yml — read the service block (around L70-96) for the established service-block style.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md § "`tests/compose-override.yml` (NEW test fixture)" — for the verbatim recommended fixture content.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-CONTEXT.md § D-09 — for the compose-override semantic the fixture must demonstrate.
  </read_first>
  <behavior>
    - `docker compose -f tests/compose-override.yml up -d` succeeds (compose syntax valid).
    - `docker inspect --format '{{json .Config.Healthcheck.Test}}'` on the running container outputs `["CMD-SHELL","/cronduit health"]` (the operator override), NOT `["CMD","/cronduit","health"]` (the Dockerfile default).
    - The interval/timeout/retries/start_period values from the compose override are visible in `docker inspect` output, distinguishable from the Dockerfile defaults.
  </behavior>
  <action>
Ensure `tests/` exists (Task 1 may have created it; if not, create it now).

Create `tests/compose-override.yml` with EXACTLY the following content:

```yaml
# tests/compose-override.yml — Phase 12 D-09 fixture.
#
# Asserts compose `healthcheck:` stanzas override the Dockerfile HEALTHCHECK
# (Success Criterion #3, T-V11-HEALTH-02). Distinguishable from Dockerfile
# defaults so `docker inspect` makes the override trivially observable:
#
#   Dockerfile default: CMD ["/cronduit", "health"], interval=30s
#   compose override:   CMD-SHELL "/cronduit health", interval=7s
#
# The compose-smoke workflow asserts the inspected Healthcheck.Test array
# starts with "CMD-SHELL" (proving the override won) — the Dockerfile default
# would start with "CMD".

services:
  cronduit:
    image: cronduit:ci
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info,cronduit=debug
      - DATABASE_URL=sqlite:///data/cronduit.db
    healthcheck:
      test: ["CMD-SHELL", "/cronduit health"]
      interval: 7s
      timeout: 4s
      retries: 2
      start_period: 15s
    restart: unless-stopped
```

Notes:
- The service is named `cronduit` (matches `examples/docker-compose.yml`).
- `image: cronduit:ci` references the locally-built image — the workflow builds it before running compose.
- `healthcheck.test` uses **CMD-SHELL** form (the override) instead of CMD exec form (the Dockerfile default) — this is the most obvious distinguishing marker for `docker inspect`.
- `interval: 7s` and `start_period: 15s` are tight so the test reaches a verdict quickly.
- DO NOT mount the docker socket (`/var/run/docker.sock:/var/run/docker.sock`) — the override smoke test does not schedule docker-type jobs.
- DO NOT add a named volume — ephemeral container is fine for the smoke test.
- DO NOT set `group_add` — not needed without docker socket access.

Verify locally if docker is available:

```bash
docker build -t cronduit:ci .
docker compose -f tests/compose-override.yml up -d
sleep 5
docker inspect --format '{{json .Config.Healthcheck.Test}}' tests-cronduit-1
# expected: ["CMD-SHELL","/cronduit health"]
docker compose -f tests/compose-override.yml down -v
```
  </action>
  <verify>
    <automated>test -f tests/compose-override.yml && grep -F 'image: cronduit:ci' tests/compose-override.yml && grep -F 'test: ["CMD-SHELL", "/cronduit health"]' tests/compose-override.yml && grep -F 'interval: 7s' tests/compose-override.yml</automated>
  </verify>
  <acceptance_criteria>
    - `tests/compose-override.yml` exists (verifiable: `test -f tests/compose-override.yml`).
    - Contains `image: cronduit:ci` (verifiable: `grep -F 'image: cronduit:ci' tests/compose-override.yml`).
    - Contains the literal line `      test: ["CMD-SHELL", "/cronduit health"]` (verifiable: `grep -F 'test: ["CMD-SHELL", "/cronduit health"]' tests/compose-override.yml`).
    - Contains `interval: 7s`, `timeout: 4s`, `retries: 2`, `start_period: 15s` (verifiable: each as a `grep -F` match).
    - Comment block references `Phase 12 D-09` (verifiable: `grep -F 'Phase 12 D-09' tests/compose-override.yml`).
    - Does NOT contain `/var/run/docker.sock` (no docker socket mount — verifiable: `! grep -F '/var/run/docker.sock' tests/compose-override.yml`).
    - Does NOT contain `group_add` (verifiable: `! grep -F 'group_add' tests/compose-override.yml`).
    - Single top-level `services:` key (verifiable: `grep -c '^services:' tests/compose-override.yml` returns 1).
  </acceptance_criteria>
  <done>
    `tests/compose-override.yml` defines a `cronduit` service with a CMD-SHELL healthcheck distinguishable from the Dockerfile default.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create .github/workflows/compose-smoke.yml workflow with 3 assertions</name>
  <files>.github/workflows/compose-smoke.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read the full file (especially the `compose-smoke` job at L133-366) for the established style: `set -eu`, `::group::`, env-routing, build-push-action with `cache-from`/`cache-to`, diagnostic-on-failure, tear-down, no inline `${{ ... }}` in shell blocks.
    - .github/workflows/release.yml — for `docker/setup-buildx-action@v3` and `docker/build-push-action@v6` invocations.
    - examples/docker-compose.yml — read the full file so the executor knows what `image:` field needs overriding (the shipped file references `ghcr.io/simplicityguy/cronduit:latest`; the test must exercise the locally-built `cronduit:ci`).
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md § "`.github/workflows/compose-smoke.yml`" — for the workflow header, build step, healthcheck-assertion, diagnostic, and tear-down patterns.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-CONTEXT.md § D-08, D-09 — for the workflow's three required assertions.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-VALIDATION.md § "Per-Task Verification Map" rows 12-04-01 through 12-04-05 — for the per-assertion exit criteria the workflow must satisfy.
  </read_first>
  <behavior>
    - The workflow file is valid YAML and parses with `yamllint .github/workflows/compose-smoke.yml` (or equivalent CI YAML check).
    - On `pull_request`, `push: branches: [main]`, and `push: tags: ['v*']`, the workflow triggers.
    - Workflow runs on `ubuntu-latest` with a single `compose-smoke` job that completes within ~6 minutes.
    - All three assertions are independent steps within the same job (so a failure in one does not block the diagnostic dump for the others).
    - Failure of ANY assertion fails the workflow.
    - The OPS-08 OLD-state divergence path (12-04-05): if OLD reports `healthy` instead of `unhealthy`, the workflow logs a clear divergence message, asserts the NEW image is healthy regardless, and exits 0 (the fix is correct because it removes wget; the divergence just means the original root cause was different).
  </behavior>
  <action>
Ensure `.github/workflows/` directory exists (it does — `ci.yml` and `release.yml` live there).

Create `.github/workflows/compose-smoke.yml` with EXACTLY the following content:

```yaml
# .github/workflows/compose-smoke.yml
# Phase 12 OPS-07 / OPS-08 — dedicated compose-smoke workflow.
# Standalone (NOT extending ci.yml per D-09) because the runner needs docker
# daemon + buildx + compose CLI, which don't belong in the fast unit-test tier.
#
# Three assertions (independent steps within one job):
#   (1) shipped-compose smoke — `examples/docker-compose.yml` reports `healthy`
#       within 90s of `docker compose up -d` (T-V11-HEALTH-01 / OPS-07).
#   (2) compose-override smoke — operator `healthcheck:` in
#       `tests/compose-override.yml` wins over the Dockerfile HEALTHCHECK
#       (T-V11-HEALTH-02 / OPS-07).
#   (3) OPS-08 before/after — build OLD-state image with `wget --spider`
#       HEALTHCHECK and assert (unhealthy); build NEW image with the
#       `cronduit health` HEALTHCHECK and assert (healthy). Per D-08, if the
#       OLD-state image returns `healthy` (root cause was different), the
#       workflow logs the divergence and still passes — the fix is correct
#       regardless because it removes busybox wget from the path.
name: compose-smoke

on:
  pull_request:
  push:
    branches: [main]
    tags: ['v*']

concurrency:
  group: compose-smoke-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  compose-smoke:
    name: compose-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build local cronduit:ci image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: cronduit:ci
          cache-from: type=gha,scope=cronduit-compose-smoke
          cache-to: type=gha,mode=max,scope=cronduit-compose-smoke

      # ----------------------------------------------------------------------
      # Assertion 1: shipped-compose smoke — examples/docker-compose.yml
      # reports `healthy` within 90s using the locally-built image.
      # ----------------------------------------------------------------------
      - name: Write override to point examples/docker-compose.yml at cronduit:ci
        run: |
          set -eu
          mkdir -p /tmp/compose-smoke-shipped
          cp examples/docker-compose.yml /tmp/compose-smoke-shipped/docker-compose.yml
          # Override the image: ghcr.io/... line to use the locally-built tag.
          # Use a separate override file so the shipped file is never edited.
          cat > /tmp/compose-smoke-shipped/docker-compose.override.yml <<'EOF'
          services:
            cronduit:
              image: cronduit:ci
          EOF
          # Provide a minimal cronduit.toml next to the compose file (the
          # shipped compose mounts ./cronduit.toml via volumes:).
          cp examples/cronduit.toml /tmp/compose-smoke-shipped/cronduit.toml || true

      - name: Up shipped compose stack
        working-directory: /tmp/compose-smoke-shipped
        run: |
          set -eu
          docker compose up -d
          docker compose ps

      - name: Wait for shipped stack `healthy` (max 90s)
        id: shipped_health
        working-directory: /tmp/compose-smoke-shipped
        run: |
          set -eu
          container=$(docker compose ps -q cronduit)
          if [ -z "$container" ]; then
            echo "::error::could not resolve cronduit container id"
            exit 1
          fi
          for i in $(seq 1 90); do
            status=$(docker inspect --format '{{.State.Health.Status}}' "$container" 2>/dev/null || echo "missing")
            echo "[shipped t+${i}s] status=$status"
            if [ "$status" = "healthy" ]; then
              echo "shipped stack reached healthy after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "::error::shipped stack never reached healthy within 90s (got: $status)"
          exit 1

      - name: Tear down shipped compose stack
        if: always()
        working-directory: /tmp/compose-smoke-shipped
        run: docker compose down -v

      # ----------------------------------------------------------------------
      # Assertion 2: compose-override smoke — operator `healthcheck:` stanza
      # in tests/compose-override.yml wins over the Dockerfile HEALTHCHECK.
      # ----------------------------------------------------------------------
      - name: Up compose-override stack
        run: |
          set -eu
          docker compose -f tests/compose-override.yml up -d
          docker compose -f tests/compose-override.yml ps

      - name: Assert override wins over Dockerfile HEALTHCHECK
        run: |
          set -eu
          container=$(docker compose -f tests/compose-override.yml ps -q cronduit)
          test_array=$(docker inspect --format '{{json .Config.Healthcheck.Test}}' "$container")
          echo "Inspected Healthcheck.Test: $test_array"
          # The override uses CMD-SHELL form; the Dockerfile default uses CMD form.
          # If the override won, the first element will be "CMD-SHELL".
          first=$(echo "$test_array" | python3 -c 'import json,sys; print(json.load(sys.stdin)[0])')
          if [ "$first" != "CMD-SHELL" ]; then
            echo "::error::override did NOT win — first element of Healthcheck.Test is '$first' (expected 'CMD-SHELL')"
            exit 1
          fi
          echo "compose override won: Healthcheck.Test starts with CMD-SHELL"

      - name: Diagnostics — compose-override stack
        if: failure()
        run: |
          echo "::group::compose-override container logs"
          docker compose -f tests/compose-override.yml logs --tail=200 || true
          echo "::endgroup::"

      - name: Tear down compose-override stack
        if: always()
        run: docker compose -f tests/compose-override.yml down -v

      # ----------------------------------------------------------------------
      # Assertion 3: OPS-08 before/after reproduction.
      # ----------------------------------------------------------------------
      - name: Build OLD-state image (busybox wget HEALTHCHECK)
        run: |
          set -eu
          docker build -t cronduit:ops08-old -f tests/Dockerfile.ops08-old .
          docker inspect --format '{{json .Config.Healthcheck.Test}}' cronduit:ops08-old

      - name: Run OLD image and observe Health.Status (max 60s)
        id: ops08_old
        run: |
          set -eu
          docker run -d --name cronduit-ops08-old -p 18080:8080 cronduit:ops08-old
          old_status=missing
          for i in $(seq 1 60); do
            old_status=$(docker inspect --format '{{.State.Health.Status}}' cronduit-ops08-old 2>/dev/null || echo "missing")
            echo "[ops08-old t+${i}s] status=$old_status"
            if [ "$old_status" = "unhealthy" ] || [ "$old_status" = "healthy" ]; then
              break
            fi
            sleep 1
          done
          echo "old_status=$old_status" >> "$GITHUB_OUTPUT"
          echo "OLD-state final status: $old_status"

      - name: Run NEW image and assert healthy (max 90s)
        run: |
          set -eu
          docker run -d --name cronduit-ops08-new -p 28080:8080 cronduit:ci
          for i in $(seq 1 90); do
            new_status=$(docker inspect --format '{{.State.Health.Status}}' cronduit-ops08-new 2>/dev/null || echo "missing")
            echo "[ops08-new t+${i}s] status=$new_status"
            if [ "$new_status" = "healthy" ]; then
              echo "NEW image reached healthy after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "::error::NEW image never reached healthy within 90s (last: $new_status)"
          exit 1

      - name: Evaluate OPS-08 reproduction outcome
        env:
          OLD_STATUS: ${{ steps.ops08_old.outputs.old_status }}
        run: |
          set -eu
          if [ "$OLD_STATUS" = "unhealthy" ]; then
            echo "OPS-08 reproduced cleanly: OLD=unhealthy → NEW=healthy. Fix verified."
          elif [ "$OLD_STATUS" = "healthy" ]; then
            echo "::warning::OPS-08 OLD-state returned healthy on this runner (root cause differs from documented hypothesis). The cronduit health fix path remains correct because it removes busybox wget from the healthcheck path entirely. Documented per D-08 / 12-04-05."
          else
            echo "::error::OPS-08 OLD-state never reached a terminal Health.Status (got: $OLD_STATUS). Likely runner-environment issue; investigate."
            exit 1
          fi

      - name: Diagnostics — OPS-08 containers
        if: failure()
        run: |
          echo "::group::cronduit-ops08-old logs"
          docker logs cronduit-ops08-old --tail=200 || true
          echo "::endgroup::"
          echo "::group::cronduit-ops08-new logs"
          docker logs cronduit-ops08-new --tail=200 || true
          echo "::endgroup::"
          echo "::group::cronduit-ops08-old healthcheck inspect"
          docker inspect --format '{{json .State.Health}}' cronduit-ops08-old || true
          echo "::endgroup::"
          echo "::group::cronduit-ops08-new healthcheck inspect"
          docker inspect --format '{{json .State.Health}}' cronduit-ops08-new || true
          echo "::endgroup::"

      - name: Tear down OPS-08 containers
        if: always()
        run: |
          docker rm -f cronduit-ops08-old cronduit-ops08-new || true
```

Key constraints (DO NOT violate):
- Use `env:` blocks for routing `${{ ... }}` values into shell commands; NEVER inline `${{ ... }}` directly in a `run:` block (RESEARCH.md §Pattern 5 + ci.yml convention).
- `set -eu` at the top of every multi-line shell block.
- Use `::group::` / `::endgroup::` for collapsible diagnostic output.
- Use `if: failure()` for diagnostic steps; `if: always()` for tear-down.
- DO NOT add the workflow to `ci.yml` — D-09 forbids it.
- DO NOT add Postgres or testcontainers — D-09 explicitly rejects testcontainers-based OPS-08 repro.
- DO NOT push the built images anywhere — `push: false` on the build, no `docker login`, no `docker push`.
- DO NOT add a `just` recipe — compose-smoke.yml is exempt from FOUND-12 / D-10 (the `just`-only invariant is scoped to ci.yml; PATTERNS.md confirms via reference to `docs/CI_CACHING.md` § "Deliberate cache gaps").
- The healthcheck assertion uses `python3` for JSON parsing because `jq` is not always preinstalled on `ubuntu-latest`. (If `jq` is present, equivalent: `jq -r '.[0]' <<< "$test_array"`.)

After committing the file, verify the YAML parses (locally if `yamllint` or `actionlint` is available):

```bash
yamllint .github/workflows/compose-smoke.yml || true
actionlint .github/workflows/compose-smoke.yml || true
```

Neither must report a syntax error. Final correctness is validated when GitHub Actions runs the workflow on the feature-branch PR.
  </action>
  <verify>
    <automated>test -f .github/workflows/compose-smoke.yml && grep -q '^name: compose-smoke$' .github/workflows/compose-smoke.yml && grep -F 'cronduit:ci' .github/workflows/compose-smoke.yml && grep -F 'tests/Dockerfile.ops08-old' .github/workflows/compose-smoke.yml && grep -F 'tests/compose-override.yml' .github/workflows/compose-smoke.yml && grep -F 'examples/docker-compose.yml' .github/workflows/compose-smoke.yml && ( (python3 -c "import yaml" 2>/dev/null && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/compose-smoke.yml'))") || actionlint .github/workflows/compose-smoke.yml || (echo "Neither PyYAML nor actionlint available; install one of: 'pip install pyyaml' or 'brew install actionlint'" && false) )</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/compose-smoke.yml` exists (verifiable: `test -f .github/workflows/compose-smoke.yml`).
    - Contains `name: compose-smoke` (verifiable: `grep -E '^name: compose-smoke$' .github/workflows/compose-smoke.yml` returns one match).
    - Triggers on pull_request, push to main, AND tag pushes (verifiable: workflow contains `pull_request:`, `branches: [main]`, AND `tags: ['v*']`).
    - Contains exactly one `jobs.compose-smoke` job (verifiable: `grep -E '^  compose-smoke:$' .github/workflows/compose-smoke.yml` returns one match).
    - Builds `cronduit:ci` via `docker/build-push-action@v6` with `cache-from: type=gha,scope=cronduit-compose-smoke` (verifiable: `grep -F 'scope=cronduit-compose-smoke' .github/workflows/compose-smoke.yml` returns at least one match).
    - References `tests/Dockerfile.ops08-old` (verifiable: `grep -F 'tests/Dockerfile.ops08-old' .github/workflows/compose-smoke.yml`).
    - References `tests/compose-override.yml` (verifiable: `grep -F 'tests/compose-override.yml' .github/workflows/compose-smoke.yml`).
    - References `examples/docker-compose.yml` (verifiable: `grep -F 'examples/docker-compose.yml' .github/workflows/compose-smoke.yml`).
    - Contains the 90s healthy-poll loop for the shipped compose stack (verifiable: `grep -F 'shipped stack reached healthy' .github/workflows/compose-smoke.yml`).
    - Contains the `CMD-SHELL` override assertion (verifiable: `grep -F "first element of Healthcheck.Test is" .github/workflows/compose-smoke.yml`).
    - Contains the OPS-08 OLD-state evaluation step with both `unhealthy` and divergence-`healthy` branches (verifiable: `grep -F 'OPS-08 reproduced cleanly' .github/workflows/compose-smoke.yml && grep -F 'OPS-08 OLD-state returned healthy' .github/workflows/compose-smoke.yml`).
    - YAML parses without error (verifiable: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/compose-smoke.yml'))"` exits 0).
    - Every multi-line shell block starts with `set -eu` (verifiable by reading: at least 6 occurrences of `set -eu` in the workflow file — count: `grep -c 'set -eu' .github/workflows/compose-smoke.yml` returns ≥ 6).
    - `ci.yml` is unchanged (verifiable: `git diff .github/workflows/ci.yml` from start of plan shows zero changes).
    - Does NOT contain `docker login` or `push: true` (verifiable: `! grep -E 'docker login|push: true' .github/workflows/compose-smoke.yml`).
    - Does NOT contain `testcontainers` (verifiable: `! grep -F 'testcontainers' .github/workflows/compose-smoke.yml`).
    - When the workflow runs on the feature branch's PR, the GHA check named `compose-smoke / compose-smoke` reports a green status. (Validated post-commit by GHA itself; not grep-checkable locally.)
  </acceptance_criteria>
  <done>
    `.github/workflows/compose-smoke.yml` ships with all three assertions, builds the local image, parses as valid YAML, and (when CI runs it) the GHA check reports green on the PR.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA workflow trigger (`github.ref`, `github.event.pull_request.*`) → workflow steps | server-controlled metadata; safe to use in `${{ }}` per RESEARCH.md (no workflow injection risk for the contexts used). |
| Workflow `run:` shell blocks → docker daemon | The runner's docker daemon executes builds + runs; tests must clean up via `if: always()` to avoid leaking state across PRs (mitigated by tear-down steps). |
| Built `cronduit:ci` image → docker run on the runner | Image is built fresh from PR checkout — only as trusted as the PR. The workflow does NOT push the image, so PR contributors cannot poison GHCR via this workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | GHA workflow injection via `${{ }}` in shell | mitigate | Per RESEARCH.md §Pattern 5: route values through `env:` blocks (used in the `Evaluate OPS-08 reproduction outcome` step for `OLD_STATUS`). The trigger contexts (`github.ref`, etc.) are server-controlled (not user-controlled) so direct interpolation is safe. |
| T-12-04-02 | Tampering | Pushing the built image to GHCR | mitigate | Acceptance criterion: `! grep -E 'docker login\|push: true'` — workflow does NOT push, so PR-supplied Dockerfile changes cannot poison the registry. |
| T-12-04-03 | Information Disclosure | Compose service exposes 8080 on the runner | accept | The runner is ephemeral; the port is bound to the runner's loopback only by docker compose default (no `0.0.0.0` binding); no inbound network from outside the runner. |
| T-12-04-04 | Denial of Service | Workflow hangs on a stalled docker compose | mitigate | `timeout-minutes: 15` on the job; explicit `for i in $(seq 1 90)` loops bound each healthcheck wait. |
| T-12-04-05 | Elevation of Privilege | Container exec via the HEALTHCHECK | mitigate | The HEALTHCHECK CMD runs as the container's `cronduit` user (Plan 03 acceptance criterion: HEALTHCHECK placed AFTER `USER cronduit:cronduit`). The probe target is loopback-only `/health`. |
| T-12-04-06 | Repudiation | Workflow result divergence on OPS-08 OLD-state | mitigate | The Evaluate step explicitly emits `::warning::` (not silently passes) when OLD returns healthy; the divergence is captured in the workflow log and is grep-checkable as the literal string in workflow output. The PR check still passes because the fix is correct regardless (D-08 + 12-04-05). |
</threat_model>

<verification>
- All three fixture / workflow files exist with the prescribed content.
- YAML parses clean.
- On feature-branch PR: GHA `compose-smoke` check is green.
- Workflow log shows: shipped-compose `healthy` within 90 s; compose-override CMD-SHELL wins; OPS-08 OLD `unhealthy` (or warning if divergent), NEW `healthy`.
- `ci.yml` is unchanged (D-09).
- No tests pushed to GHCR.
- No testcontainers usage.
</verification>

<success_criteria>
- The `compose-smoke` workflow runs and passes on the feature branch PR.
- All three assertions exit with the documented criteria.
- OPS-08 reproduction provides observable evidence in the workflow log (either `OPS-08 reproduced cleanly` or the `::warning::` divergence message).
- All three test fixtures (`tests/Dockerfile.ops08-old`, `tests/compose-override.yml`, `.github/workflows/compose-smoke.yml`) committed to the feature branch.
</success_criteria>

<output>
After completion, create `.planning/phases/12-docker-healthcheck-rc-1-cut/12-04-SUMMARY.md` capturing:
- Files created (3) with line counts.
- Workflow run URL on the feature branch PR + green-check screenshot or paste of the final workflow log lines.
- Whether the OPS-08 reproduction was clean (`OPS-08 reproduced cleanly`) or divergent (`::warning::`); if divergent, document the observed root cause.
- The `docker inspect` outputs for `cronduit:ci`, `cronduit:ops08-old`, and the override-running container, captured from the workflow log.
- Any deviations from the planned workflow shape.
</output>
