---
phase: 11
plan: 14
type: execute
wave: 13
depends_on: ["11-13"]
files_modified:
  - tests/schema_parity.rs
  - .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md
autonomous: false
requirements: [DB-09, DB-10, DB-11, DB-12, DB-13, UI-16, UI-17, UI-18, UI-19, UI-20]
must_haves:
  truths:
    - "tests/schema_parity.rs is green with all Phase 11 migrations applied on both backends"
    - "cargo test --all-features passes every v11_* test"
    - "cargo clippy --all-targets --all-features -- -D warnings is clean"
    - "All five Phase 11 Success Criteria (per ROADMAP) are met"
  artifacts:
    - path: "tests/schema_parity.rs"
      provides: "Parity test updated to cover new columns if needed"
      contains: "job_run_number"
    - path: ".planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md"
      provides: "Phase close-out document summarizing all plans + UAT + verification matrix"
      contains: "Phase 11 complete"
  key_links:
    - from: "Phase 11 success criteria 1-5"
      to: "Per-plan test IDs"
      via: "Phase Verification Matrix"
      pattern: "T-V11-"
---

<objective>
Phase 11 close-out — run the full test suite including integration-tier Postgres tests,
confirm schema_parity stays green, confirm every Phase 11 requirement has at least one
passing test, and author the phase-level summary that the orchestrator and Phase 12
planner will consume.

Purpose: Final gate. Any regression caught at this stage blocks the phase.

Output:
- tests/schema_parity.rs: minor update only if the parity test needs explicit handling
  for the new columns.
- .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md: close-out
  doc mapping each of the 5 Success Criteria to the test IDs that verify it, plus any
  residual technical debt carried to Phase 12+.
- Final browser UAT confirming all three bug-fix user stories are closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-per-job-run-numbers-log-ux-fixes/11-CONTEXT.md
@.planning/phases/11-per-job-run-numbers-log-ux-fixes/11-VALIDATION.md
@tests/schema_parity.rs

<interfaces>
From .planning/ROADMAP.md Phase 11 Success Criteria (5 criteria, ground truth):
1. Run-history renders `#1..#N` per-job with global id as hover tooltip (UI-16)
2. Upgrade-in-place migration backfills every existing job_runs row with no NULLs; idempotent; INFO progress (DB-09/10/12)
3. Back-nav to a running job renders persisted lines + live SSE with zero gap/duplicate (UI-17/18, T-V11-BACK-01/02)
4. No transient "error getting logs" after Run Now (UI-19, T-V11-LOG-08/09)
5. /jobs/{job_id}/runs/{run_id} permalinks resolve by global id (DB-13, UI-16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite + clippy + schema_parity with all backends</name>
  <read_first>
    - All existing Phase 11 tests (tests/v11_*.rs)
    - tests/schema_parity.rs
    - .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-VALIDATION.md Per-Task Verification Map
  </read_first>
  <files>tests/schema_parity.rs</files>
  <action>
Run the full suite:

```bash
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features 2>&1 | tee /tmp/phase-11-test-output.log
cargo test --features integration --test schema_parity
```

IF schema_parity fails: update the expected-columns list to include `next_run_number`
and `job_run_number`. IF any v11_* test fails: return `## PHASE INCOMPLETE` rather than
silently fixing.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo test --all-features 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - cargo fmt --check is clean
    - cargo clippy --all-targets --all-features -- -D warnings is clean
    - cargo test --all-features all tests pass
    - cargo test --features integration --test schema_parity passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 11-PHASE-SUMMARY.md mapping requirements to tests</name>
  <read_first>
    - .planning/ROADMAP.md Phase 11 section
    - .planning/REQUIREMENTS.md DB-09..13 + UI-16..20 + Traceability
    - .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-VALIDATION.md
    - All plan SUMMARY.md files (11-00-SUMMARY.md through 11-13-SUMMARY.md)
  </read_first>
  <files>.planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md</files>
  <action>
Author the phase close-out document mapping each Success Criterion to the test IDs
that verify it. Structure per RESEARCH.md template — include:

- Success Criteria → Test Verification (all 5)
- Requirement → Plan Map (DB-09..13 + UI-16..20)
- T-V11-LOG-02 benchmark result (from 11-01-SUMMARY.md)
- Residual Technical Debt (sentinel-table decision confirmed no-split; HTMX hook confirmed sseBeforeMessage)
- THREAT_MODEL.md Impact (no change)
- Next Steps (Phase 12)

Populate the T-V11-LOG-02 benchmark number from 11-01-SUMMARY.md and residual tech-debt
items from each per-plan SUMMARY.md. Expected new test files count: 10 (7 VALIDATION-
required + 3 additional — log_id_plumbing, runnum_counter, log_dedupe_contract).
  </action>
  <verify>
    <automated>test -f .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md &amp;&amp; grep -q "Phase 11" .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md &amp;&amp; grep -q "Success Criteria" .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md &amp;&amp; grep -c "T-V11-" .planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md | awk '{ if ($1 >= 15) print "ok" }'</automated>
  </verify>
  <done>
    - 11-PHASE-SUMMARY.md exists with all five Success Criteria mapped to test IDs
    - Every requirement (DB-09..13, UI-16..20) has at least one test cited
    - Residual debt + open questions captured
    - T-V11-LOG-02 benchmark result recorded
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final UAT — all three Phase 11 user-visible fixes confirmed</name>
  <what-built>
    - Run-history shows per-job numbering with global-id hover tooltips.
    - Run-detail header shows `Run #N (id X)`.
    - Back-nav to running job: persisted logs render inline, SSE attaches, no gap/duplicate.
    - Run Now → immediate navigation: no "error getting logs" flash.
    - Graceful live→static transition on run completion.
  </what-built>
  <how-to-verify>
    Prerequisites: All of Plans 11-00..11-13 landed. Local dev server running against
    a populated SQLite DB with at least one job having > 100 historical runs.

    Test 1 (UI-16): Dashboard → job detail → run-history shows `#N` per job with global-id tooltip on hover.

    Test 2 (DB-09/10/12): Restart cronduit with a pre-populated DB. Observe `docker logs`
    / stderr for INFO lines matching `cronduit.migrate: job_run_number backfill: batch=X/Y rows=...`
    during startup. Confirm startup completes; dashboard loads.

    Test 3 (UI-17/18, T-V11-BACK-01/02): Start a 60s job via Run Now. Navigate to run-detail.
    Navigate away. Wait 10s. Navigate back. Confirm all previously-seen log lines are still
    present with no duplicates; live stream continues for new lines.

    Test 4 (UI-19): Click Run Now → immediately click run-detail link. Confirm NO "error
    getting logs" flash appears. Repeat 5x.

    Test 5 (D-10): Let a running job complete while run-detail is open. Confirm transition
    to terminal status is clean — no flicker, no log-pane jitter, no scroll jump.

    Test 6 (DB-13): Open a run-detail URL from bookmark / manually-typed URL. Confirm
    URL form `/jobs/{job_id}/runs/{global_id}` and page loads.
  </how-to-verify>
    <files>(manual verification — no file edits)</files>
  <action>Perform the manual verification steps.</action>
  <verify>
    <automated>echo "Manual UAT — no automated verification; see &lt;how-to-verify&gt; above"</automated>
  </verify>
  <done>All verification steps pass; user types the resume signal.</done>
  <resume-signal>
    Type "verified" if all six tests pass.
    Type "issue: [description]" otherwise.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase close-out | No new runtime surface; only aggregates existing test + documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14-01 | — | phase summary | n/a | Documentation only — no attack surface. Phase 11 aggregate: "no new attack surface" per RESEARCH.md. |
</threat_model>

<verification>
- All automated test suites green (cargo test --all-features + --features integration).
- cargo clippy + cargo fmt clean.
- 11-PHASE-SUMMARY.md complete.
- Final UAT confirms all three Phase 11 user-visible fixes are closed.
</verification>

<success_criteria>
1. All of cargo fmt, clippy, test --all-features, test --features integration pass.
2. schema_parity remains green after all three migration files land.
3. 11-PHASE-SUMMARY.md exists with requirements → test mapping and benchmark result.
4. Human UAT verifies all six tests.
</success_criteria>

<output>
Phase 11 shipped. Orchestrator advances to Phase 12 planning per ROADMAP.

Create `.planning/phases/11-per-job-run-numbers-log-ux-fixes/11-PHASE-SUMMARY.md` as the
canonical close-out.
</output>
</content>
</invoke>