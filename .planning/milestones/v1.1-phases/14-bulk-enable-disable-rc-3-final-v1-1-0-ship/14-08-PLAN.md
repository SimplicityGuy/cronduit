---
phase: 14
plan: 08
type: execute
wave: 5
depends_on: [14-04, 14-05, 14-06, 14-07]
files_modified:
  - .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md
autonomous: false
requirements: [ERG-01, ERG-02, ERG-03, ERG-04, DB-14]
tags: [docs, uat, release, phase-14, wave-5]

must_haves:
  truths:
    - "HUMAN-UAT.md exists with 6-8 concrete steps covering the bulk-toggle end-to-end flow per D-17"
    - "Every UAT step references an existing `just` recipe (honors feedback_uat_use_just_commands.md); no ad-hoc cargo/docker/curl commands"
    - "Step 3 covers ERG-02: bulk-disable while a job is running → running job finishes naturally (NOT `stopped`)"
    - "Steps 4 + 7 cover ERG-04: `just reload` preserves override; removing job from config clears override"
    - "Step 5 covers ERG-03: settings page shows Currently Overridden section"
    - "Step 6 covers per-row Clear button flow"
    - "User validates each step; Claude does NOT mark UAT passed from automated runs (honors feedback_uat_user_validates.md)"
    - "Document includes a pre-UAT checklist (rc.3 tag exists? image pullable? just recipes listed?)"
    - "Document includes a post-UAT action: if all checkboxes green, maintainer runs the v1.1.0 promotion commands (full sequence from 14-RESEARCH.md § Release Engineering Commands)"
  artifacts:
    - path: .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md
      provides: "Maintainer UAT checklist + rc.3 → v1.1.0 promotion sequence"
      contains: "Currently Overridden"
  key_links:
    - from: 14-HUMAN-UAT.md
      to: justfile
      via: "every step is a `just <recipe>` invocation"
      pattern: "`just compose-up-rc3`|`just reload`|`just dev`"
---

<objective>
Build the HUMAN-UAT.md checklist that gates the v1.1.0 ship: operator runs 6-8 concrete steps to exercise the bulk-toggle end-to-end flow on the rc.3 image; each step uses an existing `just` recipe; user validates and checks off each step; only after every box is ticked does the maintainer run the v1.1.0 tag-promotion sequence.

Purpose: honor D-17 + auto-memory `feedback_uat_user_validates.md` + `feedback_uat_use_just_commands.md`. Gate rc.3 → v1.1.0 on human-verified behavior, not automated green-bar alone.
Output: `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md`. This plan is `autonomous: false` because the work product EXISTS to be executed by a human — Claude drafts it; a human runs it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-RESEARCH.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-VALIDATION.md
@docs/release-rc.md
@justfile
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-HUMAN-UAT.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → rc.3 image | trusted-local execution; compose stack is on operator's host |
| rc.3 image → GHCR | read-only pull from immutable tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08-01 | Tampering | Maintainer skips a UAT step and marks pass anyway | accept | Honesty-based process; auto-memory `feedback_uat_user_validates.md` codifies the expectation; no technical enforcement |
| T-14-08-02 | Information Disclosure | UAT runs on a host with operator's real `cronduit.toml` alongside test config | accept | UAT uses `examples/docker-compose.yml` with `examples/cronduit.toml`; operator isolates via compose project or separate port |
| T-14-08-03 | Denial of Service | rc.3 image pulls fail (tag doesn't exist yet) | mitigate | Pre-UAT checklist explicitly confirms `docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1.0-rc.3` succeeds before step 1 |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Draft 14-HUMAN-UAT.md with 8 steps + pre-UAT checklist + v1.1.0 promotion sequence</name>
  <read_first>
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-HUMAN-UAT.md (shape reference — existing milestone UAT doc)
    - 14-CONTEXT.md D-17 (the 8-step draft shape — each step maps to an ERG-XX req)
    - 14-CONTEXT.md D-14 (reuse docs/release-rc.md verbatim) + D-16 (v1.1.0 retags rc.3 SHA) + D-18 (`:latest` implicit advance) + D-19 (cumulative git-cliff)
    - 14-RESEARCH.md § Release Engineering Commands (complete verbatim bash command sequences for rc.3 cut + v1.1.0 promotion + post-push verification)
    - docs/release-rc.md (Phase 12 runbook — reused verbatim)
    - justfile (confirm `compose-up-rc3` and `reload` recipes exist from Plan 07)
    - Auto-memory `feedback_uat_use_just_commands.md` + `feedback_uat_user_validates.md` + `feedback_tag_release_version_match.md`
  </read_first>
  <what-built>
    Claude (the planner) drafts the HUMAN-UAT.md document in full. The PLAN task is `checkpoint:human-action` because the DOCUMENT exists to be EXECUTED by a human after the phase ships — the maintainer runs each step, validates, and ticks the checkboxes. Claude does not mark UAT passed.
  </what-built>
  <how-to-verify>
    After Claude writes the document, the maintainer:

    1. Opens `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md`
    2. Scans the 8 steps; confirms each is concrete and actionable
    3. Confirms each step uses a `just` recipe (not ad-hoc docker/cargo/curl)
    4. Confirms the pre-UAT checklist covers: rc.3 tag exists, image pullable, `just --list` shows both new recipes
    5. Confirms the post-UAT promotion sequence matches 14-RESEARCH.md § Release Engineering Commands
    6. Resume-signal: "UAT doc approved — ready to execute after rc.3 is tagged"
  </how-to-verify>
  <resume-signal>
    Maintainer types "UAT doc approved" OR describes missing steps.
  </resume-signal>
  <action>
    Create `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md` with EXACT structure below. Expected length ~250-350 lines.

    ```markdown
    # Phase 14 HUMAN UAT — Bulk Enable/Disable + rc.3 → v1.1.0 Promotion

    **Status:** awaiting user validation
    **Owner:** Robert (maintainer)
    **Prerequisites:** Phase 14 merged to `main`; `v1.1.0-rc.3` tag cut + GHCR image pushed
    **Honors:** auto-memory `feedback_uat_user_validates.md` + `feedback_uat_use_just_commands.md` + `feedback_tag_release_version_match.md`

    > Claude does NOT mark UAT passed. Each checkbox below must be ticked by a human
    > running the command, verifying the expected outcome, and observing reality.

    ---

    ## Pre-UAT Checklist

    - [ ] `v1.1.0-rc.3` tag exists on `origin`: `git ls-remote --tags origin | grep v1.1.0-rc.3`
    - [ ] rc.3 image pullable: `docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1.0-rc.3` returns a multi-arch (linux/amd64 + linux/arm64) manifest
    - [ ] `:latest` still points at `v1.0.1`: `docker manifest inspect ghcr.io/simplicityguy/cronduit:latest` digest matches the v1.0.1 digest (D-10 gating should have blocked `:latest` advancement on the rc.3 push)
    - [ ] `just --list | grep -E 'compose-up-rc3|^\s*reload|^\s*health|^\s*metrics-check'` lists ALL FOUR recipes (Plan 07 adds health + metrics-check per Warning #8)
    - [ ] No cronduit container running on the UAT host: `docker ps --filter name=cronduit --format '{{.Names}}'` is empty

    ---

    ## UAT Steps

    ### Step 1 — Compose up pinned to rc.3

    **Command:** `just compose-up-rc3`

    **Expected:**
    - Pulls `ghcr.io/simplicityguy/cronduit:1.1.0-rc.3` if not present
    - Container starts
    - Within 90 seconds, `docker ps` shows `cronduit` as `Up N seconds (healthy)`
    - `just health` prints `healthy` (wraps `curl -sf /health | jq -r '.status'` per Plan 07 Task 2 — Warning #8)

    **Validation:**
    - [ ] Container is `(healthy)` within 90s
    - [ ] `just health` prints `healthy` (no raw curl)

    ### Step 2 — Dashboard shows new bulk-select chrome

    **Command:** Open `http://127.0.0.1:8080/` in a browser

    **Expected:**
    - Leftmost checkbox column visible on every job row
    - Header row has a select-all checkbox
    - No bulk action bar visible (zero selected)
    - Click one row checkbox → sticky action bar appears between filter and table with "{N} selected" + Disable selected + Enable selected + Clear buttons
    - Click header select-all → all visible-filtered rows become checked; bar shows count
    - Apply filter (type in filter box) → select-all selects only visible filtered rows
    - Scroll the page → bar stays attached to viewport top

    **Validation:**
    - [ ] Checkbox column visible on every row
    - [ ] Select-all works (including filtered view)
    - [ ] Action bar appears/disappears correctly with selection count
    - [ ] Bar sticks to viewport top on scroll
    - [ ] Row checkboxes keep their checked state across 3s table-body polls (wait 6+ seconds with rows selected; check state persists)

    ### Step 3 — Bulk-disable 3 jobs including one currently-running (ERG-02)

    **Preparation:**
    - Ensure at least 3 jobs are in `examples/cronduit.toml` (add dummy `command` jobs if needed)
    - Start one of them running; use "Run Now" on a job with a ~30-second command (e.g., `sleep 30`)

    **Command:** In the browser: check 3 rows including the currently-running job, click "Disable selected"

    **Expected:**
    - Toast fires: `"3 jobs disabled. 1 currently-running job will complete naturally."`
    - Dashboard polls within 3s and the 2 non-running jobs no longer appear in the schedule / their badges show DISABLED state
    - The currently-running job continues to run to its natural terminal status (`success` / `failed` / `timeout` — NOT `stopped`)
    - `/metrics` `cronduit_runs_total{status="stopped"}` counter does NOT increment from this action

    **Validation:**
    - [ ] Toast copy matches expected verbatim
    - [ ] Running job completes to its natural terminal status (not `stopped`)
    - [ ] Other 2 jobs stop firing (confirmed by waiting 5+ minutes without new runs for those jobs)

    ### Step 4 — Reload preserves override (ERG-04)

    **Command:** `just reload`

    **Expected:**
    - `SIGHUP sent to cronduit container` printed
    - Server logs a `config reload` event (check `docker logs cronduit --tail 20`)
    - The 3 bulk-disabled jobs STAY disabled after reload (their `enabled_override` was preserved by `upsert_job`)
    - Dashboard still reflects the disabled state within 3s

    **Validation:**
    - [ ] Reload logs visible
    - [ ] All 3 bulk-disabled jobs still disabled post-reload

    ### Step 5 — Settings "Currently Overridden" audit (ERG-03)

    **Command:** Open `http://127.0.0.1:8080/settings` in a browser

    **Expected:**
    - "Currently Overridden" section appears below the 6-card grid
    - Section lists all 3 bulk-disabled jobs alphabetically by name
    - Each row shows `Name | DISABLED badge | Clear button`
    - Section is full-width and visually consistent with the dashboard table

    **Validation:**
    - [ ] Section exists and is visible
    - [ ] All 3 jobs listed
    - [ ] Rows alphabetical
    - [ ] DISABLED badge uses the terminal-yellow color token

    ### Step 6 — Per-row Clear button restores override (ERG-03)

    **Command:** On the settings page, click "Clear" on one of the 3 rows

    **Expected:**
    - Toast fires EXACTLY: `"1 job: override cleared."` (uses the shared multi-row formatter with rows_affected=1 — same wording operators see from the bulk bar; no special per-row casing)
    - Reload the `/settings` page
    - The cleared job drops off the Currently Overridden list (2 jobs remain)
    - Navigate to `/` dashboard; the cleared job returns to active scheduling (fires on its next cron slot)

    **Validation:**
    - [ ] Toast appears with correct copy
    - [ ] Row removed from settings list on `/settings` reload
    - [ ] Job returns to active state on dashboard

    ### Step 7 — Remove one bulk-disabled job from config + reload (ERG-04 symmetry)

    **Command:**
    1. Edit `examples/cronduit.toml`; remove one of the still-disabled jobs (leave 1 still overridden + in config)
    2. `just reload`

    **Expected:**
    - Removed job: `enabled_override` is cleared alongside `enabled = 0` (via `disable_missing_jobs` extension)
    - Remaining overridden job: still has override=0 after reload
    - Dashboard: removed job disappears from the list entirely (not just disabled)
    - Settings → Currently Overridden: the removed job is gone; 1 job still listed

    **Validation:**
    - [ ] Removed job disappears from dashboard + settings list
    - [ ] Remaining overridden job still shows DISABLED in settings list
    - [ ] Re-add the removed job to config + reload → it returns as active (no stale override)
    - [ ] Re-added job confirmed active on dashboard

    ### Step 8 — /metrics health

    **Command:** `just metrics-check`

    **Expected:**
    - Output contains `cronduit_scheduler_up 1` (liveness)
    - Output contains `cronduit_runs_total{...}` lines
    - No `cronduit_runs_total{status="stopped"}` counter entries attributable to the UAT bulk-disable actions (running jobs completed naturally, so `stopped` count is unchanged from pre-UAT baseline)
    - The recipe wraps the raw curl per Plan 07 Task 2 (Warning #8 — no raw HTTP calls in UAT)

    **Validation:**
    - [ ] `just metrics-check` exits 0 and prints the scheduler_up + runs_total series lines
    - [ ] scheduler_up = 1
    - [ ] No spurious `stopped` run increments attributable to bulk-disable

    ---

    ## UAT Sign-Off

    - [ ] All 8 step-level checklists ticked
    - [ ] No observed regressions in existing Phase 10–13 behavior (Stop button, timeline page, sparkline, healthcheck)
    - [ ] Browser console clean (no JS errors from the inline `__cdBulk*` helpers)

    **If any box above is unticked, UAT FAILS — do NOT promote rc.3 to v1.1.0.**

    ---

    ## Post-UAT: v1.1.0 Promotion Sequence (Maintainer Action)

    Once ALL boxes above are ticked, execute the promotion commands VERBATIM from `docs/release-rc.md` Phase 12 runbook. Summary (full details in the runbook):

    ```bash
    # 1. Confirm on the rc.3 merge commit
    git fetch --tags
    git checkout main && git pull --ff-only origin main

    # 2. Resolve the rc.3 SHA (v1.1.0 will retag the same commit per D-16)
    RC3_SHA=$(git rev-list -n 1 v1.1.0-rc.3)
    echo "$RC3_SHA"

    # 3. Preview cumulative release notes (D-19)
    git cliff --unreleased --tag v1.1.0 -o /tmp/v1.1.0-preview.md
    cat /tmp/v1.1.0-preview.md
    # Sanity-check: cumulative across Phases 10-14.

    # 4. Tag the rc.3 commit as v1.1.0 (signed preferred)
    git tag -a -s v1.1.0 -m "v1.1 — Operator Quality of Life" "$RC3_SHA"
    git tag -v v1.1.0

    # 5. Push the tag — release.yml auto-advances :latest, :1, :1.1 per D-18
    git push origin v1.1.0
    gh run watch --exit-status

    # 6. Post-push verification (D-18)
    ./scripts/verify-latest-retag.sh 1.1.0
    # MUST exit 0 — :latest digest must now equal :1.1.0 digest on both platforms.
    docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1.0
    docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1
    docker manifest inspect ghcr.io/simplicityguy/cronduit:1
    docker manifest inspect ghcr.io/simplicityguy/cronduit:latest
    # All four should have identical digests.
    ```

    If `verify-latest-retag.sh` fails, `:latest` did NOT advance — that's a release.yml regression and is a hotfix event, NOT Phase 14 scope.

    ---

    ## Post-Promotion Close-Out (Plan 09 scope)

    After v1.1.0 is tagged and `:latest` advances:
    - [ ] Plan 09 runs: REQUIREMENTS.md ERG-01..04 + DB-14 checkboxes flip `[ ] → [x]`
    - [ ] Plan 09 runs: MILESTONES.md + README.md v1.1 entries appended
    - [ ] `/gsd-complete-milestone v1.1` invoked to archive the milestone

    ---

    *Doc created: by Phase 14 Plan 08. Awaiting user validation after rc.3 is tagged.*
    ```

    Do NOT add a mermaid diagram to HUMAN-UAT.md (optional — the UAT is text-heavy; a diagram doesn't aid the checklist). If any diagram is added, it MUST be mermaid (auto-memory `feedback_diagrams_mermaid`).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/cronduit &amp;&amp; test -f .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md &amp;&amp; grep -q "just compose-up-rc3" .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md &amp;&amp; grep -q "just reload" .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md &amp;&amp; grep -q "just health" .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md &amp;&amp; grep -q "just metrics-check" .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md &amp;&amp; [ "$(grep -cE '^### Step [1-8]' .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md)" = "8" ] &amp;&amp; [ "$(grep -cE '^[[:space:]]*(curl|wget)[[:space:]]' .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md` exists
    - `grep -c "^### Step [1-8]" 14-HUMAN-UAT.md` returns exactly 8
    - `grep -c "\- \[ \]" 14-HUMAN-UAT.md` returns >= 15 (pre-UAT + 8 steps + sign-off)
    - `grep -q "just compose-up-rc3" 14-HUMAN-UAT.md` succeeds
    - `grep -q "just reload" 14-HUMAN-UAT.md` succeeds
    - `grep -q "just health" 14-HUMAN-UAT.md` succeeds (Warning #8 — Step 1 uses `just health` not raw curl)
    - `grep -q "just metrics-check" 14-HUMAN-UAT.md` succeeds (Warning #8 — Step 8 uses `just metrics-check` not raw curl)
    - `grep -q "Currently Overridden" 14-HUMAN-UAT.md` succeeds
    - `grep -q "will complete naturally" 14-HUMAN-UAT.md` succeeds (Step 3 toast)
    - `grep -q "1 job: override cleared\." 14-HUMAN-UAT.md` succeeds (Step 6 toast — literal expected-string assertion)
    - `grep -q "git tag -a -s v1.1.0" 14-HUMAN-UAT.md` succeeds (promotion command)
    - `grep -q "verify-latest-retag.sh 1.1.0" 14-HUMAN-UAT.md` succeeds (post-push verification)
    - `grep -q "feedback_uat_user_validates.md\|Claude does NOT mark UAT passed" 14-HUMAN-UAT.md` succeeds
    - `grep -q "feedback_uat_use_just_commands.md\|honors.*use_just_commands" 14-HUMAN-UAT.md` succeeds
    - `grep -c "cargo " 14-HUMAN-UAT.md` returns 0 (no raw cargo invocations in UAT steps — use `just` recipes)
    - `grep -cE '^[[:space:]]*(curl|wget)[[:space:]]' .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md` returns 0 (Warning #8 — no raw HTTP calls in UAT steps; all health/metrics probes go through `just` recipes)
    - No ASCII-art diagrams (`! grep -E '^\s*\+-+\+' 14-HUMAN-UAT.md`)
  </acceptance_criteria>
  <done>
    14-HUMAN-UAT.md exists with 8 steps; every step references a `just` recipe; pre-UAT checklist + post-UAT promotion sequence present; auto-memory rules honored; maintainer-approval resume-signal recorded.
  </done>
</task>

</tasks>

<verification>
Manual human action. After Claude drafts the file:

```
cat .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md | less
# Human reviews → approves → types resume-signal
```

Claude does NOT tick any step-level checkbox. The document is STATE, not an automation target. Plan 09 waits for maintainer approval before running its REQUIREMENTS.md flips.
</verification>

<success_criteria>
- 14-HUMAN-UAT.md exists and is >= 250 lines
- 8 concrete steps, each using a `just` recipe
- Pre-UAT checklist covers tag/image/recipe preconditions
- Post-UAT promotion sequence is verbatim from 14-RESEARCH.md § Release Engineering Commands
- Claude has NOT marked any checkbox passed
- Plan 09 has the document it needs for the close-out commit
</success_criteria>

<output>
Create `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-08-SUMMARY.md`. Include:
- Short note: "14-HUMAN-UAT.md drafted; awaiting maintainer validation after rc.3 is tagged; promotion sequence documented but NOT executed by this plan."
- Pointer to Plan 09 for the close-out REQUIREMENTS flips
</output>
