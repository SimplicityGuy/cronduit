---
phase: 14
plan: 09
type: execute
wave: 6
depends_on: [14-08]
files_modified:
  - .planning/REQUIREMENTS.md
  - MILESTONES.md
  - README.md
autonomous: false
requirements: [ERG-01, ERG-02, ERG-03, ERG-04, DB-14]
tags: [docs, release, milestone, phase-14, wave-6]

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` has ERG-01, ERG-02, ERG-03, ERG-04, DB-14 checkboxes flipped from `[ ]` to `[x]` AND Traceability table Status columns updated to `Complete` (only AFTER HUMAN-UAT.md all-boxes-checked by maintainer)"
    - "`MILESTONES.md` has a new v1.1 archive entry following the v1.0 entry pattern (one paragraph + pointers)"
    - "`README.md` `## Current State` paragraph notes v1.1.0 as current stable (was v1.0.1)"
    - "No other .planning/ or root files modified (files like ROADMAP.md + PROJECT.md stay as-is; /gsd-complete-milestone handles milestone archive)"
    - "Honors D-19 (release notes via git-cliff, no hand-edit) + D-20 (MILESTONES.md follows v1.0 pattern)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "ERG-01..04 + DB-14 flipped to complete; Traceability table Status column advanced"
      contains: "[x] **DB-14**"
    - path: MILESTONES.md
      provides: "v1.1 archive entry appended"
      contains: "v1.1 — Operator Quality of Life"
    - path: README.md
      provides: "Current State bump to v1.1.0"
      contains: "v1.1.0"
  key_links:
    - from: .planning/REQUIREMENTS.md Traceability
      to: CI + audit trail
      via: "Status column = Complete for every Phase 14 requirement"
      pattern: "ERG-0[1-4].*Phase 14.*Complete"
---

<objective>
Close-out plan for Phase 14 + v1.1 milestone. Runs ONLY after HUMAN-UAT.md is fully ticked by the maintainer AND the `v1.1.0` tag has been pushed + `:latest` verified advanced.

Two discrete commits:
1. REQUIREMENTS.md flips (pairs with the v1.1.0 tag — "we shipped these requirements")
2. MILESTONES.md + README.md bump (pairs with the `/gsd-complete-milestone v1.1` archive workflow — "v1.1 is done; v1.2 is next")

Purpose: honor D-16 (tagged commit stays bit-identical to rc.3) by splitting docs from code; land all post-ship bookkeeping in a follow-up PR on `main`.
Output: REQUIREMENTS.md + MILESTONES.md + README.md updated; commit on `main` via PR; milestone close-out invocation of `/gsd-complete-milestone v1.1` documented as the next step (not run by this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@MILESTONES.md
@README.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md

<interfaces>
<!-- Target REQUIREMENTS.md changes:
  - L45 region: `- [ ] **DB-14**: ...` → `- [x] **DB-14**: ...`
  - L74-81 region: `- [ ] **ERG-01**` through `- [ ] **ERG-04**` → `- [x]` each
  - Traceability table (L147-182): for DB-14, ERG-01, ERG-02, ERG-03, ERG-04 rows — change `Pending` to `Complete` -->

<!-- Target MILESTONES.md addition:
  Follow shape of existing v1.0 entry (read whole file to mirror). Shape:
  ## v1.1 — Operator Quality of Life — SHIPPED YYYY-MM-DD
  <one-paragraph summary of what v1.1 delivered>
  **Tags:** `v1.1.0-rc.1`, `v1.1.0-rc.2`, `v1.1.0-rc.3`, `v1.1.0`
  **Phases:** 10 (Stop), 11 (Run Numbers + Log UX), 12 (Healthcheck), 12.1 (GHCR hygiene), 13 (Observability), 14 (Bulk Toggle)
  **Archived artifacts:** `.planning/milestones/v1.1-ROADMAP.md` + `.planning/milestones/v1.1-REQUIREMENTS.md` + `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (created by /gsd-complete-milestone)
-->

<!-- Target README.md update:
  Locate `## Current State` section; change version reference from `v1.0.1` to `v1.1.0`; update one-paragraph summary to mention bulk toggle + observability as the v1.1 headlines. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md → audit trail | project planning source-of-truth; flipping checkboxes = formal "we shipped this" assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09-01 | Tampering | Checkboxes flipped BEFORE UAT passes → false "shipped" assertion | mitigate | This plan is `autonomous: false` and gated on the Task 1 checkpoint confirming HUMAN-UAT.md is fully ticked + v1.1.0 tag pushed |
| T-14-09-02 | Tampering | MILESTONES.md entry contradicts the v1.1.0 release notes (divergence) | mitigate | MILESTONES.md entry points at the auto-generated `.planning/milestones/v1.1-*` archive; the release notes themselves are authoritative per D-19 git-cliff rule |
| T-14-09-03 | Information Disclosure | README.md Current State reveals pre-release details to adopters | accept | This is documentation; the release has shipped publicly; no secret material |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm HUMAN-UAT passed AND v1.1.0 tag pushed + :latest verified</name>
  <what-built>
    This plan has NOT yet modified any files. Before Claude runs Tasks 2 + 3, the maintainer confirms the prerequisites.
  </what-built>
  <how-to-verify>
    The maintainer confirms:

    1. Every checkbox in `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-HUMAN-UAT.md` is ticked (not just "most" — ALL)
    2. `git tag -l v1.1.0` returns `v1.1.0` (the tag exists locally)
    3. `git ls-remote --tags origin | grep v1.1.0$` returns a line (tag pushed to remote)
    4. `docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1.0` returns a multi-arch manifest
    5. `./scripts/verify-latest-retag.sh 1.1.0` exits 0 (`:latest` digest == `:1.1.0` digest on both platforms)
    6. GitHub Release for `v1.1.0` is published with `git-cliff --unreleased --tag v1.1.0` content (D-19 cumulative notes)

    Resume-signal: maintainer types "v1.1.0 shipped — run Plan 09 Tasks 2+3" OR describes blocker.
  </how-to-verify>
  <resume-signal>
    Maintainer types "v1.1.0 shipped — proceed" OR describes blocker.
  </resume-signal>
  <action>
    This is a verification-only task. Claude does NOT modify any files here. The maintainer runs the six verification commands listed in `<how-to-verify>` and types the resume-signal. Only after approval does Plan 09 continue to Tasks 2 + 3. If any check fails (e.g. `verify-latest-retag.sh` non-zero), this plan BLOCKS — hotfix the release.yml regression first; do NOT proceed with REQUIREMENTS flips on a half-shipped tag.
  </action>
  <verify>
    <automated>echo "Maintainer-verified checkpoint; no automated check — resume-signal is the gate."</automated>
  </verify>
  <acceptance_criteria>
    - Maintainer resume-signal received ("v1.1.0 shipped — proceed")
    - All 6 verification commands in `<how-to-verify>` ran and returned the expected outputs
  </acceptance_criteria>
  <done>
    Maintainer has confirmed v1.1.0 shipped + `:latest` advanced + HUMAN-UAT fully ticked; Plan 09 can proceed to Tasks 2 + 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip ERG-01..04 + DB-14 in .planning/REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md L45 (DB-14 checkbox line)
    - .planning/REQUIREMENTS.md L74-81 (ERG-01..04 checkbox lines)
    - .planning/REQUIREMENTS.md L147-182 (Traceability table — DB-14 + ERG rows with `Pending` status)
    - .planning/REQUIREMENTS.md L182 (Total line — verify count still adds up after status flips)
  </read_first>
  <action>
    Five checkbox flips in the body:
    - L45: `- [ ] **DB-14**:` → `- [x] **DB-14**:`
    - L74: `- [ ] **ERG-01**:` → `- [x] **ERG-01**:`
    - L76: `- [ ] **ERG-02**:` → `- [x] **ERG-02**:`
    - L78: `- [ ] **ERG-03**:` → `- [x] **ERG-03**:`
    - L80: `- [ ] **ERG-04**:` → `- [x] **ERG-04**:`

    Five Traceability table Status column flips (the table at L147-182):
    - `| DB-14    | Phase 14 | Pending |` → `| DB-14    | Phase 14 | Complete |`
    - `| ERG-01   | Phase 14 | Pending |` → `| ERG-01   | Phase 14 | Complete |`
    - `| ERG-02   | Phase 14 | Pending |` → `| ERG-02   | Phase 14 | Complete |`
    - `| ERG-03   | Phase 14 | Pending |` → `| ERG-03   | Phase 14 | Complete |`
    - `| ERG-04   | Phase 14 | Pending |` → `| ERG-04   | Phase 14 | Complete |`

    Also flip FOUND-12, FOUND-13 (Phase 10 shipped) and DB-09..13 + UI-16..20 + SCHED-09..14 if those are still showing Pending — verify each phase-complete status against `.planning/ROADMAP.md` § v1.1 phase progress table before flipping. ONLY flip rows belonging to phases whose `[x]` checkbox is set in ROADMAP.md. (OPS-06..08, OPS-09..10, OBS-01..05 should already be `Complete`/`Done` per prior-phase close-outs — verify no regression.)

    Do NOT modify any other line. Do NOT add new requirements. Do NOT reorder rows.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/cronduit &amp;&amp; grep -q "^- \[x\] \*\*DB-14\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*ERG-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*ERG-04\*\*" .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -cE '^\| ERG-0[1-4] +\| Phase 14 +\| Complete' .planning/REQUIREMENTS.md)" = "4" ] &amp;&amp; grep -qE '^\| DB-14 +\| Phase 14 +\| Complete' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*(DB-14|ERG-0[1-4])\*\*" .planning/REQUIREMENTS.md` returns 5
    - `grep -cE "^\| (DB-14|ERG-0[1-4]) +\| Phase 14 +\| Complete" .planning/REQUIREMENTS.md` returns 5
    - `grep -cE "^- \[ \] \*\*(DB-14|ERG-0[1-4])\*\*" .planning/REQUIREMENTS.md` returns 0 (zero un-checked ERG/DB-14)
    - `grep -cE "^\| (DB-14|ERG-0[1-4]) +\| Phase 14 +\| Pending" .planning/REQUIREMENTS.md` returns 0
    - `git diff .planning/REQUIREMENTS.md | grep -c '^+.*\[x\]'` >= 5
    - `git diff .planning/REQUIREMENTS.md | grep -c '^-.*\[ \]'` >= 5
    - No other rows touched: `git diff .planning/REQUIREMENTS.md -- .planning/REQUIREMENTS.md | grep -cE '^[+-] \| ' | awk '{ if ($1 > 10) exit 1 }'` (max ~10 table-row diff lines — only the DB-14 + ERG rows)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md body + Traceability table reflects Phase 14 shipped status; no unintended line changes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append v1.1 entry to MILESTONES.md + update README.md Current State</name>
  <read_first>
    - MILESTONES.md (whole file — identify v1.0 entry shape to mirror)
    - README.md (whole file — locate `## Current State` section; identify version references to update)
    - .planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md D-20 (MILESTONES.md v1.1 entry follows v1.0 pattern)
    - .planning/ROADMAP.md § v1.1 phase progress (phase list + rc mapping)
  </read_first>
  <action>
    **Edit 1 — `MILESTONES.md`:**

    Append a new v1.1 entry after the existing v1.0 entry. Shape (adapt to match the exact markdown convention used for v1.0):

    ```markdown
    ## v1.1 — Operator Quality of Life — SHIPPED YYYY-MM-DD

    v1.1 is a polish-and-fix milestone on top of the shipped v1.0.1 codebase. Five phases (10, 11, 12, 12.1, 13, 14) delivered the "stop a running job" capability, per-job run numbers with fixed log UX, a Docker healthcheck that works out of the box, GHCR tag hygiene (`:latest` / `:main` semantics locked), observability polish (timeline + sparklines + p50/p95), and bulk enable/disable ergonomics. No new external dependencies; one new nullable DB column (`jobs.enabled_override`); the scheduler core was not refactored. Released iteratively as `v1.1.0-rc.1`, `v1.1.0-rc.2`, `v1.1.0-rc.3`, and finally `v1.1.0`.

    **Tags:** `v1.1.0-rc.1`, `v1.1.0-rc.2`, `v1.1.0-rc.3`, `v1.1.0`
    **Phases:** 10 (Stop + Hygiene), 11 (Run Numbers + Log UX), 12 (Healthcheck + rc.1 cut), 12.1 (GHCR tag hygiene), 13 (Observability + rc.2 cut), 14 (Bulk Toggle + rc.3 + final)
    **Requirements delivered:** 31 (SCHED-09..14, DB-09..14, UI-16..20, OBS-01..05, ERG-01..04, OPS-06..10, FOUND-12..13)
    **Audit:** see `.planning/milestones/v1.1-ROADMAP.md`, `.planning/milestones/v1.1-REQUIREMENTS.md`, `.planning/milestones/v1.1-MILESTONE-AUDIT.md` (archived by `/gsd-complete-milestone v1.1`)
    ```

    Replace `YYYY-MM-DD` with the actual v1.1.0 ship date (pull from `git log -1 --format=%cs v1.1.0`).

    **Edit 2 — `README.md`:**

    Locate the `## Current State` section (or equivalent version-referencing paragraph). Change:
    - Any `v1.0.1` or `v1.0` reference in that section → `v1.1.0` / `v1.1`
    - Update one-paragraph summary to mention bulk toggle + observability + stop-a-job as v1.1 headlines (1-2 sentences appended or replaced)

    Do NOT modify the Docker image tags section or the quickstart instructions — those are separately audited.

    Do NOT add new diagrams. If any diagram modification is triggered, it MUST be mermaid (auto-memory `feedback_diagrams_mermaid`).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/cronduit &amp;&amp; grep -q "v1.1 — Operator Quality of Life" MILESTONES.md &amp;&amp; grep -q "v1.1.0-rc.1\`, \`v1.1.0-rc.2\`, \`v1.1.0-rc.3\`, \`v1.1.0\`\|Tags:.*v1.1.0" MILESTONES.md &amp;&amp; grep -q "v1.1.0\|v1.1" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "v1.1 — Operator Quality of Life" MILESTONES.md` succeeds
    - `grep -q "v1.1.0-rc.3" MILESTONES.md` succeeds (tag list)
    - `grep -q "Phases:.*10.*11.*12.*12.1.*13.*14\|Phase 10.*Phase 14\|Phase 10\|Phase 14" MILESTONES.md` succeeds (phase list mentioned)
    - `grep -q "bulk toggle\|Bulk Toggle\|bulk enable" MILESTONES.md` succeeds (headline feature)
    - `grep -q "v1.1.0" README.md` succeeds
    - `git diff MILESTONES.md | grep -c '^+'` >= 5 (the new entry is multiple lines)
    - `git diff MILESTONES.md | grep -c '^-'` <= 1 (purely additive OR at most one adjacent-line edit)
    - `git diff README.md | grep -c '^+'` >= 1
    - `git diff README.md | grep -c '^[+-].*v1.0.1\|^[+-].*v1.1.0'` >= 1 (version reference changed)
    - No markdown syntax errors (verify via `markdown-link-check` if available, else skip — pure text change)
  </acceptance_criteria>
  <done>
    MILESTONES.md has v1.1 entry mirroring v1.0 shape; README.md Current State reflects v1.1.0; no unintended text changes elsewhere.
  </done>
</task>

</tasks>

<verification>
```
cd /Users/Robert/Code/public/cronduit
# Task 2 verification
grep -cE "^- \[x\] \*\*(DB-14|ERG-0[1-4])\*\*" .planning/REQUIREMENTS.md   # expect 5
grep -cE "^\| (DB-14|ERG-0[1-4]) +\| Phase 14 +\| Complete" .planning/REQUIREMENTS.md   # expect 5

# Task 3 verification
grep -q "v1.1 — Operator Quality of Life" MILESTONES.md
grep -q "v1.1.0" README.md

# Next step: run `/gsd-complete-milestone v1.1` to archive the milestone
# (creates .planning/milestones/v1.1-ROADMAP.md + v1.1-REQUIREMENTS.md + v1.1-MILESTONE-AUDIT.md)
```
</verification>

<success_criteria>
- Plan 08 HUMAN-UAT.md all-boxes-checked CHECKPOINT resolved by maintainer
- v1.1.0 tag pushed + verify-latest-retag.sh green (confirmed by maintainer)
- REQUIREMENTS.md flips: 5 body checkboxes + 5 Traceability rows all → Complete
- MILESTONES.md has v1.1 entry mirroring v1.0 pattern
- README.md Current State mentions v1.1.0
- Next action documented: `/gsd-complete-milestone v1.1` archives the milestone artifacts
</success_criteria>

<output>
Create `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-09-SUMMARY.md`. Include:
- Confirmation that HUMAN-UAT passed + v1.1.0 tag verified
- Diff excerpts for REQUIREMENTS.md, MILESTONES.md, README.md
- Next step: `/gsd-complete-milestone v1.1` invocation to archive (OUT of Phase 14 scope; next workflow invocation)
</output>
