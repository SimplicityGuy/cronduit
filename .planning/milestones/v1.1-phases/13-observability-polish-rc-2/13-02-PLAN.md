---
phase: 13
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/queries.rs
autonomous: true
requirements: [OBS-02, OBS-03, OBS-04, OBS-05]
tags: [observability, db-queries, dual-path-sql, sparkline, duration, timeline]

must_haves:
  truths:
    - "`queries::get_dashboard_job_sparks(pool)` returns up to the last 20 terminal runs per job in a single SQL query (no N+1)"
    - "`queries::get_recent_successful_durations(pool, job_id, limit)` returns `Vec<u64>` of only `status='success'` runs with non-null duration_ms"
    - "`queries::get_timeline_runs(pool, window_start_rfc3339)` returns up to 10000 runs from enabled jobs in the window, ordered by job name then start_time"
    - "All three queries execute identically on SQLite (`?1` placeholders, `j.enabled = 1`) and Postgres (`$1` placeholders, `j.enabled = true`) with lowercase status string literals"
    - "No query contains `percentile_cont`, `percentile_disc`, or any SQL-native percentile function (OBS-05 structural parity)"
    - "Timeline query filters on `start_time >= ?1` so `idx_job_runs_start_time` is used (T-V11-TIME-02)"
  artifacts:
    - path: "src/db/queries.rs"
      provides: "Three new pub async fns + two new pub structs (DashboardSparkRow, TimelineRun) for Phase 13 observability"
      exports: ["get_dashboard_job_sparks", "get_recent_successful_durations", "get_timeline_runs", "DashboardSparkRow", "TimelineRun"]
      contains: "ROW_NUMBER() OVER (PARTITION BY job_id ORDER BY id DESC)"
  key_links:
    - from: "src/db/queries.rs::get_dashboard_job_sparks"
      to: "job_runs table via window function"
      via: "ROW_NUMBER() partitioned by job_id with rn <= 20 filter"
      pattern: "ROW_NUMBER\\(\\) OVER \\(PARTITION BY job_id ORDER BY id DESC\\)"
    - from: "src/db/queries.rs::get_timeline_runs"
      to: "idx_job_runs_start_time"
      via: "WHERE jr.start_time >= ?1 predicate"
      pattern: "start_time >=\\s*[?$]1"
---

<objective>
Land all three new read-only SQL queries for Phase 13 in a single plan, each with the dual-path `PoolRef` match that every Cronduit query uses. No percentile_cont, no SQL-native math, no N+1. Every query pulls raw rows; downstream handlers (plans 03-05) fold them into view models.

Purpose: Queries are isolated here because they share `src/db/queries.rs` and must ship as one atomic change. Consumer handlers in Wave 2+ can then reference these three functions directly.

Output: Three new `pub async fn`s plus two new `pub struct`s in `src/db/queries.rs`, with dual-path SQLite + Postgres implementations and lowercase status literals across both branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-observability-polish-rc-2/13-CONTEXT.md
@.planning/phases/13-observability-polish-rc-2/13-RESEARCH.md
@.planning/phases/13-observability-polish-rc-2/13-PATTERNS.md
@src/db/queries.rs
@migrations/sqlite/20260410_000000_initial.up.sql

<interfaces>
Existing dual-path pattern in `src/db/queries.rs` (lines 580-653 in `get_dashboard_jobs`):

```rust
pub enum PoolRef<'a> {
    Sqlite(&'a SqlitePool),
    Postgres(&'a PgPool),
}

match pool.reader() {
    PoolRef::Sqlite(p) => {
        // "?1", "?2" placeholders, j.enabled = 1, lowercase status literals
        let rows = sqlx::query(&sqlite_sql).bind(arg).fetch_all(p).await?;
        Ok(rows.into_iter().map(|r| Struct { field: r.get("col"), ... }).collect())
    }
    PoolRef::Postgres(p) => {
        // "$1", "$2" placeholders, j.enabled = true, same status literals
        let rows = sqlx::query(&pg_sql).bind(arg).fetch_all(p).await?;
        Ok(rows.into_iter().map(|r| Struct { ... }).collect())
    }
}
```

Shipped indexes:
- `idx_job_runs_start_time` ON `job_runs(start_time DESC)`
- `idx_job_runs_job_id_start` ON `job_runs(job_id, start_time DESC)` (composite)

Status values in `job_runs.status` (lowercase text): `'success'`, `'failed'`, `'timeout'`, `'cancelled'`, `'stopped'`, `'running'`.

Relevant `job_runs` columns: id, job_id, job_run_number (Phase 11), status, trigger, start_time, end_time, duration_ms, exit_code, error_message.
Relevant `jobs` columns: id, name, enabled (SQLite INTEGER 0/1; Postgres BOOLEAN).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DashboardSparkRow struct + get_dashboard_job_sparks query (OBS-03)</name>
  <files>src/db/queries.rs</files>
  <read_first>
    - src/db/queries.rs (lines 528-653 for get_dashboard_jobs dual-path shape; 557-575 for ROW_NUMBER OVER PARTITION BY pattern; line 15 for PoolRef enum; line 30 for DbPool::reader())
    - .planning/phases/13-observability-polish-rc-2/13-RESEARCH.md section "OBS-03: Dashboard sparkline" lines 544-580 for verbatim SQL plus struct
    - .planning/phases/13-observability-polish-rc-2/13-PATTERNS.md section "Dual-path SQL with PoolRef match"
  </read_first>
  <action>
    Append a new public struct and a new async function to `src/db/queries.rs`. Place them AFTER the existing `get_dashboard_jobs` function.

    Struct body:

    ```
    #[derive(Debug, Clone)]
    pub struct DashboardSparkRow {
        pub job_id: i64,
        pub id: i64,
        pub job_run_number: i64,
        pub status: String,
        pub duration_ms: Option<i64>,
        pub start_time: String,
        pub rn: i64,
    }
    ```

    Function body follows the dual-path `PoolRef` match pattern. SQL (identical on both branches — no bindings):

    ```
    SELECT job_id, id, job_run_number, status, duration_ms, start_time, rn
    FROM (
        SELECT job_id, id, job_run_number, status, duration_ms, start_time,
               ROW_NUMBER() OVER (PARTITION BY job_id ORDER BY id DESC) AS rn
        FROM job_runs
        WHERE status IN ('success','failed','timeout','cancelled','stopped')
    ) t
    WHERE rn <= 20
    ORDER BY job_id ASC, rn ASC
    ```

    Status literals MUST be lowercase on both branches. Row mapping: `r.get("job_id")`, `r.get("id")`, `r.get("job_run_number")`, `r.get("status")`, `r.get("duration_ms")`, `r.get("start_time")`, `r.get("rn")`.

    Function signature:
    `pub async fn get_dashboard_job_sparks(pool: &DbPool) -> anyhow::Result<Vec<DashboardSparkRow>>`

    Postgres `start_time` handling: if the shipped `get_dashboard_jobs` Postgres arm uses `r.get::<chrono::DateTime<chrono::Utc>, _>("start_time").to_rfc3339()` for text conversion, mirror that pattern. If it fetches as `String` directly, do the same. Read `get_dashboard_jobs` Postgres arm first and mirror exactly.
  </action>
  <verify>
    <automated>cargo build --lib &amp;&amp; grep -q 'pub struct DashboardSparkRow' src/db/queries.rs &amp;&amp; grep -q 'pub async fn get_dashboard_job_sparks' src/db/queries.rs &amp;&amp; grep -q 'ROW_NUMBER() OVER (PARTITION BY job_id ORDER BY id DESC)' src/db/queries.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/queries.rs` contains `pub struct DashboardSparkRow` with fields `job_id`, `id`, `job_run_number`, `status`, `duration_ms`, `start_time`, `rn`
    - `src/db/queries.rs` contains `pub async fn get_dashboard_job_sparks(pool: &DbPool) -> anyhow::Result<Vec<DashboardSparkRow>>`
    - The function body contains `match pool.reader()` with both `PoolRef::Sqlite` and `PoolRef::Postgres` arms
    - Both arms contain the literal `ROW_NUMBER() OVER (PARTITION BY job_id ORDER BY id DESC) AS rn`
    - Both arms contain the literal `WHERE rn <= 20`
    - Both arms contain `status IN ('success','failed','timeout','cancelled','stopped')` (lowercase, no 'running')
    - `grep -E "'(SUCCESS\|Success\|FAILED\|Running)'" src/db/queries.rs` returns no new matches in the new function body
    - `cargo build --lib` exits 0
    - `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Sparkline query compiles and is callable via `queries::get_dashboard_job_sparks(&pool)` from any handler.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add get_recent_successful_durations query (OBS-04, OBS-05)</name>
  <files>src/db/queries.rs</files>
  <read_first>
    - src/db/queries.rs (lines 706-782 `get_run_history` for simple LIMIT/bind pattern; line 15 for `PoolRef` enum)
    - .planning/phases/13-observability-polish-rc-2/13-RESEARCH.md section "OBS-04 (cont.): Duration card on job_detail" lines 434-480 for verbatim SQL
    - .planning/phases/13-observability-polish-rc-2/13-CONTEXT.md D-20 (strict `status = 'success'` only)
  </read_first>
  <action>
    Append this function to `src/db/queries.rs`, adjacent to the sparkline query added in Task 1.

    Function signature:
    `pub async fn get_recent_successful_durations(pool: &DbPool, job_id: i64, limit: i64) -> anyhow::Result<Vec<u64>>`

    SQL (SQLite arm, `?1`/`?2` placeholders):

    ```
    SELECT duration_ms FROM job_runs
    WHERE job_id = ?1
      AND status = 'success'
      AND duration_ms IS NOT NULL
    ORDER BY id DESC
    LIMIT ?2
    ```

    Postgres arm: identical SQL but with `$1`/`$2` placeholders.

    Row mapping (both branches): `r.get::<i64, _>("duration_ms") as u64` then `.collect::<Vec<u64>>()`.

    Bindings order: `.bind(job_id).bind(limit)`.

    Critical OBS-05 constraint: BOTH branches return `Vec<u64>` raw samples. Do NOT compute any aggregate in SQL. Do NOT emit a Postgres-specific `percentile_cont` branch. The function signature (`Vec<u64>` not `f64` or `Option<u64>`) is the type-level enforcement.
  </action>
  <verify>
    <automated>grep -q 'pub async fn get_recent_successful_durations' src/db/queries.rs &amp;&amp; grep -q "status = 'success'" src/db/queries.rs &amp;&amp; grep -q 'duration_ms IS NOT NULL' src/db/queries.rs &amp;&amp; cargo build --lib</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/queries.rs` contains `pub async fn get_recent_successful_durations(pool: &DbPool, job_id: i64, limit: i64) -> anyhow::Result<Vec<u64>>`
    - Body contains `match pool.reader()` with both arms
    - Both arms contain `status = 'success'` (lowercase)
    - Both arms contain `duration_ms IS NOT NULL`
    - Both arms contain `ORDER BY id DESC`
    - SQLite arm contains `?1` and `?2`; Postgres arm contains `$1` and `$2`
    - Both arms contain `r.get::<i64, _>("duration_ms") as u64`
    - Return type is literally `Vec<u64>` — no `f64`, no `Option<u64>`, no other type (OBS-05 structural parity)
    - `grep -E 'percentile_cont|percentile_disc|PERCENTILE' src/db/queries.rs` returns no matches in this function
    - `cargo build --lib` exits 0
    - `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Duration query returns raw `Vec<u64>` from both backends; ready for `percentile()` consumer in plan 03.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add TimelineRun struct + get_timeline_runs query (OBS-01, OBS-02)</name>
  <files>src/db/queries.rs</files>
  <read_first>
    - src/db/queries.rs (lines 528-653 for enabled-filter dual-path pattern; line 15 for `PoolRef` enum)
    - .planning/phases/13-observability-polish-rc-2/13-RESEARCH.md section "OBS-01, OBS-02: /timeline page" lines 632-695 (verbatim struct and SQL, including the researcher's conclusion that `start_time >= ?1` beats `end_time >= ?1` for index usage)
    - .planning/phases/13-observability-polish-rc-2/13-CONTEXT.md D-07, D-08, D-11 (alphabetical job order, 24h/7d window, running bars rendered)
    - .planning/phases/13-observability-polish-rc-2/13-RESEARCH.md Open Question #1 (planner recommendation: filter on `start_time`; this plan locks that choice per assumption A2)
    - migrations/sqlite/20260410_000000_initial.up.sql (confirm `idx_job_runs_start_time` and its column)
  </read_first>
  <action>
    Append a new public struct and a new async function to `src/db/queries.rs`.

    Struct body:

    ```
    #[derive(Debug, Clone)]
    pub struct TimelineRun {
        pub run_id: i64,
        pub job_id: i64,
        pub job_name: String,
        pub job_run_number: i64,
        pub status: String,
        pub start_time: String,
        pub end_time: Option<String>,
        pub duration_ms: Option<i64>,
    }
    ```

    Function signature:
    `pub async fn get_timeline_runs(pool: &DbPool, window_start_rfc3339: &str) -> anyhow::Result<Vec<TimelineRun>>`

    **Filter choice LOCKED:** filter on `jr.start_time >= ?1` (Research Open Question #1, recommendation adopted). Semantics: "runs that started in the last 24h/7d." Runs that started before the window and ended inside it are intentionally excluded — homelab-scale 24h+ runs are an edge case, and using `start_time` filter lets `idx_job_runs_start_time` be used by the query planner.

    SQL (SQLite arm, `?1` placeholder, `j.enabled = 1`):

    ```
    SELECT jr.id AS run_id,
           jr.job_id,
           j.name AS job_name,
           jr.job_run_number,
           jr.status,
           jr.start_time,
           jr.end_time,
           jr.duration_ms
    FROM job_runs jr
    JOIN jobs j ON j.id = jr.job_id
    WHERE j.enabled = 1
      AND jr.start_time >= ?1
    ORDER BY j.name ASC, jr.start_time ASC
    LIMIT 10000
    ```

    Postgres arm: same SQL, `$1` placeholder, `j.enabled = true`.

    Row mapping (both branches):
    - `run_id: r.get("run_id")`
    - `job_id: r.get("job_id")`
    - `job_name: r.get("job_name")`
    - `job_run_number: r.get("job_run_number")`
    - `status: r.get("status")`
    - `start_time: r.get("start_time")`
    - `end_time: r.get("end_time")`
    - `duration_ms: r.get("duration_ms")`

    Bindings order: `.bind(window_start_rfc3339)`.

    Postgres start_time / end_time handling: mirror whatever the shipped `get_dashboard_jobs` and `get_run_history` use. If Postgres stores `start_time` as TIMESTAMPTZ, fetch as `DateTime<Utc>` and `.to_rfc3339()` for the `String` field. Check the existing pattern first, do not invent a new one.

    Critical:
    - ORDER BY columns are exactly `j.name ASC, jr.start_time ASC` (alphabetical by job name = deterministic row order; within job, chronological bars).
    - `LIMIT 10000` is a hard literal (not a parameter) — OBS-02 lock.
    - No `end_time` predicate — filtering by `start_time` only.
    - Status includes all six values (no filtering); consumer handler clamps running runs on the timeline.
  </action>
  <verify>
    <automated>grep -q 'pub struct TimelineRun' src/db/queries.rs &amp;&amp; grep -q 'pub async fn get_timeline_runs' src/db/queries.rs &amp;&amp; grep -q 'LIMIT 10000' src/db/queries.rs &amp;&amp; grep -qE 'jr\.start_time\s*&gt;=\s*[?$]1' src/db/queries.rs &amp;&amp; grep -qE 'ORDER BY j\.name ASC, jr\.start_time ASC' src/db/queries.rs &amp;&amp; cargo build --lib</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/queries.rs` contains `pub struct TimelineRun` with fields `run_id`, `job_id`, `job_name`, `job_run_number`, `status`, `start_time`, `end_time: Option<String>`, `duration_ms: Option<i64>`
    - `src/db/queries.rs` contains `pub async fn get_timeline_runs(pool: &DbPool, window_start_rfc3339: &str) -> anyhow::Result<Vec<TimelineRun>>`
    - Body contains `match pool.reader()` with both arms
    - Both arms contain the literal `LIMIT 10000` (not parameterized)
    - Both arms filter on `start_time >= ?1` or `start_time >= $1` (NOT `end_time`)
    - Both arms contain `ORDER BY j.name ASC, jr.start_time ASC`
    - SQLite arm contains `j.enabled = 1`
    - Postgres arm contains `j.enabled = true`
    - `cargo build --lib` exits 0
    - `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Timeline query compiles and can be called from the timeline handler in plan 05. Index usage will be verified by plan 06's dual-backend `EXPLAIN QUERY PLAN` test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Plan 02 adds only read-only SELECT queries; they are invoked from handlers that trust `AppState.pool`. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information disclosure | `get_dashboard_job_sparks` — returns runs for disabled jobs too | accept | Disabled-job filtering is the caller's responsibility (handler in plan 04 hydrates only enabled-job rows). Pre-existing dashboard pattern; no new disclosure. Web UI is unauthenticated in v1 (documented in THREAT_MODEL.md); loopback-bind default stands. |
| T-13-02-02 | DoS | `get_timeline_runs` unbounded window | mitigate | `LIMIT 10000` is a hard SQL literal. Caller validates `?window` to `{24h, 7d}`. No attacker-controlled `window_start_rfc3339` shape (handler computes it from server `Utc::now()` and the fixed window choice). |
| T-13-02-03 | Tampering | SQL injection via window_start parameter | mitigate | Bound via `.bind()`; sqlx parameterizes; no format-string concatenation. |
| T-13-02-04 | Information disclosure | Enumerating other operators' jobs | n/a | Single-operator tool (CLAUDE.md constraint); no multi-tenant isolation requirement. |
| T-13-02-05 | DoS | Very large `job_runs` tables stalling the dashboard query | accept | `ROW_NUMBER` partitioned scan is O(N log N); for a 100k-row table this is ~1.7M ops. Homelab scale typically < 10k rows. If a site exceeds this, pre-existing v1.0 retention policy (Phase 6) prunes. No new mitigation needed. |
</threat_model>

<verification>
After all three tasks:

```bash
cargo build --lib
cargo clippy --lib -- -D warnings
cargo fmt --check
cargo nextest run --lib  # should not regress any existing lib test
```

Manual structural check:
```bash
grep -c 'pub async fn get_dashboard_job_sparks\|pub async fn get_recent_successful_durations\|pub async fn get_timeline_runs' src/db/queries.rs  # expect 3
grep -E 'percentile_cont|percentile_disc|PERCENTILE' src/db/queries.rs  # expect no matches
```

Commit: `feat(13): three read-only observability SQL queries (OBS-02, OBS-03, OBS-04, OBS-05)`
</verification>

<success_criteria>
- Three new public async fns and two new public structs compile and clippy clean
- All queries use dual-path `PoolRef` match
- All queries use the READER pool
- All SQL uses lowercase status literals, parameterized bindings, and no SQL-native percentile
- Timeline query filters on `start_time` (index hit) not `end_time`
- Existing `cargo nextest run --lib` passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-observability-polish-rc-2/13-02-SUMMARY.md` with:
- Signatures of the three new functions
- Signatures of the two new structs
- SQL snippets actually shipped (dark-box verbatim) for audit traceability
- Confirmation that the Postgres row-mapping pattern matches the shipped `get_dashboard_jobs` and `get_run_history` style
- `cargo nextest run --lib` result (green baseline)
</output>
