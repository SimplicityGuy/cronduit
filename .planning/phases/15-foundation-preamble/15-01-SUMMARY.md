---
phase: 15-foundation-preamble
plan: 01
subsystem: infra
tags: [cargo, version-bump, rust, semver, milestone, v1.2]

# Dependency graph
requires:
  - phase: 14-v1.1-ship-it
    provides: shipped v1.1.0 (the previous milestone version this commit supersedes)
  - phase: 10-stop-a-running-job-hygiene-preamble
    provides: D-12 atomic version-bump-as-first-commit precedent (1.0.1 -> 1.1.0); reused verbatim shape here for 1.1.0 -> 1.2.0
provides:
  - Cargo.toml [package].version = 1.2.0 (the in-flight v1.2 milestone version, on the very first v1.2 commit)
  - Cargo.lock cronduit package entry refreshed to 1.2.0
  - cronduit --version reports 1.2.0 from this commit forward (no drift between in-flight milestone and binary)
  - The gate-keeper commit that unlocks plans 15-02 (cargo-deny preamble) and 15-03..05 (webhook worker scaffold)
affects: [15-02-cargo-deny, 15-03-webhook-worker-scaffold, 15-04-webhook-worker-wiring, 15-05-webhook-worker-tests, 24-v1.2-ship-it]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Atomic version-bump-as-first-commit per D-12: the `Cargo.toml` version line moves to the next milestone in a single isolated commit at the start of each release window, mirroring Phase 10 D-12 (1.0.1 -> 1.1.0). Guarantees `cronduit --version` matches the in-flight milestone for every bisect, PR check, and rc tag."

key-files:
  created: []
  modified:
    - "Cargo.toml (line 3: [package].version 1.1.0 -> 1.2.0)"
    - "Cargo.lock (cronduit package entry refreshed via cargo build)"

key-decisions:
  - "Bump-only commit: no cargo-deny work, no webhook worker code, no justfile changes. D-12 mandates the version bump is atomic and lands first so every subsequent v1.2 commit reports 1.2.0 from --version."
  - "rust-version (L5), edition (L4), and the [dependencies] block were intentionally untouched — this commit is a one-line change to L3 plus the lockfile refresh that cargo build produces."

patterns-established:
  - "Phase-15 commit-subject convention: `chore(15): bump workspace version to 1.2.0 (FOUND-15)` — lifts the Phase 10 D-12 commit shape verbatim with phase number and FOUND-id substituted."

requirements-completed: [FOUND-15]

# Metrics
duration: 3min
completed: 2026-04-26
---

# Phase 15 Plan 01: Cargo Workspace Version Bump (1.1.0 -> 1.2.0) Summary

**`Cargo.toml` `[package].version` bumped from `1.1.0` to `1.2.0` as the very first commit of v1.2; `cronduit --version` now prints `cronduit 1.2.0` and `Cargo.lock` is refreshed to match.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-26T21:38:26Z
- **Completed:** 2026-04-26T21:41:30Z
- **Tasks:** 1
- **Files modified:** 2 (`Cargo.toml`, `Cargo.lock`)

## Accomplishments

- `Cargo.toml:3` flipped `version = "1.1.0"` -> `version = "1.2.0"` (single-line edit, no other manifest fields touched).
- `cargo build -p cronduit` succeeded; `Cargo.lock`'s `[[package]] name = "cronduit"` block now carries `version = "1.2.0"`.
- `./target/debug/cronduit --version` reports `cronduit 1.2.0` (string substring `1.2.0` present, `1.1.0` absent).
- Regression baseline preserved: `cargo test -p cronduit --lib` passes 194/194; `cargo clippy -p cronduit --all-targets -- -D warnings` is clean.
- Satisfies project rule D-16 (git tag = `Cargo.toml` version) for every future v1.2 tag (`v1.2.0-rc.1`, `v1.2.0-rc.2`, `v1.2.0-rc.3`, `v1.2.0`).

## Task Commits

1. **Task 1: Bump Cargo.toml version 1.1.0 -> 1.2.0** — `da2bd5c` (chore)

_No metadata-only follow-up commit was created here: per the orchestrator parallel-executor protocol, the worktree agent does not modify shared orchestrator artifacts (`STATE.md`, `ROADMAP.md`); the orchestrator commits those after the wave merges. SUMMARY.md and REQUIREMENTS.md are committed by the execute-plan tail step._

## Files Created/Modified

- `Cargo.toml` — `[package].version` flipped from `1.1.0` to `1.2.0` on line 3. No other lines changed.
- `Cargo.lock` — `[[package]] name = "cronduit"` block's `version` field flipped from `1.1.0` to `1.2.0` (only entry that referenced cronduit by name); regenerated by `cargo build -p cronduit`.

## Decisions Made

- **Atomic single-commit bump (D-12).** Did not bundle any cargo-deny work (plan 15-02) or webhook worker scaffold (plans 15-03..05) into this commit. The D-12 rule (codified after informal v1.0/v1.1 friction) requires the version bump to be the very first commit of the milestone so that every bisect, PR check, and `cronduit --version` from this point forward reports `1.2.0`.
- **Lockfile committed alongside Cargo.toml.** `cargo build -p cronduit` produces a single `Cargo.lock` line change (cronduit's `[[package]]` entry); both files are part of one logical change and committed together. No other dependency rows in `Cargo.lock` shifted (verified — diff is exactly two lines, one in `Cargo.toml` and one in `Cargo.lock`).

## Deviations from Plan

None - plan executed exactly as written. The structural twin (`.planning/milestones/v1.1-phases/10-stop-a-running-job-hygiene-preamble/10-01-PLAN.md`) was followed verbatim with version, phase number, and FOUND-id substituted.

## Issues Encountered

None. Build succeeded on first attempt (47.11s); lib tests passed (194/194); clippy clean.

## User Setup Required

None — no external service configuration, no environment variables, no secrets. Pure metadata version bump.

## Next Phase Readiness

- **Plan 15-02 unblocked.** The cargo-deny preamble (`deny.toml` at project root, `just deny` recipe in `justfile`, new step in `lint` job of `.github/workflows/ci.yml` with `continue-on-error: true`) can now land on top of this commit. Plan 15-02 reviews under the active cargo-deny check posture, so any new transitive duplicates surface as warns in PR checks instead of hidden landmines.
- **Plans 15-03..05 unblocked.** The webhook worker scaffold (module skeleton + `RunFinalized` struct + `WebhookDispatcher` trait + `NoopDispatcher` + bounded mpsc + worker spawn + scheduler wiring + drop counter + integration tests) can begin once 15-02 lands. Plans 15-03..05 are reviewed under the new cargo-deny check.
- **No outstanding concerns.** No deferred items, no auth gates, no architectural decisions surfaced. The bump is mechanical; the next plans build on top of it.

## Self-Check: PASSED

Verification of the four claims in this SUMMARY:

- `Cargo.toml` (modified): present in the working tree at `Cargo.toml`, line 3 is `version = "1.2.0"` — VERIFIED
- `Cargo.lock` (modified): cronduit `[[package]]` block carries `version = "1.2.0"` — VERIFIED
- Commit `da2bd5c` (`chore(15): bump workspace version to 1.2.0 (FOUND-15)`): present in `git log --oneline` — VERIFIED
- `./target/debug/cronduit --version` prints `cronduit 1.2.0` — VERIFIED

---
*Phase: 15-foundation-preamble*
*Completed: 2026-04-26*
