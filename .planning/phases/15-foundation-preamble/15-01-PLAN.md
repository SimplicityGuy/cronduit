---
phase: 15-foundation-preamble
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true
requirements:
  - FOUND-15
user_setup: []
tags:
  - cargo
  - version-bump
  - rust

must_haves:
  truths:
    - "cronduit --version prints exactly the string 1.2.0 (no drift from the in-flight v1.2 milestone)"
    - "Cargo workspace package version matches the very first v1.2 commit (D-12)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace package version = 1.2.0"
      contains: "version = \"1.2.0\""
    - path: "Cargo.lock"
      provides: "Resolved cronduit package version = 1.2.0"
      contains: "name = \"cronduit\""
  key_links:
    - from: "Cargo.toml [package] version"
      to: "env!(\"CARGO_PKG_VERSION\") at src/web/mod.rs (AppState.version) and src/cli/mod.rs (--version flag)"
      via: "compile-time environment variable"
      pattern: "CARGO_PKG_VERSION"
---

<objective>
Bump the workspace package version from `1.1.0` to `1.2.0` as the very first commit of Phase 15 / v1.2, so every subsequent v1.2 build (including all rc cuts and the final `v1.2.0` ship) reports the milestone-in-progress via `cronduit --version`.

Purpose: Close FOUND-15. Per D-12, this lands as the very first commit of v1.2 — guaranteeing zero drift between the in-flight milestone version and the binary, which is the rule that emerged from informal v1.0/v1.1 friction and was codified in Phase 10 D-12. Also satisfies project-rule D-16 (git tag = `Cargo.toml` version) for every future v1.2 tag (`v1.2.0-rc.1`, `v1.2.0-rc.2`, `v1.2.0-rc.3`, `v1.2.0`).
Output: One-line change to `Cargo.toml`; regenerated `Cargo.lock`; green `cargo build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-foundation-preamble/15-CONTEXT.md
@.planning/phases/15-foundation-preamble/15-PATTERNS.md
@.planning/milestones/v1.1-phases/10-stop-a-running-job-hygiene-preamble/10-01-PLAN.md
@Cargo.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump Cargo.toml version 1.1.0 → 1.2.0</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (current state — confirm line 3 is `version = "1.1.0"` before editing)
    - .planning/milestones/v1.1-phases/10-stop-a-running-job-hygiene-preamble/10-01-PLAN.md (the structural twin per D-12; lift its acceptance criteria shape verbatim with version substituted)
    - .planning/phases/15-foundation-preamble/15-CONTEXT.md (D-12: very first commit of v1.2; D-16: tag = version)
    - .planning/phases/15-foundation-preamble/15-PATTERNS.md § "Cargo.toml (L3 version bump) — Plan 15-01" (the exact edit pattern: L3 mechanical change, no other metadata fields)
    - .planning/REQUIREMENTS.md § FOUND-15 (semver, rc format `v1.2.0-rc.N` with dot)
  </read_first>
  <action>
Edit `Cargo.toml` line 3. Change:

```
version = "1.1.0"
```

to:

```
version = "1.2.0"
```

No other changes in this task. Do NOT touch `rust-version = "1.94.1"` at L5, edition at L4, or any `[dependencies]`/`[dev-dependencies]` entries. Do NOT touch any other file. Do NOT bundle the cargo-deny work (plan 15-02) or the webhook worker scaffold (plans 15-03..05) into this commit — D-12 mandates atomic single-commit version bump as the very first v1.2 commit so that every bisect, every PR check, and every `cronduit --version` from this point forward reports `1.2.0`.

After the edit, run `cargo build -p cronduit` to regenerate `Cargo.lock`. The lockfile update (`name = "cronduit"` block flips `version = "1.1.0"` → `version = "1.2.0"`) is the only downstream artifact — commit `Cargo.toml` and `Cargo.lock` together.

Per D-12 + D-13: this is the very first commit of Phase 15 / v1.2 on a feature branch. No webhook code, no `deny.toml`, no `justfile` recipe — just the version bump and its lockfile refresh.

Commit message shape (mirrors Phase 10's `chore(10): bump workspace version to 1.1.0 (FOUND-13)`): `chore(15): bump workspace version to 1.2.0 (FOUND-15)`.
  </action>
  <verify>
    <automated>grep -q '^version = "1.2.0"$' Cargo.toml &amp;&amp; cargo build -p cronduit 2&gt;&amp;1 | tail -5 &amp;&amp; ./target/debug/cronduit --version | grep -q '1.2.0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "1.2.0"$' Cargo.toml` returns exactly `1`
    - `grep -c '^version = "1.1.0"$' Cargo.toml` returns exactly `0`
    - `cargo build -p cronduit` exits 0 with no warnings beyond the existing baseline
    - `./target/debug/cronduit --version` prints a line containing `1.2.0` and NOT containing `1.1.0`
    - `grep -A1 '^name = "cronduit"$' Cargo.lock | grep -q 'version = "1.2.0"'` exits 0
    - `git diff --name-only` returns exactly `Cargo.toml` and `Cargo.lock` (no other files modified)
    - `git log -1 --format=%s` includes the substring `chore(15)` and the substring `FOUND-15`
  </acceptance_criteria>
  <done>`./target/debug/cronduit --version` reports `cronduit 1.2.0`; `Cargo.lock` updated to match; commit is a pure version bump with no other changes; this commit is the first commit on the Phase 15 feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Metadata-only version bump. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | `Cargo.toml` version string (supply-chain) | accept | Version string is public build metadata; git history is the audit log. Project rule D-16 enforces `git tag` == `Cargo.toml` version for every future v1.2 tag (`v1.2.0-rc.1`, `v1.2.0`). No runtime trust impact. |

Security posture: metadata-only change, no new input surface, no code path exercised. ASVS V14 (Configuration) does not apply — this is a version string, not a security-relevant configuration knob.
</threat_model>

<verification>
- `cargo build -p cronduit` exits 0
- `cargo test -p cronduit --lib` passes (regression baseline for subsequent plans 15-02..05)
- `cargo clippy -p cronduit --all-targets -- -D warnings` clean
- `./target/debug/cronduit --version` prints `cronduit 1.2.0`
</verification>

<success_criteria>
Workspace version reports `1.2.0` from this commit forward. No other behavioral change. All existing tests still pass. This is the gate-keeper commit that unlocks plans 15-02 (cargo-deny) and 15-03..05 (webhook worker scaffold). Satisfies success-criterion #1 of the Phase 15 ROADMAP.md entry: "An operator running `cronduit --version` on the first v1.2 commit sees `1.2.0` (not `1.1.0`)."
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation-preamble/15-01-SUMMARY.md` recording the version delta and the lockfile impact.
</output>
