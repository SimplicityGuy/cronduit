---
phase: 17
plan: 05
type: execute
wave: 3
depends_on: [17-01]
files_modified:
  - README.md
autonomous: true
requirements:
  - LBL-01
  - LBL-02
  - LBL-03
  - LBL-04
  - LBL-05
  - LBL-06
requirements_addressed:
  - LBL-01
  - LBL-02
  - LBL-03
  - LBL-04
  - LBL-05
  - LBL-06
must_haves:
  truths:
    - "Operators reading the README's `## Configuration` section find a labels subsection (~30-40 lines) covering all six LBL requirements (D-04)"
    - "Subsection contains a mermaid merge-precedence diagram showing four steps: defaults.labels → per-job labels → use_defaults=false replaces → cronduit-internal labels override (D-04 / D-07 — mermaid only, no ASCII art)"
    - "Subsection contains a 3-row merge-semantics table covering: per-job labels absent, present with use_defaults unset/true, present with use_defaults=false"
    - "Subsection documents the reserved namespace rule (`cronduit.*` rejected at LOAD) with a single-line example (LBL-03)"
    - "Subsection documents the type-gate rule (labels on command/script jobs rejected at LOAD) with a single-line example (LBL-04)"
    - "Subsection documents the size limits (per-value ≤ 4 KB, per-set ≤ 32 KB) (LBL-06)"
    - "Subsection documents the env-var interpolation rule: `${VAR}` interpolated in label VALUES; keys never interpolated (LBL-05)"
  artifacts:
    - path: "README.md"
      provides: "Labels subsection inside the existing `## Configuration` section"
      contains: "### Labels"
    - path: "README.md"
      provides: "Mermaid merge-precedence diagram"
      contains: "mermaid"
    - path: "README.md"
      provides: "Merge-semantics table with use_defaults rows"
      contains: "use_defaults"
  key_links:
    - from: "README.md § Configuration > Labels"
      to: "examples/cronduit.toml inline `# Phase 17 / SEED-001` comments"
      via: "operators navigating from example to docs (Plan 17-04 cross-references this README section)"
      pattern: "Labels"
    - from: "README.md merge-precedence mermaid diagram"
      to: "Plan 17-01 apply_defaults + Plan 17-03 docker.rs label-build site"
      via: "diagram visualizes the four-step chain that those two plans implement"
      pattern: "mermaid"
---

<objective>
Add a comprehensive `### Labels` subsection inside the existing `## Configuration`
section of `README.md`. The subsection documents all six LBL requirements
(LBL-01..LBL-06), includes a mermaid merge-precedence diagram (per project rule
D-07: no ASCII art), a 3-row merge-semantics table, and operator-facing rules
for each of: reserved namespace, type-gate, size limits, env-var interpolation.

Purpose: After Plans 17-01 + 17-02 + 17-03 + 17-04, operators encounter the
labels feature first via `examples/cronduit.toml` inline comments — those
comments deliberately point operators here. This is the single source of truth
documentation surface; without it, operators have to grep source for the
validators' error messages to learn the rules.

Output: One modified file (`README.md`); no new code; existing README
sections unchanged except for the addition of the new `### Labels` subsection
inside `## Configuration`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md
@.planning/phases/17-custom-docker-labels-seed-001/17-RESEARCH.md
@.planning/phases/17-custom-docker-labels-seed-001/17-PATTERNS.md
@.planning/phases/17-custom-docker-labels-seed-001/17-01-SUMMARY.md
@README.md
@examples/cronduit.toml

<interfaces>
Existing README.md § Configuration structure (line numbers approximate from prior scan):
- L170: `## Configuration`
- L177: `### Server Settings`
- L190: `### Default Job Settings`
- L205: `### Job Types`
- L254: `## Monitoring`

Insertion point: AFTER `### Default Job Settings` (around L190+) and BEFORE `### Job Types` (around L205) — labels live in [defaults] AND [[jobs]] so this placement mirrors the [defaults] documentation depth per D-04. Placement after `### Job Types` is also acceptable provided it stays inside `## Configuration`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `### Labels` subsection to README.md § Configuration with mermaid diagram + 3-row table + 6 documented rules</name>
  <read_first>
    - README.md (full — focus on `## Configuration` section starting around L170 through L254 to understand documentation depth, code-fence style, table style; confirm whether mermaid blocks are already used elsewhere — `grep -c "^\`\`\`mermaid" README.md`)
    - .planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md (D-04 verbatim — full subsection structure: prose intro, mermaid diagram, merge-semantics table, reserved-namespace rule, type-gate rule, size limits, env-var interpolation note)
    - .planning/phases/17-custom-docker-labels-seed-001/17-PATTERNS.md (section "README.md::§ Configuration > Labels")
    - examples/cronduit.toml (post Plan 17-04 — confirm inline comments cross-reference "README § Configuration > Labels"; section name MUST match)
    - .planning/seeds/SEED-001-custom-docker-labels.md (read merge-semantics table — plan must mirror what was locked at seed time)
  </read_first>
  <files>
    README.md
  </files>
  <behavior>
    - The README's existing `## Configuration` section gains a `### Labels` subsection
    - Subsection placement: AFTER `### Default Job Settings` (or after `### Job Types` — placement is editor's choice but must be inside `## Configuration`)
    - Subsection contains a fenced mermaid block visualizing the four-step merge precedence
    - Subsection contains a markdown table with 3 data rows on per-job labels presence × use_defaults
    - Subsection covers reserved namespace, type-gate, size limits, and env-var interpolation in distinct paragraphs
    - All other README sections unchanged
  </behavior>
  <action>
    Implements D-04 verbatim per CONTEXT.md. ALL content lives in `README.md`. Do NOT touch any other file.

    **Step 1 — Locate the insertion point.** The existing README has (line numbers approximate from prior scan): `## Configuration` (L170), then `### Server Settings` (L177), `### Default Job Settings` (L190), `### Job Types` (L205), then `## Monitoring` (L254). The new `### Labels` subsection goes AFTER `### Default Job Settings` and BEFORE `### Job Types`. Confirm by reading the file in this range first. (If the actual flow makes placement after `### Job Types` clearer because `### Job Types` lists docker as a job type and labels are docker-only, that placement is also acceptable — the executor picks based on what reads better; either is inside `## Configuration`.)

    **Step 2 — Insert the new `### Labels` subsection.** Use the EXACT content below. The executor MAY rephrase prose for flow but the load-bearing structure (the `### Labels` heading, the mermaid block, the 3-row table, the six documented rules: reserved-namespace, type-gate, size limits, env-var interpolation, plus the "values are not secrets" advisory) MUST all be present.

    The full subsection content to insert (replace each `MERMAID_FENCE` token with three backticks `mermaid` to open and three backticks `` to close — the planner is showing the structure here without confusing the outer plan-file fences):

    ```text
    ### Labels

    Cronduit attaches arbitrary Docker labels to spawned containers. Operators
    use this to integrate cronduit with reverse proxies (Traefik / Caddy),
    update tooling (Watchtower), backup tooling, and any other Docker
    ecosystem tool that filters or routes by container label.

    Labels are configured in two places — `[defaults].labels` (inherited by
    every docker job) and per-job `[[jobs]].labels` (merges with or replaces
    the defaults). The merge precedence is:

    MERMAID_FENCE_OPEN
    flowchart LR
      A["[defaults].labels"] -->|"per-job present + use_defaults != false"| B["merge: defaults ∪ per-job<br/>(per-job wins on collision)"]
      A -->|"per-job present + use_defaults = false"| C["replace: per-job only<br/>(defaults discarded)"]
      A -->|"per-job absent"| D["inherit: defaults verbatim"]
      B --> E["operator label set"]
      C --> E
      D --> E
      E -->|"cronduit-internal labels<br/>(cronduit.run_id, cronduit.job_name)<br/>added — internal wins on impossible-collision"| F["bollard Config::labels<br/>(reaches Docker daemon)"]
    MERMAID_FENCE_CLOSE

    The merge semantics in tabular form:

    | per-job `labels` field | per-job `use_defaults` | Resulting label set on container |
    |------------------------|------------------------|----------------------------------|
    | absent                 | unset / `true`         | `[defaults].labels` verbatim (inherit) |
    | present                | unset / `true`         | `[defaults].labels` ∪ per-job; per-job key wins on collision |
    | present                | `false`                | per-job ONLY; defaults entirely discarded |

    In all cases, cronduit's two internal labels — `cronduit.run_id` and
    `cronduit.job_name` — are added to every spawned container after the
    operator-defined merge. These power orphan reconciliation; they are not
    operator-configurable.

    **Reserved namespace** — keys under the `cronduit.*` prefix are rejected
    at config-LOAD with a clear error. The prefix is reserved for cronduit
    internal use:

    TOML_FENCE_OPEN
    # REJECTED at load:
    [[jobs]]
    labels = { "cronduit.foo" = "bar" }   # error: cronduit.* is reserved
    TOML_FENCE_CLOSE

    **Type-gate (docker-only)** — labels are valid only on docker jobs (jobs
    with `image = "..."` set, either directly or via `[defaults].image`).
    Setting `labels` on a `command` or `script` job is rejected at config-LOAD;
    there's no container to attach labels to:

    TOML_FENCE_OPEN
    # REJECTED at load — labels on a command job:
    [[jobs]]
    name = "no-good"
    command = "echo hi"
    labels = { "team" = "ops" }   # error: labels are docker-only
    TOML_FENCE_CLOSE

    **Size limits** — each label value must be ≤ 4 KB; the total byte length
    of all keys + values for a single job must be ≤ 32 KB. Both limits are
    enforced at config-LOAD. The 32 KB ceiling sits well below dockerd's
    informal label-size threshold so cronduit's error fires first with a
    clear message.

    **Env-var interpolation** — `${VAR}` placeholders in label VALUES are
    interpolated at config-LOAD using the same pipeline that handles env-var
    interpolation elsewhere in the config (e.g., `command`, `image`). Label
    KEYS are NEVER interpolated — keys are validated against the strict
    pattern `^[a-zA-Z0-9_][a-zA-Z0-9._-]*$` and any leftover `${`/`}`
    characters are rejected:

    TOML_FENCE_OPEN
    # ALLOWED — value is interpolated at load (DEPLOYMENT_ID env var must be set):
    [[jobs]]
    labels = { "deployment.id" = "${DEPLOYMENT_ID}" }

    # NOT INTERPOLATED — key remains literal "${TEAM}" and is rejected by the
    # strict char regex (the `$`, `{`, `}` chars are not in the allowed set):
    [[jobs]]
    labels = { "${TEAM}" = "ops" }   # rejected: invalid label key
    TOML_FENCE_CLOSE

    Label values are NOT secrets. Anyone with access to the Docker daemon can
    read them via `docker inspect`. Use env vars (the `env =` field on docker
    jobs) for anything sensitive — env values are redacted in cronduit logs
    and never surface in `docker inspect` for the spawned container.

    See `examples/cronduit.toml` for three integration patterns: Watchtower
    exclusion in `[defaults]`, Traefik routing labels merged onto a per-job
    block, and a `use_defaults = false` job that establishes its own clean
    label set.
    ```

    **Replace tokens before inserting:**
    - `MERMAID_FENCE_OPEN` → three backticks then `mermaid` (literal: opens a mermaid code block)
    - `MERMAID_FENCE_CLOSE` → three backticks (closes the code block)
    - `TOML_FENCE_OPEN` → three backticks then `toml`
    - `TOML_FENCE_CLOSE` → three backticks
    These tokens are placeholders only because the plan file itself is markdown and embedded triple-backtick fences would close the plan's own enclosing code blocks. The README MUST contain ACTUAL triple-backtick mermaid and toml fences, NOT the literal token strings.

    **Step 3 — Verify the diagram renders as mermaid (not ASCII).** Per project rule D-07 (carried as D-07 here), every diagram in any artifact MUST be a mermaid code block. The diagram above uses a triple-backtick mermaid opening fence and a triple-backtick closing fence. Do NOT replace with ASCII boxes; do NOT use plain Unicode arrows.

    **Step 4 — Verify the table uses GFM table syntax** (the rest of the README does — confirm by `grep -c "^|" README.md` returning a non-zero count before this edit; the new table adds 5 more lines starting with `|`).

    **Step 5 — Cross-reference check.** Per Plan 17-04, the inline comments in `examples/cronduit.toml` reference "README § Configuration > Labels" (or near-equivalent phrasing). Confirm the section heading inserted here matches the cross-reference text. The canonical form is: `### Labels` heading; cross-references read "README § Configuration > Labels". Document the choice in the Plan 17-05 SUMMARY.

    Per D-06 (PR-only on feature branch — already on `phase-17-custom-docker-labels`).
    Per D-07 (mermaid only — load-bearing for THIS plan; the merge-precedence diagram is the load-bearing instance per CONTEXT.md `<specifics>`).
  </action>
  <verify>
    <automated>grep -c '### Labels' README.md &amp;&amp; grep -c 'mermaid' README.md &amp;&amp; grep -c 'use_defaults' README.md &amp;&amp; grep -c 'cronduit.run_id' README.md &amp;&amp; grep -E '4 ?KB|4096' README.md | head -1 &amp;&amp; grep -E '32 ?KB' README.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Labels' README.md` returns at least `1`
    - `grep -c 'mermaid' README.md` returns at least `1` (the new diagram fence; per project rule D-07, mermaid is the only allowed diagram syntax)
    - `grep -c '^| ' README.md` returns at least `4` more lines than before this edit (header + separator + 3 data rows = 5 new table lines)
    - `grep -c 'use_defaults' README.md` returns at least `3` (the table rows + at least one prose mention)
    - `grep -c 'cronduit.run_id' README.md` returns at least `1` (internal-label override is documented)
    - `grep -E '4 ?KB|4096' README.md` finds the size-limit prose
    - `grep -E '32 ?KB' README.md` finds the size-limit prose
    - `grep -c 'cronduit\.\*' README.md` returns at least `1` (reserved namespace rule documented)
    - `grep -c 'docker-only' README.md` returns at least `1` (type-gate rule documented)
    - `grep -c 'NOT secret' README.md` returns at least `1` (security advisory documented)
    - `grep -c 'DEPLOYMENT_ID' README.md` returns at least `1` (env-var interpolation example documented)
    - `grep -c 'examples/cronduit.toml' README.md` returns at least `1` more after the edit (cross-reference to example file)
    - The mermaid block is well-formed: `awk '/```mermaid/,/```/' README.md | grep -c 'flowchart'` returns at least `1`
    - **WARNING #5 fix (no leaked placeholder tokens)** — `grep -E 'MERMAID_FENCE|TOML_FENCE' README.md | wc -l | grep -q '^[[:space:]]*0$'` returns 0 lines (no `MERMAID_FENCE_OPEN` / `MERMAID_FENCE_CLOSE` / `TOML_FENCE_OPEN` / `TOML_FENCE_CLOSE` token leaked from the plan into README); the executor MUST replace each token with the literal triple-backtick fence before committing.
    - **WARNING #5 fix (mermaid fence well-formed AND closed)** — `awk '/^\`\`\`mermaid$/,/^\`\`\`$/' README.md | grep -c 'flowchart'` returns at least `1` (the awk range from opening fence to closing fence captures a `flowchart` line — confirms the fence opens at column 0 with `\`\`\`mermaid` and closes at column 0 with bare `\`\`\``)
  </acceptance_criteria>
  <done>The README's `## Configuration` section gains a comprehensive `### Labels` subsection covering all six LBL requirements with a mermaid merge-precedence diagram, a 3-row merge-semantics table, and rule-by-rule prose paragraphs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → operator (read) | Trusted — README is the canonical operator-facing documentation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-I | Information disclosure | Operator interprets "labels" as a safe place for secrets and pastes credentials | mitigate | The new subsection includes an explicit "Label values are NOT secrets" advisory paragraph + cross-reference to the existing `env =` field for actual secrets handling. |
| T-17-05-T | Tampering (Display) | Mermaid diagram fails to render on GitHub or operator's local viewer, leaving a confusing code block | accept | LOW — GitHub natively renders mermaid in markdown since 2022; project rule D-07 already enforces mermaid project-wide so this surface is well-tested. If a viewer doesn't render mermaid, the code block is still readable plain text. |

No new attack surface; documentation-grade content.
</threat_model>

<verification>
- `grep -c '### Labels' README.md` returns at least `1` (subsection added)
- `grep -c 'mermaid' README.md` returns at least `1` (diagram fence — per D-07 enforcement)
- `awk '/```mermaid/,/```/' README.md | grep -c 'flowchart\|graph'` returns at least `1` (mermaid block is well-formed)
- The 3-row merge-semantics table is GFM-formatted and renders on GitHub
- Reserved-namespace rule, type-gate rule, size limits, env-var interpolation rule, and "values are not secrets" advisory are all present
- All other README sections (security, quickstart, monitoring, troubleshooting, etc.) are unchanged — diff scope is bounded to inside `## Configuration`
- `just fmt-check` exits 0 (no source code change; this is a sanity gate)
</verification>

<success_criteria>
- [ ] `### Labels` subsection present inside `## Configuration` section of README.md
- [ ] Mermaid merge-precedence diagram present (4-step chain: defaults → per-job → cronduit-internal → bollard)
- [ ] 3-row merge-semantics table present
- [ ] Reserved-namespace rule (`cronduit.*`) documented with example
- [ ] Type-gate rule (docker-only) documented with example
- [ ] Size limits (4 KB / 32 KB) documented
- [ ] Env-var interpolation rule (values yes, keys no) documented with example
- [ ] "Label values are NOT secrets" advisory paragraph present
- [ ] Cross-reference to `examples/cronduit.toml` for integration patterns
- [ ] All other README sections unchanged
- [ ] No source-code changes; documentation-only diff
</success_criteria>

<output>
After completion, create `.planning/phases/17-custom-docker-labels-seed-001/17-05-SUMMARY.md`
</output>
