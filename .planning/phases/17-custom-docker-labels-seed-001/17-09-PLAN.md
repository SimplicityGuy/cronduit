---
phase: 17
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [LBL-01, LBL-02, LBL-03, LBL-04, LBL-05, LBL-06]
must_haves:
  truths:
    - "After this plan ships, `.planning/REQUIREMENTS.md` lines 186-191 show LBL-01..LBL-06 with `Complete` status (mirroring the FCTX-04 / FCTX-07 `Complete` pattern at lines 195/198) — bookkeeping consistent with the shipped state of Phase 17"
    - "The Phase column for each LBL-XX row continues to point at `17` (correct phase attribution; this plan does NOT change phase mappings)"
    - "No other rows in the requirements table are modified"
    - "The body of REQUIREMENTS.md outside the LBL-01..LBL-06 status table rows is untouched"
    - "Per D-06: changes land via PR on `phase-17-custom-docker-labels` feature branch (no direct main commits)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated tracking table with LBL-01..LBL-06 flipped from Pending to Complete"
      contains: "LBL-01    | 17    | Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md status table"
      to: "Phase 17 verification + UAT outcomes (17-VERIFICATION.md + 17-HUMAN-UAT.md)"
      via: "tracking-table mirrors the actual shipped + verified state per the FCTX-04 / FCTX-07 precedent"
      pattern: "Complete"
---

<objective>
Close the bookkeeping drift flagged by the verifier (17-VERIFICATION.md line 131:
"Tracking-table drift (Info): REQUIREMENTS.md lines 186-191 still show
LBL-01..LBL-06 as `Pending`; should be flipped to `Complete` (mirroring FCTX-04
/ FCTX-07 at lines 195/198). Not a correctness gap — bookkeeping.").

Phase 17 has shipped (Plans 17-01..17-06 landed; UAT passed by maintainer
2026-04-29; verification flagged only CR-01 + CR-02 docs/wording gaps which
plans 17-07 + 17-08 close). The LBL-01..LBL-06 requirements are functionally
satisfied per the verifier's `requirements coverage` table at lines 120-129 of
17-VERIFICATION.md (LBL-01/02/03/06 SATISFIED; LBL-04/05 PARTIAL pending the
gap-closure plans 17-07/17-08 — but those plans do NOT introduce new
requirement IDs, they close attribution + documentation gaps within the
existing LBL-04 + LBL-05 requirements).

Per the v1.0 close-out precedent and the FCTX-04 / FCTX-07 pattern at
REQUIREMENTS.md lines 195/198, requirements move to `Complete` when the
phase ships and the verification report shows the requirement as SATISFIED
or PARTIAL-with-gap-closure-planned. The final flip to `Validated` is the
v1.2 milestone close-out's job (Phase 24).

Purpose: Keep planning tracking in sync with the shipped state. Failure to flip
introduces freshness landmines for future maintainers reading the tracking
table during v1.2 close-out.

Output:
- `.planning/REQUIREMENTS.md` (six rows: LBL-01..LBL-06 Status column flipped from
  `Pending` to `Complete`)

This is a planning-artifact-only plan. No code changes. No tests. Two-line
diff per row × 6 rows = single-cell-update × 6 in the table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-custom-docker-labels-seed-001/17-VERIFICATION.md
@.planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md

<interfaces>
The current state of REQUIREMENTS.md lines 186-191:

```markdown
| LBL-01    | 17    | Pending |
| LBL-02    | 17    | Pending |
| LBL-03    | 17    | Pending |
| LBL-04    | 17    | Pending |
| LBL-05    | 17    | Pending |
| LBL-06    | 17    | Pending |
```

The precedent pattern at lines 172-174 + 195/198 (FOUND-14, FOUND-15, FOUND-16,
FCTX-04, FCTX-07 — already-shipped phases):

```markdown
| FOUND-14  | 16    | Complete |
| FOUND-15  | 15    | Complete |
| FOUND-16  | 15    | Complete |
...
| FCTX-04   | 16    | Complete |
...
| FCTX-07   | 16    | Complete |
```

Target state of REQUIREMENTS.md lines 186-191 after this plan:

```markdown
| LBL-01    | 17    | Complete |
| LBL-02    | 17    | Complete |
| LBL-03    | 17    | Complete |
| LBL-04    | 17    | Complete |
| LBL-05    | 17    | Complete |
| LBL-06    | 17    | Complete |
```

The column widths matter: the existing rows use `Complete` (7 chars + trailing
spaces) padded to a column width that matches the table header. Verify the
exact column padding by reading the FOUND-14 line and matching the same
spacing pattern. (Inspecting `cat -A` output of the file is the safest way to
confirm tab vs space alignment, but markdown tables in this file use spaces, so
`grep` + visual inspection is sufficient.)

No phase mapping changes. No requirement IDs added or removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip LBL-01..LBL-06 status from Pending to Complete in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 165-210 — confirm current shape of the status table; the LBL-XX rows are at lines 186-191; FCTX-04 and FCTX-07 at lines 195/198 are the `Complete`-pattern templates)
    - .planning/phases/17-custom-docker-labels-seed-001/17-VERIFICATION.md (lines 119-131 — the requirements-coverage table that justifies the flip; "Tracking-table drift" note at line 131)
    - .planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md (D-06 — PR-only workflow; this single-file edit lands via PR)
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <behavior>
    - Six rows updated: LBL-01, LBL-02, LBL-03, LBL-04, LBL-05, LBL-06
    - Each row's Status column changes from `Pending` to `Complete`
    - Column padding matches the existing `Complete` rows (FOUND-14, FCTX-04, FCTX-07)
    - The Phase column for each LBL-XX row is unchanged (`17`)
    - No other rows in the table are modified
    - No other section of REQUIREMENTS.md is touched
  </behavior>
  <action>
    Implements the bookkeeping flip flagged at 17-VERIFICATION.md line 131.

    **Step 1 — Read the current file** to confirm exact line numbers and column
    padding. The lines should look like:

    ```
    | LBL-01    | 17    | Pending |
    | LBL-02    | 17    | Pending |
    | LBL-03    | 17    | Pending |
    | LBL-04    | 17    | Pending |
    | LBL-05    | 17    | Pending |
    | LBL-06    | 17    | Pending |
    ```

    **Step 2 — Use the Edit tool to update each row.** For each of the six rows,
    change `| Pending |` to `| Complete |`. Verbatim before/after:

    | Line | Before | After |
    |------|--------|-------|
    | 186  | `\| LBL-01    \| 17    \| Pending \|` | `\| LBL-01    \| 17    \| Complete \|` |
    | 187  | `\| LBL-02    \| 17    \| Pending \|` | `\| LBL-02    \| 17    \| Complete \|` |
    | 188  | `\| LBL-03    \| 17    \| Pending \|` | `\| LBL-03    \| 17    \| Complete \|` |
    | 189  | `\| LBL-04    \| 17    \| Pending \|` | `\| LBL-04    \| 17    \| Complete \|` |
    | 190  | `\| LBL-05    \| 17    \| Pending \|` | `\| LBL-05    \| 17    \| Complete \|` |
    | 191  | `\| LBL-06    \| 17    \| Pending \|` | `\| LBL-06    \| 17    \| Complete \|` |

    Each replacement is a single `Pending` -> `Complete` substitution within the
    LBL-XX row. Since `Pending` is exactly 7 characters and `Complete` is 8
    characters, the right-side `|` shifts by one space; this matches the
    existing `Complete` rows (FCTX-04, FCTX-07) — visual inspection of those
    rows confirms the column has accommodation for `Complete`.

    Use the Edit tool with `replace_all: false` (default) and apply six separate
    Edit calls — one per row — to keep the diff narrow and reviewable.
    Alternatively, the executor MAY use a single `sed` invocation if the
    Edit-per-row approach is too verbose: `sed -i '' '186,191 s/| Pending |/| Complete |/' .planning/REQUIREMENTS.md` (BSD sed syntax for macOS; on Linux drop the empty `''` argument). The single-file invariant of this plan means either approach is acceptable.

    **Step 3 — Verify the diff is exactly 6 lines changed (or 12 if counting
    `-`/`+` separately):**

    ```bash
    git diff --stat .planning/REQUIREMENTS.md
    # 1 file changed, 6 insertions(+), 6 deletions(-)
    ```

    **Step 4 — Verify no other section was touched:**

    ```bash
    git diff .planning/REQUIREMENTS.md | grep -E '^[-+]' | grep -v '^[-+]{3} ' | wc -l
    # Should be 12 (6 minus-lines + 6 plus-lines)
    ```

    **Step 5 — Verify the post-state directly:**

    ```bash
    grep -E '^\| LBL-0[1-6]\s+\| 17\s+\| Complete \|' .planning/REQUIREMENTS.md | wc -l
    # Should be 6
    grep -E '^\| LBL-0[1-6]\s+\| 17\s+\| Pending \|' .planning/REQUIREMENTS.md | wc -l
    # Should be 0
    ```

    Per D-06 (PR-only): changes land on `phase-17-custom-docker-labels` branch.
    Per D-07 (mermaid only) — not applicable; no diagrams in this edit.
    Per D-08 (UAT just recipes) — not applicable; this is a planning-artifact
    edit, no UAT step needed.
  </action>
  <verify>
    <automated>grep -cE '^\| LBL-0[1-6]\s+\| 17\s+\| Complete \|' .planning/REQUIREMENTS.md | grep -q '^6$' &amp;&amp; grep -cE '^\| LBL-0[1-6]\s+\| 17\s+\| Pending \|' .planning/REQUIREMENTS.md | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| LBL-0[1-6]\s+\| 17\s+\| Complete \|' .planning/REQUIREMENTS.md` returns `6` (all six LBL-XX rows show Complete)
    - `grep -cE '^\| LBL-0[1-6]\s+\| 17\s+\| Pending \|' .planning/REQUIREMENTS.md` returns `0` (no LBL-XX row still shows Pending)
    - `grep -c '^| LBL-01    | 17    | Complete |' .planning/REQUIREMENTS.md` returns `1` (exact line shape preserved)
    - `grep -c '^| FCTX-04   | 16    | Complete |' .planning/REQUIREMENTS.md` returns `1` (the FCTX-04 precedent row is unchanged — sanity check that the edit didn't bleed into other rows)
    - `grep -c '^| FCTX-07   | 16    | Complete |' .planning/REQUIREMENTS.md` returns `1` (FCTX-07 precedent row unchanged)
    - `grep -c '^| WH-01     | 18    | Pending |' .planning/REQUIREMENTS.md` returns `1` (WH-01 row unchanged — phase 18 still pending; sanity check that the edit didn't bleed into the WH rows)
    - `git diff --stat .planning/REQUIREMENTS.md` shows exactly 1 file changed, 6 insertions, 6 deletions
    - `git diff .planning/REQUIREMENTS.md | grep -cE '^\+\| LBL-0[1-6]'` returns `6` (six new lines added)
    - `git diff .planning/REQUIREMENTS.md | grep -cE '^-\| LBL-0[1-6]'` returns `6` (six old lines removed)
  </acceptance_criteria>
  <done>LBL-01..LBL-06 rows in REQUIREMENTS.md status table show Complete; tracking-table drift closed; no other rows or sections modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/REQUIREMENTS.md → maintainer (read) | Trusted — planning artifact; correctness is critical to milestone close-out audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09-T | Tampering | Edit accidentally bleeds into adjacent rows (FCTX-* or WH-* lines) and corrupts unrelated tracking rows | mitigate | Acceptance criteria pin the FCTX-04, FCTX-07, and WH-01 row shapes — any unintended edit to those rows fails the acceptance check. The narrow-diff acceptance criteria (`6 insertions(+), 6 deletions(-)`) further bound the blast radius. |

No new attack surface. Single-file planning-artifact edit; no code touched.
</threat_model>

<verification>
- All six LBL-XX rows show `Complete` status
- No LBL-XX row still shows `Pending`
- The FCTX-04, FCTX-07, and WH-01 rows are unchanged (no bleed-edits)
- `git diff --stat` shows exactly 1 file changed with 6 insertions + 6 deletions
- The body of REQUIREMENTS.md outside the LBL-XX rows is untouched
</verification>

<success_criteria>
- [ ] LBL-01 status: Complete
- [ ] LBL-02 status: Complete
- [ ] LBL-03 status: Complete
- [ ] LBL-04 status: Complete
- [ ] LBL-05 status: Complete
- [ ] LBL-06 status: Complete
- [ ] No LBL-XX row still says Pending
- [ ] Adjacent rows (FCTX-*, WH-*) unchanged
- [ ] Diff is exactly 1 file, 6 insertions, 6 deletions
</success_criteria>

<output>
After completion, create `.planning/phases/17-custom-docker-labels-seed-001/17-09-SUMMARY.md`. The SUMMARY should be brief (this is a 6-line bookkeeping flip):
- Reference the verification info-level item closed (17-VERIFICATION.md line 131)
- Note that the FCTX-04 / FCTX-07 precedent at lines 195/198 was the structural template
- Cross-reference 17-VERIFICATION.md and the verifier's requirements-coverage table at lines 119-129 (which justifies why the flip is appropriate now)
- Note the final `Validated` flip is Phase 24's responsibility (v1.2 milestone close-out), not this plan's
</output>
