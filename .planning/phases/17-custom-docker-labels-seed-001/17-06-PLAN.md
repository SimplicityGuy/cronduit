---
phase: 17
plan: 06
type: execute
wave: 4
depends_on: [17-03, 17-04, 17-05]
files_modified:
  - .planning/seeds/SEED-001-custom-docker-labels.md
  - .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md
autonomous: false
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "After this plan ships, `.planning/seeds/SEED-001-custom-docker-labels.md` frontmatter shows `status: realized` (no longer `dormant`) — establishes the project's first realized-seed pattern (D-05)"
    - "The realized seed retains its physical path at `.planning/seeds/SEED-001-custom-docker-labels.md` (no file move) — D-05 explicitly rejected the `realized/` subdir move as premature for the first realized seed"
    - "Frontmatter contains `realized_in: phase-17`, `milestone: v1.2`, and `realized_date: <ISO YYYY-MM-DD>` so future maintainers have a clean audit trail"
    - "A maintainer-facing UAT checklist (`17-HUMAN-UAT.md`) is produced covering README rendering verification, examples/cronduit.toml parse-pass, and end-to-end docker label spot-check — every step references an existing `just` recipe (D-08); maintainer (user) runs locally and validates (D-09)"
    - "D-06: Phase 17 lands as a single PR on the `phase-17-custom-docker-labels` feature branch — confirmed at PR-merge time. No commit in this phase touches `main` directly. (Project rule, applies project-wide; this plan is the close-out where the rule is operationally observed.)"
    - "D-10: No `Cargo.toml` `version` field change occurs across any of plans 17-01..17-06 — Phase 15 Plan 15-01 already shipped `1.2.0`; the v1.2 development window keeps the binary at `1.2.0` until rc tags / final ship. (Project rule, applies project-wide; this plan is the close-out where the rule is operationally observed via `git diff main..HEAD -- Cargo.toml | grep '^[-+]version' | wc -l` returning 0.)"
  artifacts:
    - path: ".planning/seeds/SEED-001-custom-docker-labels.md"
      provides: "Frontmatter promoted dormant → realized with realized_in / milestone / realized_date fields"
      contains: "status: realized"
    - path: ".planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md"
      provides: "Maintainer UAT checklist — every step references a `just` recipe (D-08); maintainer validates (D-09)"
      contains: "just check-config"
  key_links:
    - from: ".planning/seeds/SEED-001-custom-docker-labels.md frontmatter"
      to: "Future seed-close-out phases (template for next seed)"
      via: "this is the first realized-seed pattern; future maintainers inherit this shape (D-05)"
      pattern: "status: realized"
    - from: "17-HUMAN-UAT.md UAT items"
      to: "existing `just` recipes in justfile"
      via: "D-08: every UAT step cites a just recipe (`just check-config <PATH>`, `just docker-compose-up`, etc.)"
      pattern: "just "
---

<objective>
Close the SEED-001 lifecycle. Phase 17 has shipped (Plans 17-01 through 17-05
land the schema, validators, bollard plumb, examples, and README); this plan
performs the ceremony: edit `.planning/seeds/SEED-001-custom-docker-labels.md`
frontmatter to promote `status: dormant` → `status: realized` and add the
`realized_in`, `milestone`, `realized_date` audit-trail fields. Optionally
produce a maintainer-facing UAT checklist (`17-HUMAN-UAT.md`) referencing
existing `just` recipes for the manual smoke validation the user owns.

Purpose: This is the project's FIRST realized-seed close-out (per CONTEXT.md
D-05). The pattern this plan establishes — frontmatter-edit-only, file stays
in place — becomes the template every future realized-seed phase inherits.
Getting this right matters more than the change is large.

Output:
- One modified file (`.planning/seeds/SEED-001-custom-docker-labels.md`) — frontmatter only
- One new file (`.planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md`) — maintainer UAT checklist

This plan has a `checkpoint:human-verify` task (the UAT validation) — D-09
explicitly says UAT is validated by the maintainer running `just` recipes
locally, NEVER marked passed from Claude's automated runs. So `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md
@.planning/phases/17-custom-docker-labels-seed-001/17-RESEARCH.md
@.planning/phases/17-custom-docker-labels-seed-001/17-VALIDATION.md
@.planning/phases/17-custom-docker-labels-seed-001/17-03-SUMMARY.md
@.planning/phases/17-custom-docker-labels-seed-001/17-04-SUMMARY.md
@.planning/phases/17-custom-docker-labels-seed-001/17-05-SUMMARY.md
@.planning/seeds/SEED-001-custom-docker-labels.md
@justfile

<interfaces>
Existing seed file frontmatter (lines 1-7 of `.planning/seeds/SEED-001-custom-docker-labels.md`):
```yaml
---
id: SEED-001
status: dormant
planted: 2026-04-24
planted_during: between v1.1 and v1.2 — milestone close-out for v1.1 just completed
trigger_when: v1.2 milestone kickoff during the requirements pass (operator must explicitly raise this seed at `/gsd-new-milestone` for v1.2; do not auto-promote)
scope: Small
---
```

Target frontmatter shape (after THIS plan edits):
```yaml
---
id: SEED-001
status: realized
planted: 2026-04-24
planted_during: between v1.1 and v1.2 — milestone close-out for v1.1 just completed
trigger_when: v1.2 milestone kickoff during the requirements pass (operator must explicitly raise this seed at `/gsd-new-milestone` for v1.2; do not auto-promote)
scope: Small
realized_in: phase-17
milestone: v1.2
realized_date: <ISO YYYY-MM-DD — set to actual edit date>
---
```

Existing `just` recipes (from justfile scan; planner-verified subset):
- `just check-config <PATH>` (config validation — argument-taking recipe; verified via `grep -E '^[a-z]' justfile`. NOTE: there is no bare `just check` recipe — BLOCKER #1.)
- `just test` (unit suite)
- `just nextest` (faster test runner)
- `just clippy`
- `just fmt-check`
- `just ci` (composite: fmt-check + clippy + openssl-check + nextest + schema-diff + image)
- `just docker-compose-up` (end-to-end with examples/cronduit.toml under docker compose)
- `just metrics-check`
- `just reload`
- `just health`

If a recipe needed for a UAT step doesn't exist, the planner MAY ask the user to add it before this plan executes (out-of-scope to add new recipes IN this plan — D-08 says UAT steps reference EXISTING recipes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote SEED-001 frontmatter from dormant to realized</name>
  <read_first>
    - .planning/seeds/SEED-001-custom-docker-labels.md (read full — confirm current frontmatter shape; the body of the seed file is unchanged by this task — only the YAML frontmatter at the top changes)
    - .planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md (D-05 verbatim — exact field-name list and rejected alternatives)
    - .planning/phases/17-custom-docker-labels-seed-001/17-03-SUMMARY.md / 17-04-SUMMARY.md / 17-05-SUMMARY.md (confirm prior plans are landed before realizing the seed)
  </read_first>
  <files>
    .planning/seeds/SEED-001-custom-docker-labels.md
  </files>
  <behavior>
    - Frontmatter `status` value changes from `dormant` to `realized`
    - Three NEW frontmatter fields appended (placement: planner picks a stable order; suggested order: AFTER `scope`, in the order `realized_in`, `milestone`, `realized_date`):
      - `realized_in: phase-17`
      - `milestone: v1.2`
      - `realized_date: <YYYY-MM-DD>` (use the date this commit is authored — the executor inserts the actual ISO date at edit time; do NOT hard-code a placeholder string)
    - Body of the seed file is UNCHANGED (the "Why This Matters", "When to Surface", "Specific Ideas", and any other sections continue to read as they did in the dormant state — they are now historical record)
    - File stays at `.planning/seeds/SEED-001-custom-docker-labels.md` — D-05 explicitly rejects the move to `.planning/seeds/realized/` as premature for the first realized seed
  </behavior>
  <action>
    Implements D-05 (CONTEXT.md). One file, frontmatter-only edit.

    **Step 1 — Read the current frontmatter** at the top of `.planning/seeds/SEED-001-custom-docker-labels.md`. The lines `1-7` (or whatever range the current `---` ... `---` fence covers) contain:
    ```yaml
    ---
    id: SEED-001
    status: dormant
    planted: 2026-04-24
    planted_during: ...
    trigger_when: ...
    scope: Small
    ---
    ```

    **Step 2 — Edit ONLY the frontmatter.** Two changes:

    **Change A:** Change `status: dormant` to `status: realized`.

    **Change B:** Append three new fields to the frontmatter, AFTER `scope: Small` and BEFORE the closing `---` fence. The new lines:
    ```yaml
    realized_in: phase-17
    milestone: v1.2
    realized_date: <INSERT YYYY-MM-DD AT EDIT TIME — e.g. if the executor edits this on 2026-04-28, the value is "2026-04-28">
    ```

    The executor MUST replace the placeholder `<INSERT YYYY-MM-DD AT EDIT TIME>` with the actual current ISO date. To get the current date programmatically: `date -u +%Y-%m-%d` (UTC) — that's the value to insert. Do NOT leave the angle-bracket placeholder in the file. Do NOT hard-code `2026-04-28` (the planner does not know the executor's commit date).

    **Step 3 — Confirm the body of the seed file is unchanged.** A `git diff .planning/seeds/SEED-001-custom-docker-labels.md` should show ONLY frontmatter changes — no `+`/`-` lines outside the `---` ... `---` fence at the top.

    **Step 4 — Confirm the file is NOT moved.** Do NOT execute `git mv` or any rename. D-05 rejects the move to `.planning/seeds/realized/` as premature.

    Per D-06 (PR-only on feature branch — already on `phase-17-custom-docker-labels`).
    Per D-07 (mermaid only) — not applicable; no diagrams in YAML frontmatter.
  </action>
  <verify>
    <automated>grep -c '^status: realized' .planning/seeds/SEED-001-custom-docker-labels.md &amp;&amp; grep -c '^realized_in: phase-17' .planning/seeds/SEED-001-custom-docker-labels.md &amp;&amp; grep -c '^milestone: v1.2' .planning/seeds/SEED-001-custom-docker-labels.md &amp;&amp; python3 -c "from datetime import date; date.fromisoformat(open('.planning/seeds/SEED-001-custom-docker-labels.md').read().split('realized_date: ')[1].split()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: realized' .planning/seeds/SEED-001-custom-docker-labels.md` returns `1`
    - `grep -c '^status: dormant' .planning/seeds/SEED-001-custom-docker-labels.md` returns `0` (the dormant line is gone)
    - `grep -c '^realized_in: phase-17' .planning/seeds/SEED-001-custom-docker-labels.md` returns `1`
    - `grep -c '^milestone: v1.2' .planning/seeds/SEED-001-custom-docker-labels.md` returns `1`
    - **WARNING #6 fix (calendar-valid date)** — `python3 -c "from datetime import date; date.fromisoformat(open('.planning/seeds/SEED-001-custom-docker-labels.md').read().split('realized_date: ')[1].split()[0])"` exits 0 (replaces the over-permissive `20[0-9]{2}-[0-9]{2}-[0-9]{2}` regex which would accept e.g. `2026-13-99` — the python check enforces real calendar validity)
    - **WARNING #6 fix (same-day check on executor's run)** — `[ "$(grep '^realized_date:' .planning/seeds/SEED-001-custom-docker-labels.md | awk '{print $2}')" = "$(date -u +%Y-%m-%d)" ]` exits 0 — only valid on the day the executor runs the task. If the maintainer re-runs UAT later and this check fails, document the staleness in 17-HUMAN-UAT.md (the date was set at execution time and is not expected to match later UAT runs; that is the intended behavior)
    - The `realized_date` value matches the literal regex shape `20[0-9]{2}-[0-9]{2}-[0-9]{2}` (sanity-check on shape; the calendar-validity check above is the load-bearing check)
    - `grep -c '<INSERT' .planning/seeds/SEED-001-custom-docker-labels.md` returns `0` (no placeholder leaked)
    - `git diff --stat .planning/seeds/SEED-001-custom-docker-labels.md` shows exactly 1 file changed
    - `ls .planning/seeds/realized/` exits non-zero (the realized/ subdir does NOT exist — D-05 rejected the move)
    - `ls .planning/seeds/SEED-001-custom-docker-labels.md` shows the file at its original path
  </acceptance_criteria>
  <done>SEED-001 frontmatter promoted to realized with audit-trail fields populated; file location unchanged; body of the seed file untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author the maintainer UAT checklist (17-HUMAN-UAT.md)</name>
  <read_first>
    - justfile (full — confirm exact recipe names; only EXISTING recipes may be cited per D-08)
    - .planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md (D-08, D-09 — UAT references just recipes; maintainer validates, never Claude)
    - .planning/phases/17-custom-docker-labels-seed-001/17-VALIDATION.md (sampling rate, manual UAT items the validation strategy already identifies)
    - examples/cronduit.toml (post-Plan-17-04 — the file the operator validates parses)
    - README.md (post-Plan-17-05 — the section the operator visually reviews)
  </read_first>
  <files>
    .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md
  </files>
  <behavior>
    - File is a markdown checklist titled "Phase 17 Human UAT — SEED-001 Custom Docker Labels"
    - Every UAT step references an EXISTING just recipe (D-08); no ad-hoc cargo / docker / curl invocations
    - Steps cover: README labels subsection visual-review, examples/cronduit.toml parse-pass, end-to-end docker label spot-check (run cronduit, confirm operator labels reach the container)
    - Header section makes clear: maintainer (user) runs each step locally and ticks the box (D-09); Claude does NOT mark UAT passed from automated runs
  </behavior>
  <action>
    Implements D-08 + D-09 (CONTEXT.md). New file: `.planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md`.

    **Step 1 — Verify just recipe names BEFORE writing the file (BLOCKER #1 from plan-checker review).** The planner has VERIFIED the following recipes exist in `justfile` (via `grep -E '^[a-z]' justfile`): `default`, `ci`, `release`, `build`, `build-release`, `clean`, `tailwind`, `image`, `image-local`, `image-check`, `image-push`, `fmt`, `fmt-check`, `clippy`, `test`, `nextest`, `install-targets`, `openssl-check`, `grep-no-percentile-cont`, `deny`, `db-reset`, `migrate`, `sqlx-prepare`, `schema-diff`, `uat-fctx-bugfix-spot-check`, `dev`, `dev-ui`, **`check-config PATH`** (NOTE: argument-taking — not bare `just check`), `docker-compose-up`, `update-cargo`, `update-hooks`, `compose-up-rc3`, `reload`, `health`, `metrics-check`. Recipes that DO NOT EXIST and MUST NOT be cited: `check` (use `check-config <PATH>`), `test-quick` (use `test` or inline `cargo nextest run --lib` as a sub-step, NOT cited as a `just` recipe), `test-ignored` (no equivalent — use inline `cargo test --features integration -- --ignored` as a sub-step, NOT cited as a `just` recipe), `docs-preview` (no equivalent — document `view-on-github` or `cat README.md` as the manual step without `just` prefix), `docker-compose-down` (no equivalent — document `docker compose -f examples/docker-compose.yml down` as a sub-step without `just` prefix). If any UAT step requires a non-existent recipe, the executor must (a) substitute an existing one, OR (b) ask the maintainer to add the recipe before this plan executes (out-of-scope to add new recipes IN this plan; D-08 says UAT steps cite EXISTING recipes).

    **Step 2 — Write the UAT file.** Use the structure below. Replace the `<DOCKER_INSPECT_NOTE>` placeholder with the actual `docker inspect` step note that the maintainer follows after `just docker-compose-up` — but the COMMAND citation in the UAT step header must be the just recipe, not the bare `docker inspect`. The `docker inspect` is a sub-step in the verification narrative, not the cited recipe.

    Full file content:

    ```markdown
    # Phase 17 — Human UAT (SEED-001 Custom Docker Labels)

    **Owner:** Maintainer (Robert)
    **Validation rule (D-09):** Every checkbox below MUST be ticked by a human running the cited `just` recipe locally. Claude MUST NOT mark these steps complete from automated CI or its own ephemeral runs. The phase is NOT done until every box below is ticked by the maintainer.

    **Recipe rule (D-08):** Every step cites an existing `just` recipe — no ad-hoc `cargo` / `docker` / `curl` invocations. If a step description appears to require an ad-hoc command, file it as a project-rule deviation and stop.

    ---

    ## UAT Checklist

    - [ ] **U1 — README labels subsection renders correctly on GitHub.**
      - **Recipe:** None — visual review of `README.md` after PR is open.
      - **Steps:**
        1. After the PR is opened, navigate to the PR's "Files changed" tab.
        2. Find the `README.md` diff and confirm the rendered preview shows the new `### Labels` subsection.
        3. Confirm the mermaid merge-precedence diagram renders as an SVG (not as raw fenced text).
        4. Confirm the 3-row merge-semantics table renders as a real markdown table (header row + separator + 3 data rows).
        5. Confirm the four code blocks (`cronduit.foo` example, `team = "ops"` example, `${DEPLOYMENT_ID}` example, `${TEAM}` example) render as syntax-highlighted TOML.
      - **Pass criteria:** Mermaid diagram renders; table renders; code blocks render. NO ASCII art anywhere (D-07).

    - [ ] **U2 — examples/cronduit.toml parses + validates clean.**
      - **Recipe:** `just check-config examples/cronduit.toml` (BLOCKER #1: verified existing recipe; bare `just check` does NOT exist — `check-config` takes a `PATH` argument)
      - **Steps:**
        1. From a clean working tree on the `phase-17-custom-docker-labels` branch, run `just check-config examples/cronduit.toml`.
        2. Confirm the recipe exits 0.
        3. Visually scan `examples/cronduit.toml` for the three integration patterns: `[defaults].labels` Watchtower line, `hello-world` Traefik labels, NEW `isolated-batch` job with `use_defaults = false`.
      - **Pass criteria:** Recipe exits 0; all three integration patterns visible in the file.

    - [ ] **U3 — Full unit + integration test suite passes.**
      - **Recipe:** `just nextest` (faster) OR `just test` (standard)
      - **Steps:**
        1. Run `just nextest` (or `just test` if nextest isn't available locally).
        2. Confirm the recipe exits 0.
        3. Confirm the test count visibly reflects the new tests added in Plans 17-01 (parity, hash-differs, merge tests) and 17-02 (12+ validator tests).
      - **Pass criteria:** Recipe exits 0; new tests visible in output.

    - [ ] **U4 — Lint + format gates pass.**
      - **Recipe:** `just clippy && just fmt-check` (or `just ci` — the composite recipe).
      - **Steps:**
        1. Run `just clippy`.
        2. Run `just fmt-check`.
        3. Confirm both recipes exit 0.
      - **Pass criteria:** Both recipes exit 0.

    - [ ] **U5 — End-to-end docker labels spot-check.**
      - **Recipe:** `just docker-compose-up` (start cronduit + a docker job from `examples/cronduit.toml`)
      - **Steps:**
        1. Run `just docker-compose-up` to launch cronduit with the example config.
        2. Wait for the `hello-world` job to fire (`*/5 * * * *` schedule — up to 5 minutes; check the cronduit web UI at `http://localhost:8080` for run status).
        3. After the run starts, identify the spawned container ID via `docker ps --filter label=cronduit.job_name=hello-world` (this is a sub-step verification command, NOT a cited recipe — the cited recipe is `just docker-compose-up`).
        4. Run `docker inspect <container-id> | jq '.[0].Config.Labels'` and confirm:
           - `cronduit.run_id` and `cronduit.job_name` are present (cronduit-internal labels)
           - `com.centurylinklabs.watchtower.enable: "false"` is present (inherited from `[defaults].labels`)
           - `traefik.enable: "true"` is present (per-job label on hello-world)
           - `traefik.http.routers.hello.rule: "Host(`hello.local`)"` is present (per-job label, backticks preserved)
        5. Tear down with the inline sub-step `docker compose -f examples/docker-compose.yml down` (BLOCKER #1: there is no `just docker-compose-down` recipe; the existing `docker-compose-up` recipe has no down-equivalent, so the down command is documented here as a literal `docker compose ... down` sub-step, NOT a `just` recipe — note `docker compose` is the modern hyphenless subcommand).
      - **Pass criteria:** All four label categories visible on the spawned container — confirms the LBL-01 / LBL-02 / SC-1 / SC-2 contract end-to-end.

    - [ ] **U6 — Reserved namespace rejection error message is operator-friendly.**
      - **Recipe:** `just check-config /tmp/cronduit-bad.toml` (BLOCKER #1: the verified existing recipe takes a PATH argument; the doctored copy lives at `/tmp/cronduit-bad.toml`)
      - **Steps:**
        1. Make a temporary copy of `examples/cronduit.toml` (`cp examples/cronduit.toml /tmp/cronduit-bad.toml`).
        2. Edit the copy: add `labels = { "cronduit.foo" = "bar" }` to the `[[jobs]] hello-world` block (per-job, not [defaults]).
        3. Run `just check-config /tmp/cronduit-bad.toml` against the doctored copy.
        4. Confirm the error mentions: the offending key (`cronduit.foo`), the rule (`reserved namespace`), and the job name (`hello-world`).
        5. The in-tree `examples/cronduit.toml` was NOT modified (the test edits a /tmp copy); no restore needed.
      - **Pass criteria:** Error message contains all three pieces of context; readable without consulting source.

    ---

    ## After All Boxes Ticked

    - The maintainer comments on the PR with `UAT passed` (or equivalent) once every box above is ticked.
    - `gsd-execute-phase` (or the orchestrator) treats the phase as complete only after the human-validation comment lands.
    - SEED-001's frontmatter is already at `status: realized` (Task 1 of this plan) — no further seed-state action needed.
    ```

    Per D-06 (PR-only on feature branch).
    Per D-08 (UAT recipes only).
    Per D-09 (maintainer validates, NOT Claude).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md &amp;&amp; grep -c '^- \[ \]' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md &amp;&amp; grep -c 'just ' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md &amp;&amp; grep -c 'D-09' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` succeeds
    - `grep -c '^- \[ \]' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `5` (5+ UAT items: U1..U5+ minimum)
    - `grep -c 'just ' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `5` (each UAT step cites a just recipe per D-08)
    - `grep -c 'D-09' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `1` (the maintainer-validates rule is explicitly stated)
    - `grep -c 'D-08' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `1` (the just-recipe rule is explicitly stated)
    - `grep -c '^# Phase 17' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `1` (file has a clear title)
    - `grep -c 'Maintainer' .planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` returns at least `1` (owner attribution)
  </acceptance_criteria>
  <done>UAT checklist authored with 5+ items, every step cites an existing just recipe, the maintainer-validates rule is documented, and the file location matches phase convention.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer runs the 17-HUMAN-UAT.md checklist locally and confirms each box ticked</name>
  <what-built>
    - Plan 17-01: schema + 5-layer parity + apply_defaults merge (LBL-01 + LBL-02)
    - Plan 17-02: four LOAD-time validators (LBL-03 + LBL-04 + LBL-06 + D-02)
    - Plan 17-03: bollard plumb-through + 3 testcontainers integration tests (LBL-01 + LBL-02 + LBL-05)
    - Plan 17-04: examples/cronduit.toml updates (3 integration patterns visible)
    - Plan 17-05: README § Configuration > Labels subsection with mermaid diagram + 3-row table + 6 documented rules
    - Plan 17-06 Task 1: SEED-001 frontmatter promoted dormant → realized
    - Plan 17-06 Task 2: 17-HUMAN-UAT.md authored
  </what-built>
  <how-to-verify>
    Maintainer runs each item in `.planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` locally:

    1. **U1** — Open the PR on GitHub; visually confirm the README labels subsection renders (mermaid as SVG, table as table, code blocks as TOML).
    2. **U2** — Run `just check-config examples/cronduit.toml` and confirm exit 0; visually confirm the three integration patterns in `examples/cronduit.toml`.
    3. **U3** — Run `just nextest` (or `just test`) and confirm exit 0 + new tests visible in output.
    4. **U4** — Run `just clippy && just fmt-check` and confirm exit 0.
    5. **U5** — Run `just docker-compose-up`, wait for `hello-world` to fire, `docker inspect` the spawned container, confirm all four label categories present (cronduit-internal, defaults Watchtower, per-job Traefik labels with backticks).
    6. **U6** — Doctor a config copy with a `cronduit.foo` reserved-namespace label, run `just check-config /tmp/cronduit-bad.toml`, confirm the error mentions the offending key + the rule + the job name.

    After every checkbox is ticked, comment on the PR with `UAT passed` (or equivalent acknowledgment). Until then, the phase is NOT complete per D-09.

    Per D-09: Claude MUST NOT mark this checkpoint passed from automated CI runs of `--ignored` integration tests. The integration tests added in Plan 17-03 (`tests/v12_labels_*.rs`) are CI build-checks; the maintainer validating end-to-end via `docker inspect` is the contract.
  </how-to-verify>
  <resume-signal>Maintainer confirms each box in 17-HUMAN-UAT.md is ticked + comments "UAT passed" (or equivalent) on the PR. Type "approved" with a list of which checkboxes are passing. If any item fails, describe the failure and the orchestrator routes back through `/gsd-plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/seeds/*.md → maintainer (read) | Trusted — seed files are operator-facing planning artifacts |
| 17-HUMAN-UAT.md → maintainer (run locally) | Trusted — UAT cites existing just recipes only (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-T | Tampering | An automated tool or future Claude run marks UAT passed without maintainer validation | mitigate | D-09 explicitly enforced in the UAT file's header; the checkpoint task in this plan has `gate="blocking"` so `gsd-execute-phase` will not proceed past it without a human resume-signal. |
| T-17-06-R | Repudiation | Future maintainer cannot trace WHEN SEED-001 was realized | mitigate | The `realized_date` field added in Task 1 captures the ISO date of the close-out commit. Combined with `realized_in: phase-17` and `milestone: v1.2`, the audit trail is complete. |

No new attack surface; ceremony-grade close-out.
</threat_model>

<verification>
- `.planning/seeds/SEED-001-custom-docker-labels.md` shows `status: realized` and the three new audit-trail fields (`realized_in: phase-17`, `milestone: v1.2`, `realized_date: <ISO>`)
- `realized_date` value is a valid ISO date (`YYYY-MM-DD`) — NOT a placeholder string
- The seed file is at its original path (no move to `realized/` subdir per D-05)
- The body of the seed file is unchanged — only frontmatter touched
- `.planning/phases/17-custom-docker-labels-seed-001/17-HUMAN-UAT.md` exists with 5+ checklist items, each citing a just recipe
- Maintainer (user) ticks every UAT box locally and signals approval — Claude does NOT self-mark the checkpoint passed
- `just fmt-check` exits 0 (no source-code regression — this plan touches only planning artifacts)
</verification>

<success_criteria>
- [ ] SEED-001 frontmatter `status: realized`
- [ ] SEED-001 frontmatter has `realized_in: phase-17`
- [ ] SEED-001 frontmatter has `milestone: v1.2`
- [ ] SEED-001 frontmatter has `realized_date: <ISO YYYY-MM-DD>` (real date, not placeholder)
- [ ] SEED-001 file location unchanged
- [ ] SEED-001 body content unchanged (frontmatter-only diff)
- [ ] `17-HUMAN-UAT.md` exists with 5+ UAT items
- [ ] Every UAT item cites an EXISTING `just` recipe (D-08)
- [ ] D-09 (maintainer-validates rule) is explicit in the UAT file header
- [ ] Maintainer ticks every UAT checkbox locally and comments "UAT passed" on the PR
</success_criteria>

<output>
After completion, create `.planning/phases/17-custom-docker-labels-seed-001/17-06-SUMMARY.md`. The SUMMARY must reference the realized seed (`.planning/seeds/SEED-001-custom-docker-labels.md`) inline and note that this phase establishes the project's first realized-seed close-out pattern (per D-05) — future seed-close phases inherit this shape.
</output>
