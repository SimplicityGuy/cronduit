---
phase: 22
plan: 06
type: execute
wave: 5
depends_on: [22-05]
files_modified:
  - .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md
autonomous: false
requirements: [TAG-01, TAG-02, TAG-03, TAG-04, TAG-05]
requirements_addressed: [TAG-01, TAG-02, TAG-03, TAG-04, TAG-05]
tags: [uat, human-validation, maintainer, tagging]
objective: |
  Write the maintainer-validated UAT runbook
  `.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md`
  with four scenarios per D-10:
  1. Persistence spot-check (TAG-02)
  2. Validator error UX walk (TAG-03 + TAG-04 + TAG-05 + D-08)
  3. Dedup-collapse WARN (TAG-03)
  4. End-to-end webhook backfill (WH-09)

  All scenarios reference the three `just uat-tags-*` recipes from
  Plan 05 (D-11 / project memory `feedback_uat_use_just_commands.md`).
  Each scenario has a maintainer eyeball criterion documenting what
  "good" looks like; Claude does NOT mark UAT passed — only the
  maintainer signs off (project memory `feedback_uat_user_validates.md`).

  This plan's frontmatter declares `autonomous: false` because the
  RUNBOOK CONTENT (the four scenario eyeball criteria) is validated
  by the maintainer in a separate `/gsd-verify-work` session, not by
  this plan's automated tasks. The single task in this plan, however,
  is `type="auto"` and produces a deterministic file-write whose
  acceptance is automated (file existence + grep counts on structural
  invariants). Maintainer execution + sign-off of the runbook
  scenarios is a follow-on session.
must_haves:
  truths:
    - "`.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` exists"
    - "The UAT runbook has exactly four maintainer-validated scenarios per D-10 (persistence, validator UX, dedup WARN, webhook backfill)"
    - "Every scenario step references an existing `just` recipe (D-11 / D-14 / project memory `feedback_uat_use_just_commands.md`) — no ad-hoc `cargo` / `docker` / curl invocations"
    - "Every scenario has at least one clear maintainer-eyeball criterion (e.g., 'confirm the column shows `[\"backup\",\"prod\",\"weekly\"]`'); criteria are specific, not 'looks good'"
    - "The runbook frontmatter contains `autonomous: false` and cites project memory `feedback_uat_user_validates.md`"
    - "The runbook closes WH-09 end-to-end (TOML → validators → DB → webhook payload) — Scenario 4 is the explicit closure step"
    - "Each scenario maps to one or more requirement IDs (TAG-01..05, WH-09) for traceability"
    - "The runbook contains exactly TWO `^---$` lines (the YAML frontmatter open + close delimiters) — verified by deterministic grep"
    - "The runbook contains ZERO `BEGIN_FRONTMATTER` or `END_FRONTMATTER` placeholder strings (the action writes literal `---` delimiters, not HTML-comment placeholders)"
  artifacts:
    - path: .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md
      provides: "Maintainer UAT runbook for Phase 22 (4 scenarios per D-10)"
      contains: "Scenario 1"
  key_links:
    - from: 22-HUMAN-UAT.md scenarios
      to: justfile uat-tags-* recipes (Plan 05 output)
      via: "every scenario step cites a `just uat-tags-*` recipe"
      pattern: "just uat-tags-"
    - from: 22-HUMAN-UAT.md sign-off
      to: maintainer (NOT Claude)
      via: "explicit autonomous: false frontmatter + project-memory citation"
      pattern: "autonomous: false"
---

<objective>
Author the maintainer UAT runbook for Phase 22 — the four scenarios
locked by D-10:

1. **Persistence spot-check** (TAG-02) — write a tagged TOML, run
   cronduit, query the `jobs.tags` column, confirm sorted-canonical
   JSON.
2. **Validator error UX walk** (TAG-03 + TAG-04 + TAG-05 + D-08) —
   for each invalid case (charset, reserved, substring-collision,
   count cap), eyeball each error message for operator readability.
3. **Dedup-collapse WARN** (TAG-03) — confirm the WARN line names
   the original inputs (not just the canonical form).
4. **End-to-end webhook backfill** (WH-09) — configure a webhook on
   a tagged job, fire, confirm the delivered payload contains real
   tag values.

The runbook is the human side of Plan 05's automation. Plan 05 wrote
the `just uat-tags-*` recipes; this plan writes the runbook that
tells the maintainer what to look for when running them.

Purpose: Phase 22 is config + DB only — no UI surface — so the
verification path is "operator runs cronduit, eyeballs error
messages and DB column values, confirms WH-09 closure". The runbook
is the lock for that maintainer signal.

Output: One new markdown file:
`.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md`.
NO code changes; NO test changes (Plan 05 owns those).

**Why `autonomous: false` at the plan level but `type="auto"` at the
task level:**

- The plan-level `autonomous: false` declares: "the runbook CONTENT
  must be executed and signed by the maintainer; Phase 22 PR merge
  blocks until that happens, in a future `/gsd-verify-work` session
  separate from this planning session."
- The task-level `type="auto"` declares: "Claude writes the runbook
  file deterministically in this session; the file's STRUCTURE
  (presence, frontmatter shape, scenario count, recipe-citation count,
  delimiter count) is verified by automated greps."
- The maintainer's eyeball validation of scenario CRITERIA happens in
  the separate `/gsd-verify-work` session — not by this task's done
  criteria. This task's done criteria are file-existence + structural
  grep gates only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-job-tagging-schema-validators/22-CONTEXT.md
@.planning/phases/22-job-tagging-schema-validators/22-RESEARCH.md
@.planning/phases/22-job-tagging-schema-validators/22-PATTERNS.md
@.planning/phases/22-job-tagging-schema-validators/22-VALIDATION.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-HUMAN-UAT.md

<interfaces>
<!-- The Phase 20 UAT (P20-08-PLAN.md output) is the closest structural analog
     for an autonomous=false runbook with multiple scenarios. The planner reads
     it for the runbook shape (frontmatter, scenario structure, eyeball
     criteria, maintainer-checkbox format). -->

<!-- The three just recipes from Plan 05 are the action steps for each scenario:
     - just uat-tags-persist (Scenario 1)
     - just uat-tags-validators (Scenario 2 + 3 — covers both invalid cases AND dedup WARN)
     - just uat-tags-webhook (Scenario 4)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `22-HUMAN-UAT.md` runbook (deterministic file-write; maintainer execution is a separate session)</name>
  <files>.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-HUMAN-UAT.md (closest structural analog — autonomous=false runbook with multiple scenarios; observe its frontmatter format and scenario structure)
    - .planning/phases/22-job-tagging-schema-validators/22-CONTEXT.md `<decisions>` D-10 (four scenarios, exact text) and D-11 (three just recipes)
    - .planning/phases/22-job-tagging-schema-validators/22-VALIDATION.md § "Manual-Only Verifications" table (lines 78-83)
    - justfile (after Plan 05 lands — confirm `uat-tags-persist`, `uat-tags-validators`, `uat-tags-webhook` exist; also confirm `uat-webhook-mock` and `uat-webhook-verify` exist for Scenario 4 cross-references)
  </read_first>
  <action>
**Note on plan-level vs task-level autonomy:** This plan's frontmatter has
`autonomous: false` because the maintainer must EXECUTE the runbook (run
recipes, eyeball outputs, tick checkboxes) in a separate `/gsd-verify-work`
session before Phase 22 PR merges. This TASK, however, is `type="auto"` —
Claude writes the file deterministically and the `<verify>` block is a
runnable grep command. The runbook's content quality (whether the eyeball
criteria are specific enough to validate operator readability) is judged
during the maintainer's UAT session, NOT by this task's done criteria.

**Write the file** `.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md`
with the exact contents below. Use the `Write` tool — do NOT use heredoc /
`cat << EOF`. The frontmatter delimiters MUST be literal `---` lines (not
HTML-comment placeholders).

Write this file content verbatim:

Runbook content to write (verbatim, as the body of `22-HUMAN-UAT.md`):

    ---
    phase: 22
    plan: 06
    title: "Phase 22 — Job Tagging Schema + Validators — Human UAT"
    autonomous: false
    maintainer_validated: true
    created: 2026-05-04
    requirements: [TAG-01, TAG-02, TAG-03, TAG-04, TAG-05]
    status: pending
    ---

    # Phase 22 — Maintainer UAT Runbook

    > **`autonomous: false`** — Claude does NOT mark this UAT passed.
    > Per project memory `feedback_uat_user_validates.md`: every UAT step
    > requires maintainer execution + eyeball validation; Claude's
    > automated tests cover the unit/integration surface (Plan 05), but
    > the operator-readable error UX, the WARN-line shape, and the
    > end-to-end webhook delivery require human judgment.
    >
    > Per project memory `feedback_uat_use_just_commands.md`: every
    > scenario below references a `just` recipe (Plan 05 / D-11). NO
    > ad-hoc `cargo` / `docker` / curl invocations.

    ## Prerequisites

    - [ ] Plans 01–05 are merged (or applied locally on a feature branch).
    - [ ] `cargo build` succeeds on the working tree.
    - [ ] `cargo test --test v12_tags_validators` exits 0 (Plan 05 lock).
    - [ ] `just --list` shows `uat-tags-persist`, `uat-tags-validators`, `uat-tags-webhook`.

    ## Scenario 1 — Persistence spot-check (TAG-02 / D-10 step 1)

    **Goal:** Confirm a TOML with `tags = ["backup", "weekly", "prod"]` round-trips to the `jobs.tags` column as the sorted-canonical JSON `["backup","prod","weekly"]`.

    **Steps:**

    1. Run `just uat-tags-persist` from a fresh terminal.
    2. Watch for the recipe's output of `SELECT name, tags FROM jobs WHERE name = 'uat-tags-persist-demo';`.
    3. **Eyeball criterion:** the `tags` column shows the JSON string `["backup","prod","weekly"]` exactly (sorted-canonical; alphabetized).

    **Sign-off:**

    - [ ] Scenario 1 passed (column shows the expected sorted JSON array).

    ## Scenario 2 — Validator error UX walk (TAG-03 + TAG-04 + TAG-05 + D-08 / D-10 step 2)

    **Goal:** Confirm each rejection produces an operator-readable error.

    **Steps:**

    1. Run `just uat-tags-validators`.
    2. For each emitted error, confirm:
       - **Case 1 (charset, TAG-04):** input `tags = ["MyTag!"]`. Error message names the offending tag and the regex (`^[a-z0-9][a-z0-9_-]{0,30}$`). Example expected fragment: ``tags fail charset ... `mytag!` ``.
       - **Case 2 (reserved, TAG-04):** input `tags = ["cronduit"]`. Error message names the reserved list `["cronduit", "system", "internal"]`.
       - **Case 3 (substring-collision pair, TAG-05):** two jobs with `["back"]` and `["backup"]`. Exactly ONE error message of the shape: ``tag 'back' (used by '<job-a>') is a substring of 'backup' (used by '<job-b>'); rename or remove one to avoid SQL substring false-positives at filter time.``
       - **Case 4 (count cap, D-08):** a job with 17 tags. Error: ``[[jobs]] '<name>': has 17 tags; max is 16. Remove tags or split into multiple jobs.``

    **Eyeball criteria for "operator-readable":**

    - Each error names the offending VALUE (not just the field).
    - Each error states the RULE violated (charset, reserved, collision, cap).
    - Each error suggests a FIX ("Rename or remove these tags", "Split into multiple jobs").

    **Sign-off:**

    - [ ] Scenario 2 passed (all four cases produce operator-readable errors).

    ## Scenario 3 — Dedup-collapse WARN (TAG-03 / D-10 step 3)

    **Goal:** Confirm the WARN line names the ORIGINAL inputs that collapsed.

    **Steps:**

    1. Run `just uat-tags-validators` (the dedup case is part of the same recipe).
    2. For input `tags = ["Backup", "backup ", "BACKUP"]`, watch for a `tracing::warn!` line.

    **Eyeball criterion:** the WARN line names ALL THREE original inputs (e.g., `tags ["Backup", "backup ", "BACKUP"] collapsed to ["backup"]`), NOT just the canonical form `["backup"]`. The operator must be able to tell that they wrote three things and cronduit treated them as one.

    **Reference WARN shape** (from CONTEXT.md `<specifics>` lines 548-553):

    > `WARN job 'nightly-backup': tags ["Backup", "backup ", "BACKUP"] collapsed to ["backup"] (case + whitespace normalization)`

    **Sign-off:**

    - [ ] Scenario 3 passed (WARN line names original inputs + canonical form).

    ## Scenario 4 — End-to-end webhook backfill (WH-09 / D-10 step 4)

    **Goal:** Confirm a webhook configured on a tagged failing job delivers a payload containing real tag values. This is the WH-09 closure proof end-to-end.

    **Steps:**

    1. Run `just uat-tags-webhook` (chains `just uat-webhook-mock` from P18).
    2. Run `just uat-webhook-verify` to surface the last 30 lines of `/tmp/cronduit-webhook-mock.log`.
    3. Inspect the delivered POST body in the log.

    **Eyeball criterion:** the JSON body contains the substring `"tags":["backup","weekly"]` (sorted-canonical order). NOT `"tags":[]` (the Phase 18 placeholder is gone). NOT `"tags":["weekly","backup"]` (insert-order would be a regression).

    **Reference payload shape** (excerpt):

    ```json
    {
      "payload_version": "v1",
      "event_type": "run_finalized",
      "job_name": "uat-tags-webhook-demo",
      "tags": ["backup", "weekly"],
      ...
    }
    ```

    **Sign-off:**

    - [ ] Scenario 4 passed (delivered payload contains real tag values; WH-09 closed end-to-end).

    ## Final sign-off

    When all four scenarios above are checked:

    - [ ] **Maintainer:** I have run all four scenarios on a clean working tree against a feature branch with Plans 01–05 applied. Each scenario produced the expected operator-readable output. WH-09 is closed end-to-end. Phase 22 is UAT-complete and ready to merge.

    Maintainer name: ________
    Date: ________


CRITICAL — The runbook MUST contain the four scenarios in the exact order above (each maps to one of D-10's four steps).

CRITICAL — The runbook MUST cite project memories `feedback_uat_user_validates.md` and `feedback_uat_use_just_commands.md` near the top.

CRITICAL — The runbook frontmatter MUST have `autonomous: false`.

CRITICAL — The runbook frontmatter delimiters MUST be literal `---` lines (NOT HTML-comment placeholders like `<!-- BEGIN_FRONTMATTER -->`). The acceptance grep enforces this.

CRITICAL — Each scenario MUST reference at least one `just uat-tags-*` recipe (NOT ad-hoc `cargo`/`docker`/curl).

CRITICAL — Claude in THIS task writes the file deterministically. The maintainer in a SEPARATE `/gsd-verify-work` session executes the runbook and ticks the checkboxes. Claude does NOT tick the boxes; Claude does NOT mark Plan 06 complete by running through the scenarios itself. The maintainer's sign-off (in the future session) is what gates Phase 22 PR merge.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md && [ "$(grep -c '^---$' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md)" = "2" ] && [ "$(grep -cE 'BEGIN_FRONTMATTER|END_FRONTMATTER' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md)" = "0" ] && [ "$(grep -cE '^## Scenario [1-4] ' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md)" = "4" ] && grep -q '^autonomous: false' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md && grep -qF 'feedback_uat_user_validates.md' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md && grep -qF 'feedback_uat_use_just_commands.md' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md && [ "$(grep -cE 'just uat-tags-(persist|validators|webhook)' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md)" -ge "4" ] && grep -qF 'Final sign-off' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` exists: `test -f .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md`
    - Frontmatter delimiters are literal `---` lines: `grep -c '^---$' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` returns exactly `2` (open + close)
    - Frontmatter contains `autonomous: false`: `grep -q '^autonomous: false' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md`
    - NO HTML-comment placeholder strings remain: `grep -cE 'BEGIN_FRONTMATTER|END_FRONTMATTER' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` returns exactly `0`
    - File contains exactly four scenarios in order: `grep -cE '^## Scenario [1-4] ' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` returns exactly `4`
    - Every scenario step references a `just uat-tags-*` recipe: `grep -cE 'just uat-tags-(persist|validators|webhook)' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` returns at least `4` (one per scenario; more is fine because Scenario 2 and 3 share `uat-tags-validators`)
    - File cites both project memories: `grep -F 'feedback_uat_user_validates.md' ...` returns ≥1 match AND `grep -F 'feedback_uat_use_just_commands.md' ...` returns ≥1 match
    - File contains a "Final sign-off" section: `grep -F 'Final sign-off' ...` returns ≥1 match
    - File contains NO ad-hoc `cargo run` / `docker run` / `curl` invocations in the scenario steps. Verified by `grep -cE '^\s*(cargo run|docker run|curl )' .planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` returns `0` (the Prerequisites section MAY mention `cargo build` / `cargo test` for pre-flight; those are bullet TEXT, not executable lines, and the regex above only matches leading-whitespace command lines)
  </acceptance_criteria>
  <done>
File `.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` exists. Structural greps all pass:
1. Exactly 2 lines matching `^---$` (frontmatter delimiters).
2. Zero `BEGIN_FRONTMATTER` / `END_FRONTMATTER` placeholder strings.
3. Exactly 4 `^## Scenario [1-4] ` headings.
4. `autonomous: false` is present in frontmatter.
5. Both project memories cited.
6. ≥4 `just uat-tags-*` recipe references.
7. Final sign-off section present.

The runbook's content QUALITY (whether the eyeball criteria genuinely
validate operator readability, whether the WARN-shape reference is
accurate, etc.) is the maintainer's call during the future
`/gsd-verify-work` session — NOT this task's responsibility. Claude
declares this task done as soon as the structural greps pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → runbook file | Claude writes the maintainer-readable text; no code execution; no automated assertions of scenario CONTENT (only structural greps) |
| runbook → maintainer terminal | maintainer executes existing `just` recipes; no new attack surface |
| maintainer → sign-off boxes | only the maintainer (not Claude, not CI) ticks the boxes — repudiation surface against false sign-off is closed by the project memory `feedback_uat_user_validates.md` lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Repudiation | Claude marks UAT passed without maintainer eyeball | mitigate | plan-level `autonomous: false` + project memory `feedback_uat_user_validates.md` citation; this task's `done` criteria are file-existence + structural greps ONLY (NOT scenario content quality); Phase 22 PR merge gates on the maintainer's `/gsd-verify-work` sign-off, not on this task being marked done |
| T-22-06-02 | Tampering | runbook recommends ad-hoc shell commands instead of just recipes | mitigate | every scenario step references a `just uat-tags-*` recipe (D-11 / D-14 / project memory `feedback_uat_use_just_commands.md`); recipe-calls-recipe pattern keeps maintainer in known-recipe surface; acceptance grep enforces zero raw `cargo run`/`docker run`/`curl ` invocations in scenario steps |
| T-22-06-03 | Information Disclosure | runbook contains secrets | accept | runbook uses synthetic tag values (`backup`, `weekly`, `prod`); no PII; demo-only |
| T-22-06-04 | Elevation of Privilege | runbook has no code surface | accept | pure markdown; no execution |

**Security gate:** No `high` severity threats. Plan 06 is a maintainer-readable runbook; the plan-level `autonomous: false` lock + project-memory citations close the repudiation surface against false sign-off. The structural-grep verification is deterministic and matches the task's `type="auto"` posture.

</threat_model>

<verification>
After Task 1 completes:

1. The file `.planning/phases/22-job-tagging-schema-validators/22-HUMAN-UAT.md` exists.
2. The file's frontmatter delimiters are exactly TWO literal `---` lines.
3. The file's frontmatter has `autonomous: false`.
4. The file contains exactly four `^## Scenario [1-4] ` headings (D-10 order).
5. Every scenario step cites a `just uat-tags-*` recipe.
6. The "Final sign-off" section exists at the bottom.
7. Zero `BEGIN_FRONTMATTER` / `END_FRONTMATTER` placeholder strings remain.

After the maintainer EXECUTES the runbook in a SEPARATE `/gsd-verify-work` session (not in this planning session):

8. All checkboxes in all four scenarios are ticked.
9. The "Final sign-off" maintainer name + date is filled in.
10. ONLY THEN does Phase 22 UAT close — the PR merge gates on this signal, not on this task being marked done.
</verification>

<success_criteria>
- [ ] `22-HUMAN-UAT.md` written with the structure documented in Task 1's `<action>` block
- [ ] Frontmatter has `autonomous: false`, delimited by literal `---` lines (verified by grep `^---$` count = 2)
- [ ] Zero `BEGIN_FRONTMATTER`/`END_FRONTMATTER` placeholder strings (verified by grep count = 0)
- [ ] Four scenarios in D-10 order (Persistence / Validator UX / Dedup WARN / Webhook Backfill)
- [ ] Every scenario cites a `just uat-tags-*` recipe (≥4 matches)
- [ ] Project memories `feedback_uat_user_validates.md` and `feedback_uat_use_just_commands.md` cited
- [ ] Each scenario has a clear maintainer eyeball criterion (validated by maintainer in `/gsd-verify-work`, NOT here)
- [ ] Final sign-off section exists at bottom
- [ ] Task 1's `<verify><automated>` grep pipeline exits 0
- [ ] Maintainer executes the runbook in a SEPARATE `/gsd-verify-work` session and ticks every box (NOT Claude's responsibility, NOT this plan's done criterion)
- [ ] Phase 22 PR merge blocks until the runbook is fully signed off by the maintainer (gating signal lives in `/gsd-verify-work`, not in this plan)
</success_criteria>

<output>
After Task 1's automated `<verify>` grep pipeline exits 0:

1. Create `.planning/phases/22-job-tagging-schema-validators/22-06-SUMMARY.md` documenting:
   - The runbook file path and its scenario list
   - Confirmation that the runbook is `autonomous: false` and cites both project memories
   - Confirmation that the structural greps pass (frontmatter delimiters, scenario count, recipe references)
   - A note that this plan is "written but NOT executed" — the maintainer-execution step is a separate (future) `/gsd-verify-work` session
   - A pointer to the Final sign-off section the maintainer will fill in
   - Cross-reference to Plan 05 (the recipes the runbook uses) and the Phase 22 PR-merge gating criterion

2. Phase 22 PR description (when the maintainer opens it) MUST mention that the runbook (`22-HUMAN-UAT.md`) needs to be executed and signed before merge — this is part of the PR's manual review step, NOT something Claude can self-assert.
</output>
