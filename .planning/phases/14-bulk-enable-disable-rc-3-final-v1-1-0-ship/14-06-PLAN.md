---
phase: 14
plan: 06
type: execute
wave: 4
depends_on: [14-03, 14-04]
files_modified:
  - src/web/handlers/settings.rs
  - templates/pages/settings.html
autonomous: true
requirements: [ERG-03]
tags: [rust, axum, askama, htmx, phase-14, wave-4]

must_haves:
  truths:
    - "Settings page shows a 'Currently Overridden' section listing every job whose `enabled_override` is non-NULL (ERG-03)"
    - "Section is hidden entirely when `overridden_jobs.is_empty()` (D-10a — no empty-state noise)"
    - "Rows ordered alphabetically by job name (D-10b)"
    - "Each row has a Clear button that POSTs to `/api/jobs/bulk-toggle` with `action=enable&job_ids={id}` — clears the override"
    - "DISABLED badge uses shipped `cd-badge--disabled`; defensive FORCED ON badge uses new `cd-badge--forced` (from Plan 05)"
    - "settings_empty_state_hides_section test from Plan 01 turns GREEN (final scoreboard completion)"
  artifacts:
    - path: src/web/handlers/settings.rs
      provides: "SettingsPage view model threads overridden_jobs: Vec<OverriddenJobView>"
      contains: "overridden_jobs"
    - path: templates/pages/settings.html
      provides: "<section>Currently Overridden</section> with table + per-row Clear form"
      contains: "Currently Overridden"
  key_links:
    - from: settings.rs::settings()
      to: queries::get_overridden_jobs
      via: "view hydration"
      pattern: "queries::get_overridden_jobs"
    - from: settings.html per-row Clear button
      to: /api/jobs/bulk-toggle
      via: "HTMX hx-post with action=enable + single job_ids"
      pattern: "hx-post=\"/api/jobs/bulk-toggle\""
---

<objective>
Deliver the settings-page audit surface (ERG-03): a full-width "Currently Overridden" section below the existing 6-card grid that lists every job with a non-null `enabled_override`, with a per-row Clear button that reuses the Plan 04 bulk-toggle endpoint.

Purpose: operators can see at a glance which jobs have been bulk-disabled (so backups don't silently skip runs for months). Closes out Plan 01's `settings_empty_state_hides_section` test (final red-bar item).
Output: extended settings handler view model + new `<section>` in settings.html; alphabetical ordering; empty-state hides the section entirely; clicking Clear on any row POSTs to bulk-toggle endpoint and refreshes state via HX-Trigger toast + HTMX partial re-render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-UI-SPEC.md
@.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-PATTERNS.md
@src/web/handlers/settings.rs
@templates/pages/settings.html
@templates/partials/job_table.html
@tests/v11_bulk_toggle.rs

<interfaces>
<!-- Target settings handler (settings.rs §11 adaptation notes): -->
```rust
#[derive(Template)]
#[template(path = "pages/settings.html")]
pub struct SettingsPage {
    // ... existing fields ...
    pub overridden_jobs: Vec<OverriddenJobView>,
    pub csrf_token: String,  // confirm existing; used by per-row Clear forms
}

pub struct OverriddenJobView {
    pub id: i64,
    pub name: String,
    pub enabled_override: i64,  // non-null guaranteed by get_overridden_jobs filter
}
```

<!-- Hydration: -->
```rust
let overridden_jobs = queries::get_overridden_jobs(&state.pool).await
    .unwrap_or_else(|err| { tracing::error!(target: "cronduit.web", error = %err, "settings: get_overridden_jobs failed"); Vec::new() })
    .into_iter()
    .filter_map(|j| j.enabled_override.map(|ov| OverriddenJobView { id: j.id, name: j.name, enabled_override: ov }))
    .collect();
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.html per-row Clear form → /api/jobs/bulk-toggle | CSRF-gated via shared token; same Plan 04 handler |
| browser → settings page render | unauth v1.1 UI; data-disclosure equivalent to rest of dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Spoofing | Per-row Clear form missing CSRF token | mitigate | Each `<form>` includes `<input type="hidden" name="csrf_token" value="{{ csrf_token }}">`; Plan 04 handler rejects on mismatch (test handler_csrf) |
| T-14-06-02 | Tampering | Forgetting `<input name="action" value="enable">` → Clear button accidentally disables (the default post-validation branch doesn't exist; Plan 04 handler rejects unknown action with 400) | mitigate | Handler explicitly validates `action ∈ {disable, enable}` and rejects unknowns; test `handler_csrf`-adjacent cases cover this |
| T-14-06-03 | Information Disclosure | Section renders even when empty → operator confusion | mitigate | D-10a: askama `{% if !overridden_jobs.is_empty() %}...{% endif %}` wraps the entire `<section>`; test `settings_empty_state_hides_section` asserts absence |
| T-14-06-04 | Tampering | XSS via job name in table | mitigate | askama `{{ }}` default HTML-escapes — attribute/text safe; no `| safe` filter used |
| T-14-06-05 | Denial of Service | `get_overridden_jobs` query failure crashes settings page | mitigate | Non-fatal degradation: log + empty Vec. Settings page still renders (matches dashboard.rs error-swallowing pattern) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SettingsPage + settings() handler with overridden_jobs</name>
  <read_first>
    - src/web/handlers/settings.rs L1-128 (entire file — confirm shape; identify insertion points)
    - src/web/handlers/settings.rs (look for existing `SettingsPage` struct + `settings()` handler signature)
    - src/db/queries.rs (confirm `queries::get_overridden_jobs` exists and is `pub` — landed in Plan 03)
    - 14-PATTERNS.md §11 (settings.rs adaptation — exact struct shape + hydration idiom)
    - tests/v11_bulk_toggle.rs (`settings_empty_state_hides_section` — must pass after this task + Task 2)
  </read_first>
  <behavior>
    After this task:
    - `SettingsPage` struct has `pub overridden_jobs: Vec<OverriddenJobView>` field
    - New type `OverriddenJobView { id, name, enabled_override }` defined inside settings.rs (co-located per dashboard.rs precedent)
    - `settings()` handler hydrates `overridden_jobs` by calling `queries::get_overridden_jobs(&state.pool)` and mapping to the view model
    - Query failure logs + returns empty Vec (non-fatal; settings page still renders)
    - `cargo build --quiet` succeeds (askama template in Task 2 will compile-error until its template references the field — rank these tasks in tandem)
  </behavior>
  <action>
    1. In `src/web/handlers/settings.rs`, add at the top of the file (after existing imports, before existing struct definitions):
       ```rust
       /// Minimal view of a job whose `enabled_override` is non-null, for ERG-03 audit.
       pub struct OverriddenJobView {
           pub id: i64,
           pub name: String,
           pub enabled_override: i64,
       }
       ```

    2. Extend the existing `SettingsPage` struct (derive #[derive(Template)]) by adding the new field alongside the existing view-model fields:
       ```rust
       pub overridden_jobs: Vec<OverriddenJobView>,
       ```
       Also confirm `pub csrf_token: String` is already present (needed for the per-row Clear forms in Task 2). If NOT present, add it and populate from `state.csrf_token` (or the existing csrf cookie retrieval used elsewhere in the file).

    3. In the existing `settings()` async handler fn, AFTER the existing `last_reload` hydration and BEFORE constructing the `SettingsPage` instance, add:
       ```rust
       let overridden_jobs: Vec<OverriddenJobView> = queries::get_overridden_jobs(&state.pool)
           .await
           .unwrap_or_else(|err| {
               tracing::error!(
                   target: "cronduit.web",
                   error = %err,
                   "settings: get_overridden_jobs failed"
               );
               Vec::new()
           })
           .into_iter()
           .filter_map(|j| j.enabled_override.map(|ov| OverriddenJobView {
               id: j.id,
               name: j.name,
               enabled_override: ov,
           }))
           .collect();
       ```

    4. Pass `overridden_jobs` into the `SettingsPage { .. overridden_jobs, .. }` construction.

    5. Do NOT touch existing fields/hydration — purely additive. `cargo build --quiet` should succeed (askama template compile-error on the template path will land in Task 2 when we reference the new field).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/cronduit &amp;&amp; grep -q "pub struct OverriddenJobView" src/web/handlers/settings.rs &amp;&amp; grep -q "overridden_jobs: Vec<OverriddenJobView>" src/web/handlers/settings.rs &amp;&amp; grep -q "queries::get_overridden_jobs" src/web/handlers/settings.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub struct OverriddenJobView" src/web/handlers/settings.rs` succeeds
    - `grep -q "pub id: i64" src/web/handlers/settings.rs` succeeds (OverriddenJobView field)
    - `grep -q "pub enabled_override: i64" src/web/handlers/settings.rs` succeeds
    - `grep -q "overridden_jobs: Vec<OverriddenJobView>" src/web/handlers/settings.rs` succeeds
    - `grep -q "queries::get_overridden_jobs" src/web/handlers/settings.rs` succeeds
    - `grep -q "pub csrf_token: String" src/web/handlers/settings.rs || grep -q "csrf_token: String" src/web/handlers/settings.rs` succeeds (CSRF token reachable from template for per-row forms)
    - After Task 2 completes, `cargo build --quiet` exits 0
    - `cargo clippy --quiet -- -D warnings` exits 0 (running on completed Tasks 1+2)
  </acceptance_criteria>
  <done>
    View model extended; hydration wired via queries::get_overridden_jobs (with non-fatal error handling); OverriddenJobView type defined co-located. Template (Task 2) consumes this state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append "Currently Overridden" section to templates/pages/settings.html</name>
  <read_first>
    - templates/pages/settings.html L1-73 (whole file — confirm line numbers; grid closes at L71, block close at L73)
    - templates/partials/job_table.html L26-29 (per-row Run Now form — EXACT analog for the per-row Clear form shape)
    - 14-PATTERNS.md §14 (settings.html adaptation — `<section>` insertion between L71 and L73)
    - 14-UI-SPEC.md § Surface D (complete verbatim section markup: heading, description, table, rows, badges, Clear button form)
    - 14-UI-SPEC.md § Copywriting Contract § Settings (heading `Currently Overridden`, description `These jobs have an override set...`, column headers `NAME` / `OVERRIDE STATE` / `CLEAR`, badge labels `DISABLED` / `FORCED ON`, button `Clear` + aria-label `Clear override for {{ job.name }}`)
    - tests/v11_bulk_toggle.rs::settings_empty_state_hides_section (the green-bar target — assert `!rendered.contains("Currently Overridden")` when `overridden_jobs` is empty)
  </read_first>
  <behavior>
    After this task:
    - settings.html has a new `<section>` inserted BETWEEN the closing `</div>` of the 6-card grid (L71) and the `{% endblock %}` (L73)
    - The entire section is wrapped in `{% if !overridden_jobs.is_empty() %}...{% endif %}` so it renders nothing when empty
    - Section has a `<h2>Currently Overridden</h2>` with `--cd-text-lg` + weight 700 + `margin-top: var(--cd-space-8)`
    - Description line: `<p>These jobs have an override set; they do NOT follow the <code>enabled</code> flag from <code>cronduit.toml</code>.</p>`
    - Table has three columns: `Name | Override State | Clear`; alphabetical iteration via `{% for job in overridden_jobs %}`
    - DISABLED badge when `job.enabled_override == 0` using `cd-badge cd-badge--disabled`
    - FORCED ON badge when `job.enabled_override == 1` using `cd-badge cd-badge--forced` (defensive rendering)
    - Per-row Clear `<form>` with hidden `csrf_token`, `action=enable`, `job_ids={{ job.id }}` inputs; `hx-post="/api/jobs/bulk-toggle" hx-swap="none"`; Clear button uses `cd-btn-secondary text-sm py-1 px-3`
    - Test `settings_empty_state_hides_section` from Plan 01 now passes
    - `cargo build --quiet` succeeds (askama compile-time template check)
  </behavior>
  <action>
    Append the following `<section>` between the closing `</div>` of the 6-card grid (current L71) and the `{% endblock %}` line (current L73). Exact markup per 14-UI-SPEC.md § Surface D:

    ```html
    {% if !overridden_jobs.is_empty() %}
    <section style="margin-top:var(--cd-space-8)">
      <h2 style="font-size:var(--cd-text-lg);font-weight:700;color:var(--cd-text-primary);margin-bottom:var(--cd-space-2)">Currently Overridden</h2>
      <p style="font-size:var(--cd-text-sm);color:var(--cd-text-secondary);margin-bottom:var(--cd-space-4)">
        These jobs have an override set; they do NOT follow the <code style="font-family:inherit;color:var(--cd-text-accent)">enabled</code> flag from <code style="font-family:inherit;color:var(--cd-text-accent)">cronduit.toml</code>.
      </p>
      <div class="overflow-x-auto" style="background:var(--cd-bg-surface);border:1px solid var(--cd-border);border-radius:var(--cd-radius-md)">
        <table style="width:100%;border-collapse:collapse">
          <thead>
            <tr style="background:var(--cd-bg-surface-raised)">
              <th class="text-left py-2 px-4" style="font-size:var(--cd-text-xs);font-weight:700;text-transform:uppercase;letter-spacing:0.1em;color:var(--cd-text-secondary)">Name</th>
              <th class="text-left py-2 px-4" style="font-size:var(--cd-text-xs);font-weight:700;text-transform:uppercase;letter-spacing:0.1em;color:var(--cd-text-secondary)">Override State</th>
              <th class="text-left py-2 px-4" style="font-size:var(--cd-text-xs);font-weight:700;text-transform:uppercase;letter-spacing:0.1em;color:var(--cd-text-secondary)">Clear</th>
            </tr>
          </thead>
          <tbody>
            {% for job in overridden_jobs %}
            <tr class="hover:bg-(--cd-bg-hover) border-b border-(--cd-border-subtle)">
              <td class="py-2 px-4">
                <a href="/jobs/{{ job.id }}" class="text-(--cd-text-accent) hover:underline font-bold">{{ job.name }}</a>
              </td>
              <td class="py-2 px-4">
                {% if job.enabled_override == 0 %}
                <span class="cd-badge cd-badge--disabled">DISABLED</span>
                {% else if job.enabled_override == 1 %}
                <span class="cd-badge cd-badge--forced">FORCED ON</span>
                {% else %}
                <span class="cd-badge cd-badge--disabled">STATE {{ job.enabled_override }}</span>
                {% endif %}
              </td>
              <td class="py-2 px-4">
                <form hx-post="/api/jobs/bulk-toggle" hx-swap="none">
                  <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                  <input type="hidden" name="action" value="enable">
                  <input type="hidden" name="job_ids" value="{{ job.id }}">
                  <button type="submit"
                          class="cd-btn-secondary text-sm py-1 px-3"
                          aria-label="Clear override for {{ job.name }}">
                    Clear
                  </button>
                </form>
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </section>
    {% endif %}
    ```

    Load-bearing requirements:
    - The `{% if !overridden_jobs.is_empty() %}` wrapper is MANDATORY (D-10a) — without it, `settings_empty_state_hides_section` test from Plan 01 fails.
    - The heading uses `--cd-text-lg` (NOT `--cd-text-xl`) per D-09 — the section is subordinate to the page `<h1>Settings`.
    - Column headers use `--cd-text-xs` + `color: var(--cd-text-secondary)` (matches dashboard thead convention).
    - Per-row Clear form uses `hx-swap="none"` — the HX-Trigger toast fires, and the settings page auto-refreshes via a separate mechanism (follow-up to future work; for v1.1, operator manually reloads `/settings` to see the list update; Plan 10 HUMAN-UAT step 6 covers this). Alternative: could use `hx-swap="outerHTML" hx-target="closest tr"` to remove the row — planner chose `none` to keep the shape identical to the dashboard bulk-bar pattern and reuse the single Plan 04 handler contract. NOTE: Add `hx-on::after-request` that reloads the settings page on a small delay, if desired; the minimum viable ship is just `hx-swap="none"`.

    After edits: `cargo build --quiet` must succeed (askama compile-time template check against the extended SettingsPage struct from Task 1).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/cronduit &amp;&amp; cargo build --quiet &amp;&amp; cargo nextest run --test v11_bulk_toggle::settings_empty_state_hides_section 2>&amp;1 | tee /tmp/wave4_settings.out &amp;&amp; grep -qE "settings_empty_state_hides_section.*(ok|PASS)" /tmp/wave4_settings.out</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Currently Overridden" templates/pages/settings.html` succeeds
    - `grep -q "{% if !overridden_jobs.is_empty() %}" templates/pages/settings.html` succeeds
    - `grep -q "{% endif %}" templates/pages/settings.html` succeeds (wrapper close)
    - `grep -q "{% for job in overridden_jobs %}" templates/pages/settings.html` succeeds
    - `grep -q "hx-post=\"/api/jobs/bulk-toggle\"" templates/pages/settings.html` succeeds
    - `grep -c 'name="action" value="enable"' templates/pages/settings.html` returns 1 (per-row Clear form is `enable` — clears override to NULL per D-05)
    - `grep -q "cd-badge--disabled" templates/pages/settings.html` succeeds
    - `grep -q "cd-badge--forced" templates/pages/settings.html` succeeds
    - `grep -q "font-size:var(--cd-text-lg)" templates/pages/settings.html` succeeds (heading size per D-09)
    - `grep -q "var(--cd-space-8)" templates/pages/settings.html` succeeds (section margin-top)
    - `grep -q 'aria-label="Clear override for {{ job.name }}"' templates/pages/settings.html` succeeds
    - `cargo build --quiet` exits 0
    - `cargo nextest run --test v11_bulk_toggle::settings_empty_state_hides_section` passes
    - `cargo nextest run --test v11_bulk_toggle` — ALL 14 SQLite tests pass (Plan 01 scoreboard fully green)
  </acceptance_criteria>
  <done>
    Settings page renders "Currently Overridden" section (hidden when empty); per-row Clear form POSTs to bulk-toggle handler with action=enable; Plan 01 test `settings_empty_state_hides_section` GREEN; cargo build succeeds.
  </done>
</task>

</tasks>

<verification>
```
cd /Users/Robert/Code/public/cronduit
cargo build --quiet
cargo clippy --quiet -- -D warnings
cargo nextest run --test v11_bulk_toggle
# All 14 SQLite tests pass — Plan 01 scoreboard now 100% green

# Smoke (deferred to HUMAN-UAT Plan 09):
# - just dev → bulk-disable 3 jobs on dashboard → navigate to /settings → section renders
# - Click Clear on one row → toast fires → reload /settings → row gone
```
</verification>

<success_criteria>
- SettingsPage view model carries overridden_jobs + OverriddenJobView defined
- settings() handler hydrates via get_overridden_jobs with non-fatal error handling
- settings.html has `<section>` wrapped in `{% if !overridden_jobs.is_empty() %}`
- Section uses `--cd-text-lg` heading, `--cd-space-8` margin-top, three-column table
- Per-row Clear form POSTs to bulk-toggle endpoint with action=enable
- DISABLED + FORCED ON badges render correctly (defensive)
- `settings_empty_state_hides_section` test green — Plan 01 scoreboard 100% complete
</success_criteria>

<output>
Create `.planning/phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-06-SUMMARY.md`. Include:
- Final nextest output showing all 14 SQLite + 5 Postgres v11_bulk_toggle tests green
- "Plan 01 scoreboard: 19/19 green" announcement
- Screenshot of the rendered section (deferred to HUMAN-UAT)
</output>
