---
phase: 19-webhook-hmac-signing-receiver-examples
plan: 05
type: execute
wave: 3
depends_on: [19-02, 19-03, 19-04]
files_modified:
  - docs/WEBHOOKS.md
  - docs/CONFIG.md
  - README.md
  - examples/cronduit.toml
autonomous: true
requirements: [WH-04]
must_haves:
  truths:
    - "Operators reading `docs/WEBHOOKS.md` see the SHA-256-only note (verbatim per SC-3), the 3-header semantics, the constant-time-compare per-language primitive table, the 5-min anti-replay constant, the verbatim D-12 retry-aware response codes table, and links to all 3 receiver examples — formatted as a flat 10-section operator hub doc with mermaid diagrams (D-19, no ASCII art)."
    - "Operators reading `docs/CONFIG.md` find a back-link from the webhook section to `docs/WEBHOOKS.md` (no content duplication per D-07)."
    - "Operators reading `README.md` find a one-line pointer to `docs/WEBHOOKS.md` next to the existing QUICKSTART/CONFIG pointers (no README sprawl per D-08)."
    - "Operators uncommenting the 3 new `wh-example-receiver-{python,go,node}` jobs in `examples/cronduit.toml` see a webhook URL targeting the matching port (9991/9992/9993) for end-to-end UAT."
    - "D-06: `docs/WEBHOOKS.md` is the operator-facing hub doc with 10 sections: Overview + Standard Webhooks v1 link, 3 headers + semantics, SHA-256-only note (LOCKED v1.2), secret rotation, constant-time compare table, anti-replay window, idempotency guidance, retry-aware response codes (verbatim D-12 table), links to 3 receiver examples, pointer to `examples/webhook_mock_server.rs`."
    - "D-18: [informational] Phase 19 lands via PR on the `phase-19-webhook-hmac-receivers` feature branch — no direct commits to `main`. This plan's docs/example-config additions are part of that PR-only change set."
    - "D-23: [informational] No UI surface in Phase 19 — webhook operator visibility on the dashboard is post-v1.2 territory. ROADMAP marks Phase 19 `UI hint: no`. The docs hub + receiver READMEs are the only operator-facing surface this phase ships."
  artifacts:
    - path: "docs/WEBHOOKS.md"
      provides: "10-section operator-facing hub doc with mermaid diagrams"
      contains: "SHA-256 only"
      min_lines: 200
    - path: "docs/CONFIG.md"
      provides: "Webhook section + back-link to docs/WEBHOOKS.md"
      contains: "WEBHOOKS.md"
    - path: "README.md"
      provides: "One-line pointer to docs/WEBHOOKS.md under existing Configuration section"
      contains: "docs/WEBHOOKS.md"
    - path: "examples/cronduit.toml"
      provides: "3 new commented-out wh-example-receiver-{python,go,node} jobs"
      contains: "wh-example-receiver-python"
  key_links:
    - from: "docs/WEBHOOKS.md"
      to: "examples/webhook-receivers/{python,go,node}/README.md"
      via: "Markdown links in §9 Receiver examples"
      pattern: "examples/webhook-receivers"
    - from: "docs/CONFIG.md"
      to: "docs/WEBHOOKS.md"
      via: "Markdown link in webhook back-link block"
      pattern: "WEBHOOKS\\.md"
    - from: "README.md"
      to: "docs/WEBHOOKS.md"
      via: "One-line markdown pointer"
      pattern: "docs/WEBHOOKS\\.md"
    - from: "examples/cronduit.toml"
      to: "examples/webhook-receivers/{python,go,node}/receiver.{py,go,js}"
      via: "Webhook URL targets matching port (9991/9992/9993)"
      pattern: "127\\.0\\.0\\.1:999[123]"
---

<objective>
Wave 3 — ship the operator-facing documentation hub. Phase 19's success criterion SC-3 requires that operators reviewing the receiver-example docs see an explicit note that v1.2 ships SHA-256 only. This plan delivers:

1. **`docs/WEBHOOKS.md`** — the new 10-section operator hub (~250-400 lines) per CONTEXT D-06: Overview + Standard Webhooks v1 link, 3 headers, SHA-256-only note, secret rotation guidance, constant-time compare table, anti-replay window, idempotency guidance, verbatim D-12 retry-aware codes table, links to the 3 receiver examples, and a pointer to `examples/webhook_mock_server.rs`.
2. **`docs/CONFIG.md`** — back-link from the existing webhook coverage to `docs/WEBHOOKS.md` (D-07: no content duplication).
3. **`README.md`** — one-line pointer to `docs/WEBHOOKS.md` (D-08: no sprawl).
4. **`examples/cronduit.toml`** — 3 new commented-out `wh-example-receiver-{python,go,node}` jobs (Pitfall 7: cronduit URL is config-time, NOT runtime override) targeting ports 9991/9992/9993 respectively.

The 3 mermaid diagrams already drafted in `19-RESEARCH.md` (System Architecture sequenceDiagram, Verify Decision Tree flowchart, Phase 20 Retry stateDiagram) are reused verbatim in `docs/WEBHOOKS.md` (D-19 mermaid-only).

Output: 1 new doc + 3 modified files (back-link + pointer + 3 example jobs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-hmac-signing-receiver-examples/19-CONTEXT.md
@.planning/phases/19-webhook-hmac-signing-receiver-examples/19-RESEARCH.md
@.planning/phases/19-webhook-hmac-signing-receiver-examples/19-PATTERNS.md
@docs/CONFIG.md
@README.md
@examples/cronduit.toml

<interfaces>
<!-- Plans 02/03/04 produced these receivers; Plan 05 consumes their existence in cross-references. -->

Receivers shipped (Plans 02/03/04):
- `examples/webhook-receivers/python/receiver.py` + `README.md` — port 9991, primitive `hmac.compare_digest`
- `examples/webhook-receivers/go/receiver.go` + `README.md` — port 9992, primitive `hmac.Equal`
- `examples/webhook-receivers/node/receiver.js` + `README.md` — port 9993, primitive `crypto.timingSafeEqual` (with mandatory length guard per Pitfall 2)

D-12 retry contract (verbatim section in docs/WEBHOOKS.md §8):
| Outcome | HTTP | Cronduit Phase 20 |
|---|---|---|
| Missing/malformed headers | 400 | Permanent — drop |
| Drift > 5 min | 400 | Permanent — drop |
| HMAC mismatch | 401 | Permanent — drop |
| Verify success | 200 | Counter increment |
| Unexpected exception | 503 | Transient — retry |

10 sections per CONTEXT D-06:
1. Overview + Standard Webhooks v1 link (NO paraphrase)
2. Three required headers + semantics
3. SHA-256-only note (LOCKED v1.2 — verbatim per SC-3)
4. Secret rotation (receiver-side dual-secret window)
5. Constant-time compare requirement + per-language table (Pattern 3)
6. Anti-replay (5-min drift constant)
7. Idempotency (dedupe by webhook-id; production guidance)
8. Retry-aware response codes (verbatim D-12 table)
9. Receiver examples (links)
10. Loopback Rust mock pointer

3 mermaid diagrams from RESEARCH.md (re-use verbatim):
- System Architecture sequenceDiagram (Cronduit → Receiver delivery flow)
- Verify Decision Tree flowchart (header check → drift → multi-token parse → constant-time compare → 200/400/401)
- Phase 20 Retry stateDiagram (Sent → Verified | Permanent | Transient → DeadLetter)

examples/cronduit.toml structure (existing, lines 218-256):
- `wh-example-unsigned` is enabled by default at line 218 (uncommented)
- `wh-example-signed` template at line 240 (commented, 6 lines)
- `wh-example-fire-every-zero` template at line 251 (commented, 6 lines)
- Plan 05's 3 new jobs append after line 256, all commented-out (matches Phase 18 D-05 — `docker compose up` smoke stays clean)

docs/CONFIG.md structure:
- Line 1: # Cronduit Configuration Reference
- Line 5: existing pointer "For a step-by-step walkthrough... QUICKSTART.md... SPEC.md"
- Line 228: `## [[jobs]] blocks` heading
- The CONFIG.md does NOT currently have a webhook section (Phase 18 didn't add one — webhook config lived in inline TOML examples). Plan 05 adds a NEW `## Webhooks` section near the end (after line 542's existing Validation section, OR before § Hot reload at line 575) — placement is Claude's discretion; the back-link to `docs/WEBHOOKS.md` is the load-bearing content.

README.md structure:
- Line 174-175: existing pointer block ("New to Cronduit? ... QUICKSTART... CONFIG...")
- Plan 05 adds a third pointer line directly after this block

Pitfall 7 (CONTEXT/RESEARCH): Cronduit's webhook URL is parsed once at config-load and bound to `WebhookConfig.url`. NO runtime override exists. Plan 05 adds 3 NEW jobs to `examples/cronduit.toml` (not an env-var override of `wh-example-signed`).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → docs/WEBHOOKS.md prose | The doc text describes the wire format, secret rotation, and retry contract. Operator-facing content trust is correctness (no misleading guidance) — verifiable by Maintainer review (Plan 06 UAT). |
| Operator → examples/cronduit.toml templates | Templates ship commented-out (D-05 stays clean for `docker compose up` smoke); operators uncomment intentionally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-24 | Information Disclosure | docs/WEBHOOKS.md prose | mitigate | Doc defers wire format to the upstream Standard Webhooks v1 spec (D-06; CONTEXT line 51 — "do NOT paraphrase"); cronduit-specific deviations (1 secret only, SHA-256 only) are stated explicitly. Maintainer Plan 06 UAT visually validates the rendered prose. |
| T-19-25 | Tampering / Operator confusion | examples/cronduit.toml | mitigate | New jobs ship commented-out (D-05 precedent); each has a `command = "false"` (always-fails — exercises failure firing path); webhook URLs are loopback-only; secret is `${WEBHOOK_SECRET}` env-var (NEVER plaintext-checked-in secrets). |
| T-19-26 | Repudiation / Drift | D-12 retry-contract documentation | mitigate | The D-12 table is reproduced verbatim in `docs/WEBHOOKS.md` §8; Phase 20's retry implementation MUST inherit this contract unchanged (CONTEXT line 137-138 explicit). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `docs/WEBHOOKS.md` (10-section operator hub doc, ~250-400 lines, mermaid diagrams, verbatim SHA-256-only note + D-12 retry contract)</name>
  <files>docs/WEBHOOKS.md</files>
  <read_first>
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-CONTEXT.md` (D-06 10-section list, D-19 mermaid-only)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-RESEARCH.md` (3 mermaid diagrams — System Architecture, Verify Decision Tree, Phase 20 Retry; Open Q3 short-table for §5 constant-time table)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-PATTERNS.md` (`docs/WEBHOOKS.md` section — heading hierarchy from CONFIG.md, section shape)
    - `docs/CONFIG.md` lines 1-60 (heading-style precedent for sibling hub-doc intro)
    - `examples/webhook-receivers/python/README.md` (Plan 02; for cross-link consistency)
    - `examples/webhook-receivers/go/README.md` (Plan 03)
    - `examples/webhook-receivers/node/README.md` (Plan 04)
    - `src/webhooks/dispatcher.rs:235-253` (the cronduit-side header-build path, to confirm doc accurately describes 3 headers)
  </read_first>
  <behavior>
    - File length: 250-400 lines
    - All 10 sections per D-06 (heading levels: `#` for title, `##` for sections)
    - 3 mermaid diagrams from RESEARCH.md re-used verbatim (sequenceDiagram, flowchart, stateDiagram)
    - **NO ASCII art diagrams** (D-19)
    - Section 3 contains the verbatim SHA-256-only note (SC-3): "Cronduit v1.2 ships SHA-256 only. No algorithm-agility (SHA-384/512/Ed25519/etc.) and no multi-secret rotation cronduit-side; secret rotation lives on the receiver via a dual-secret verify window."
    - Section 5 has a 3-row Python/Go/Node primitive table with documentation URLs (Open Q3 recommendation)
    - Section 6 names `MAX_TIMESTAMP_DRIFT_SECONDS = 300` and explains the 5-min Standard Webhooks default
    - Section 8 is the verbatim D-12 retry-aware response codes table
    - Section 9 links to all 3 receiver READMEs (`examples/webhook-receivers/{python,go,node}/README.md`)
    - Section 10 links to `examples/webhook_mock_server.rs`
    - Top-of-doc cross-references mirror CONFIG.md line 5 pattern (link back to QUICKSTART, CONFIG, SPEC)
    - Standard Webhooks v1 spec link points to https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md
  </behavior>
  <action>
    Use the Write tool. Create `docs/WEBHOOKS.md` with the following content:

    ```markdown
    # Cronduit Webhooks

    Cronduit emits Standard Webhooks v1 deliveries on terminal job-run events
    (`failed`, `timeout`, `stopped` by default — configurable per job). This
    document is the operator-facing hub for receiver implementation and
    verification. For TOML config field reference, see
    [`CONFIG.md`](./CONFIG.md). For the architectural picture, see
    [`SPEC.md`](./SPEC.md). For a step-by-step walkthrough, start with
    [`QUICKSTART.md`](./QUICKSTART.md).

    ## Table of contents

    1. [Overview](#overview)
    2. [Three required headers](#three-required-headers)
    3. [SHA-256 only](#sha-256-only)
    4. [Secret rotation](#secret-rotation)
    5. [Constant-time compare](#constant-time-compare)
    6. [Anti-replay window](#anti-replay-window)
    7. [Idempotency](#idempotency)
    8. [Retry-aware response codes](#retry-aware-response-codes)
    9. [Receiver examples](#receiver-examples)
    10. [Loopback Rust mock](#loopback-rust-mock)

    ---

    ## Overview

    Cronduit signs every webhook delivery using the [Standard Webhooks v1
    specification](https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md).
    The wire format (signing-string composition, base64 alphabet, header
    semantics) is the spec's — this document does NOT paraphrase the spec; it
    points to it and documents only the cronduit-specific operator-facing
    surface (which fields ship, how to verify, retry semantics, secret
    rotation).

    Operators implementing a receiver should:

    1. Read the [Standard Webhooks v1 spec](https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md) (~10 minutes).
    2. Pick a reference receiver in their language: [Python](../examples/webhook-receivers/python/README.md), [Go](../examples/webhook-receivers/go/README.md), or [Node](../examples/webhook-receivers/node/README.md).
    3. Run it against a real cronduit delivery via the corresponding `just uat-webhook-receiver-{python,go,node}` recipe.

    The full delivery flow:

    ```mermaid
    sequenceDiagram
        autonumber
        participant Cron as Cronduit (HttpDispatcher)
        participant Recv as Receiver (Python/Go/Node)
        participant Log as Receiver log

        Note over Cron: RunFinalized event
        Cron->>Cron: build payload (16 fields)
        Cron->>Cron: serialize → body_bytes (compact JSON)
        Cron->>Cron: webhook_id = ULID
        Cron->>Cron: webhook_ts = now() Unix seconds
        Cron->>Cron: sig = base64(HMAC-SHA256(secret, "${id}.${ts}.${body}"))
        Cron->>Recv: POST /<path><br/>webhook-id, webhook-timestamp,<br/>webhook-signature: v1,&lt;b64&gt;<br/>body=body_bytes
        Recv->>Recv: parse 3 headers (400 if missing)
        Recv->>Recv: |now - ts| ≤ 300s? (400 if not)
        Recv->>Recv: HMAC-SHA256(secret, id.ts.body) → expected
        Recv->>Recv: constant_time_compare(expected, decoded_sig)
        alt match
            Recv->>Log: verified run_id=N status=...
            Recv-->>Cron: 200 OK
        else mismatch
            Recv-->>Cron: 401 Unauthorized
        else exception
            Recv-->>Cron: 503 Service Unavailable
        end
        Note over Cron: Phase 18: log only<br/>Phase 20: 4xx=drop, 5xx=retry
    ```

    ---

    ## Three required headers

    Every signed delivery carries exactly three Standard Webhooks v1 headers
    (cronduit also sends `content-type: application/json`):

    | Header | Format | Source |
    |---|---|---|
    | `webhook-id` | 26-char Crockford-base32 ULID | `ulid::Ulid::new().to_string()` |
    | `webhook-timestamp` | 10-digit Unix epoch seconds | `chrono::Utc::now().timestamp()` |
    | `webhook-signature` | `v1,<base64-of-hmac-sha256>` (space-delimited multi-token; cronduit currently emits one) | HMAC-SHA256 over `${webhook-id}.${webhook-timestamp}.${body}` raw bytes |

    The signature header is space-delimited so future cronduit versions can
    emit multiple tokens during multi-secret rotation (v1.3+ candidate). Modern
    receivers SHOULD parse all `v1,...` tokens and accept on first match; the
    [shipped reference receivers](#receiver-examples) demonstrate this.

    ---

    ## SHA-256 only

    **Cronduit v1.2 ships SHA-256 only.** No algorithm-agility (SHA-384/512/Ed25519/etc.)
    and no multi-secret rotation cronduit-side; secret rotation lives on the
    receiver via a dual-secret verify window (see [§4 Secret rotation](#secret-rotation)).

    If your operator workflow requires algorithm-agility, file a v1.3 roadmap
    issue. The Standard Webhooks v1 spec reserves `v1a` (Ed25519) and `v1b`
    (asymmetric) tokens for future use; cronduit currently emits only `v1`.

    ---

    ## Secret rotation

    Cronduit holds **one secret per job** (the `webhook.secret` config value,
    typically `${WEBHOOK_SECRET}` env-interpolated). Multi-secret rotation
    happens on the **receiver side** with a dual-secret verify window:

    1. Receiver enters dual-secret mode: it accepts deliveries signed by
       either OLD or NEW secret.
    2. Operator updates cronduit's `webhook.secret` config value to the NEW
       secret, reloads cronduit (SIGHUP / `POST /api/reload`).
    3. After all in-flight retries drain (Phase 20 max retry window: ~5 min),
       the receiver drops OLD secret support and continues with NEW only.

    The reference receivers shipped with cronduit (Python, Go, Node) are
    SINGLE-secret for clarity; operators implementing rotation extend the
    `verify_signature` function to iterate over a `[OLD, NEW]` secret list and
    accept on first match.

    ---

    ## Constant-time compare

    Constant-time HMAC comparison is the central security requirement of
    WH-04. **Plain `==` on hex/base64 strings is forbidden** — short-circuit
    comparison leaks timing information that lets an attacker extract the
    expected signature one byte at a time.

    Each language ships a stdlib constant-time primitive:

    | Language | Primitive | Stdlib module | Length-guard required? |
    |---|---|---|---|
    | Python | [`hmac.compare_digest(a, b)`](https://docs.python.org/3/library/hmac.html#hmac.compare_digest) | `hmac` | No (handles unequal length internally) |
    | Go | [`hmac.Equal(macA, macB)`](https://pkg.go.dev/crypto/hmac#Equal) | `crypto/hmac` | No (handles unequal length internally) |
    | Node | [`crypto.timingSafeEqual(bufA, bufB)`](https://nodejs.org/api/crypto.html#cryptotimingsafeequala-b) | `crypto` (built-in) | **YES** — throws `RangeError` on length mismatch |

    The Node receiver's MANDATORY length guard (`if (received.length !== expected.length) continue;`)
    prevents the receiver from crashing when a malformed signature with the
    wrong byte-length arrives. The length check itself is non-constant-time,
    which is fine: HMAC-SHA256 output is fixed at 32 bytes, so a length
    mismatch only means the signature is structurally malformed (no secret
    material is leaked).

    The receiver verify decision tree:

    ```mermaid
    flowchart TD
        A[POST request arrives] --> B{All 3 headers present?}
        B -- no --> X1[400 Bad Request]
        B -- yes --> C{webhook-timestamp parses as int?}
        C -- no --> X1
        C -- yes --> D{|now - ts| ≤ 300s?}
        D -- no --> X1
        D -- yes --> E{webhook-signature starts with 'v1,'?}
        E -- no --> X2[401 Unauthorized]
        E -- yes --> F[Decode each v1,&lt;b64&gt; token<br/>space-delimited]
        F --> G[Compute HMAC-SHA256<br/>over id.ts.body]
        G --> H{Length-equal AND<br/>constant-time match<br/>against ANY token?}
        H -- no --> X2
        H -- yes --> I[Log verified outcome]
        I --> Y[200 OK]

        style X1 fill:#fdd
        style X2 fill:#fdd
        style Y fill:#dfd
    ```

    ---

    ## Anti-replay window

    Each delivery's `webhook-timestamp` MUST be within 5 minutes of the
    receiver's clock — `MAX_TIMESTAMP_DRIFT_SECONDS = 300` per the [Standard
    Webhooks v1 reference implementation](https://github.com/standard-webhooks/standard-webhooks/blob/main/libraries/javascript/src/index.ts)
    (`WEBHOOK_TOLERANCE_IN_SECONDS = 5 * 60`).

    A stale-timestamp delivery is rejected with **400 Bad Request** (permanent
    — no retry). The [retry-aware response codes table](#retry-aware-response-codes)
    documents this contract.

    The drift window is hard-coded in the reference receivers. Operators who
    need a different window edit `MAX_TIMESTAMP_DRIFT_SECONDS` in their fork
    of the receiver. Configurability is not in v1.2's scope.

    ---

    ## Idempotency

    Cronduit may redeliver a webhook on transient receiver failures (Phase
    20: 5xx response → retry t=30s, t=300s; max 3 attempts). To prevent
    side-effect duplication, receivers SHOULD dedupe by `webhook-id`:

    - **In-memory short-TTL Set/Map** (sufficient for stateless receivers
      with low traffic; 5-minute TTL covers the retry window).
    - **DB unique constraint on `webhook-id`** (durable; survives receiver
      restarts).

    The shipped reference receivers ship a comment block, NOT working dedup
    code (D-10). Working dedup needs a TTL story and state management that
    distracts from the HMAC focus. Operators implementing production
    receivers add their own.

    ---

    ## Retry-aware response codes

    Each receiver outcome maps to a specific HTTP status code that cronduit
    interprets per the table below. **This contract is locked at v1.2.0;
    Phase 20's retry implementation inherits it unchanged.**

    | Receiver outcome | HTTP status | Cronduit (Phase 20) interpretation |
    |---|---|---|
    | Missing/malformed headers | 400 | Permanent — drop, no retry |
    | Timestamp drift > 5 min | 400 | Permanent — drop, no retry |
    | HMAC mismatch | 401 | Permanent — drop, no retry |
    | Verify success | 200 | Success — counter increment |
    | Unexpected exception | 503 | Transient — Phase 20 retries (3 attempts t=0/30s/300s) |

    The retry state machine:

    ```mermaid
    stateDiagram-v2
        [*] --> Sent
        Sent --> Verified: 200 OK
        Sent --> Permanent: 4xx (400/401)
        Sent --> Transient: 5xx (500/503/timeout)
        Transient --> Sent: retry t=30s, t=300s
        Transient --> DeadLetter: after 3 attempts
        Verified --> [*]
        Permanent --> [*]: drop, no retry
        DeadLetter --> [*]: row in webhook_deliveries
    ```

    ---

    ## Receiver examples

    Three reference receivers ship with cronduit. Each is stdlib-only
    (no `pip install`/`go mod download`/`npm install` required), ~80-150 LOC,
    and demonstrates constant-time HMAC verify + 5-min anti-replay + the
    D-12 retry-aware response code mapping.

    | Language | Port | Path |
    |---|---|---|
    | Python | 9991 | [`examples/webhook-receivers/python/`](../examples/webhook-receivers/python/README.md) |
    | Go | 9992 | [`examples/webhook-receivers/go/`](../examples/webhook-receivers/go/README.md) |
    | Node | 9993 | [`examples/webhook-receivers/node/`](../examples/webhook-receivers/node/README.md) |

    Each receiver supports two modes:

    - **HTTP server mode** (default) — listens on `127.0.0.1:<port>`, verifies
      live deliveries from cronduit. Set `WEBHOOK_SECRET_FILE` to point at the
      secret file your cronduit instance signs with.
    - **`--verify-fixture <dir>` mode** — reads the 5 files in
      `tests/fixtures/webhook-v1/`, runs the same `verify_signature`
      function used by the HTTP path, exits 0 on canonical match / 1 on
      tamper. Used by the CI `webhook-interop` matrix job to lock the wire
      format across all 3 languages.

    The `just` recipes are the operator-facing surface:

    | Recipe | Purpose |
    |---|---|
    | `just uat-webhook-receiver-python` | Run Python receiver against real cronduit delivery |
    | `just uat-webhook-receiver-go` | Run Go receiver against real cronduit delivery |
    | `just uat-webhook-receiver-node` | Run Node receiver against real cronduit delivery |
    | `just uat-webhook-receiver-python-verify-fixture` | Verify Python receiver against fixture (canonical + 3 tamper variants) |
    | `just uat-webhook-receiver-go-verify-fixture` | Verify Go receiver against fixture (canonical + 3 tamper variants) |
    | `just uat-webhook-receiver-node-verify-fixture` | Verify Node receiver against fixture (canonical + 3 tamper variants) |

    ---

    ## Loopback Rust mock

    Phase 18 ships a Rust loopback mock at
    [`examples/webhook_mock_server.rs`](../examples/webhook_mock_server.rs)
    that listens on port 9999 and **always returns 200 — it does not verify
    HMAC**. Use it for inspecting raw headers, payload shape, and signature
    format during initial integration; switch to one of the verifying
    receivers (Python/Go/Node) once you've confirmed the wire format end to
    end.

    Run via `just uat-webhook-mock`. Pair with `just uat-webhook-fire <JOB_NAME>`
    to force a delivery and `just uat-webhook-verify` to tail the receiver
    log.
    ```

    Verify the file has all 10 sections:
    ```
    grep -c '^## ' docs/WEBHOOKS.md
    ```
    Must report 10 (TOC + 10 sections; actually 11 if TOC has its own `##` heading).

    Verify SHA-256-only note (SC-3):
    ```
    grep -q "Cronduit v1.2 ships SHA-256 only" docs/WEBHOOKS.md
    ```

    Verify the 3 mermaid diagrams are present:
    ```
    grep -c '^```mermaid$' docs/WEBHOOKS.md
    ```
    Must report 3.

    Verify NO ASCII-art diagrams (D-19):
    ```
    grep -E '┌|└|│|├|┤|─' docs/WEBHOOKS.md
    ```
    Must be empty (a few box-drawing chars OK in cron syntax fence in CONFIG.md, but Plan 05 doc should have NONE).

    Verify line count in the 200-450 range:
    ```
    wc -l docs/WEBHOOKS.md
    ```
  </action>
  <verify>
    <automated>test -f docs/WEBHOOKS.md && grep -q "Cronduit v1.2 ships SHA-256 only" docs/WEBHOOKS.md && test "$(grep -c '^```mermaid$' docs/WEBHOOKS.md)" = "3" && grep -q "examples/webhook-receivers/python/README.md" docs/WEBHOOKS.md && grep -q "examples/webhook-receivers/go/README.md" docs/WEBHOOKS.md && grep -q "examples/webhook-receivers/node/README.md" docs/WEBHOOKS.md && grep -q "MAX_TIMESTAMP_DRIFT_SECONDS = 300" docs/WEBHOOKS.md && grep -q "webhook_mock_server.rs" docs/WEBHOOKS.md && ! grep -E '┌|└|├|┤' docs/WEBHOOKS.md && test "$(wc -l < docs/WEBHOOKS.md)" -ge 200</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/WEBHOOKS.md` exit 0
    - `grep -q "Cronduit v1.2 ships SHA-256 only" docs/WEBHOOKS.md` (verbatim SC-3)
    - `test "$(grep -c '^```mermaid$' docs/WEBHOOKS.md)" = "3"` (3 mermaid diagrams)
    - `! grep -E '┌|└|├|┤' docs/WEBHOOKS.md` (D-19 — no ASCII box-drawing)
    - `grep -q "examples/webhook-receivers/python/README.md" docs/WEBHOOKS.md`
    - `grep -q "examples/webhook-receivers/go/README.md" docs/WEBHOOKS.md`
    - `grep -q "examples/webhook-receivers/node/README.md" docs/WEBHOOKS.md`
    - `grep -q "MAX_TIMESTAMP_DRIFT_SECONDS = 300" docs/WEBHOOKS.md` (5-min anti-replay constant)
    - `grep -q "webhook_mock_server.rs" docs/WEBHOOKS.md` (Phase 18 mock pointer)
    - `grep -q "hmac.compare_digest" docs/WEBHOOKS.md` (Python primitive in §5 table)
    - `grep -q "hmac.Equal" docs/WEBHOOKS.md` (Go primitive in §5 table)
    - `grep -q "crypto.timingSafeEqual" docs/WEBHOOKS.md` (Node primitive in §5 table)
    - `grep -q "standard-webhooks/standard-webhooks" docs/WEBHOOKS.md` (spec link)
    - `test "$(wc -l < docs/WEBHOOKS.md)" -ge "200"` (lower bound)
    - `test "$(wc -l < docs/WEBHOOKS.md)" -le "450"` (upper bound)
    - `grep -c '^## ' docs/WEBHOOKS.md` returns at least `10` (10 sections + TOC)
  </acceptance_criteria>
  <done>
    - `docs/WEBHOOKS.md` exists with all 10 sections
    - SHA-256-only note present verbatim (SC-3)
    - 3 mermaid diagrams (System Architecture, Verify Decision Tree, Phase 20 Retry)
    - No ASCII art (D-19)
    - Cross-links to all 3 receiver READMEs (Python, Go, Node)
    - Verbatim D-12 retry-aware response codes table in §8
    - Per-language constant-time primitive table in §5 with documentation URLs
    - Line count between 200 and 450
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add webhook back-link in `docs/CONFIG.md` + one-line pointer in `README.md`</name>
  <files>docs/CONFIG.md, README.md</files>
  <read_first>
    - `docs/CONFIG.md` (read entire file to find the right insertion point — after § Validation around line 542, before § Hot reload around line 575; OR add a new short `## Webhooks` section as the back-link landing pad)
    - `README.md` lines 170-180 (existing pointer block at lines 174-175 — Plan 05 adds a third pointer line)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-CONTEXT.md` (D-07 no content duplication, D-08 no README sprawl)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-PATTERNS.md` (`docs/CONFIG.md (MODIFY...)` and `README.md (MODIFY...)` sections)
  </read_first>
  <behavior>
    - `docs/CONFIG.md`: ADD a short `## Webhooks` section near the end of the file (after § Validation at line 542, before § Hot reload at line 575) that contains the back-link to `docs/WEBHOOKS.md` and a one-paragraph TOML field summary (NOT the full receiver implementation guidance — that lives in WEBHOOKS.md per D-07)
    - `README.md`: ADD a third pointer line in the existing pointer block at lines 174-175 (right after the CONFIG.md line) — a single line per D-08
    - Both modifications use the existing precedent of cross-references (CONFIG.md line 5 pattern; README.md line 174-175 pattern)
  </behavior>
  <action>
    **Step 1 — Modify `docs/CONFIG.md`.** Use the Edit tool. Find the line `## Hot reload` (around line 575) and INSERT the following block IMMEDIATELY BEFORE it:

    ```markdown
    ## Webhooks

    Cronduit emits Standard Webhooks v1 deliveries on terminal job-run events.
    Configure per-job:

    ```toml
    [[jobs]]
    name = "my-job"
    schedule = "* * * * *"
    command = "..."
    webhook = { url = "https://hook.example.com", secret = "${WEBHOOK_SECRET}" }
    ```

    Or in `[defaults]` (with the `use_defaults = false` per-job disable
    pattern) — see Phase 18's `WH-01` for the merge semantics.

    For receiver implementation guidance (Python, Go, Node), the verify
    protocol, secret rotation, retry semantics, and constant-time compare
    primitives, see [`WEBHOOKS.md`](./WEBHOOKS.md).

    ```

    **Step 2 — Modify `README.md`.** Use the Edit tool. Find the line:

    ```
    > **Looking up a specific field?** The complete reference is in **[docs/CONFIG.md](./docs/CONFIG.md)**. The section below is a cheat sheet.
    ```

    INSERT the following line IMMEDIATELY AFTER it (matching the existing blockquote pointer style):

    ```
    > **Verifying webhook deliveries?** Receiver examples (Python/Go/Node) and the verify protocol live in **[docs/WEBHOOKS.md](./docs/WEBHOOKS.md)**.
    ```

    Verify both insertions:
    ```
    grep -q "## Webhooks" docs/CONFIG.md
    grep -q "WEBHOOKS\.md" docs/CONFIG.md
    grep -q "Verifying webhook deliveries" README.md
    grep -q "docs/WEBHOOKS.md" README.md
    ```
  </action>
  <verify>
    <automated>grep -q "^## Webhooks" docs/CONFIG.md && grep -q "WEBHOOKS\.md" docs/CONFIG.md && grep -q "Verifying webhook deliveries" README.md && grep -q "docs/WEBHOOKS\.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Webhooks" docs/CONFIG.md`
    - `grep -q "WEBHOOKS\.md" docs/CONFIG.md`
    - `grep -q "Verifying webhook deliveries" README.md`
    - `grep -q "docs/WEBHOOKS\.md" README.md`
    - `! grep -q "verify_signature" docs/CONFIG.md` (D-07 — receiver implementation guidance lives in WEBHOOKS.md, NOT CONFIG.md)
  </acceptance_criteria>
  <done>
    - `docs/CONFIG.md` contains a `## Webhooks` section with a back-link to `WEBHOOKS.md` (D-07: no duplication; the section is short and points OUT)
    - `README.md` contains a one-line pointer to `docs/WEBHOOKS.md` in the existing pointer block (D-08: no sprawl)
    - The insertion points preserve existing CONFIG.md and README.md prose
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 3 commented-out `wh-example-receiver-{python,go,node}` jobs to `examples/cronduit.toml`</name>
  <files>examples/cronduit.toml</files>
  <read_first>
    - `examples/cronduit.toml` lines 218-256 (existing webhook example jobs — `wh-example-unsigned` enabled, `wh-example-signed` and `wh-example-fire-every-zero` commented templates)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-RESEARCH.md` (Code Examples §Example 7 — the 3 new jobs literal; Pitfall 7 — config-time URL, NOT runtime override)
    - `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-PATTERNS.md` (`examples/cronduit.toml (MODIFY...)` section — same `wh-example-signed` shape verbatim; commented-out per Phase 18 D-05)
  </read_first>
  <behavior>
    - 3 new TOML jobs APPENDED after the existing Phase 18 webhook templates (after line 256)
    - All 3 ship COMMENTED-OUT (`#` prefix on every line) — matches Phase 18 D-05 (keep `docker compose up` smoke clean)
    - Each job:
      - `command = "false"` (always-fails — exercises failure firing path)
      - `use_defaults = false` (locked LBL-04 + Watchtower-inheritance precedent)
      - `timeout = "5m"`
      - `webhook = { url = "http://127.0.0.1:99XX/", secret = "${WEBHOOK_SECRET}" }`
      - URL ports: 9991 (Python), 9992 (Go), 9993 (Node)
    - Block introduction comment explains the 3 receiver-target jobs (mirrors lines 226-234's introduction of the existing signed templates)
    - Cross-link in the comment block to `docs/WEBHOOKS.md`
  </behavior>
  <action>
    Use the Edit tool. Find the end of the existing webhook example jobs in `examples/cronduit.toml` (after line 256, the line ending with `webhook = { url = "http://127.0.0.1:9999/timeouts-only", states = ["timeout"], fire_every = 0, secret = "${WEBHOOK_SECRET}" }`).

    APPEND the following block AFTER that line (preserve trailing blank line if present in original; insert after the existing block):

    ```toml

    # --- Phase 19 receiver examples (loopback HMAC verification) ----------------
    #
    # These three jobs target the Python/Go/Node example receivers shipped at
    # examples/webhook-receivers/{python,go,node}/. They are commented-out
    # templates by default — uncomment to enable, and `export WEBHOOK_SECRET` first.
    # See docs/WEBHOOKS.md for the receiver run instructions.
    #
    # [[jobs]]
    # name = "wh-example-receiver-python"
    # schedule = "* * * * *"
    # command = "false"
    # use_defaults = false
    # timeout = "5m"
    # webhook = { url = "http://127.0.0.1:9991/", secret = "${WEBHOOK_SECRET}" }
    #
    # [[jobs]]
    # name = "wh-example-receiver-go"
    # schedule = "* * * * *"
    # command = "false"
    # use_defaults = false
    # timeout = "5m"
    # webhook = { url = "http://127.0.0.1:9992/", secret = "${WEBHOOK_SECRET}" }
    #
    # [[jobs]]
    # name = "wh-example-receiver-node"
    # schedule = "* * * * *"
    # command = "false"
    # use_defaults = false
    # timeout = "5m"
    # webhook = { url = "http://127.0.0.1:9993/", secret = "${WEBHOOK_SECRET}" }
    ```

    Verify the 3 jobs are present (commented-out — so `grep` for the `name = "wh-example-receiver-..."` line):
    ```
    grep -E "^# name = \"wh-example-receiver-(python|go|node)\"" examples/cronduit.toml
    ```
    Must produce 3 lines.

    Verify URLs target the correct ports:
    ```
    grep -E "127\.0\.0\.1:999[123]" examples/cronduit.toml
    ```
    Must produce 3 lines.

    Verify the in-tree config still parses (the new templates are commented out, so this should pass unchanged):
    ```
    just check-config examples/cronduit.toml
    ```
    Must exit 0 (templates ship commented-out per D-05). Note: this requires `WEBHOOK_SECRET` env var because `wh-example-unsigned` is uncommented and references `${WEBHOOK_SECRET}` — set `WEBHOOK_SECRET=test-secret` before invocation, OR confirm whether `wh-example-unsigned` actually uses the env var. If just-check-config fails on the existing config, that is a Phase 18 issue, NOT a Plan 05 regression — check by running `just check-config examples/cronduit.toml` BEFORE the edit.
  </action>
  <verify>
    <automated>test "$(grep -cE '^# name = \"wh-example-receiver-(python|go|node)\"' examples/cronduit.toml)" = "3" && test "$(grep -cE '127\.0\.0\.1:999[123]' examples/cronduit.toml)" = "3" && grep -q "docs/WEBHOOKS.md" examples/cronduit.toml</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -cE '^# name = \"wh-example-receiver-(python|go|node)\"' examples/cronduit.toml)" = "3"`
    - `test "$(grep -cE '127\.0\.0\.1:999[123]' examples/cronduit.toml)" = "3"`
    - `grep -q "127.0.0.1:9991" examples/cronduit.toml` (Python port)
    - `grep -q "127.0.0.1:9992" examples/cronduit.toml` (Go port)
    - `grep -q "127.0.0.1:9993" examples/cronduit.toml` (Node port)
    - `grep -q "docs/WEBHOOKS.md" examples/cronduit.toml` (cross-link in introduction comment)
    - `! grep -E '^\[\[jobs\]\]\s*$' examples/cronduit.toml | tail -3 | grep -v "^# "` (the 3 new jobs ship commented; the new uncommented `[[jobs]]` block count is unchanged from Phase 18 baseline)
    - `WEBHOOK_SECRET=test-secret-shh just check-config examples/cronduit.toml` exit 0 (templates commented-out preserve D-05 smoke)
  </acceptance_criteria>
  <done>
    - `examples/cronduit.toml` contains 3 new commented-out jobs: `wh-example-receiver-python`, `wh-example-receiver-go`, `wh-example-receiver-node`
    - URL ports are 9991, 9992, 9993 respectively (D-03)
    - All 3 jobs ship commented-out (preserves clean `docker compose up` smoke per D-05)
    - Block introduction comment cross-links to `docs/WEBHOOKS.md`
  </done>
</task>

</tasks>

<verification>
Phase-level verification for Plan 05:

1. **`docs/WEBHOOKS.md` is the 10-section operator hub** (D-06):
   ```
   grep -c '^## ' docs/WEBHOOKS.md       # 10 sections + TOC
   wc -l docs/WEBHOOKS.md                # 200-450 lines
   ```

2. **SHA-256-only note present verbatim (SC-3)**:
   ```
   grep -q "Cronduit v1.2 ships SHA-256 only" docs/WEBHOOKS.md
   ```

3. **3 mermaid diagrams (D-19, no ASCII)**:
   ```
   test "$(grep -c '^```mermaid$' docs/WEBHOOKS.md)" = "3"
   ! grep -E '┌|└|├|┤' docs/WEBHOOKS.md
   ```

4. **Cross-links to all 3 receiver READMEs**:
   ```
   for lang in python go node; do
     grep -q "examples/webhook-receivers/${lang}/README.md" docs/WEBHOOKS.md
   done
   ```

5. **CONFIG.md back-link present**:
   ```
   grep -q "WEBHOOKS\.md" docs/CONFIG.md
   ```

6. **README.md pointer present**:
   ```
   grep -q "docs/WEBHOOKS\.md" README.md
   ```

7. **3 new example jobs added (commented-out)**:
   ```
   test "$(grep -cE '^# name = \"wh-example-receiver-(python|go|node)\"' examples/cronduit.toml)" = "3"
   ```

8. **`just check-config examples/cronduit.toml`** still passes (templates commented-out per D-05; you may need `WEBHOOK_SECRET=test-secret` env-var prefix per the existing `wh-example-unsigned` job's reference).

9. **Zero new Rust crates** (D-24 — Plan 05 is doc-only):
   ```
   just openssl-check
   ```

10. **Mermaid render check** (manual; covered by Plan 06 UAT) — Maintainer verifies via GitHub PR preview that the 3 mermaid diagrams render as SVG, NOT raw fenced text.
</verification>

<success_criteria>
- [ ] `docs/WEBHOOKS.md` exists with all 10 sections
- [ ] `docs/WEBHOOKS.md` line count is between 200 and 450
- [ ] `docs/WEBHOOKS.md` contains the verbatim SHA-256-only note (SC-3)
- [ ] `docs/WEBHOOKS.md` contains 3 mermaid code blocks (System Architecture sequenceDiagram, Verify Decision Tree flowchart, Phase 20 Retry stateDiagram)
- [ ] `docs/WEBHOOKS.md` contains NO box-drawing characters (D-19 mermaid-only enforcement)
- [ ] `docs/WEBHOOKS.md` cross-links to all 3 receiver READMEs (Python/Go/Node)
- [ ] `docs/WEBHOOKS.md` contains the verbatim D-12 retry-aware response codes table in §8
- [ ] `docs/WEBHOOKS.md` contains the per-language constant-time primitive table in §5
- [ ] `docs/CONFIG.md` contains a `## Webhooks` section with a back-link to `WEBHOOKS.md` (D-07 no duplication)
- [ ] `README.md` contains a one-line pointer to `docs/WEBHOOKS.md` in the existing pointer block (D-08 no sprawl)
- [ ] `examples/cronduit.toml` contains 3 new commented-out `wh-example-receiver-{python,go,node}` jobs targeting ports 9991/9992/9993
- [ ] `just check-config examples/cronduit.toml` exits 0 (templates commented-out preserve D-05)
- [ ] `just openssl-check` returns empty (D-24)
</success_criteria>

<output>
After completion, create `.planning/phases/19-webhook-hmac-signing-receiver-examples/19-05-SUMMARY.md` documenting:
- The new `docs/WEBHOOKS.md` (line count, 10 sections, 3 mermaid diagrams)
- The CONFIG.md back-link and README.md pointer additions (single-line each)
- The 3 new `wh-example-receiver-*` jobs (all commented-out per D-05)
- Confirmation `just check-config` is green
- Plan 06 unblocked: CI matrix + maintainer UAT can now reference the docs and example jobs
</output>
