---
phase: 23
plan: 08
type: execute
wave: 7
depends_on: [23-07]
files_modified:
  - .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md
autonomous: false
requirements: []
requirements_addressed: []
tags: [rc-cut, release, preflight, maintainer, ghcr]
threat_model: included
must_haves:
  truths:
    - "`.planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` exists and mirrors `21-RC2-PREFLIGHT.md` verbatim modulo `rc.2 → rc.3`, `P21 → P23`, `FCTX UI panel + exit-code histogram → dashboard tag filter chips` substitutions"
    - "Frontmatter: `phase: 23`, `plan: 08`, `type: rc-preflight`, `autonomous: false`, `rc_tag: v1.2.0-rc.3`, `created: 2026-05-04`, `status: pending-maintainer-execution`"
    - "Section 1 (PR-merged checklist): replaces P21 plans 01-10 with P23 plans 01-07 (Plan 23-08 is THIS file; the rc.3 cut happens AFTER the entire P23 PR merges with all preceding plans inside)"
    - "Section 5 (cardinality discipline) — replaces EXIT-06 grep with the analogous P23 invariant: tags do NOT appear as a Prometheus label per CONTEXT § Out of scope"
    - "Section 8 (tag command, copy-paste verbatim per D-15): `git tag -a -s v1.2.0-rc.3 -m \"v1.2.0-rc.3 — dashboard tag filter chips (P23)\"`"
    - "Section 9 (post-publish verification) — substitutes `rc.2 → rc.3` in every digest-comparison reference; `:latest` invariant assertion still references `cronduit:1.1.0` (NOT `1.2.0`) per D-15"
    - "Out-of-scope section per D-16: NO modifications to `release.yml` / `cliff.toml` / `docs/release-rc.md`"
    - "Cross-reference footer: `21-RC2-PREFLIGHT.md` reference extended to `20-RC1-PREFLIGHT.md → 21-RC2-PREFLIGHT.md → 23-RC3-PREFLIGHT.md`"
    - "Maintainer-only execution per D-15 / D-26 + project memory `feedback_no_direct_main_commits.md` (the rc tag is cut from `main` AFTER P23 PR merges; the maintainer executes locally on a clean checkout)"
    - "V-17 satisfied — preflight checklist exists; maintainer execution + sign-off + GHCR multi-arch publish verification documented for execution at the rc.3 cut moment"
    - "D-18: [informational] all Phase 23 changes (including this rc.3 preflight) land via PR on a feature branch — `git push origin v1.2.0-rc.3` in the runbook is a maintainer-local action AFTER the P23 PR merges to main; no direct commits to main (per CONTEXT decision register / project memory feedback_no_direct_main_commits.md)."
    - "D-22: [informational] Cargo.toml stays at `1.2.0` for rc.3 — the `-rc.3` suffix is tag-only (per CONTEXT decision register / project memory feedback_tag_release_version_match.md). The runbook explicitly verifies `cronduit --version` returns `cronduit 1.2.0` after the rc.3 image publish."
  artifacts:
    - path: .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md
      provides: "Maintainer-validated rc.3 cut runbook (mirrors P21 RC2-PREFLIGHT verbatim)"
      contains: "v1.2.0-rc.3"
  key_links:
    - from: 23-RC3-PREFLIGHT.md
      to: docs/release-rc.md (UNCHANGED — reused verbatim per D-15 / D-16)
      via: "preflight references the runbook by relative path; NO edits to release.yml / cliff.toml / docs/release-rc.md"
      pattern: "docs/release-rc.md"
    - from: 23-RC3-PREFLIGHT.md Section 7
      to: 23-HUMAN-UAT.md (Plan 23-07 output; signed by maintainer)
      via: "Section 7 blocks on every scenario in HUMAN-UAT.md being ticked + Final sign-off filled in"
      pattern: "23-HUMAN-UAT.md"
    - from: 23-RC3-PREFLIGHT.md Section 8 tag command
      to: maintainer git/GPG environment
      via: "literal `git tag -a -s v1.2.0-rc.3 ...` command — Claude does NOT execute this"
      pattern: "v1.2.0-rc.3"
---

<objective>
**Wave 6 — rc.3 cut preflight runbook.** Author `23-RC3-PREFLIGHT.md` mirroring `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` verbatim modulo the substitutions: `rc.2 → rc.3`, `P21 → P23`, `FCTX UI panel + exit-code histogram → dashboard tag filter chips`, `EXIT-06 → tags-as-Prometheus-label-out-of-scope`, plan-list `01-10 → 01-07`. Per CONTEXT D-15, this is the structural lock for the rc.3 cut.

Purpose: cuts `v1.2.0-rc.3` AFTER the Phase 23 PR merges to `main`. The runbook is the maintainer-only checklist + literal tag command + post-publish verification matrix. `:latest` GHCR tag stays at `v1.1.0` (the hyphen-gate from P12 D-10 enforces this on rc tags). `Cargo.toml` stays at `1.2.0` (the `-rc.3` suffix is tag-only).

**Plan-level autonomous=false** because the maintainer must EXECUTE the runbook locally (cuts the tag, pushes, monitors GHCR publish). Claude writes the file deterministically; structural greps verify shape; the maintainer fills in every checkbox in a future session.

Output: one new markdown file mirroring P21 RC2-PREFLIGHT verbatim with the substitutions applied. NO modifications to `release.yml` / `cliff.toml` / `docs/release-rc.md` (D-16). NO modifications to `Cargo.toml` (D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-CONTEXT.md
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-09-PLAN.md
@docs/release-rc.md

<interfaces>
LOAD-BEARING analog the executor must read first AND COPY VERBATIM:

From `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` — the entire 206-line file. The Phase 23 RC3-PREFLIGHT is a LITERAL MIRROR with the substitutions below.

**Substitution table (apply to the entire file):**

| Find | Replace |
|------|---------|
| `phase: 21` | `phase: 23` |
| `plan: 11` | `plan: 08` |
| `rc_tag: v1.2.0-rc.2` | `rc_tag: v1.2.0-rc.3` |
| `created: 2026-05-02` | `created: 2026-05-04` |
| `Phase 21` (every occurrence) | `Phase 23` |
| `v1.2.0-rc.2` (every occurrence) | `v1.2.0-rc.3` |
| `rc.2` (every occurrence — verify context, NOT inside historical references like `rc.1 → rc.2`) | `rc.3` |
| `Failure-Context UI Panel + Exit-Code Histogram Card` | `Job Tagging Dashboard Filter Chips` |
| `FCTX UI panel + exit-code histogram (P21)` | `dashboard tag filter chips (P23)` |
| `D-22..D-26` (informational citations of P21 decisions) | `D-15..D-16` (corresponding P23 decisions) |
| `Plan 01 (\`scheduled_for\` migration ...) merged.` ... through `Plan 10 (...) merged.` (lines 26-35 — the ten-plan list for P21) | NEW list of seven P23 plans (01..07; this rc preflight is plan 08, NOT in the list) |
| `EXIT-06 cardinality discipline holds (out-of-scope verification)` (Section 5 heading) | `Tags-as-Prometheus-label out-of-scope verification (per CONTEXT § deferred)` |
| `grep -rn 'exit_code' src/telemetry.rs` AND surrounding two grep commands | new greps confirming tags do not become a Prometheus label (see Step C below) |
| `21-HUMAN-UAT.md` references | `23-HUMAN-UAT.md` references |
| `21-RC2-PREFLIGHT.md` (in cross-reference footer) | `23-RC3-PREFLIGHT.md` |
| `mirroring v1.1 P12 + v1.2 P20 discipline` | `mirroring v1.1 P12 + v1.2 P20/P21 discipline` |

**`:latest` invariant — UNCHANGED:** in Section 9 verification, the assertion is still `LATEST_DIGEST == V1_1_0_DIGEST` (latest still equals `:1.1.0`, NOT `:1.2.0`). Per D-15 the `:latest` tag stays at v1.1.0 until Phase 24 final ship.

**Plan list substitution (Section 1) — full replacement:**

The P21 plan list at lines 26-35 covers plans 01-10 of P21. Replace with:

```
- [ ] PR for Plan 01 (Wave-0 test scaffolding — tests/v12_tags_dashboard.rs + dashboard.rs::tests stubs) merged.
- [ ] PR for Plan 02 (DB layer — DashboardJob.tags + get_dashboard_jobs SELECT/WHERE widening for AND-tag filter) merged.
- [ ] PR for Plan 03 (handler — axum_extra::Query swap + DashboardParams.tags + fleet-tag fold + active-set sort/dedup/intersect) merged.
- [ ] PR for Plan 04 (CSS — cd-tag-chip-* family in @layer components + reduced-motion + print extensions) merged.
- [ ] PR for Plan 05 (template — chip strip insert + sort-header href widening + poll hx-include widening + OOB swap response composition) merged.
- [ ] PR for Plan 06 (UAT — 3 just uat-chips-* recipes + README Tag Filter Chips subsection) merged.
- [ ] PR for Plan 07 (HUMAN-UAT — 23-HUMAN-UAT.md autonomous=false maintainer plan) merged.
```

(Plan 08 is THIS rc preflight; it doesn't get a "merged" checkbox because the rc cut happens AFTER it lands in the same PR. The verification command stays `gh pr list --state merged --search "phase-23" --limit 30`.)

**Section 5 substitution (cardinality discipline):**

Replace P21's EXIT-06 grep block:

```bash
grep -rn 'exit_code' src/telemetry.rs              # MUST return empty
grep -rn 'cronduit_runs_total.*exit_code' src/    # MUST return empty
grep -rn 'exit_code' src/web/handlers/metrics.rs   # MUST return empty (no such file)
```

with the P23 tags equivalent:

```bash
grep -rn 'tags' src/telemetry.rs                              # MUST return empty (or no Prometheus label use)
grep -rn 'cronduit_runs_total.*tags' src/                     # MUST return empty
grep -rn 'tags' src/web/handlers/metrics.rs                    # MUST return empty (no such file in tree)
```

The semantics: confirm Phase 23 did NOT add `tags` as a Prometheus label per CONTEXT § Out of scope ("Tags as Prometheus label — explicit out-of-scope (cardinality discipline; same posture as exit codes per EXIT-06)").

**Section 8 substitution (tag command):**

```bash
git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"
```

**Section 9 substitution (post-publish verification):** every reference to `rc.2`, `v1.2.0-rc.2` becomes `rc.3`, `v1.2.0-rc.3`. The `:latest` invariant block STAYS the same (compares `:latest` to `:1.1.0`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 23-RC3-PREFLIGHT.md as a verbatim mirror of 21-RC2-PREFLIGHT.md with locked substitutions</name>
  <files>.planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md (the ENTIRE 206-line file — read in full; this is the verbatim mirror source)
    - .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-CONTEXT.md § decisions D-15, D-16 (the rc.3 cut substitutions + the no-edit lock on release engineering files)
    - docs/release-rc.md (UNCHANGED — but read to understand what the runbook references)
    - .planning/REQUIREMENTS.md § Out of scope re: Tags-as-Prometheus-label (the EXIT-06 → tags-out-of-scope swap)
  </read_first>
  <action>
**Approach:** read `21-RC2-PREFLIGHT.md` end-to-end. Write `23-RC3-PREFLIGHT.md` by APPLYING the substitution table from `<interfaces>` section. Use the `Write` tool — DO NOT use heredoc. Frontmatter delimiters MUST be literal `---` lines.

**Step A — Read 21-RC2-PREFLIGHT.md fully** before writing. Note the structure:
1. Frontmatter (lines 1-9)
2. Title + intro paragraph (lines 11-23)
3. Section 1: P21 plans merged on `main` (lines 25-41)
4. Section 2: CI matrix green on main (lines 43-56)
5. Section 3: rustls invariant intact (lines 58-66)
6. Section 4: release.yml `:latest` gate logic intact (lines 68-82)
7. Section 5: EXIT-06 cardinality discipline (lines 84-95)
8. Section 6: git-cliff release-notes preview (lines 97-107)
9. Section 7: HUMAN-UAT sign-off (lines 109-114)
10. Section 8: Tag command (lines 116-133)
11. Section 9: Post-publish verification (lines 135-156)
12. Out-of-scope section (lines 158-167)
13. What if UAT fails (lines 169-176)
14. Sign-off table (lines 178-196)
15. Cross-reference footer (lines 198-205)

**Step B — Apply substitutions and write the new file.** Substitute every occurrence per the substitution table from `<interfaces>`. Pay special attention to:

- Frontmatter: phase 21→23, plan 11→08, rc_tag rc.2→rc.3, created date.
- Title: "Phase 21 — v1.2.0-rc.2 Tag Cut Pre-Flight" → "Phase 23 — v1.2.0-rc.3 Tag Cut Pre-Flight"
- Intro paragraph references to FCTX/EXIT histograms → "dashboard tag filter chips (P23)"
- Section 1: replace the 10-plan list with the 7-plan list per the `<interfaces>` substitution
- Section 5: replace EXIT-06 greps with tags-Prometheus greps per the `<interfaces>` substitution
- Section 8: tag command becomes `git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"`
- Section 9: every `rc.2` → `rc.3`, every `v1.2.0-rc.2` → `v1.2.0-rc.3`. The `:latest` invariant block (`LATEST_DIGEST == V1_1_0_DIGEST`) STAYS — `:latest` still references `:1.1.0`.
- Out-of-scope section: keep verbatim — same release engineering files (`release.yml`, `cliff.toml`, `docs/release-rc.md`) untouched per D-16.
- Cross-reference footer: extend the chain from `20-RC1 → 21-RC2` to `20-RC1 → 21-RC2 → 23-RC3`.

**Step C — Section 1 plan list — write the seven plans Phase 23 ships:**

```
- [ ] PR for Plan 01 (Wave-0 test scaffolding — tests/v12_tags_dashboard.rs + dashboard.rs::tests stubs) merged.
- [ ] PR for Plan 02 (DB layer — DashboardJob.tags + get_dashboard_jobs SELECT/WHERE widening for AND-tag filter) merged.
- [ ] PR for Plan 03 (handler — axum_extra::Query swap + DashboardParams.tags + fleet-tag fold + active-set sort/dedup/intersect) merged.
- [ ] PR for Plan 04 (CSS — cd-tag-chip-* family in @layer components + reduced-motion + print extensions) merged.
- [ ] PR for Plan 05 (template — chip strip insert + sort-header href widening + poll hx-include widening + OOB swap response composition) merged.
- [ ] PR for Plan 06 (UAT — 3 just uat-chips-* recipes + README Tag Filter Chips subsection) merged.
- [ ] PR for Plan 07 (HUMAN-UAT — 23-HUMAN-UAT.md autonomous=false maintainer plan) merged.
```

(Plan 08 = this preflight; it doesn't appear in the merged-plan list because it's part of the SAME PR that contains plans 01-07.)

**Step D — Section 5 cardinality discipline — write the tag-Prometheus-label greps:**

```markdown
## 5. Tags-as-Prometheus-label out-of-scope verification (per CONTEXT § deferred)

- [ ] `grep -rn 'tags' src/telemetry.rs` returns empty — confirms Phase 23 did NOT add a per-job `tags` Prometheus label per CONTEXT § Out of scope ("Tags as Prometheus label — explicit out-of-scope; same cardinality posture as exit codes per EXIT-06").
- [ ] `grep -rn 'cronduit_runs_total.*tags' src/` returns empty — confirms the `cronduit_runs_total` counter family has only `{job, status}` labels (Phase 22's TAG additions did not extend the metric family).
- [ ] `grep -rn 'tags' src/web/handlers/metrics.rs` returns empty (vacuously satisfied — no such file in tree; metrics route lives in `src/telemetry.rs`).

Verification:
\`\`\`bash
grep -rn 'tags' src/telemetry.rs              # MUST return empty
grep -rn 'cronduit_runs_total.*tags' src/    # MUST return empty
grep -rn 'tags' src/web/handlers/metrics.rs   # MUST return empty (no such file)
\`\`\`
```

**Step E — Section 7 (HUMAN-UAT sign-off) — point at 23-HUMAN-UAT.md:**

```markdown
## 7. 23-HUMAN-UAT.md sign-off

- [ ] All maintainer checkboxes in `23-HUMAN-UAT.md` are ticked `[x]` (six scenarios + the rustls spot check).
- [ ] The Sign-off block at the bottom of `23-HUMAN-UAT.md` is filled in (maintainer name + date + comment).

> Per D-15 + project memory `feedback_uat_user_validates.md`: Claude does NOT mark UAT passed; the maintainer flips every checkbox manually.
```

**Step F — Section 8 (tag command) — verbatim per D-15:**

```markdown
## 8. Tag command (maintainer runs LOCALLY) — D-15

On a clean checkout of `main` at the merge commit:

\`\`\`bash
git checkout main
git pull --ff-only origin main
git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"
git push origin v1.2.0-rc.3
\`\`\`

> Per D-15 + project memory `feedback_no_direct_main_commits.md`: this is a maintainer-local action. Claude does NOT execute it. The `-s` flag requires the maintainer's GPG signing key (per `docs/release-rc.md` Step 2a). If the maintainer's git is not GPG-configured, fall back to `docs/release-rc.md` Step 2b (unsigned annotated tag) — the runbook covers both paths.

The literal tag command (copy-paste verbatim):

\`\`\`bash
git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"
\`\`\`
```

**Step G — Cross-reference footer (extend the chain):**

```markdown
**Cross-reference:** this runbook mirrors `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` with `rc.2 → rc.3`, `P21 → P23`, `FCTX UI panel + exit-code histogram → dashboard tag filter chips`, and the EXIT-06 cardinality verification swapped for the analogous P23 invariant (tags-as-Prometheus-label out-of-scope). The structural reuse is intentional per D-15: same runbook (`docs/release-rc.md`), same gate semantics, same `:latest` invariant detection. The only Phase-23-specific additions are the tags-cardinality grep verification (§5) and the tag-chip-scoped tag message (§8). The chain extends `.planning/phases/20-...-rc-1/20-RC1-PREFLIGHT.md → .planning/phases/21-...-rc-2/21-RC2-PREFLIGHT.md → .planning/phases/23-...-rc-3/23-RC3-PREFLIGHT.md`.
```

**CRITICAL — Verbatim mirror.** Every section that doesn't appear in the substitution table stays IDENTICAL to P21 RC2-PREFLIGHT. Section 2 (CI matrix), Section 3 (rustls), Section 4 (release.yml gate), Section 6 (git-cliff preview), the Out-of-scope block, "What if UAT fails" — all stay verbatim modulo the rc.2→rc.3 / P21→P23 substitutions.

**CRITICAL — `:latest` invariant.** Section 9 still asserts `LATEST_DIGEST == V1_1_0_DIGEST` (latest equals 1.1.0). Per D-15: `:latest` stays at v1.1.0 until Phase 24 final ship. Do NOT change this to `1.2.0`.

**CRITICAL — `Cargo.toml` stays at 1.2.0.** The intro paragraph mentions this (Phase 15 set the version; rc tags are tag-only suffix per project memory `feedback_tag_release_version_match.md`). Verify the runbook's intro reflects this.

**CRITICAL — NO modifications to `release.yml`, `cliff.toml`, `docs/release-rc.md`.** Per D-16. The Out-of-scope section MUST contain explicit "DO NOT modify" warnings for these three files. Acceptance grep verifies this.

**CRITICAL — Use `Write` tool (the file is new); do NOT use `cat << EOF`.**

**CRITICAL — Frontmatter delimiters MUST be literal `---` lines** (not HTML-comment placeholders). Acceptance grep enforces.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; [ "$(grep -c '^---$' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md)" = "2" ] &amp;&amp; grep -q 'rc_tag: v1.2.0-rc.3' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; grep -q 'autonomous: false' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; grep -qF 'git tag -a -s v1.2.0-rc.3' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; grep -qF 'dashboard tag filter chips (P23)' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; grep -qF '23-HUMAN-UAT.md' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md &amp;&amp; grep -qF 'cronduit:1.1.0' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - Frontmatter delimiters: `grep -c '^---$' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` returns `2`
    - `autonomous: false` in frontmatter: `grep -q '^autonomous: false' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - `rc_tag: v1.2.0-rc.3`: `grep -q '^rc_tag: v1.2.0-rc.3' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - `phase: 23`: `grep -q '^phase: 23' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - Tag command literal: `grep -qF 'git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - Section 1 lists seven plans: `grep -cE '^- \[ \] PR for Plan 0[1-7]' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` returns `7`
    - References 23-HUMAN-UAT.md: `grep -qF '23-HUMAN-UAT.md' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - `:latest` invariant assertion preserved (still references 1.1.0): `grep -qF 'cronduit:1.1.0' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md`
    - Out-of-scope section forbids release engineering edits: `grep -F 'DO NOT modify' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` matches at least 3 times (release.yml, cliff.toml, docs/release-rc.md)
    - Tags cardinality verification present: `grep -F 'cronduit_runs_total.*tags' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` matches at least 1
    - NO references to rc.2 outside historical context: a grep for `v1.2.0-rc.2` in the file should match only in cross-reference footer (informational); `grep -c 'v1.2.0-rc.2' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` returns ≤ `2` (one in the substitution-source-citation, one in the cross-reference footer)
    - NO HTML-comment placeholders: `grep -cE 'BEGIN_FRONTMATTER|END_FRONTMATTER' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` returns `0`
    - Cross-reference footer extends the chain: `grep -F '23-RC3-PREFLIGHT.md' .planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-RC3-PREFLIGHT.md` matches at least 1 (self-reference)
  </acceptance_criteria>
  <done>File `23-RC3-PREFLIGHT.md` exists with the verbatim P21 RC2-PREFLIGHT structure modulo the locked substitutions. Frontmatter is `autonomous: false`. The maintainer runs the runbook in a future `/gsd-verify-work` session AFTER P23 PR merges, then cuts the `v1.2.0-rc.3` tag locally per D-15. Plan 08 is marked complete only after the maintainer's sign-off table is filled in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → preflight runbook | Claude writes the maintainer-readable text; structural greps verify shape; the maintainer EXECUTES (cuts tag, pushes, monitors GHCR) in a future session |
| maintainer → release engineering files | D-16 explicitly forbids modification of `release.yml` / `cliff.toml` / `docs/release-rc.md` in this phase; out-of-scope section enforces the lock by warning |
| `:latest` invariant gate | Phase 12 D-10 hyphen-gate in `release.yml` is the structural lock that prevents rc.3 from promoting `:latest`; preflight verification step confirms the gate is unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | Repudiation | Claude executes the rc.3 tag command instead of the maintainer | mitigate | Plan-level `autonomous: false`; the runbook explicitly states "maintainer runs LOCALLY"; project memory `feedback_no_direct_main_commits.md` cited. Acceptance criterion: file contains the literal tag command but NOT a wrapper script that Claude could execute. |
| T-23-08-02 | Tampering (release engineering files) | rc.3 cut accidentally edits release.yml / cliff.toml / docs/release-rc.md | mitigate | D-16 explicit lock; out-of-scope section contains "DO NOT modify" warnings for all three files; pre-flight Section 4 has a grep verification confirming the rc.3 PR contains no edits to these files |
| T-23-08-03 | Tampering (`:latest` promoted to rc.3) | `release.yml` hyphen-gate broken or rc tag accidentally cuts as non-prerelease | mitigate | Section 4 of the runbook re-verifies the hyphen-gate logic in release.yml is unchanged from P12 D-10. Section 9 post-publish verification compares `LATEST_DIGEST` against `V1_1_0_DIGEST` (must equal). Two independent verification steps; either failure flag-stops the rc.3 cut. |
| T-23-08-04 | Information Disclosure (release notes leak secrets) | git-cliff preview includes secret content from commit messages | accept | Project memory + v1.1 P14 demonstrated git-cliff is conventional-commit-driven; secrets in commit messages would be a developer hygiene failure surfaced PR review, not an rc.3 cut concern. |
| T-23-08-05 | Logic violation (Cargo.toml version mismatch) | rc.3 tag points at a binary that reports a different version | mitigate | D-15 lock: Cargo.toml stays at `1.2.0`; the `-rc.3` is tag-only. Section 9 verification: `docker run --rm ghcr.io/...:v1.2.0-rc.3 cronduit --version` returns `cronduit 1.2.0`. Project memory `feedback_tag_release_version_match.md`. |
| T-23-08-06 | Logic violation (HUMAN-UAT not signed) | rc.3 cut without all 6 scenarios in 23-HUMAN-UAT.md signed off | mitigate | Section 7 of preflight requires every checkbox in 23-HUMAN-UAT.md to be ticked; the runbook says "Claude does NOT mark UAT passed". Plan 23-07 owns the HUMAN-UAT runbook; Plan 23-08 owns the gating reference. Acceptance criterion: preflight references `23-HUMAN-UAT.md`. |

**Security gate:** No HIGH severity threats. The release-engineering posture (T-23-08-02, T-23-08-03) is structurally locked by P12 D-10 + D-16 + dual verification (preflight Section 4 + post-publish Section 9). The maintainer-only execution lock (T-23-08-01) is enforced via project memory + `autonomous: false`. The version-tag match (T-23-08-05) is verified at post-publish time.
</threat_model>

<verification>
**Wave 6 gate (this plan):** the preflight file exists with the verbatim P21 mirror + locked substitutions; structural greps all pass.

**Phase 23 SHIP gate (future maintainer-execution session):** every checkbox in the preflight file ticked; sign-off table filled in; `v1.2.0-rc.3` tag pushed; GHCR multi-arch publish verified; `:latest` invariant held.
</verification>

<success_criteria>
- [ ] `23-RC3-PREFLIGHT.md` exists with `autonomous: false` frontmatter
- [ ] Mirrors `21-RC2-PREFLIGHT.md` verbatim modulo locked substitutions (rc.2→rc.3, P21→P23, FCTX→tag-chips, EXIT-06→tags-Prometheus, plan list 01-10→01-07)
- [ ] Tag command exact: `git tag -a -s v1.2.0-rc.3 -m "v1.2.0-rc.3 — dashboard tag filter chips (P23)"`
- [ ] Section 1 plan list has 7 entries (one per Plan 23-01..07; this Plan 08 not in the list)
- [ ] Section 5 swaps EXIT-06 grep for tags-Prometheus-label grep
- [ ] Section 7 references 23-HUMAN-UAT.md
- [ ] `:latest` invariant assertion preserved (still references `:1.1.0`)
- [ ] Out-of-scope section forbids edits to `release.yml` / `cliff.toml` / `docs/release-rc.md`
- [ ] Cross-reference footer extends 20-RC1 → 21-RC2 → 23-RC3 chain
- [ ] All structural greps pass (frontmatter delimiters, scenario count, tag-command exact match, etc.)
- [ ] Maintainer EXECUTES in a future session AFTER P23 PR merges (NOT Claude's responsibility)
</success_criteria>

<output>
After Task 1 completes, create `.planning/phases/23-job-tagging-dashboard-filter-chips-rc-3/23-08-SUMMARY.md` documenting:
- The file path + structural mirror summary (P21 RC2 → P23 RC3 substitutions applied)
- Confirmation that the runbook is `autonomous: false` and cites the locked decisions (D-15, D-16)
- The exact tag command + the post-publish `:latest`-invariant verification step
- A note that this plan is "written but NOT executed" — the maintainer-execution step is a separate (future) `/gsd-verify-work` session AFTER the entire P23 PR merges
- A pointer to the rc.3 cut moment (post-PR-merge maintainer action)
- Confirmation that NO modifications were made to `release.yml` / `cliff.toml` / `docs/release-rc.md`
- Confirmation that NO modifications were made to `Cargo.toml` (still at `1.2.0`; `-rc.3` is tag-only)
</output>
