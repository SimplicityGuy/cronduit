---
phase: 10-stop-a-running-job-hygiene-preamble
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
autonomous: true
requirements:
  - FOUND-13
user_setup: []

must_haves:
  truths:
    - "cronduit --version prints exactly the string 1.1.0 (no drift from the in-flight milestone)"
    - "Cargo workspace package version matches the first v1.1 commit"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace package version = 1.1.0"
      contains: "version = \"1.1.0\""
  key_links:
    - from: "Cargo.toml [package] version"
      to: "env!(\"CARGO_PKG_VERSION\") at src/web/mod.rs (AppState.version)"
      via: "compile-time environment variable"
      pattern: "CARGO_PKG_VERSION"
---

<objective>
Bump the workspace package version from `1.0.1` to `1.1.0` as the very first commit of Phase 10, so every subsequent v1.1 build (including all rc cuts) reports the milestone-in-progress via `cronduit --version`.

Purpose: Close FOUND-13. D-12 locks this as the very first commit of v1.1 so there is zero drift between the in-flight milestone version and the binary.
Output: One-line change to `Cargo.toml`; regenerated `Cargo.lock`; green `cargo build` + `cargo test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-stop-a-running-job-hygiene-preamble/10-CONTEXT.md
@.planning/phases/10-stop-a-running-job-hygiene-preamble/10-RESEARCH.md
@.planning/phases/10-stop-a-running-job-hygiene-preamble/10-PATTERNS.md
@Cargo.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump Cargo.toml version 1.0.1 → 1.1.0</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current state — confirm line 3 is `version = "1.0.1"` before editing)
    - .planning/phases/10-stop-a-running-job-hygiene-preamble/10-CONTEXT.md (D-12: first commit of v1.1)
    - .planning/REQUIREMENTS.md (FOUND-13: semver, rc format `v1.1.0-rc.N` with dot)
    - .planning/phases/10-stop-a-running-job-hygiene-preamble/10-PATTERNS.md §1 (the exact edit pattern: L3 mechanical change, no other metadata fields)
  </read_first>
  <action>
Edit `Cargo.toml` line 3. Change:

```
version = "1.0.1"
```

to:

```
version = "1.1.0"
```

No other changes in this task. Do NOT touch the `rand = "0.8"` line at L106 — that is plan 10-02's scope. Do NOT touch any dev-dependencies or edition/rust-version fields.

After the edit, run `cargo build` to regenerate `Cargo.lock`. The lockfile update is the only downstream artifact; commit both `Cargo.toml` and `Cargo.lock` together.

Per D-12: this is the very first commit of Phase 10 / v1.1. No Stop work, no rand migration, no code changes are allowed in this commit — just the version bump and its lockfile refresh.

Commit message shape: `chore(10): bump workspace version to 1.1.0 (FOUND-13)`.
  </action>
  <verify>
    <automated>grep -q '^version = "1.1.0"$' Cargo.toml &amp;&amp; cargo build -p cronduit 2&gt;&amp;1 | tail -5 &amp;&amp; ./target/debug/cronduit --version | grep -q '1.1.0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "1.1.0"$' Cargo.toml` returns exactly `1`
    - `grep -c '^version = "1.0.1"$' Cargo.toml` returns exactly `0`
    - `cargo build -p cronduit` exits 0 with no warnings beyond the baseline
    - `./target/debug/cronduit --version` prints a line containing `1.1.0` and NOT containing `1.0.1`
    - `Cargo.lock` contains a `name = "cronduit"` block with `version = "1.1.0"` (grep `-A1 'name = "cronduit"' Cargo.lock` shows 1.1.0)
    - No other files are modified in this commit (`git diff --name-only HEAD~1` returns exactly `Cargo.toml` and `Cargo.lock`)
  </acceptance_criteria>
  <done>`cargo --version` reporting `cronduit 1.1.0`; `Cargo.lock` updated to match; commit is a pure version bump with no other changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a metadata-only version bump. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | `Cargo.toml` version string (supply-chain) | accept | Version string is public build metadata; git history is the audit log. CI enforces `git tag` == `Cargo.toml` version per CLAUDE.md memory entry `feedback_tag_release_version_match.md`. No runtime trust impact. |

Security posture: metadata-only change, no new input surface, no code path exercised.
</threat_model>

<verification>
- `cargo build -p cronduit` exits 0
- `cargo test -p cronduit --lib` passes (regression baseline for subsequent plans)
- `cargo clippy -p cronduit --all-targets -- -D warnings` clean
- `./target/debug/cronduit --version` prints `cronduit 1.1.0`
</verification>

<success_criteria>
Workspace version reports `1.1.0` from this commit forward. No other behavioral change. All existing tests still pass. This is the gate-keeper commit that unlocks every downstream Phase 10 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/10-stop-a-running-job-hygiene-preamble/10-01-SUMMARY.md` recording the version delta and the lockfile impact.
</output>
