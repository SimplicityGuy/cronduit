---
phase: 08-v1-final-human-uat-validation
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md
  - .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md
  - .planning/BACKLOG.md
autonomous: false
requirements: [UI-05, UI-06, UI-09, UI-12, OPS-05, UI-14]
requirements_addressed: [UI-05, UI-06, UI-09, UI-12, OPS-05, UI-14]
tags: [uat, user-driven, backlog, release-gate]
must_haves:
  truths:
    - "A new 06-HUMAN-UAT.md file exists in .planning/phases/06-.../ with YAML frontmatter matching the 03-HUMAN-UAT.md template, covering the OPS-05 quickstart end-to-end test and the UI-14 SSE live log streaming test"
    - "A new 08-HUMAN-UAT.md index file exists in .planning/phases/08-.../ listing every per-phase UAT file touched in Phase 8 and the final status of each item"
    - "A .planning/BACKLOG.md file exists (created if missing) with a v1.1 seed-entry template at the top so UAT-surfaced issues have a place to land"
    - "Each UAT entry in the new 06-HUMAN-UAT.md has a result: [pending] placeholder — NOT a pre-filled pass/fail"
    - "The 08-HUMAN-UAT.md index includes a triage rubric (functional breakage -> fix in Phase 8; visual polish / copy -> v1.1 backlog)"
    - "Claude does NOT flip any existing result: field in 03-HUMAN-UAT.md, 07-UAT.md, or the new 06-HUMAN-UAT.md — those are user-driven flips during the walkthrough"
    - "A checkpoint pauses execution for the user to run through every UAT item and record results in place"
  artifacts:
    - path: ".planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md"
      provides: "User-driven UAT file for OPS-05 quickstart and UI-14 SSE live log streaming"
      contains: "OPS-05"
    - path: ".planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md"
      provides: "Phase 8 UAT index listing every per-phase UAT file touched and final status"
      contains: "03-HUMAN-UAT.md"
    - path: ".planning/BACKLOG.md"
      provides: "v1.1 backlog seed file with entry template"
      contains: "999."
  key_links:
    - from: "08-HUMAN-UAT.md index"
      to: "03-HUMAN-UAT.md / 06-HUMAN-UAT.md / 07-UAT.md"
      via: "Markdown links + status table"
      pattern: "03-HUMAN-UAT.md"
    - from: "08-HUMAN-UAT.md triage rubric"
      to: ".planning/BACKLOG.md"
      via: "Markdown link pointing to the v1.1 seed file"
      pattern: "\\.planning/BACKLOG\\.md"
---

<objective>
Orchestrate the Phase 8 human UAT walkthrough. This plan does NOT flip any UAT result
fields — per the project memory rule "UAT requires user validation", the user runs
the tests and records the outcome in place. Claude's job is:

1. **Create the new UAT file** for Phase 6's OPS-05 quickstart + UI-14 SSE live log
   tests (a file that does not exist yet — we cannot ask the user to record into
   something that isn't there).
2. **Create the Phase 8 UAT index** listing every per-phase UAT file the walkthrough
   touches, a triage rubric for what qualifies as a Phase 8 fix vs a v1.1 backlog
   entry, and fixture setup instructions so the user can run through the walkthrough
   in one sitting.
3. **Create `.planning/BACKLOG.md`** with a v1.1 seed entry template so the user has
   somewhere to drop deferred issues during the walkthrough.
4. **Pause at a checkpoint** that tells the user exactly which tests to run, how to
   launch the fixtures (both compose files), and where to record results. The user
   comes back and types "approved" when the walkthrough is done or describes issues.

After the checkpoint, this plan is done. A follow-up SUMMARY.md reports final status
per-file; if the user flagged functional breakage, the orchestrator decides whether
to launch a gap-closure plan. If the user flagged v1.1 items, those already live in
BACKLOG.md from the walkthrough.

Purpose: Phase 8 is the last gate before v1.0 archive. The six human-verification
requirements (UI-05, UI-06, UI-09, UI-12 from Phase 3; OPS-05, UI-14 from Phase 6)
plus the three blocked Phase 7 tests must all be closed before the milestone is
declared complete. This plan is the orchestration scaffold for that walkthrough —
no Rust code, no CI changes, only documentation and a carefully-scoped checkpoint.

Output: Three new files (06-HUMAN-UAT.md, 08-HUMAN-UAT.md, .planning/BACKLOG.md)
and a completed human walkthrough with results recorded in the existing per-phase
UAT files by the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-v1-final-human-uat-validation/08-CONTEXT.md
@.planning/phases/08-v1-final-human-uat-validation/08-VALIDATION.md
@.planning/phases/03-read-only-web-ui-health-endpoint/03-HUMAN-UAT.md
@.planning/phases/07-v1-cleanup-bookkeeping/07-UAT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing per-phase UAT file YAML frontmatter shape (from 03-HUMAN-UAT.md — canonical):
 ---
status: partial
phase: 03-read-only-web-ui-health-endpoint
source: [03-VERIFICATION.md]
started: 2026-04-10T00:00:00Z
updated: 2026-04-10T00:00:00Z
 ---

## Tests

### 1. Terminal-green design system rendering
expected: Dark background #050508, green accent #34d399, JetBrains Mono font, nav bar correct
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
-->

<!-- 07-UAT.md uses an expanded entry shape for issue/blocked entries:
### 2. Job Detail Run History Auto-Refresh — Running → Terminal
expected: |
  <multi-line markdown>
result: issue
reported: |
  <multi-line evidence>
severity: blocker
-->

<!-- The four pending items in 03-HUMAN-UAT.md that Phase 8's user walkthrough will flip:
  1. Terminal-green design system rendering (UI-05)
  2. Dark/light mode toggle (UI-06)
  3. Run Now toast notification (UI-09)
  4. ANSI log rendering in Run Detail (UI-12)

  The tests in 07-UAT.md the user will re-run (Test 2 + Test 3):
  Test 2: Job Detail Run History Auto-Refresh — currently result: issue, severity: blocker
          Needs to flip to pass (or issue with a new severity) now that 08-01's
          longer-running jobs (http-healthcheck + disk-usage) exist.
  Test 3: Job Detail Polling Stops When Idle — currently result: blocked
          Transitively unblocks once Test 2 has runs to observe.
-->

<!-- CONTEXT.md D-27 says v1.1 entries use "999.x numbering convention per project docs".
     There is no existing .planning/BACKLOG.md per the file system check at plan time, so
     Task 3 creates it fresh with an explanatory header and one template entry. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 06-HUMAN-UAT.md with OPS-05 quickstart + UI-14 SSE tests</name>
  <files>.planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/03-read-only-web-ui-health-endpoint/03-HUMAN-UAT.md (canonical template — frontmatter shape, test entry shape, Summary + Gaps blocks)
    - .planning/phases/07-v1-cleanup-bookkeeping/07-UAT.md (expanded entry shape for multi-line expected/result blocks)
    - .planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md (OPS-05 + UI-14 human_verification rationale)
    - .planning/phases/08-v1-final-human-uat-validation/08-VALIDATION.md (lines 67-79 — manual-only verifications table, exact test instructions per item)
  </read_first>
  <action>
    Create a new file at
    `.planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md`.
    Do NOT touch 06-VERIFICATION.md — that file is already verified (status: passed)
    and its human_verification items are being redirected into this new UAT file.

    Use the 03-HUMAN-UAT.md frontmatter shape verbatim, with two tests. Every
    `result:` MUST be `[pending]` — Claude does not flip results. Full file content:

    ```markdown
    ---
    status: pending
    phase: 06-live-events-metrics-retention-release-engineering
    source:
      - 06-VERIFICATION.md
      - .planning/phases/08-v1-final-human-uat-validation/08-CONTEXT.md
    started: 2026-04-13T00:00:00Z
    updated: 2026-04-13T00:00:00Z
    ---

    # Phase 6 — Human UAT

    **Purpose:** Close the two human-verification items carried over from
    `06-VERIFICATION.md` (OPS-05 quickstart end-to-end + UI-14 SSE live log
    streaming) that cannot be asserted programmatically with confidence. Phase 8
    owns the walkthrough; results record in place here.

    Prerequisite for both tests: Phase 8 Plans 01-04 are merged and CI
    `compose-smoke` is green on both matrix axes. Without that, the fixtures are
    not trustworthy.

    ## Current Test

    [awaiting human testing]

    ## Tests

    ### 1. Quickstart end-to-end (OPS-05)
    requirement: OPS-05
    expected: |
      A new operator, starting from a fresh clone of the repository and a
      working Docker daemon, can run the quickstart in under 5 minutes.

      Fixture setup (run one of the two before starting the test):
        # Default variant (Linux with group_add)
        docker compose -f examples/docker-compose.yml up -d

        # Secure variant (macOS / Docker Desktop / defense-in-depth)
        docker compose -f examples/docker-compose.secure.yml up -d

      Test steps:
        1. Clone the repo fresh into a scratch directory.
        2. From the repo root, pick ONE of the two fixture commands above.
        3. Wait for /health to respond (curl http://localhost:8080/health).
        4. Open http://localhost:8080/ in a browser — the dashboard should load
           and show all four example jobs (echo-timestamp, http-healthcheck,
           disk-usage, hello-world).
        5. Within ~60 seconds, the echo-timestamp job should fire automatically
           (it's scheduled */1 * * * *); a new row should appear in its run
           history with status=success.
        6. Confirm the 5-minute budget: from `git clone` to "first echo-timestamp
           row visible with status=success", elapsed wall clock under 5 minutes.

      Record the result below. If any step fails, record result: issue with
      severity (blocker / major / minor) and reported: details.
    result: [pending]

    ### 2. SSE live log streaming (UI-14)
    requirement: UI-14
    expected: |
      Triggering a long-running job shows live log streaming in the Run Detail
      view via Server-Sent Events, without a page reload, and cleanly transitions
      to the static log viewer on completion.

      Fixture setup: Same as Test 1 above (either compose file works).

      Test steps:
        1. On the dashboard, click the `http-healthcheck` or `disk-usage` job.
        2. Click "Run Now". A new run row should appear in RUNNING state.
        3. Click into the RUNNING run row to open the Run Detail page.
        4. Confirm the LIVE badge is visible next to the log viewer.
        5. Watch for log lines to stream in real time as the job runs (wget
           response headers for http-healthcheck, du/df output for disk-usage).
        6. When the job completes, the LIVE badge should disappear and the view
           should transition to the static log viewer — WITHOUT a manual page
           reload.
        7. Confirm the final log content matches what the live stream showed
           (no lost lines, no duplication).

      Record the result below. If the LIVE badge never appears, if log lines
      don't stream, if the transition requires a manual reload, or if the
      static viewer shows different content than the live stream, record
      result: issue with severity and reported: details.
    result: [pending]

    ## Summary

    total: 2
    passed: 0
    issues: 0
    pending: 2
    skipped: 0
    blocked: 0

    ## Gaps

    (User adds entries here if tests surface functional or observable gaps.
    Triage rubric: see 08-HUMAN-UAT.md § Triage. Functional breakage gets a
    gap entry + Phase 8 fix plan; visual/copy issues get a v1.1 BACKLOG entry.)
    ```

    Do not pre-fill any result. Do not flip any existing file. Do not modify
    06-VERIFICATION.md.

    No mermaid diagrams needed here.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md &amp;&amp; grep -c "result: \[pending\]" .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` exits 0
    - `grep -c 'status: pending' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 1
    - `grep -c 'phase: 06-live-events-metrics-retention-release-engineering' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 1
    - `grep -c 'requirement: OPS-05' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 1
    - `grep -c 'requirement: UI-14' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 1
    - `grep -c 'result: \[pending\]' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 2
    - `grep -c 'result: pass' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 0
    - `grep -c 'result: issue' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 0
    - `grep -c 'docker-compose.secure.yml' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns at least 1
    - `grep -c 'pending: 2' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` returns 1
    - `git status .planning/phases/06-live-events-metrics-retention-release-engineering/06-VERIFICATION.md` shows no modifications (file untouched)
  </acceptance_criteria>
  <done>
    New 06-HUMAN-UAT.md exists with the canonical YAML frontmatter shape, two
    [pending] tests (OPS-05 + UI-14), complete fixture setup instructions, and
    placeholder Summary / Gaps blocks. No existing UAT or verification files are
    modified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .planning/BACKLOG.md with v1.1 seed template</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>
    - .planning/phases/08-v1-final-human-uat-validation/08-CONTEXT.md (D-26, D-27, D-28 — v1.1 routing rule + entry format + default-to-backlog tiebreaker)
    - .planning/REQUIREMENTS.md (reference for requirement ID format used in entries)
  </read_first>
  <action>
    Create `.planning/BACKLOG.md` (new file — verify it doesn't exist with `test -f`
    first; if it does, STOP and ask the orchestrator). Full content:

    ```markdown
    # Cronduit v1.1 Backlog

    **Purpose:** Seed list of items deferred from the v1.0 milestone, primarily
    surfaced during Phase 8's human UAT walkthrough. This file is committed so the
    v1.1 milestone kickoff has a ready-made starting point.

    **Entry numbering:** Uses the `999.X` convention per project docs — 999.1,
    999.2, ... — to distinguish v1.1 backlog items from v1.0 requirement IDs
    (FOUND-01, UI-05, etc.).

    **Triage rubric (from Phase 8 D-26 / D-28):**

    - **Functional breakage → fix in Phase 8 before v1.0 archive.** Examples: a
      job fails to run, a page crashes, a toast never appears, a live log stream
      hangs, auto-refresh stops working, a docker pull errors out silently.
    - **Visual polish / copy / edge cases → v1.1 backlog entry.** Examples:
      spacing or color contrast tweaks within brand tolerance, copy wording
      nitpicks, dark-mode rendering edge cases that still render, cosmetic
      alignment on narrow viewports.
    - **Ambiguous → default to backlog** unless it blocks a v1.0 success
      criterion in ROADMAP.md.

    ## Entry Template

    Copy this block for each new entry:

    ```
    ### 999.X — <short title>

    - **Surfaced from:** <UAT file + section, e.g. "03-HUMAN-UAT.md § Test 2">
    - **Observed:** <what the user saw>
    - **Expected:** <what the user expected>
    - **Why not a v1.0 blocker:** <one sentence>
    - **Suggested fix:** <optional — high-level approach, not a design>
    - **Target:** v1.1
    ```

    ## Entries

    (No entries yet. First entries will be added during Phase 8's human UAT
    walkthrough — see 08-HUMAN-UAT.md § Triage.)

    ---

    _Created: 2026-04-13 as part of Phase 8 Plan 05._
    _Owner: project maintainer._
    _Next review: at v1.1 milestone kickoff._
    ```

    Note the nested code fence: the outer markdown uses triple backticks for the
    file, the inner "Entry Template" block uses triple backticks too — write them
    as literal characters in the output. When the executor creates the file, the
    body should contain a triple-backtick-fenced template block that lives inside
    the outer markdown file (which itself is NOT wrapped in backticks — the outer
    block above is only for THIS plan's readability).

    Specifically: the actual file content starts with `# Cronduit v1.1 Backlog`
    on line 1 and contains a nested ```...``` block around the "Entry Template"
    paragraph.
  </action>
  <verify>
    <automated>test -f .planning/BACKLOG.md &amp;&amp; grep -c "Cronduit v1.1 Backlog" .planning/BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/BACKLOG.md` exits 0
    - `grep -c '# Cronduit v1.1 Backlog' .planning/BACKLOG.md` returns 1
    - `grep -c '999.X' .planning/BACKLOG.md` returns at least 1 (in the template)
    - `grep -c 'Triage rubric' .planning/BACKLOG.md` returns 1
    - `grep -c 'Functional breakage' .planning/BACKLOG.md` returns 1
    - `grep -c 'v1.1 backlog entry' .planning/BACKLOG.md` returns at least 1
    - `grep -c 'No entries yet' .planning/BACKLOG.md` returns 1
    - `grep -c '08-HUMAN-UAT.md' .planning/BACKLOG.md` returns 1
  </acceptance_criteria>
  <done>
    New .planning/BACKLOG.md exists with the v1.1 header, triage rubric quoted
    from CONTEXT.md D-26, an entry template, and a placeholder "no entries yet"
    section. Committed alongside the new UAT files so the walkthrough has a
    destination for deferred issues.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 08-HUMAN-UAT.md index with triage rubric and walkthrough plan</name>
  <files>.planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/03-read-only-web-ui-health-endpoint/03-HUMAN-UAT.md (pending items this phase flips — 4 tests)
    - .planning/phases/07-v1-cleanup-bookkeeping/07-UAT.md (blocked tests this phase re-runs — Tests 2 and 3)
    - .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md (after Task 1 — 2 new tests)
    - .planning/phases/08-v1-final-human-uat-validation/08-CONTEXT.md (D-23..D-28 — walkthrough layout, result field schema, backlog routing)
    - .planning/phases/08-v1-final-human-uat-validation/08-VALIDATION.md (manual-only verifications table + sign-off checklist)
    - .planning/BACKLOG.md (after Task 2 — the backlog destination this index links to)
  </read_first>
  <action>
    Create a new file at
    `.planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md`. This file is
    an **index**, not a results file — it does not hold individual test results.
    Those live in the per-phase UAT files. This file:
    - lists every UAT file the Phase 8 walkthrough touches
    - documents the fixture setup for each test
    - provides the triage rubric for Phase 8 fix vs v1.1 backlog
    - summarizes final status once the user completes the walkthrough

    Full content:

    ```markdown
    ---
    status: pending
    phase: 08-v1-final-human-uat-validation
    kind: index
    source:
      - 03-HUMAN-UAT.md
      - 06-HUMAN-UAT.md
      - 07-UAT.md
      - 08-CONTEXT.md
      - 08-VALIDATION.md
    started: 2026-04-13T00:00:00Z
    updated: 2026-04-13T00:00:00Z
    ---

    # Phase 8 — Human UAT Index

    **Purpose:** Walkthrough index for the v1.0 final human UAT. This file lists
    every per-phase UAT file touched in Phase 8, the final status of each item
    once walked through, and the fixture setup each test needs. Individual
    results live in the per-phase files to preserve audit provenance (per D-23).

    **This is a prep-and-surface index.** Per project memory rule "UAT requires
    user validation", Claude does NOT flip any `result:` field. The user runs
    each test and records `result: pass` / `result: issue` / `result: blocked`
    directly in the per-phase file. Once every row in the table below is
    terminal, the user updates this index's `status:` frontmatter to `complete`.

    ## Scope

    ```mermaid
    flowchart TD
        Start([Phase 8 walkthrough])
        P3[03-HUMAN-UAT.md<br/>4 pending tests<br/>UI-05, UI-06, UI-09, UI-12]
        P6[06-HUMAN-UAT.md<br/>2 new tests<br/>OPS-05, UI-14]
        P7[07-UAT.md<br/>2 blocked tests<br/>Test 2 + Test 3]
        BL[.planning/BACKLOG.md<br/>v1.1 deferred items]
        End([All items terminal<br/>→ v1.0 archive gate])

        Start --> P3
        Start --> P6
        Start --> P7
        P3 -->|visual polish| BL
        P6 -->|copy nitpicks| BL
        P7 -->|edge cases| BL
        P3 --> End
        P6 --> End
        P7 --> End
        BL --> End

        classDef pending fill:#1a1a1a,stroke:#888,color:#eee
        classDef index fill:#0a3d0a,stroke:#00ff7f,color:#e0ffe0
        class P3,P6,P7,BL pending
        class Start,End index
    ```

    ## Fixture Setup

    Every test below runs against a live cronduit instance. Before starting the
    walkthrough, pick ONE of the two compose files per test session and bring
    the stack up:

    **Default (Linux):**
    ```
    docker compose -f examples/docker-compose.yml up -d
    curl -sSf http://localhost:8080/health
    ```

    Ensure `DOCKER_GID` matches your host (default fallback is `999`). Derive
    via `stat -c %g /var/run/docker.sock` and export before compose up if
    different.

    **Secure (macOS / defense-in-depth):**
    ```
    docker compose -f examples/docker-compose.secure.yml up -d
    curl -sSf http://localhost:8080/health
    ```

    No `DOCKER_GID` needed — the docker-socket-proxy sidecar mediates all
    Docker API traffic.

    When done with a test session:
    ```
    docker compose -f examples/docker-compose.yml down -v
    # or
    docker compose -f examples/docker-compose.secure.yml down -v
    ```

    ## UAT Item Index

    | # | File | Test | Requirement | Current Result | Final Result |
    |---|------|------|-------------|----------------|--------------|
    | 1 | 03-HUMAN-UAT.md | Terminal-green design system rendering | UI-05 | [pending] | _user fills_ |
    | 2 | 03-HUMAN-UAT.md | Dark/light mode toggle persistence | UI-06 | [pending] | _user fills_ |
    | 3 | 03-HUMAN-UAT.md | Run Now toast notification | UI-09 | [pending] | _user fills_ |
    | 4 | 03-HUMAN-UAT.md | ANSI log rendering in Run Detail | UI-12 | [pending] | _user fills_ |
    | 5 | 06-HUMAN-UAT.md | Quickstart end-to-end | OPS-05 | [pending] | _user fills_ |
    | 6 | 06-HUMAN-UAT.md | SSE live log streaming | UI-14 | [pending] | _user fills_ |
    | 7 | 07-UAT.md | Job Detail Run History Auto-Refresh (re-run) | n/a | issue/blocker | _user fills_ |
    | 8 | 07-UAT.md | Job Detail Polling Stops When Idle (re-run) | n/a | blocked | _user fills_ |

    When flipping Test 7 and Test 8 in 07-UAT.md, add a `re_tested_at:
    2026-04-13T..Z` annotation so the audit trail shows the retry.

    ## Triage Rubric

    Use this to decide whether a surfaced issue is a Phase 8 fix or a v1.1
    backlog entry (verbatim from 08-CONTEXT.md D-26, D-28):

    - **Fix in Phase 8:** Functional breakage. A job fails to run, a page
      crashes, a toast never appears, a live log stream hangs, auto-refresh
      stops working, a docker pull errors out silently. Open a gap-closure
      plan for it before archiving v1.0.
    - **Defer to v1.1:** Visual polish, copy wording nitpicks, dark-mode
      rendering edge cases that still render, cosmetic alignment on narrow
      viewports. Add an entry to `.planning/BACKLOG.md` using the template.
    - **Ambiguous:** Default to v1.1 unless it blocks a v1.0 success criterion
      listed in ROADMAP.md § Phase 8. Err toward shipping.

    ## Final Status

    _To be filled in by the user after the walkthrough completes._

    | Category | Count |
    |----------|-------|
    | Tests total | 8 |
    | Passed | _fill_ |
    | Issues (Phase 8 fix) | _fill_ |
    | Issues (deferred to v1.1) | _fill_ |
    | Blocked | _fill_ |

    **Phase 8 fix plans opened:** _list any gap-closure plan IDs_

    **v1.1 backlog entries added:** _list 999.X IDs from .planning/BACKLOG.md_

    **Walkthrough completed:** _timestamp_
    **User approved archive:** _yes / no_
    ```

    The mermaid diagram uses the project's standard terminal-green color tokens
    (`#0a3d0a` / `#00ff7f` / `#e0ffe0` for accent, `#1a1a1a` / `#888` / `#eee`
    for pending) — consistent with STATE.md's current-position mermaid block.
    Do not use ASCII art diagrams anywhere.

    Do not pre-fill any "Final Result" column or the "Final Status" counts.
    Those are user-driven after the walkthrough.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md &amp;&amp; grep -c "UAT Item Index" .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` exits 0
    - `grep -c 'kind: index' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'UAT Item Index' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c '03-HUMAN-UAT.md' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns at least 2 (source + table rows)
    - `grep -c '06-HUMAN-UAT.md' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns at least 2
    - `grep -c '07-UAT.md' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns at least 2
    - `grep -c 'UI-05' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'UI-06' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'UI-09' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'UI-12' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'OPS-05' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'UI-14' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c '```mermaid' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1 (mermaid diagram present)
    - `grep -c 'BACKLOG.md' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns at least 2
    - `grep -c 'Triage Rubric' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 1
    - `grep -c 'result: pass' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` returns 0 (no pre-filled results)
    - `git status .planning/phases/03-read-only-web-ui-health-endpoint/03-HUMAN-UAT.md .planning/phases/07-v1-cleanup-bookkeeping/07-UAT.md` shows both files UNmodified
  </acceptance_criteria>
  <done>
    New 08-HUMAN-UAT.md index exists with kind: index frontmatter, mermaid scope
    diagram, fixture setup for both compose files, a table listing all 8 UAT
    items with [pending]/existing status in the Current Result column and
    _user fills_ in the Final Result column, the triage rubric from D-26, and a
    Final Status block to be filled in post-walkthrough. 03-HUMAN-UAT.md and
    07-UAT.md are unmodified (user-owned).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT walkthrough checkpoint</name>
  <files>N/A — this task runs a human walkthrough against fixtures created in tasks 1-3 and the per-phase UAT files owned by the user.</files>
  <action>Pause execution and present the walkthrough script below to the user. Do NOT proceed or flip any `result:` field in any UAT file — the user edits the per-phase UAT files directly during the walkthrough. When the user types the resume signal, resume execution and write the SUMMARY.md based on whatever state the user left the UAT files in. See `<what-built>` and `<how-to-verify>` for the full walkthrough script.</action>
  <verify>
    <automated>MISSING — this is an inherently human verification task per project memory rule "UAT requires user validation"; see 08-VALIDATION.md § Manual-Only Verifications for the non-automatable items covered here</automated>
  </verify>
  <done>User has walked through every UAT item listed in 08-HUMAN-UAT.md, recorded `result: pass` or `result: issue` (with severity + reported) in the per-phase UAT files directly, and typed the resume signal — either "approved" (v1.0 archive-ready) or a structured issue list (blockers need follow-up plans before archive).</done>
  <what-built>
    Phase 8 Plans 01-04 have landed on a PR branch and CI compose-smoke is green
    on both matrix axes. Plans in this 05 plan created three new files:
    - `.planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` (OPS-05 + UI-14 tests, pending)
    - `.planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` (walkthrough index + triage rubric)
    - `.planning/BACKLOG.md` (v1.1 seed file)

    All four Phase 1 functional fixes have landed (alpine rebase, dual compose,
    daemon preflight + gauge, CI smoke matrix), so the fixtures for every UAT
    item are now real and working.

    The per-phase UAT files that will be filled in by the user during the
    walkthrough are NOT modified by Claude:
    - `03-HUMAN-UAT.md` (4 pending tests — UI-05, UI-06, UI-09, UI-12)
    - `06-HUMAN-UAT.md` (2 new tests — OPS-05, UI-14)
    - `07-UAT.md` (2 previously-blocked tests to re-run — Test 2 + Test 3)
  </what-built>
  <how-to-verify>
    This is the human UAT walkthrough itself. Run it in this order:

    **Step 1 — Confirm the new UAT files exist and the fixtures are ready:**
    ```
    cat .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md
    cat .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md
    cat .planning/BACKLOG.md
    ```

    Skim 08-HUMAN-UAT.md's scope diagram + triage rubric before starting.

    **Step 2 — Boot a fixture. Pick the default variant first:**
    ```
    cd examples
    docker compose -f docker-compose.yml up -d
    curl -sSf http://localhost:8080/health
    ```

    If running on macOS / Docker Desktop, use the secure variant instead:
    ```
    docker compose -f docker-compose.secure.yml up -d
    curl -sSf http://localhost:8080/health
    ```

    **Step 3 — Run the 03-HUMAN-UAT.md tests (visual, ~5 min):**

    Open `http://localhost:8080/` in a browser. For each of the four pending
    tests in 03-HUMAN-UAT.md, follow the expected block, then edit the file
    in place and set `result: pass` or `result: issue` (with severity + reported
    evidence):
      - Test 1 Terminal-green design system rendering (UI-05)
      - Test 2 Dark/light mode toggle persistence (UI-06)
      - Test 3 Run Now toast notification (UI-09)
      - Test 4 ANSI log rendering in Run Detail (UI-12)

    Update `03-HUMAN-UAT.md` Summary counts after each result flip.

    **Step 4 — Run the 06-HUMAN-UAT.md tests (functional, ~10 min):**

    Open `06-HUMAN-UAT.md` and follow:
      - Test 1 Quickstart end-to-end (OPS-05) — confirm the 5-minute budget
        contract end-to-end.
      - Test 2 SSE live log streaming (UI-14) — click Run Now on
        `http-healthcheck` or `disk-usage`, open the Run Detail page, confirm
        LIVE badge + streaming log lines + clean transition to static viewer.

    Record results in place. Update Summary counts.

    **Step 5 — Re-run the 07-UAT.md blocked tests (~5 min):**

    Test 2 (Job Detail Run History Auto-Refresh) is the critical re-run. Click
    Run Now 10+ times on `http-healthcheck` or `disk-usage`, watch the
    RUNNING → terminal transitions within ~2s of each completion. Flip
    `result: issue, severity: blocker` to `result: pass` (or keep as issue
    with new severity) and add a `re_tested_at: 2026-04-13T..Z` line.

    Test 3 (Job Detail Polling Stops When Idle) — after Test 2 finishes, open
    DevTools Network tab, filter by "runs", watch for 10 seconds. Zero
    follow-up requests = pass. Flip `result: blocked` to `result: pass` or
    `result: issue`, add `re_tested_at:`.

    **Step 6 — Triage surfaced issues:**

    For each `result: issue` entry, use the triage rubric in 08-HUMAN-UAT.md:
      - Functional breakage → open a gap-closure plan (08-06+) before archive.
      - Visual / copy / edge case → add an entry to `.planning/BACKLOG.md`
        using the template (numbered 999.1, 999.2, ...).
      - Ambiguous → default to v1.1 backlog.

    **Step 7 — Update the 08-HUMAN-UAT.md Final Status block:**

    Fill in the Final Result column of the table and the Final Status counts
    at the bottom. Set `status: complete` in the frontmatter. Record whether
    you approve v1.0 archive.

    **Step 8 — Tear down the fixture:**
    ```
    docker compose -f examples/docker-compose.yml down -v
    # or
    docker compose -f examples/docker-compose.secure.yml down -v
    ```

    **Step 9 — Resume execution.** Type `approved` if all tests passed (or
    issues are documented as v1.1 backlog entries) and v1.0 is ready to
    archive. Type `issue: <summary>` if functional breakage was found that
    needs a gap-closure plan before archive.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (blocker list + desired follow-up plans)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> per-phase UAT files | User is the sole editor during the walkthrough; Claude must not flip any `result:` field (project memory rule) |
| walkthrough -> backlog routing | Surfaced issues are routed by the user via the triage rubric; Claude does not pre-decide routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Repudiation | Claude silently flips a UAT result, undermining audit trail | mitigate | Every task in this plan has explicit acceptance criteria that check `grep -c 'result: pass' ...` returns 0 in Claude-written files and that pre-existing UAT files are untouched via `git status`. The checkpoint task is `checkpoint:human-verify` with `gate: blocking`, preventing the plan from completing without user approval. |
| T-08-17 | Tampering | Claude creates a BACKLOG.md entry pre-filling a v1.1 deferral | accept | BACKLOG.md ships with "No entries yet. First entries will be added during Phase 8's human UAT walkthrough" — no pre-filled entries. The user adds entries during or after the walkthrough using the template. |
| T-08-18 | Information Disclosure | UAT files committed to git expose internal test notes | accept | All UAT files are already in the `.planning/` tree which is committed to the public repo; adding 06-HUMAN-UAT.md and 08-HUMAN-UAT.md follows the existing convention. No secrets or PII are recorded. |
</threat_model>

<verification>
- `test -f .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` = true
- `test -f .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` = true
- `test -f .planning/BACKLOG.md` = true
- `grep -c 'result: pass' .planning/phases/06-live-events-metrics-retention-release-engineering/06-HUMAN-UAT.md` = 0
- `git diff .planning/phases/03-read-only-web-ui-health-endpoint/03-HUMAN-UAT.md` shows no changes made by Claude
- `git diff .planning/phases/07-v1-cleanup-bookkeeping/07-UAT.md` shows no changes made by Claude
- `grep -c '```mermaid' .planning/phases/08-v1-final-human-uat-validation/08-HUMAN-UAT.md` = 1
- User walked through every UAT item and recorded results in the per-phase files
- User typed `approved` at the checkpoint resume signal (or a clear issue list that spawns follow-up plans)
</verification>

<success_criteria>
- 06-HUMAN-UAT.md created with two [pending] tests for OPS-05 + UI-14, full fixture instructions, no pre-filled results
- 08-HUMAN-UAT.md created as an index with mermaid scope diagram, fixture setup for both compose files, UAT item table, and triage rubric
- .planning/BACKLOG.md created with v1.1 header, triage rubric from D-26, entry template, and empty entries section
- Pre-existing 03-HUMAN-UAT.md, 07-UAT.md, and 06-VERIFICATION.md are textually unchanged by Claude (Claude only added files)
- Checkpoint paused for user walkthrough; user resumed with explicit approval or issue list
- If user reported functional breakage, orchestrator has enough info to launch a follow-up gap-closure plan
- If user reported only visual/copy issues, those live in .planning/BACKLOG.md as v1.1 entries
</success_criteria>

<output>
After the checkpoint resumes, create
`.planning/phases/08-v1-final-human-uat-validation/08-05-SUMMARY.md` with:
- Final status per UAT item (copied from 08-HUMAN-UAT.md § Final Status)
- List of v1.1 backlog entries added during walkthrough (999.X IDs + one-line titles)
- List of Phase 8 follow-up fixes requested by the user (blocker severity issues → new plans)
- User's archive approval state (approved / hold for fixes)
- Links to every UAT file modified by the user (`git log --diff-filter=M -- path` per file)
</output>
