---
phase: 12.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/main-build.yml
autonomous: true
requirements: [OPS-10]
tags: [ci, github-actions, ghcr, tag-hygiene, main-build, multi-arch]

must_haves:
  truths:
    - "Every push to main triggers a GitHub Actions workflow that builds a multi-arch (amd64+arm64) Docker image"
    - "The workflow publishes ONLY the :main tag to ghcr.io/simplicityguy/cronduit — never :latest, :rc, :X, :X.Y, :X.Y.Z, or any branch-derived tag"
    - "The workflow uses docker/build-push-action@v6 with the same Dockerfile and platforms as release.yml (release parity)"
    - "A post-push assertion step fails the workflow if :main is not multi-arch (amd64 + arm64)"
    - "Back-to-back pushes to main cancel in-flight builds (no stacked queue)"
    - "The workflow's tags block is hard-coded so a future edit cannot accidentally emit :develop, :feature-foo, or any other branch-derived tag"
  artifacts:
    - path: ".github/workflows/main-build.yml"
      provides: "Multi-arch build + GHCR push of :main on every push to main"
      contains: "type=raw,value=main"
    - path: ".github/workflows/main-build.yml"
      provides: "Post-push multi-arch assertion step"
      contains: "imagetools inspect"
  key_links:
    - from: ".github/workflows/main-build.yml"
      to: "ghcr.io/simplicityguy/cronduit:main"
      via: "docker/build-push-action@v6 with platforms: linux/amd64,linux/arm64 and tags: type=raw,value=main"
      pattern: "type=raw,value=main"
    - from: ".github/workflows/main-build.yml post-push step"
      to: ":main manifest"
      via: "docker buildx imagetools inspect --raw | jq"
      pattern: "imagetools inspect.*:main"
---

<objective>
Create `.github/workflows/main-build.yml` — a new GitHub Actions workflow that builds and pushes a multi-arch (linux/amd64 + linux/arm64) Docker image tagged **only** `:main` on every push to `main`. The workflow mirrors `release.yml`'s build plumbing (same `docker/build-push-action@v6`, same platforms, same labels/annotations) MINUS the tag-extraction, git-cliff, and gh-release steps, with the `metadata-action tags:` block narrowed to `type=raw,value=main` and the cache scoped to `cronduit-main`. A final post-push assertion step inspects `:main` and fails if either architecture is missing.

Purpose: Gives operators a bleeding-edge `:main` tag they can pin without waiting for rc cuts. Maintains the D-10 / OPS-10 tag-contract boundary: release.yml owns `:X.Y.Z / :X.Y / :X / :latest / :rc`; main-build.yml owns `:main` exclusively; ci.yml + compose-smoke.yml push nothing.

Output: New workflow file `.github/workflows/main-build.yml` containing: trigger on `push: branches: [main]`, concurrency group with `cancel-in-progress: true`, per-job `packages: write` permission, checkout + lowercase-image-name computation + buildx + GHCR login + metadata-action (narrowed tags) + build-push-action (multi-arch) + post-push multi-arch assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-ghcr-tag-hygiene/12.1-RESEARCH.md
@.planning/phases/12.1-ghcr-tag-hygiene/12.1-PATTERNS.md
@.planning/phases/12.1-ghcr-tag-hygiene/12.1-VALIDATION.md
@.github/workflows/release.yml
@.github/workflows/ci.yml

<interfaces>
<!-- Pattern source: release.yml (verbatim for most blocks) + ci.yml (concurrency pattern) -->
<!-- RESEARCH.md § "Proposed .github/workflows/main-build.yml" L197-292 has the full drafted YAML -->

From .github/workflows/release.yml L42-52 (Compute lowercase image name — copy verbatim):
```yaml
      - name: Compute lowercase image name
        # ... comment block retained ...
        env:
          REPO: ${{ github.repository }}
        run: |
          echo "IMAGE_NAME=$(echo "$REPO" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"
```

From .github/workflows/release.yml L73-82 (Buildx + GHCR login — copy verbatim):
```yaml
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
```

From .github/workflows/release.yml L83-106 (metadata-action header — copy; narrow tags):
```yaml
      - name: Extract Docker metadata (labels, tags, annotations)
        id: meta
        uses: docker/metadata-action@v5
        env:
          DOCKER_METADATA_ANNOTATIONS_LEVELS: index,manifest
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
```

**CRITICAL narrowed block (REPLACES release.yml L130-135):**
```yaml
          tags: |
            type=raw,value=main
```

From .github/workflows/release.yml L146-164 (labels + annotations — copy; optionally adjust description):
```yaml
          labels: |
            org.opencontainers.image.title=Cronduit
            org.opencontainers.image.description=Self-hosted Docker-native cron scheduler with a web UI
            ...
          annotations: |
            org.opencontainers.image.title=Cronduit
            ...
```

From .github/workflows/release.yml L166-176 (build-push-action — copy; change cache scope):
```yaml
      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha,scope=cronduit-release      # CHANGE → cronduit-main
          cache-to: type=gha,mode=max,scope=cronduit-release   # CHANGE → cronduit-main
```

From .github/workflows/ci.yml L12-14 (concurrency pattern — adapt):
```yaml
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/main-build.yml with full multi-arch build + narrowed :main tag contract + post-push assertion</name>
  <files>.github/workflows/main-build.yml</files>
  <read_first>
    - .github/workflows/release.yml (L1-184 complete file — source for every "Mirror? Yes" row in RESEARCH.md build-parity table)
    - .github/workflows/ci.yml (L12-14 concurrency pattern; L16-20 + L97-100 permissions pattern; L1-20 top-of-file comment style)
    - .planning/phases/12.1-ghcr-tag-hygiene/12.1-RESEARCH.md (§ "Proposed .github/workflows/main-build.yml" L197-292 — full drafted YAML; § "Build Parity Inventory" L296-319 — parity table; § "Code Examples / Example 3" L676-693 — post-push assertion)
    - .planning/phases/12.1-ghcr-tag-hygiene/12.1-PATTERNS.md (§ ".github/workflows/main-build.yml" L20-199 — per-block pattern assignments and verbatim excerpts)
  </read_first>
  <action>
Create `.github/workflows/main-build.yml` verbatim from RESEARCH.md § "Proposed .github/workflows/main-build.yml" (L197-292). Paste the entire YAML block EXACTLY AS DRAFTED, with one refinement detailed below (the post-push assertion step from RESEARCH.md § "Code Examples / Example 3" L676-693 is appended as a final step in the `main-build` job).

**Full file content to write** (all values concrete — no substitutions needed):

```yaml
# .github/workflows/main-build.yml
# Publishes ghcr.io/simplicityguy/cronduit:main on every push to main.
# Multi-arch (linux/amd64 + linux/arm64) via docker/build-push-action@v6 for
# build parity with release.yml (same Dockerfile, same platforms, same labels).
# Does NOT push :latest, :1, :X.Y, :X.Y.Z, or :rc — those are owned by
# release.yml on semver tag pushes.
#
# Phase 12.1 OPS-10 / D-10 tag-contract boundary:
#   release.yml       -> owns :X.Y.Z, :X.Y, :X, :latest, :rc
#   main-build.yml    -> owns :main EXCLUSIVELY
#   compose-smoke.yml -> owns no tags (build-only, never pushes)
#   ci.yml            -> owns no tags (build-only, never pushes)
#
# Security: This workflow only uses server-controlled inputs (github.repository,
# github.actor, GITHUB_TOKEN, github.ref). No user-supplied text (issue titles,
# PR bodies, commit messages) flows into run: commands.

name: main-build

on:
  push:
    branches: [main]

# Cancel in-flight runs when a newer commit lands on main. Unlike release.yml
# (which must never self-cancel during a tag push), it's safe and desirable for
# :main to always reflect the most recent push.
concurrency:
  group: main-build-${{ github.ref }}
  cancel-in-progress: true

# Top-level permissions: read-only by default. packages: write is scoped
# per-job to the main-build job only (mirrors ci.yml L97-100 pattern).
permissions:
  contents: read

env:
  REGISTRY: ghcr.io

jobs:
  main-build:
    name: multi-arch build -> :main
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute lowercase image name
        # Derive IMAGE_NAME=<lowercase-owner>/<lowercase-repo> from the
        # $REPO env var and export to $GITHUB_ENV so later steps see it
        # via ${{ env.IMAGE_NAME }}. github.repository is a server-
        # controlled identifier (strict owner/repo format), but we route
        # it through an env var anyway to follow the project-wide "never
        # interpolate ${{ }} directly into run: commands" rule.
        env:
          REPO: ${{ github.repository }}
        run: |
          echo "IMAGE_NAME=$(echo "$REPO" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (labels, tags, annotations)
        id: meta
        uses: docker/metadata-action@v5
        # DOCKER_METADATA_ANNOTATIONS_LEVELS: emit BOTH index + manifest
        # annotations (matches release.yml L86-104 rationale — without this,
        # annotations silently scope to manifest-only).
        env:
          DOCKER_METADATA_ANNOTATIONS_LEVELS: index,manifest
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          # ONLY :main. No semver, no latest, no rc, no branch-derived tags.
          # This is the D-10 / OPS-10 tag-contract boundary.
          #
          # Why type=raw,value=main (hardcoded) instead of type=ref,event=branch:
          # both produce :main on a push to main, but type=ref,event=branch
          # also fires on pushes to OTHER branches if the on.push.branches filter
          # is ever relaxed. type=raw,value=main is a hard-coded contract — no
          # matter what branch triggers the workflow, only :main can possibly
          # be published. This defends against a future "add develop branch
          # trigger" refactor that would accidentally create :develop.
          tags: |
            type=raw,value=main
          labels: |
            org.opencontainers.image.title=Cronduit
            org.opencontainers.image.description=Self-hosted Docker-native cron scheduler with a web UI (main branch HEAD)
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.vendor=SimplicityGuy
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
          annotations: |
            org.opencontainers.image.title=Cronduit
            org.opencontainers.image.description=Self-hosted Docker-native cron scheduler with a web UI (main branch HEAD)
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.vendor=SimplicityGuy
            org.opencontainers.image.source=https://github.com/${{ github.repository }}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          # Scope the GHA cache separately from release builds so main-push
          # churn doesn't invalidate the release-build cache.
          cache-from: type=gha,scope=cronduit-main
          cache-to: type=gha,mode=max,scope=cronduit-main

      - name: Assert :main is multi-arch (amd64 + arm64)
        # Post-push verification: if build-push-action silently flattened to a
        # single architecture (should be impossible with platforms: linux/amd64,
        # linux/arm64 but defense-in-depth), this step fails the workflow and
        # surfaces the regression. Routes IMG through env: per project rule.
        env:
          IMG: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:main
        run: |
          set -euo pipefail
          archs=$(docker buildx imagetools inspect "$IMG" --raw \
            | jq -r '[.manifests[] | select(.platform.os == "linux") | .platform.architecture] | sort | join(",")')
          echo "observed architectures: $archs"
          if [ "$archs" != "amd64,arm64" ]; then
            echo "::error::expected :main to publish amd64+arm64, got $archs"
            exit 1
          fi
          echo ":main is multi-arch (amd64 + arm64) — OPS-10 assertion passed"
```

**Notes on concrete choices within this YAML (all locked by RESEARCH.md — no discretion needed):**

1. **Top-of-file comment** names each workflow's tag ownership so a future reviewer has the contract in-file. Mirrors release.yml L1-9 and ci.yml L1-4 conventions (PATTERNS.md § "Top-of-file comment block" L490-492).

2. **`concurrency: cancel-in-progress: true`** — appropriate for `:main` (back-to-back pushes should not stack builds); opposite of release.yml's no-concurrency stance (tag pushes must never self-cancel). RESEARCH.md L216-221, L399-404.

3. **Permissions: per-job scoping** — `contents: read` at workflow level + `packages: write` on the main-build job. Matches ci.yml L16-20 + L97-100 pattern and cleanup-images.yml L23-33 pattern. RESEARCH.md V4 row L562.

4. **`actions/checkout@v4` WITHOUT `fetch-depth: 0`** — main-build does NOT run git-cliff; shallow clone is sufficient (PATTERNS.md L94-105, RESEARCH.md build-parity row L302).

5. **Description suffix "(main branch HEAD)"** — both labels.description and annotations.description get this suffix so GHCR's package page distinguishes the `:main` image from tagged releases. RESEARCH.md L268, L273.

6. **`type=raw,value=main`** — hardcoded. NOT `type=ref,event=branch`. Defense against future "add develop branch" refactor accidentally emitting `:develop`. RESEARCH.md L294, PATTERNS.md L142.

7. **`cache-from/cache-to: scope=cronduit-main`** — distinct from release.yml's `scope=cronduit-release`. Prevents main-push churn from evicting release-build cache. RESEARCH.md L405-409 (landmine L5).

8. **Post-push assertion step** — copies RESEARCH.md Example 3 (L676-693). Routes `IMG` through `env:` (project rule; release.yml L47-52 comment block documents the rationale). `set -euo pipefail` is the project standard for multi-line `run:` blocks (PATTERNS.md § "Bash safety flags" L443-451).

**Do NOT add:**
- No git-cliff step (not a release)
- No gh-release step (not a release)
- No tag-extraction step (main-build has no tag)
- No QEMU setup (`docker/setup-qemu-action@v3`) — buildx alone is sufficient since cargo-zigbuild handles cross-compilation in the Dockerfile; matches release.yml which also does not use QEMU
- No `--build-arg` overrides — all version pins (zig, cargo-zigbuild, Tailwind, Rust) live in the Dockerfile (RESEARCH.md L393-398 landmine L3)
- No `fetch-depth: 0` on checkout (research parity table, PATTERNS.md L94-105)
- No non-`:main` tags in the metadata-action `tags:` block (OPS-10 contract boundary)

**Quality gate:** if `actionlint` is available (install locally or in a pre-push hook), `actionlint .github/workflows/main-build.yml` MUST exit 0. If not available, fall back to YAML syntax validation via `python -c "import yaml; yaml.safe_load(open('.github/workflows/main-build.yml'))"`.

Decision ID traceability: implements ROADMAP Phase 12.1 decision "**`:main` floating tag (new):** New GHA workflow… that builds + pushes a multi-arch image tagged `:main` on every push to `main`. Same cargo-zigbuild plumbing as release.yml for parity with actual releases." and success criterion #1 ("Every push to `main` triggers a multi-arch (amd64+arm64) build + push to `ghcr.io/simplicityguy/cronduit:main`").
  </action>
  <verify>
    <automated>test -f .github/workflows/main-build.yml && grep -q 'type=raw,value=main' .github/workflows/main-build.yml && grep -q 'platforms: linux/amd64,linux/arm64' .github/workflows/main-build.yml && grep -q 'cache-from: type=gha,scope=cronduit-main' .github/workflows/main-build.yml && grep -q 'imagetools inspect' .github/workflows/main-build.yml && grep -qE '(cancel-in-progress:\s*true)' .github/workflows/main-build.yml && ! grep -E 'type=raw,value=(latest|rc)|type=semver' .github/workflows/main-build.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/main-build.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - W0-CI-02 (VALIDATION.md): `.github/workflows/main-build.yml` exists; YAML parses without error (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/main-build.yml'))"` exits 0)
    - W0-CI-03: `grep -cE 'type=raw,value=main' .github/workflows/main-build.yml` returns `1`; `! grep -E 'value=(latest|rc)|type=semver|type=ref' .github/workflows/main-build.yml` exits 0 (only :main is published)
    - W0-CI-04: `grep -E 'platforms:\s*linux/amd64,linux/arm64' .github/workflows/main-build.yml` matches (multi-arch)
    - W0-CI-05: `grep -E 'imagetools inspect.*:main' .github/workflows/main-build.yml` matches (post-push assertion present)
    - W0-CI-06: `grep -A2 'concurrency:' .github/workflows/main-build.yml | grep 'cancel-in-progress: true'` matches
    - `grep -c 'cache-from: type=gha,scope=cronduit-main' .github/workflows/main-build.yml` returns `1` (cache scope distinct from release.yml's `cronduit-release`)
    - `grep -c 'docker/build-push-action@v6' .github/workflows/main-build.yml` returns `1` (same major version as release.yml)
    - `grep -c 'packages: write' .github/workflows/main-build.yml` returns `1` (per-job scoped)
    - `grep -c 'fetch-depth: 0' .github/workflows/main-build.yml` returns `0` (shallow clone — no git-cliff)
    - `grep -c 'git-cliff\|gh-release\|softprops/action-gh-release' .github/workflows/main-build.yml` returns `0` (no release steps copy-pasted)
    - `grep -c 'on:' .github/workflows/main-build.yml` >= 1 AND `grep -E 'branches:\s*\[main\]' .github/workflows/main-build.yml` matches (trigger is push-to-main only)
    - If `actionlint` is installed: `actionlint .github/workflows/main-build.yml` exits 0
  </acceptance_criteria>
  <done>main-build.yml created, syntactically valid, contains only :main tag, multi-arch build + post-push assertion + concurrency cancel-in-progress + per-job packages:write all present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push-to-main → main-build.yml | New CI-credentialed path that writes `:main` to GHCR. Scoped to one tag only. |
| main-build.yml → ghcr.io | Outbound auth via `secrets.GITHUB_TOKEN`; auto-masked in logs. |
| Untrusted commit → `:main` tag | `:main` pulls unreviewed HEAD-of-main code; documented risk (L6 in RESEARCH.md landmines). README (Plan 03) surfaces this to operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-DRIFT-01 | Tampering | Future edit widens `tags:` block to include `:latest` or `:rc` | mitigate | `type=raw,value=main` hardcoded; comment block explicitly names the D-10 boundary with a warning. rc-guard.yml (deferred to Phase 13) would catch retroactively. W0-CI-03 static grep assertion in VALIDATION.md + acceptance_criteria above catches in PR review. |
| T-12.1-BRANCH-01 | Tampering | Future edit relaxes `on.push.branches: [main]` to `[main, develop]` accidentally emitting `:develop` | mitigate | `type=raw,value=main` hardcoded (NOT `type=ref,event=branch`) so even a branch-filter relaxation still only produces `:main`. Documented in metadata-action comment. |
| T-12.1-CACHE-01 | Tampering | Main-build cache poisons release-build cache | mitigate | Distinct cache scopes: `cronduit-main` vs `cronduit-release`. Grep-verifiable in acceptance_criteria. |
| T-12.1-PERM-01 | Elevation | Overbroad workflow permissions | mitigate | Top-level `contents: read`; per-job `packages: write` on the single `main-build` job only. Matches ci.yml L97-100 pattern. |
| T-12.1-INJECT-01 | Tampering | Unsanitized input in `run:` block | mitigate | `IMG` routed through `env:` per project rule (release.yml L47-52 comment). Only server-controlled inputs used (`github.repository`, `github.actor`, `github.sha`, `GITHUB_TOKEN`). No PR title / commit message / issue body touches `run:`. |
| T-12.1-TOKEN-01 | Info Disclosure | PAT / token leakage in logs | mitigate | Uses `secrets.GITHUB_TOKEN` (auto-masked); never echoed. `set -euo pipefail` ensures no stray variable expansion leaks. |
| T-12.1-BLEED-01 | Repudiation / Operator trust | `:main` pulls unreviewed code — operators may mistake for production-grade | mitigate | Documented in README "Docker image tags" section (Plan 03): `:main` explicitly labeled "bleeding edge, not recommended for any environment that values uptime." Per-image description suffix "(main branch HEAD)" lands on GHCR package page. |
| T-12.1-CONCUR-01 | Denial of Service | Stacked builds from rapid successive main pushes | mitigate | `concurrency: cancel-in-progress: true` cancels in-flight builds. Operator pulling during a build window gets previous build's digest (correct behavior for rolling tag). |
| T-12.1-SUPPLY-01 | Tampering / Supply Chain | Third-party action tampering | accept | All actions pinned to MAJOR version from trusted publishers (`actions/checkout@v4`, `docker/setup-buildx-action@v3`, `docker/login-action@v3`, `docker/metadata-action@v5`, `docker/build-push-action@v6`). Matches project convention (PATTERNS.md L466-467). SHA pinning is a Phase 14+ hardening step (RESEARCH.md V10 row L565). |
</threat_model>

<verification>
Post-merge verification (happens on first push to main after this workflow lands):

1. **Workflow runs:** GHA triggers `main-build` on the merge commit. Use `gh run list --workflow=main-build.yml --limit 1` to see it.
2. **Wall-time:** Cold build expected 15-20 min; warm build 3-5 min (RESEARCH.md L500-501). If cold build exceeds 30 min, flag in SUMMARY.md for cost review.
3. **Multi-arch assertion step passes:** final step logs `observed architectures: amd64,arm64` and `":main is multi-arch (amd64 + arm64) — OPS-10 assertion passed"`.
4. **GHCR state:** `docker buildx imagetools inspect ghcr.io/simplicityguy/cronduit:main` shows a manifest list with both `linux/amd64` and `linux/arm64` platform entries.
5. **`:latest` unaffected:** `docker buildx imagetools inspect ghcr.io/simplicityguy/cronduit:latest --format '{{json .Manifest.Digest}}'` returns the SAME digest as before the workflow ran — main-build.yml must never touch `:latest`.
6. **No other tags moved:** `:1`, `:1.0`, `:1.0.1`, `:rc`, `:1.1.0-rc.1` all unchanged.
</verification>

<success_criterion_handoff id="SC3">
## Success Criterion #3 — rc-tag Stability — Handoff Traceability

ROADMAP Phase 12.1 success criterion #3 ("pushing an `-rc.N` tag does NOT move `:latest`, `:1`, `:1.1`, or `:main`") is **delivered transitively** by the combination of three mechanisms; no single plan in Phase 12.1 owns SC3 end-to-end. This block records the handoff for phase-closeout verification so the criterion is not flagged as unplanned.

1. **(a) D-10 release.yml gating — shipped in Phase 12 (Plan 05).**
   `release.yml`'s `docker/metadata-action@v5` `tags:` block uses `enable=${{ ... }}` conditional filters so `:latest`, `:1`, `:1.1`, `:1.Y.Z` are ONLY emitted on stable (non-rc) semver tag pushes. An rc-tag push (e.g. `v1.1.0-rc.N`) falls through to only `:rc` + `:X.Y.Z-rc.N`. Verified in Phase 12 HUMAN-UAT with the `rc.1` cut (all stable tags unchanged post-rc.1 push).

2. **(b) Plan 01 (this phase) removes the ci.yml main-push path.**
   Before Phase 12.1, `ci.yml`'s `image` job on push-to-main ran `just image-push ...:latest` and `just image-push ...:sha-<7>`, which was the OTHER structural mechanism by which `:latest` could move outside a stable-tag push. Plan 01 deletes those three steps. After Plan 01 merges, there is no workflow path by which an rc-tag push or a main-push can move `:latest` / `:1` / `:1.1`.

3. **(c) Automated post-tag-push CI assertion (`rc-guard.yml`) — handed off to Phase 13.**
   Per RESEARCH.md § "Open Questions (RESOLVED)" item #1 and § "Open Questions (RESOLVED)" summary item #1 (both RESOLVED: defer to Phase 13), the automated workflow that asserts "rc-tag push did not advance `:latest`/`:1`/`:1.1`/`:main` digests" is deferred to Phase 13 because (i) rc.2 is the next rc and is cut at the end of Phase 13, so the assertion can be validated end-to-end against a real rc push rather than dry-runs, and (ii) shipping rc-guard.yml in Phase 12.1 without a live rc push to exercise it would ship an unverified contract. Phase 13 task owner MUST add `rc-guard.yml` before the rc.2 cut. This handoff is listed in Phase 14 final-ship dependencies.

**Verification in this phase:**
- Mechanism (a) is pre-existing and covered by Phase 12's SUMMARY.
- Mechanism (b) is covered by Plan 01's `<verification>` and `<acceptance_criteria>` blocks in this phase.
- Mechanism (c) is covered by the explicit handoff above; no in-phase task creates `rc-guard.yml`.

**Phase-closeout grep for SC3 coverage:**
`grep -l 'success_criterion_handoff id="SC3"' .planning/phases/12.1-ghcr-tag-hygiene/*-PLAN.md` MUST match at least one plan (this one). If `rc-guard.yml` is NOT shipped by end of Phase 13, SC3 remains delivered by (a)+(b) alone and the maintainer retains the runbook burden of asserting post-rc-push that stable-tag digests are unchanged — documented in Plan 04's checkpoint pattern.
</success_criterion_handoff>

<success_criteria>
- File exists, YAML-valid, `actionlint` clean (if available)
- All grep assertions in `<acceptance_criteria>` pass
- No `:latest`/`:rc`/`:X.Y.Z`/`:X.Y`/`:X` tag reference anywhere in the file (only `:main`)
- Workflow mirrors release.yml's "Mirror? Yes" column in the Build Parity Inventory
- Post-push assertion step is the final step in the `main-build` job
- Cache scope string is `cronduit-main` (distinct from `cronduit-release`)
- SC3 handoff recorded in `<success_criterion_handoff id="SC3">` above — Phase-closeout verification finds SC3 delivered by (a) Phase 12 D-10 release.yml gating + (b) Plan 01 ci.yml cleanup + (c) rc-guard.yml handed off to Phase 13
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ghcr-tag-hygiene/12.1-02-SUMMARY.md` documenting:
- File created
- Build-parity audit (YAML blocks copied verbatim vs. narrowed vs. new-to-this-phase)
- OPS-10 coverage: this plan satisfies the "new `:main` floating tag" requirement; first real proof comes on the next push to main after merge (MANUAL verification — not in plan scope)
- Known follow-ups: rc-guard.yml (deferred to Phase 13 per RESEARCH.md Open Question #2), cleanup-images.yml retention review (deferred to post-monitoring per RESEARCH.md Open Question #3)
- Dockerfile cross-reference comment recommendation (RESEARCH.md L317-318) — optional follow-up to add a "See also: main-build.yml" comment to Dockerfile so a future ARG-adding PR can't silently diverge release and main builds
</output>
