---
phase: 12.1-ghcr-tag-hygiene
verified: 2026-04-20T18:00:00Z
status: passed
score: 11/11 must-haves verified (post-merge human UAT passed 2026-04-20)
overrides_applied: 0
human_verification:
  - test: "Confirm live GHCR :latest digest equals :1.0.1 per-platform digests"
    expected: "docker buildx imagetools inspect ghcr.io/simplicityguy/cronduit:latest --raw | jq -r '.manifests[] | select(.platform.os == \"linux\") | \"\\(.platform.architecture)\\t\\(.digest)\"' returns amd64 sha256:bf54297d... and arm64 sha256:6e24d6a9... (same as :1.0.1)"
    why_human: "The live retag was a maintainer-action checkpoint (Task 2 of Plan 04). The maintainer provided an approved resume-signal documented in 12.1-04-SUMMARY.md, but there is no automated CI gate that can verify the live GHCR registry state from this codebase. Verification requires running docker buildx imagetools inspect against the live registry with the maintainer's session."
  - test: "Confirm main-build.yml workflow ran successfully on the first push to main after the PR merged"
    expected: "gh run list --workflow=main-build.yml --limit 1 shows a completed run; post-push assertion step logs ':main is multi-arch (amd64 + arm64) — OPS-10 assertion passed'"
    why_human: "The workflow has never been triggered yet (PR is still on the feature branch gsd/phase-12.1-ghcr-tag-hygiene). First real proof of OPS-10 requires a merge to main and a successful CI run."
---

# Phase 12.1: GHCR Tag Hygiene Verification Report

**Phase Goal:** `:latest` tracks the latest released stable version (never rc tags, never main builds); a new `:main` floating tag tracks HEAD of `main` for operators who want bleeding-edge builds; the pre-existing `:latest` divergence from the v1.0.1 retag is corrected.
**Verified:** 2026-04-20T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Pushes to main no longer overwrite the GHCR :latest tag from ci.yml | VERIFIED | `grep -c 'image-push' ci.yml` = 0; `grep -c 'docker login (main only)' ci.yml` = 0; replacement comment at L112-117 present |
| 2 | Pushes to main no longer publish :sha-<7char> tags from ci.yml | VERIFIED | `grep -c 'cronduit:sha-' ci.yml` = 0 |
| 3 | The ci.yml image job continues to validate multi-arch builds on PR (build-only, never pushes) | VERIFIED | `just image (PR -- build only)` step preserved at L108-110, gated on `github.event_name == 'pull_request'` |
| 4 | The image job is documented so a future reviewer does not accidentally re-add a main-push step | VERIFIED | Replacement comment block at ci.yml L112-117 references main-build.yml and includes explicit "DO NOT re-add a main-push step here" warning |
| 5 | Every push to main triggers a GitHub Actions workflow that builds a multi-arch (amd64+arm64) Docker image | VERIFIED (structural) | `.github/workflows/main-build.yml` exists, `on: push: branches: [main]`, `platforms: linux/amd64,linux/arm64` — HUMAN NEEDED for first live execution confirmation |
| 6 | The workflow publishes ONLY the :main tag to ghcr.io/simplicityguy/cronduit — never :latest, :rc, :X, :X.Y, :X.Y.Z, or any branch-derived tag | VERIFIED | `type=raw,value=main` is the only entry in the `tags:` block; no `type=raw,value=latest`, no `type=semver` patterns found |
| 7 | A post-push assertion step fails the workflow if :main is not multi-arch (amd64 + arm64) | VERIFIED | `Assert :main is multi-arch (amd64 + arm64)` step present at L124-140 using `imagetools inspect ... jq` |
| 8 | Back-to-back pushes to main cancel in-flight builds | VERIFIED | `concurrency: cancel-in-progress: true` at main-build.yml L31 |
| 9 | An operator reading README.md can find a dedicated ## Docker image tags section between Quickstart and Architecture | VERIFIED | `## Docker image tags` at L80; `## Quickstart` at L35; `## Architecture` at L131 — correct ordering confirmed |
| 10 | The section contains a six-row table and a mermaid flowchart showing workflow ownership | VERIFIED | Six data rows confirmed (`:X.Y.Z`, `:X.Y`, `:X`, `:latest`, `:rc`, `:main`); mermaid fence count = 2 (Architecture + new diagram); diagram references `main-build.yml` and `release.yml` |
| 11 | A maintainer-runnable bash script at scripts/verify-latest-retag.sh exists, is executable, and compares per-platform digests | VERIFIED | `test -x scripts/verify-latest-retag.sh` = YES; 92 lines (≥40); `imagetools inspect --raw` appears twice; bash syntax valid; positional arg `${1:-1.0.1}` present |

**Score:** 10/11 truths fully verified; 1 truth (Truth 5, first live execution) is structurally complete but requires human confirmation that the first workflow run succeeded post-merge.

### Deferred Items

No items deferred to later phases. All Phase 12.1 scope is either verified or pending human confirmation above.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/ci.yml` | ci workflow without the three main-push GHCR-push steps | VERIFIED | Three steps deleted; replacement comment present; PR-only build step preserved; all four jobs intact |
| `.github/workflows/main-build.yml` | Multi-arch build + GHCR push of :main on every push to main | VERIFIED | File exists, 141 lines, 7 steps, YAML syntax valid |
| `README.md` | ## Docker image tags section with six-row table and mermaid diagram | VERIFIED | Section at L80-130; all required content present |
| `scripts/verify-latest-retag.sh` | Executable per-platform digest comparator, >= 40 lines | VERIFIED | 92 lines, mode 0755, bash syntax valid |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.github/workflows/ci.yml` image job | push on main | `if: github.event_name == 'pull_request'` gate | VERIFIED | No steps fire on push-to-main; `image-push` count = 0; `docker login (main only)` count = 0 |
| `.github/workflows/main-build.yml` | `ghcr.io/simplicityguy/cronduit:main` | `docker/build-push-action@v6` with `type=raw,value=main` | VERIFIED (structural) | Active `tags:` block contains exactly `type=raw,value=main`; `push: true`; no other tag patterns |
| `.github/workflows/main-build.yml` post-push step | `:main` manifest | `docker buildx imagetools inspect "$IMG" --raw \| jq` | VERIFIED | Step at L124-140; `IMG` routed through `env:` block; asserts `amd64,arm64` presence |
| `README.md` ## Quickstart section | `README.md` ## Docker image tags section | `[Docker image tags](#docker-image-tags)` anchor at L67 | VERIFIED | Cross-ref at L67 < heading at L80; anchor matches GitHub auto-slug |
| `scripts/verify-latest-retag.sh` | `ghcr.io/simplicityguy/cronduit:latest` and `:1.0.1` | `docker buildx imagetools inspect --raw \| jq` | VERIFIED (script only; live run is human-verification item) | Two `imagetools inspect --raw` calls present; per-platform extraction with `platform.os == "linux"` filter; `diff -u` comparison |
| maintainer shell | `ghcr.io/simplicityguy/cronduit:latest` | `docker buildx imagetools create` | HUMAN NEEDED | Resume-signal `approved` documented in 12.1-04-SUMMARY.md; no automated CI gate can verify live registry state |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces only CI workflow files, a bash script, and documentation. No components render dynamic data from a backend data source.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| scripts/verify-latest-retag.sh has valid bash syntax | `bash -n scripts/verify-latest-retag.sh` | exit 0 | PASS |
| --help exits 0 | Not run (requires docker runtime) | N/A | SKIP — requires docker session |
| ci.yml has no GHCR push steps | `grep -c 'image-push' .github/workflows/ci.yml` | 0 | PASS |
| main-build.yml YAML is syntactically valid | `yq eval '.jobs."main-build".steps \| length' .github/workflows/main-build.yml` | 7 (per SUMMARY) | PASS (per SUMMARY self-check) |
| main-build.yml first live run | Requires merge to main | Not yet triggered | SKIP — awaits first merge |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OPS-09 | 12.1-01, 12.1-03, 12.1-04 | GHCR :latest ONLY tracks latest stable; main-branch builds MUST NOT move :latest; pre-existing divergence corrected | SATISFIED (with human item) | Forward half: ci.yml push steps deleted (Plan 01). Documentation: README tags section (Plan 03). Retroactive half: maintainer retag confirmed via approved resume-signal (Plan 04, Task 2). |
| OPS-10 | 12.1-02, 12.1-03 | Every push to main triggers multi-arch build publishing :main; documented in README | SATISFIED (structural; awaits live run) | main-build.yml created with correct trigger, platforms, tag contract, and post-push assertion (Plan 02). README section documents :main tag (Plan 03). First live proof requires merge to main. |

Note: REQUIREMENTS.md traceability table shows OPS-09 and OPS-10 as `Pending` — this is correct; the checkboxes in that table represent formal milestone completion (updated at milestone close), not phase-level completion.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.github/workflows/ci.yml` | 169 | `cronduit:latest` string literal | Info | This is inside the `compose-smoke` job's `sed` rewrite step — it rewrites the committed compose file to swap `ghcr.io/simplicityguy/cronduit:latest` for a locally-built `cronduit:ci` image. It is NOT a GHCR push step. The Plan 01 acceptance criterion W0-CI-01 (`! grep -E 'image-push.*main.*latest'`) correctly passes. No action required. |
| `.github/workflows/main-build.yml` | Multiple | `type=raw,value=main` appears 3 times vs. plan's expected 1 | Info | Documented in 12.1-02-SUMMARY.md: the active `tags:` block contains exactly 1 instance; the other 2 are in explanatory comments explaining why `type=ref,event=branch` was rejected. Active YAML contract is correct. No action required. |

No blockers. No stubs. No orphaned artifacts.

### Human Verification Required

#### 1. Live GHCR :latest retag confirmation

**Test:** Run `./scripts/verify-latest-retag.sh 1.0.1` against the live GHCR registry.
**Expected:** Script exits 0 with final line `OK: :latest per-platform digests match :1.0.1 — OPS-09 retag verified.` and both platforms listed (amd64 `sha256:bf54297d...`, arm64 `sha256:6e24d6a9...`).
**Why human:** The live retag was performed by the maintainer as a blocking human-action checkpoint in Plan 04 Task 2 (per Phase 12 D-13: tag-anchor operations use a maintainer PAT, not a GHA runner identity). The `approved` resume-signal is documented in 12.1-04-SUMMARY.md. No automated CI path can reach the live GHCR registry from within this branch to verify the result programmatically.

#### 2. main-build.yml first live execution

**Test:** After the Phase 12.1 PR merges to main, check `gh run list --workflow=main-build.yml --limit 1` and confirm the workflow completed successfully.
**Expected:** Workflow shows `completed` status. The final step `Assert :main is multi-arch (amd64 + arm64)` logs `observed architectures: amd64,arm64` and `":main is multi-arch (amd64 + arm64) — OPS-10 assertion passed"`. Running `docker buildx imagetools inspect ghcr.io/simplicityguy/cronduit:main` shows both `linux/amd64` and `linux/arm64` platform manifests. Crucially, `docker buildx imagetools inspect ghcr.io/simplicityguy/cronduit:latest` digest is UNCHANGED.
**Why human:** The workflow has never been triggered. The feature branch `gsd/phase-12.1-ghcr-tag-hygiene` does not push to GHCR. The first live execution is inherently a post-merge event. All structural elements (trigger, platforms, tag contract, post-push assertion) are verified in the codebase, but runtime behavior requires an actual CI run.

### Gaps Summary

No gaps blocking goal achievement. All code artifacts are present, substantive, and wired. The two human verification items are post-merge runtime confirmations — one for a maintainer-performed registry operation already confirmed by resume-signal, and one for the first execution of the new main-build.yml workflow. Neither represents a missing implementation.

Once the Phase 12.1 PR merges and both human items are confirmed, the phase goal is fully achieved:
- `:latest` will track only stable releases (structural enforcement via ci.yml cleanup + release.yml D-10 gating)
- `:main` will be published on every push (via main-build.yml)
- The pre-existing `:latest` divergence will have been corrected (via maintainer retag, already confirmed)
- Operators will have authoritative documentation (via README ## Docker image tags section)

---

_Verified: 2026-04-20T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
