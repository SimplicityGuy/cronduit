---
phase: 24-milestone-close-out-final-v1-2-0-ship
plan: 01
type: execute
wave: 2
depends_on: ["24-02"]
files_modified:
  - THREAT_MODEL.md
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operator reading THREAT_MODEL.md sees a canonical Threat Model 5 (Webhook Outbound) with no `Status: Words-only stub` preamble and no `### Phase 24 Close-Out` forward-pointer."
    - "Operator reading THREAT_MODEL.md sees a new Threat Model 6 (Operator-supplied Docker labels) as a peer section after TM5."
    - "Operator reading the STRIDE Summary tables sees rows T-S3 (Spoofing), T-T4 (Tampering), T-I4 (Information Disclosure), T-D4 (DoS) — all four added per Pitfall 56."
    - "Operator clicking README §Security follows links into `#threat-model-5-webhook-outbound` and `#threat-model-6-operator-supplied-docker-labels` anchors and lands on the new sections."
    - "Operator reading THREAT_MODEL.md Changelog table sees a `Phase 24 close-out | 2026-05-NN | …` row; Revision line near top of file bumped to `2026-05-NN (Phase 24 — v1.2.0 close-out)`."
    - "D-03: TM5 transformation is REPLACE-IN-PLACE rewrite of THREAT_MODEL.md:189-229 (canonical TM5 entry matching TM1–TM4 structure exactly; no stub framing, no forward-pointer subsection)."
    - "D-04: Spec literalism is HYBRID — literal Pitfall 56 text for the four STRIDE row additions (T-S3, T-T4, T-I4, T-D4) + Pitfall 56's structural list (Threat / Attack Vector / Mitigations / Residual Risk / Recommendations) as the TM5+TM6 section skeleton; narrative bodies authored fresh to ground in v1.2.0 shipped reality."
    - "D-05: TM6 lands as a standalone peer section AFTER TM5 (matches TM1–TM5 structural pattern; TM5 Recommendations cross-links to TM6; STRIDE row T-T4 references TM6)."
    - "D-06: SINGLE plan touches THREAT_MODEL.md (TM5 rewrite + TM6 + four STRIDE rows + Changelog) AND README.md §Security link-back to the two new anchors — atomic-commit-per-plan; one coherent threat-model close-out diff closes Pitfall 56 audit predicates T-V12-XCUT-05/06/07 in one PR commit."
  artifacts:
    - path: "THREAT_MODEL.md"
      provides: "TM5 canonical (in-place rewrite of L189-229); new TM6 peer section after TM5; STRIDE rows T-S3/T-T4/T-I4/T-D4; Changelog v1.2 close-out row; Revision bump."
      contains: "## Threat Model 5: Webhook Outbound"
      also_contains: "## Threat Model 6: Operator-supplied Docker labels"
    - path: "README.md"
      provides: "§Security link-back widened to include `webhook outbound (SSRF)` and `operator-supplied Docker labels` anchor links into THREAT_MODEL.md."
      contains: "#threat-model-5-webhook-outbound"
      also_contains: "#threat-model-6-operator-supplied-docker-labels"
  key_links:
    - from: "README.md §Security"
      to: "THREAT_MODEL.md#threat-model-5-webhook-outbound"
      via: "markdown anchor link"
      pattern: "#threat-model-5-webhook-outbound"
    - from: "README.md §Security"
      to: "THREAT_MODEL.md#threat-model-6-operator-supplied-docker-labels"
      via: "markdown anchor link"
      pattern: "#threat-model-6-operator-supplied-docker-labels"
    - from: "THREAT_MODEL.md TM5 Recommendations"
      to: "THREAT_MODEL.md TM6"
      via: "cross-link"
      pattern: "Threat Model 6: Operator-supplied Docker Labels"
---

<objective>
Close out the v1.2 threat-model paperwork: rewrite the existing TM5 stub at `THREAT_MODEL.md:189-229` in place as the canonical Threat Model 5 (Webhook Outbound), add a new peer Threat Model 6 (Operator-supplied Docker labels), insert four STRIDE rows (T-S3 / T-T4 / T-I4 / T-D4) into the existing summary tables, add a Changelog row + Revision bump, and widen `README.md` §Security to link the two new section anchors.

Purpose: Closes Pitfall 56 audit predicates `T-V12-XCUT-05` (TM5 + TM6 sections exist with exact titles), `T-V12-XCUT-06` (STRIDE rows T-S3/T-T4/T-I4/T-D4 exist), and `T-V12-XCUT-07` (README links to the new TM sections). Satisfies ROADMAP Phase 24 success criterion #1.

Output: One commit `docs(24): threat model close-out — canonical TM5 + new TM6 + STRIDE rows T-S3..T-D4 + README §Security link-back` inside the close-out PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md
@THREAT_MODEL.md
@README.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TM5 in place + add new TM6 section + Changelog/Revision in THREAT_MODEL.md</name>
  <files>THREAT_MODEL.md</files>
  <read_first>
    - THREAT_MODEL.md (full file — see current TM5 stub at L189-229; TM1-TM4 canonical structure at L43-186; STRIDE Summary tables at L233-281; Changelog at L294-300)
    - .planning/research/PITFALLS.md offset 1099 limit 50 (Pitfall 56 — canonical STRIDE row source + section-spec source per D-04 HYBRID literalism)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md (D-03 in-place rewrite; D-04 HYBRID literalism; D-05 TM6 anchor; D-06 single-plan bundling)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-01" (exact line refs + structural skeleton + Pitfall 56 LITERAL text for STRIDE rows + audit-predicate language)
    - .planning/phases/15-foundation-preamble/15-CONTEXT.md (webhook delivery worker bounded mpsc + isolation — TM5 Mitigations cites)
    - .planning/phases/18-webhook-payload-state-filter-coalescing/18-CONTEXT.md (Standard Webhooks v1 payload + edge-triggered coalescing — TM5 Mitigations cites)
    - .planning/phases/19-webhook-hmac-signing-receiver-examples/19-CONTEXT.md (HMAC-SHA256 + base64 signature header + constant-time receiver examples — TM5 Mitigations cites)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md (HTTPS-required validator + full-jitter retry + 30s drain + webhook_deliveries dead-letter + cronduit_webhook_* metrics — TM5 Mitigations cites)
    - .planning/phases/17-custom-docker-labels-seed-001/17-CONTEXT.md (Docker labels: reserved-namespace + type-gate + size limits — TM6 Mitigations cites)
  </read_first>
  <action>
Per D-03 (REPLACE-IN-PLACE) + D-04 (HYBRID Pitfall 56 literalism for STRIDE rows; fresh prose for narrative) + D-05 (TM6 standalone peer section) + D-06 (single plan):

**A. Bump Revision near top of file (per PATTERNS § Plan 24-01).** Find the existing `**Revision:**` line and update to: `**Revision:** 2026-05-NN (Phase 24 — v1.2.0 close-out)` — replace `NN` with the actual day-of-month at commit time.

**B. REPLACE-IN-PLACE the TM5 stub (`THREAT_MODEL.md:189-229`) with canonical TM5.**

Strip these stub framings per D-03:
- L191 `> **Status:** Words-only stub for v1.2.0 (Phase 20). The canonical close-out … lands in [Phase 24 (Milestone Close-Out)]` blockquote preamble — REMOVE.
- L206 `### Mitigations (v1.2.0)` — RENAME to `### Mitigations` (drop the suffix to match TM1-TM4 exactly).
- L214 `### Accepted Residual Risk` — RENAME to `### Residual Risk` (match TM1-TM4 heading exactly).
- L220-229 `### Phase 24 Close-Out` forward-pointer subsection — REMOVE entirely (including the "Until that lands, this stub is the holding signal" footer).

Section heading MUST be EXACTLY `## Threat Model 5: Webhook Outbound` (drop the `(SSRF Accepted Risk)` parenthetical that the stub carried — per CONTEXT § Specifics, Pitfall 56 audit predicate T-V12-XCUT-05 uses this exact title).

Use TM1's structural skeleton (`### Threat / ### Attack Vector / ### Mitigations / ### Residual Risk / ### Recommendations` — see THREAT_MODEL.md:43-83 for shape) for the new TM5 body. Match TM1-TM4 narrative depth (~50-60 lines total per PATTERNS § calibration).

TM5 Mitigations narrative cites the shipped implementation (per D-04 fresh prose grounded in v1.2.0 shipped reality):
- Phase 15: `src/webhooks/{mod,worker}.rs` bounded `tokio::sync::mpsc::channel(1024)` + dedicated worker task (scheduler emits via `try_send`, NEVER awaits — receiver outage cannot stall the scheduler loop).
- Phase 18: Standard Webhooks v1 payload (`payload_version: "v1"`) + state filter + edge-triggered coalescing (default fires only on `streak_position == 1`).
- Phase 19: HMAC-SHA256 signing of the request body using a per-job secret from env (no plaintext-in-config); base64 signature header per Standard Webhooks v1 spec; receiver-side constant-time verification examples shipped under `docs/webhooks/receivers/` (Python/Go/Node).
- Phase 20: HTTPS-required validator for non-loopback/non-RFC1918 destinations (WARN on plain-HTTP for local destinations); full-jitter retry (3 attempts at t=0 / t=30s / t=300s with rand 0.8-1.2× multiplier); 30s graceful drain on shutdown; `webhook_deliveries` dead-letter table for unrecoverable failures; `cronduit_webhook_{success,failed,retried,dropped}_total` Prometheus metrics; `strip_url_credentials` defense per Pitfall 38.

TM5 Residual Risk MUST explicitly preserve Pitfall 56's "any URL the cronduit container can reach is reachable from the webhook worker; the operator is responsible for network controls (firewall / network namespaces / outbound deny-list at the gateway layer)" framing.

TM5 Recommendations MUST defer the destination allow/block-list filter to v1.3 (preserve the language already in the stub — point to `.planning/PROJECT.md` § Future Requirements). Add a cross-link line: `See also: [Threat Model 6: Operator-supplied Docker Labels](#threat-model-6-operator-supplied-docker-labels)`.

**C. ADD new TM6 section as a peer immediately AFTER the canonical TM5 (per D-05).** Use heading EXACTLY `## Threat Model 6: Operator-supplied Docker labels` — note LOWERCASE `labels` (Pitfall 56 audit predicate T-V12-XCUT-05 uses this exact capitalization; "Operator-supplied Docker Labels" with capital L FAILS the predicate). Length: ~30-40 lines (PATTERNS § calibration).

TM6 body uses the same five-section skeleton (Threat / Attack Vector / Mitigations / Residual Risk / Recommendations). TM6 Mitigations cites Phase 17:
- `src/config/validate.rs::check_labels_reserved` — rejects any user-supplied label key under the `cronduit.*` reserved namespace at config-load.
- `src/config/validate.rs::check_labels_only_on_docker_jobs` — rejects `[[jobs]].labels` on jobs whose `type` is not `docker` (type-gate validator).
- Per-key + per-value byte-size cap from Phase 17 (size-limit DoS surface).
- Merge precedence (`use_defaults=false` → replace; default → merge with per-job-wins on collision) — operator cannot accidentally clobber a `[defaults]`-supplied label by re-declaring it.

TM6 Residual Risk: operator-controlled label values can leak into log lines and Prometheus labels of downstream tooling (Traefik / Watchtower / etc.); cronduit does not sanitize for those downstream surfaces.

TM6 Recommendations: defer size-limit refinement (per-label vs total-bytes vs cardinality) to v1.3; document reserved-namespace clobber recovery (operator removes the offending TOML key and restarts).

**D. INSERT four STRIDE rows into the existing Summary tables at L237-281 (per D-04 LITERAL Pitfall 56 text).**

Each row goes into its existing category table:
- T-S3 under Spoofing table (currently at L240-242, after the T-S1/T-S2 rows): `| T-S3 | Attacker forges webhook payload | Mitigated by HMAC signing; out-of-scope: receiver-side verification (operator's responsibility) |`
- T-T4 under Tampering table (currently at L247-250, after T-T1/T-T2/T-T3): `| T-T4 | Attacker injects label collision into ` + backtick + `cronduit.*` + backtick + ` namespace | Mitigated by validator |`
- T-I4 under Information Disclosure table (currently at L261-264): `| T-I4 | Webhook URL embeds credentials in ` + backtick + `userinfo` + backtick + ` | Mitigated by ` + backtick + `strip_url_credentials` + backtick + ` (Pitfall 38) |`
- T-D4 under Denial of Service table (currently at L269-272): `| T-D4 | Webhook receiver outage stalls scheduler loop | Mitigated by bounded mpsc + delivery worker isolation (Pitfall 28) |`

Use the existing row shape `| T-X1 | <threat> | <mitigation status> |` verbatim — keep the table column widths consistent with adjacent rows.

**E. ADD a Changelog row at the END of the Changelog table at L294-300 (per PATTERNS § Plan 24-01):**
`| Phase 24 close-out | 2026-05-NN | TM5 canonical rewrite (replaces v1.2 stub); new TM6 (Operator-supplied Docker Labels); STRIDE rows T-S3/T-T4/T-I4/T-D4 added; v1.2 milestone close. |`

NO mermaid diagrams added in this task (TM1/TM2 etc have diagrams; TM5/TM6 narrative is sufficient without one — match the closer stubless precedent of TM3/TM4 which are also diagram-light).

NO changes anywhere else in `THREAT_MODEL.md` — preserve TM1-TM4 verbatim; preserve Out-of-Band Trust Assumptions section (L284-290) verbatim.
  </action>
  <verify>
    <automated>grep -c "^## Threat Model 5: Webhook Outbound$" THREAT_MODEL.md | grep -q "^1$" && grep -c "^## Threat Model 6: Operator-supplied Docker labels$" THREAT_MODEL.md | grep -q "^1$" && grep -q "| T-S3 |" THREAT_MODEL.md && grep -q "| T-T4 |" THREAT_MODEL.md && grep -q "| T-I4 |" THREAT_MODEL.md && grep -q "| T-D4 |" THREAT_MODEL.md && grep -q "Phase 24 close-out" THREAT_MODEL.md && ! grep -q "Words-only stub" THREAT_MODEL.md && ! grep -q "### Phase 24 Close-Out" THREAT_MODEL.md && ! grep -q "### Mitigations (v1.2.0)" THREAT_MODEL.md && ! grep -q "### Accepted Residual Risk" THREAT_MODEL.md && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `THREAT_MODEL.md` contains exactly one line `## Threat Model 5: Webhook Outbound` (no `(SSRF Accepted Risk)` suffix; no `(v1.2.0)` suffix).
    - `THREAT_MODEL.md` contains exactly one line `## Threat Model 6: Operator-supplied Docker labels` (lowercase `labels` per Pitfall 56 audit predicate).
    - All four STRIDE rows `| T-S3 |`, `| T-T4 |`, `| T-I4 |`, `| T-D4 |` present in their respective category tables.
    - Changelog table has a `Phase 24 close-out` row at the bottom.
    - Stub framings removed: no `Words-only stub`, no `### Phase 24 Close-Out`, no `### Mitigations (v1.2.0)`, no `### Accepted Residual Risk` substrings remain anywhere.
    - TM1-TM4 sections unchanged (visual diff confined to TM5 / TM6 / STRIDE tables / Changelog / Revision line).
    - Revision line near top updated to `2026-05-NN (Phase 24 — v1.2.0 close-out)`.
  </acceptance_criteria>
  <done>
    The automated grep gate above passes; running `git diff THREAT_MODEL.md` shows changes only within: Revision line, TM5 rewrite range, new TM6 section, four STRIDE table rows, one Changelog row.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen README §Security link-back to include TM5 + TM6 anchors</name>
  <files>README.md</files>
  <read_first>
    - README.md offset 19 limit 20 (existing §Security section — see L19-33 for the current sentence pointing at THREAT_MODEL.md)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-01" → "README §Security link-back" (exact replacement sentence per Pitfall 56 audit predicate T-V12-XCUT-07)
    - THREAT_MODEL.md (so the anchors `#threat-model-5-webhook-outbound` and `#threat-model-6-operator-supplied-docker-labels` exist — Task 1 produces them; this task runs AFTER Task 1 inside the same commit)
  </read_first>
  <action>
Per D-06 (bundled with TM doc edits in this single plan/commit):

Locate the existing sentence in `README.md` §Security (at ~L31 per PATTERNS § Plan 24-01):
`See [THREAT_MODEL.md](./THREAT_MODEL.md) for the full threat model covering Docker socket access, untrusted clients, config tampering, and malicious images.`

Replace IN PLACE with the widened sentence (verbatim from PATTERNS § Plan 24-01 — closes audit predicate T-V12-XCUT-07):
`See [THREAT_MODEL.md](./THREAT_MODEL.md) for the full threat model covering Docker socket access, untrusted clients, config tampering, malicious images, [webhook outbound (SSRF)](./THREAT_MODEL.md#threat-model-5-webhook-outbound), and [operator-supplied Docker labels](./THREAT_MODEL.md#threat-model-6-operator-supplied-docker-labels).`

Anchors are GitHub-Markdown-derived from the H2 headings authored in Task 1 (lowercase, dash-separated, punctuation stripped). Do NOT add the closing `.` inside the link text — keep the trailing `.` outside, matching the existing sentence style.

NO other README changes in this task — §Features / §Configuration / hero block / MILESTONES cross-link are scoped to plan 24-04 (per D-13).
  </action>
  <verify>
    <automated>grep -q "#threat-model-5-webhook-outbound" README.md && grep -q "#threat-model-6-operator-supplied-docker-labels" README.md && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` §Security contains the substring `#threat-model-5-webhook-outbound`.
    - `README.md` §Security contains the substring `#threat-model-6-operator-supplied-docker-labels`.
    - Both anchors are inside a single sentence prefixed `See [THREAT_MODEL.md](./THREAT_MODEL.md)` so the link-back is discoverable from the §Security top-anchor.
    - No other `README.md` content changed in this task (diff confined to §Security paragraph).
  </acceptance_criteria>
  <done>
    `git diff README.md` shows changes only within the §Security paragraph; the §Security sentence references both TM5 and TM6 anchors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — doc-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-DOC | Information Disclosure | Threat model documentation | accept | Doc-only changes. Plan 24-01 documents existing TM5/TM6 surfaces (operator-controlled webhook URLs / operator-supplied Docker labels) that ship in v1.2.0 binaries; does not introduce new surfaces. |
</threat_model>

<verification>
After both tasks land:
- `grep -c "## Threat Model" THREAT_MODEL.md` returns ≥ 6 (TM1-TM6).
- `grep "Phase 24 close-out" THREAT_MODEL.md` matches in Changelog.
- `grep -E "threat-model-(5-webhook-outbound|6-operator-supplied-docker-labels)" README.md` matches both anchors.
- All Pitfall 56 audit predicates T-V12-XCUT-05/06/07 close in this single commit.
</verification>

<success_criteria>
- THREAT_MODEL.md has canonical TM5 (no stub framings), new TM6, four STRIDE rows (T-S3/T-T4/T-I4/T-D4), Changelog row, Revision bump.
- README.md §Security links to both new TM anchors.
- Single commit lands in the close-out PR ahead of (or alongside) plans 24-03/24-04 which depend on these anchors.
- ROADMAP Phase 24 success criterion #1 (operator sees TM5 + TM6) closed.
</success_criteria>

<output>
Create `.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-01-SUMMARY.md` documenting: line ranges actually edited in `THREAT_MODEL.md`, the four STRIDE row insertion sites, the `README.md` §Security sentence diff, the audit-predicate closures (T-V12-XCUT-05/06/07).
</output>
