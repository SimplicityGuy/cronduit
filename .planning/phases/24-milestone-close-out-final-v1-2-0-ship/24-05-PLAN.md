---
phase: 24-milestone-close-out-final-v1-2-0-ship
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - deny.toml
  - Cargo.lock
autonomous: true
requirements: []
must_haves:
  truths:
    - "Maintainer running CI on the close-out PR sees `cargo-deny` as a REQUIRED step (no `continue-on-error: true`); a failed `cargo deny check` reddens CI and blocks merge."
    - "FOUND-16 is fully closed: cargo-deny was non-blocking from Phase 15 D-02 through rc.3; this plan promotes to blocking before rc.4 cut."
    - "If any advisory has accumulated since rc.1, the close-out PR includes the remediation (deny.toml exception with documented expiry OR Cargo.lock rev) in the same plan diff."
    - "Task 1 probe log `.tmp/deny-check.log` includes an explicit `EXIT=<code>` marker line so the verify gate cannot be trivially satisfied by a `touch .tmp/deny-check.log` no-op (per checker W5)."
    - "D-11: cargo-deny WARN→ERROR promotion is its own plan and lands EARLY in the close-out PR ahead of the maintainer-EXECUTES plans 24-06..08 — surfaces any accumulated advisory / license / duplicate-version issues since rc.1 with time to fix `deny.toml` or `Cargo.lock` in the same close-out PR; plans 24-06..08 then run against a known-green CI gate."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "cargo-deny job with `continue-on-error` removed (or set to false) + updated past-tense comment block"
      contains: "just deny"
    - path: "deny.toml"
      provides: "(conditional) exceptions for accumulated advisories with timestamped re-evaluate-date comments"
      may_change: true
    - path: "Cargo.lock"
      provides: "(conditional) dep rev for any advisory remediations"
      may_change: true
  key_links:
    - from: ".github/workflows/ci.yml cargo-deny step"
      to: "CI gating behavior"
      via: "GitHub Actions `continue-on-error` field removal/flip"
      pattern: "(?!continue-on-error: true)"
---

<objective>
Promote the cargo-deny CI gate from non-blocking (warn) to blocking (error) per FOUND-16 + D-11. Single-line change at `.github/workflows/ci.yml` L58 (remove `continue-on-error: true` or flip to `false`); update the preceding comment block at L47-53 to past-tense; conditionally land `deny.toml` exceptions or `Cargo.lock` revs if `cargo deny check` surfaces accumulated advisories since rc.1.

Per CONTEXT D-11, this plan lands EARLY in the close-out PR (before the maintainer-EXECUTES plans 24-06..08) so the rc.4 cut runs against a known-green CI gate.

Per CONTEXT § Specifics two-branch outcome:
- **Branch A (uneventful):** `cargo deny check` already green at WARN level → single-line flip + comment past-tense → done.
- **Branch B (eventful):** advisories accumulated since rc.1 → fix via `deny.toml` allowlist (timestamped expiry comment) OR `Cargo.lock` rev (`cargo update -p <crate>`) → re-run CI → flip → done.

Purpose: Closes FOUND-16 (the last unticked v1.2 requirement that needed a workflow change, not source) + satisfies ROADMAP Phase 24 success criterion #4 (cargo-deny ERROR-gate before final tag).

Output: One commit `ci(24): promote cargo-deny to blocking (FOUND-16)` (Branch A) OR two commits if Branch B yields a separable dep-rev commit. Lands in the close-out PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md
@.planning/phases/15-foundation-preamble/15-CONTEXT.md
@.github/workflows/ci.yml
@deny.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cargo deny check locally + identify Branch A vs Branch B</name>
  <files>(read-only branching probe — no file mutations unless Branch B)</files>
  <read_first>
    - .github/workflows/ci.yml offset 47 limit 15 (cargo-deny block — L47-58 per PATTERNS; the comment block at L47-53 explicitly forecasts this Phase 24 flip)
    - deny.toml (current exception entries + license / advisory stance)
    - .planning/phases/15-foundation-preamble/15-CONTEXT.md § D-02 (the original FOUND-16 non-blocking decision with Phase 24 promotion forecast)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md § D-11 + § Specifics (two-branch outcome)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-05" (single-line removal at L58 + past-tense comment update at L47-53)
  </read_first>
  <action>
Branching probe — run `just deny` (which wraps `cargo deny check`) locally and inspect output.

**Capture the exit code into the log (resolves checker W5).** A trivial `touch .tmp/deny-check.log` would satisfy a bare `test -f` check; the gate MUST require an `EXIT=` marker line written from the actual probe so a no-op cannot pass.

```bash
mkdir -p .tmp
just deny 2>&1 | tee .tmp/deny-check.log
DENY_EXIT=$?
echo "EXIT=${DENY_EXIT}" >> .tmp/deny-check.log
echo "TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> .tmp/deny-check.log
```

Classify outcome by the captured exit code:

**Branch A (uneventful — preferred):** `EXIT=0` in `.tmp/deny-check.log`; all advisories / licenses / duplicate-versions / bans pass at WARN level (`cargo deny check` is green even before the `continue-on-error` flip). Proceed to Task 2 with a Branch-A scope (single-line flip + comment update only).

**Branch B (eventful):** `EXIT=<non-zero>` in `.tmp/deny-check.log`; one or more findings reported. For EACH finding, classify the remediation:
- **Advisory** (RUSTSEC-NNNN-NNNN) — preferred remediation is `Cargo.lock` rev via `cargo update -p <crate>` (with the dep author's fix shipped upstream). Falls back to `deny.toml` advisory allowlist entry with a timestamped re-evaluate-date comment (`# expires: 2026-MM-DD — re-evaluate before v1.3 close`).
- **License finding** — `deny.toml` license allowlist exception, with comment justifying the inclusion.
- **Duplicate-versions** — usually require nothing (pairs with `deny.toml`'s `bans.multiple-versions = "warn"` per Phase 15 D-10 layered-defense pattern). If a duplicate has become an error-severity finding, prefer dep-rev to converge versions.
- **Bans** — should never trigger in a clean tree; if it does, investigate as a new-dep introduction (likely a regression — block the close-out PR until resolved).

For Branch B, decide between `deny.toml` exception vs `Cargo.lock` rev per finding (CONTEXT § Claude's Discretion authorizes planner judgment). Document each finding + chosen remediation in the task notes for Task 2 to apply.

If the probe reveals findings of severity that cannot be remediated within the close-out PR window (e.g., a critical-severity advisory in a deep transitive dep with no upstream fix), STOP and escalate — Phase 24 may need to wait, or the affected dependency may need to be replaced as a hotfix before close-out continues.
  </action>
  <verify>
    <automated>test -f .tmp/deny-check.log && grep -q "^EXIT=" .tmp/deny-check.log && echo "PASS — probe logged with EXIT= marker (Branch A or B determined by EXIT value)" || (echo "FAIL — probe not run OR EXIT= marker missing (gate cannot accept a touched-empty .tmp/deny-check.log)"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `just deny` (or equivalent `cargo deny check`) ran to completion.
    - Output captured at `.tmp/deny-check.log` (transient — not committed) AND log contains an explicit `EXIT=<code>` marker line written from the actual probe (per checker W5 — guards against `touch .tmp/deny-check.log` no-op satisfying the gate).
    - Branch A vs Branch B classified BY THE EXIT VALUE (Branch A: `EXIT=0`; Branch B: `EXIT=` any non-zero).
    - If Branch B: each finding has a chosen remediation (deny.toml exception OR Cargo.lock rev) recorded for Task 2.
    - If unremediable: escalation noted in summary (the rest of P24 may need to wait).
  </acceptance_criteria>
  <done>
    Branch decision made; Task 2 has a deterministic edit list (Branch-A: 2 edits in ci.yml only; Branch-B: ci.yml edits + deny.toml entries and/or Cargo.lock rev).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply cargo-deny promotion + (conditional) advisory remediation</name>
  <files>.github/workflows/ci.yml, deny.toml (conditional), Cargo.lock (conditional)</files>
  <read_first>
    - .github/workflows/ci.yml offset 47 limit 15 (cargo-deny block — exact L47-58 region per PATTERNS § Plan 24-05)
    - deny.toml (conditional read — only if Branch B + deny.toml remediation chosen for any finding)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-05" (single-line removal + past-tense comment block — verbatim replacement text)
  </read_first>
  <action>
**A. Always — apply the ci.yml promotion:**

In `.github/workflows/ci.yml`, locate the cargo-deny step block (L47-58 per PATTERNS):

Current state (from PATTERNS § Plan 24-05):
```yaml
      # Phase 15 / FOUND-16. cargo-deny supply-chain check (advisories +
      # licenses + duplicate-versions). Non-blocking on rc.1 per D-09 — the
      # step is marked continue-on-error: true so a transient advisory or
      # transitive duplicate-version finding cannot redden CI in v1.2 hands.
      # Promoted to blocking (single-line removal of continue-on-error)
      # before final v1.2.0 ships in Phase 24. Pairs with deny.toml's
      # `bans.multiple-versions = "warn"` for two-layer non-blocking (D-10).
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny
      - run: just deny
        continue-on-error: true
```

PREFERRED replacement (remove the `continue-on-error: true` line entirely; rewrite comment in past tense — single coherent block per PATTERNS § Plan 24-05):
```yaml
      # Phase 15 / FOUND-16. cargo-deny supply-chain check (advisories +
      # licenses + duplicate-versions). PROMOTED TO BLOCKING in Phase 24 per
      # the original FOUND-16 spec. Pairs with deny.toml's
      # `bans.multiple-versions = "warn"` for layered defense (D-10).
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny
      - run: just deny
```

ACCEPTABLE alternative (keep the line, flip the value — less clean but preserves git-blame on the line):
```yaml
      - run: just deny
        continue-on-error: false   # P24 / FOUND-16 — promoted to blocking before final v1.2.0
```

Preferred shape per PATTERNS § Plan 24-05 is the FULL line removal (the original comment explicitly described this as "single-line removal of continue-on-error" — honor that spec).

**B. Branch B only — apply deny.toml or Cargo.lock remediations per Task 1's recorded decisions:**

For each finding from Task 1:

*If chosen `deny.toml` exception:*
- Open `deny.toml`.
- Add the appropriate entry under `[advisories.ignore]` (for RUSTSEC IDs), `[licenses.allow]` (for license additions), or `[bans.skip]` (for duplicate-version waivers).
- Each entry MUST carry a timestamped comment:
  ```toml
  # Phase 24 / v1.2.0 close-out: <crate> <version> — <RUSTSEC-NNNN-NNNN> / <license-id> / <duplicate-version waiver>
  # rationale: <one-line justification, e.g., "upstream fix in <crate> 0.X.Y; re-evaluate after v1.3 schedule.">
  # expires: 2026-MM-DD — re-evaluate before v1.3 close.
  ```
- The `expires:` date is a SOFT signal (cargo-deny does not enforce expiry); future maintainers (or future Phase X auditors) read this comment to know when to re-check.

*If chosen `Cargo.lock` rev:*
- Run `cargo update -p <crate>` (or `cargo update -p <crate> --precise <version>` if a specific version is needed).
- Commit the resulting `Cargo.lock` delta as part of this plan's diff.
- Verify `cargo build --workspace` still succeeds + `just deny` is now green.

**C. Re-run `just deny` locally** to confirm green at `continue-on-error: false`:
```bash
just deny && echo "GREEN — cargo-deny is ERROR-gate ready"
```

If still red, return to Task 1's classification — there may be a finding that needs a different remediation than initially chosen.

**D. Verify `cargo tree -i openssl-sys` is still empty** per CONTEXT D-19:
```bash
cargo tree -i openssl-sys 2>&1 | head -5
```
Output MUST be the message `package ID specification 'openssl-sys' did not match any packages in the workspace` or similar (the rustls invariant from PROJECT.md MUST NOT regress under any dep rev applied for Branch B).
  </action>
  <verify>
    <automated>(! grep -E "continue-on-error: true" .github/workflows/ci.yml | grep -i deny) && grep -E "just deny" .github/workflows/ci.yml && just deny && (cargo tree -i openssl-sys 2>&1 | grep -E "did not match|not found" -q) && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` cargo-deny step has NO `continue-on-error: true` (either line removed entirely OR flipped to `false`).
    - Surrounding comment block at L47-53 area reads in past tense (mentions PROMOTED TO BLOCKING or equivalent).
    - `just deny` exits 0 locally (green at the new ERROR-gate setting).
    - Branch B: any `deny.toml` exceptions carry timestamped re-evaluate comments; any `Cargo.lock` revs are minimal-diff and verified buildable.
    - `cargo tree -i openssl-sys` returns empty (rustls invariant preserved per D-19).
  </acceptance_criteria>
  <done>
    cargo-deny is the ERROR gate. rc.4 cut and final v1.2.0 tag will both run against this gate. FOUND-16 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

CI workflow change — strengthens supply-chain posture by promoting cargo-deny to blocking. Does not introduce new attack surface; mitigates dependency-advisory accumulation risk.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-SC | Tampering | npm/pip/cargo installs (supply chain) | mitigate | cargo-deny promoted to blocking — RUSTSEC advisories, license violations, duplicate-version proliferation, and ban violations now redden CI. Any `deny.toml` exceptions carry timestamped re-evaluate dates. Pairs with `deny.toml`'s `bans.multiple-versions = "warn"` for layered defense. |
| T-24-05-RUSTLS | Information Disclosure | Cross-compile / TLS surface | mitigate | `cargo tree -i openssl-sys` invariant verified empty post-remediation; rustls posture preserved regardless of Branch B dep revs. |
</threat_model>

<verification>
After both tasks land:
- `! grep "continue-on-error: true" .github/workflows/ci.yml | grep deny` (no non-blocking on the deny step).
- `just deny` exits 0 locally.
- Conditional `deny.toml` / `Cargo.lock` deltas committed if Branch B fired.
- ROADMAP Phase 24 success criterion #4 (cargo-deny ERROR-gate before final tag) partially closed (plan 24-08 verifies it stays green on the v1.2.0 tag CI run).
- FOUND-16 closed (per the v1.2-MILESTONE-AUDIT.md from plan 24-02).
</verification>

<success_criteria>
- cargo-deny is a blocking CI gate from this commit forward.
- Any accumulated advisories are remediated (deny.toml exception OR dep rev).
- rustls invariant preserved.
- All subsequent close-out PR commits + rc.4 cut + final v1.2.0 tag run against this gate.
</success_criteria>

<output>
Create `.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-05-SUMMARY.md` documenting: branch chosen (A or B); if B — exact findings + chosen remediations + expiry dates; final `just deny` exit code; rustls invariant probe output.
</output>
</content>
</invoke>