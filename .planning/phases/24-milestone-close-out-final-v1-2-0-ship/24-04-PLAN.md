---
phase: 24-milestone-close-out-final-v1-2-0-ship
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03"]
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Operator opening `README.md` sees a v1.2 'What's New' hero block above the §Security section, listing the five v1.2 features with anchor links into the relevant subsections."
    - "Operator reading §Features (or equivalent v1.2-highlights paragraph) discovers FCTX panel + exit-code histogram as new run-detail / job-detail visualizations."
    - "Operator reading §Configuration discovers a new §Webhooks subsection with a brief TOML example + 3-5 behavior bullets + forward-pointer to `docs/WEBHOOKS.md`."
    - "Operator reading §Security or §Releases footer sees a link to `MILESTONES.md` and to the GitHub Releases page."
    - "The §Features-decision (current README has NO top-level `## Features` section; the hero block IS the v1.2-features pointer surface) is RECORDED in Task 1 inventory output and the rationale appears in Task 2 acceptance criteria (per checker W2)."
    - "The §Security paragraph (lines edited by plan 24-01) is NOT modified by this plan — verified by an automated section-boundary diff check (per checker W8)."
    - "D-13: README updates scope is ALL FOUR additions — (1) §Features pointer for FCTX panel + exit-code histogram, (2) §Configuration §Webhooks subsection + forward-reference to docs/WEBHOOKS.md (mirrors P17 §Labels at L206 / P23 §Tag Filter Chips at L287 patterns), (3) v1.2 'What's New' hero block at top of README, (4) MILESTONES.md cross-link from §Security or new §Releases footer."
  artifacts:
    - path: "README.md"
      provides: "v1.2 'What's New' hero block + §Features pointer for FCTX + exit histogram + §Configuration §Webhooks subsection + MILESTONES cross-link"
      contains: "v1.2"
      also_contains: "docs/WEBHOOKS.md"
      also_contains: "MILESTONES.md"
  key_links:
    - from: "README.md hero block"
      to: "README.md §Configuration §Webhooks anchor (and the other v1.2 feature anchors)"
      via: "in-document anchor links"
      pattern: "#(webhooks|tag-filter-chips|labels)"
    - from: "README.md §Configuration §Webhooks"
      to: "docs/WEBHOOKS.md"
      via: "relative-path link"
      pattern: "docs/WEBHOOKS.md"
    - from: "README.md MILESTONES cross-link"
      to: "MILESTONES.md"
      via: "relative-path link"
      pattern: "MILESTONES.md"
---

<objective>
Apply the v1.2 README updates in one diff per D-13: (1) §Features pointer for FCTX panel + exit-code histogram; (2) §Configuration §Webhooks subsection with brief TOML example + forward-pointer to `docs/WEBHOOKS.md`; (3) v1.2 'What's New' hero block at top of README above §Security; (4) MILESTONES cross-link.

Per CONTEXT § Specifics ("README hero block timing — pre-rc.4 only"), the hero block lands in the close-out PR BEFORE rc.4 cuts so the rc.4 image's README has it.

Per CONTEXT § Claude's Discretion + PATTERNS § Plan 24-04:
- §Webhooks subsection LEAN shape (~25-40 lines: 1 intro paragraph + 1 TOML example + 3-5 behavior bullets + forward-pointer to the 649-line `docs/WEBHOOKS.md`) — mirrors §Tag Filter Chips at L287-315, NOT the deeper §Labels at L206-285.
- §Features pointer: depends on whether a top-level `## Features` section EXISTS in the current README. Task 1 inventory MUST record this decision (per checker W2). Planner-verified-at-2026-05-16: README does NOT contain a `## Features` section (top-level H2 headers are: §Security, §Quickstart, §Docker image tags, §Architecture, §Configuration, §Monitoring, §Development, §Troubleshooting, §Contributing, §License). Therefore the §Features pointer surface is the HERO BLOCK itself (point C below absorbs into point A). If Task 1 finds a §Features section, the planner-verified default flips and Task 2 adds bullets to the existing section instead.
- Hero block: single 'v1.2 highlights' paragraph with inline anchor links (planner-judged simplest shape).
- MILESTONES cross-link: §Releases footer at end of README.

Depends on plans 24-01 (Task adds anchors `#threat-model-5-webhook-outbound` / `#threat-model-6-operator-supplied-docker-labels` — hero block may reference TM5 if relevant), 24-02 (audit verdict — hero block summary mirrors audit score-summary lead), 24-03 (v1.2 entry in MILESTONES.md — cross-link target must exist).

**Section-boundary discipline (per checker W8):** Plans 24-01 and 24-04 both touch `README.md`. Plan 24-01 owns the §Security paragraph (Task 2 widens it with TM5/TM6 anchors). Plan 24-04 OWNS the four insertion sites (hero block above §Security, §Webhooks under §Configuration, FCTX+exit-histogram pointer in hero, §Releases footer). The two plans' edits MUST NOT overlap. Task 2's automated verify includes a section-boundary diff check that asserts the §Security paragraph lines are NOT in 24-04's commit diff.

Purpose: Operator-discoverability of all five v1.2 features. README is the first surface operators read after `docker run`.

Output: One commit `docs(24): README v1.2 close-out — hero block + §Features pointer + §Configuration §Webhooks + MILESTONES cross-link` inside the close-out PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-01-SUMMARY.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-03-SUMMARY.md
@README.md
@docs/WEBHOOKS.md
@MILESTONES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory current README anchors + verify docs/WEBHOOKS.md exists + record §Features decision</name>
  <files>(read-only inventory pass — no file mutations)</files>
  <read_first>
    - README.md offset 1 limit 50 (top-of-file structure — where §Security starts, what's above it)
    - README.md offset 200 limit 120 (the §Configuration zone — see §Labels at L206-285 + §Tag Filter Chips at L287-315 for precedent shape)
    - README.md offset 360 limit 30 (the §Monitoring zone at L366 area — closest in-tree precedent for FCTX + exit histogram pointer placement)
    - docs/WEBHOOKS.md offset 1 limit 30 (verify the operator-facing webhook doc exists + capture the top section title so the README §Webhooks subsection forward-pointer cites it correctly)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-04" (analog line refs + skeletons + planner-discretion guidance)
  </read_first>
  <action>
Read-only inventory pass. Capture EXACT current state so Task 2 can author idempotent inserts:

1. Find the line number where `## Security` begins. The hero block inserts on the line IMMEDIATELY ABOVE this `## Security` header (per CONTEXT § Specifics "Insert ABOVE L19").

2. **§Features decision (resolves checker W2 — MUST be recorded explicitly):** Search for a top-level `## Features` section header in the current README:
   ```bash
   grep -nE "^## Features$" README.md
   ```
   - **Planner-verified-at-2026-05-16:** the result is EMPTY — README has no `## Features` section. (Top-level H2 headers are: §Security, §Quickstart, §Docker image tags, §Architecture, §Configuration, §Monitoring, §Development, §Troubleshooting, §Contributing, §License.)
   - **Decision (default, planner-verified):** The hero block (point A in Task 2) IS the §Features pointer surface. FCTX panel + exit-code histogram are bullets #3 and #4 in the hero block. NO new top-level `## Features` section is created. Rationale: the hero block already enumerates all five v1.2 features with anchor links; adding a separate `## Features` section would duplicate that content + violate the "single source of truth" reading flow.
   - **Decision (alternative — only if Task 1 grep returns a hit):** If the executor's grep finds a `## Features` section (e.g., if README evolved between planning and execution), augment the existing section with two new bullets (FCTX panel + exit-code histogram) AND keep the hero block bullets — the two surfaces are then PARALLEL pointers, not duplicates. Document which path was taken in the Task 1 output.

3. Find the §Configuration header. Note the order of existing subsections (§Job Types / §Labels at L206 / §Tag Filter Chips at L287 / etc.). The new §Webhooks subsection inserts as a peer — placement choice per planner: directly after §Job Types so webhook configuration sits alongside core per-job config (preferred for discoverability) OR after §Labels (parallel to §Tag Filter Chips per the established v1.2 §Configuration cumulative-additions pattern).

4. Verify `docs/WEBHOOKS.md` exists (per CONTEXT § canonical_refs — P20 D-30 produced it). Capture its top H1/H2 heading verbatim so the README forward-pointer link text matches (`[docs/WEBHOOKS.md](./docs/WEBHOOKS.md)` is the form — capture the heading to use in the surrounding sentence).

5. Find the bottom of README — note whether a `## Releases` or `## License` or other terminal section exists. The MILESTONES cross-link becomes either:
   - A new `## Releases` section at the bottom with two bullets (link to `MILESTONES.md` + link to `https://github.com/SimplicityGuy/cronduit/releases`); OR
   - A footer line inside §Security ("See [MILESTONES.md](./MILESTONES.md) for version history.").
   Pick based on what feels least intrusive given the current README footer shape.

Output of this task: a brief planning-note (in the task's commit message body or kept as inline TODO comments deleted before Task 2 commits) capturing the five insertion points + the §Features decision (option chosen + rationale) + the §Webhooks placement decision + the §Releases placement decision.
  </action>
  <verify>
    <automated>grep -n "^## Security" README.md | head -1 && grep -n "^## Configuration" README.md | head -1 && grep -n "^### Labels" README.md | head -1 && grep -n "^### Tag Filter Chips" README.md | head -1 && test -f docs/WEBHOOKS.md && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `## Security` header located + line number captured.
    - `## Configuration` header located + line number captured.
    - `### Labels` and `### Tag Filter Chips` line numbers captured (precedent shape sources).
    - `docs/WEBHOOKS.md` exists.
    - **§Features decision RECORDED** (per checker W2): `grep -nE "^## Features$" README.md` result captured; decision documented as either (A) hero block IS the pointer (no new §Features section — planner-verified default) OR (B) augment existing §Features section with two bullets (only if grep returned a hit). Rationale captured in the task's output note.
    - Planner-discretion decisions recorded: §Webhooks placement (after §Labels or after §Job Types); §Features pointer placement (hero-block-absorption vs new subsection); §Releases footer placement (bottom-of-file new section vs §Security footer line).
  </acceptance_criteria>
  <done>
    All five insertion points identified; §Features decision documented; Task 2 has a deterministic edit plan with no ambiguity.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply the four README inserts in one diff</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — use Task 1's captured line numbers as anchors)
    - README.md offset 206 limit 110 (§Labels skeleton at L206-285 — use as deeper-shape precedent if planner picked §Webhooks deeper)
    - README.md offset 287 limit 35 (§Tag Filter Chips skeleton at L287-315 — use as leaner-shape precedent if planner picked §Webhooks leaner — preferred per PATTERNS § Plan 24-04 + § Discretion calibration since docs/WEBHOOKS.md is 649 lines)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md (webhook configuration shape — `[[jobs]].webhook = { url, secret_env, states, fire_every }` + `[defaults].webhook` override pattern)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-04" (skeletons + analog line refs)
    - MILESTONES.md (confirm `v1.2-…` entry from plan 24-03 exists so the cross-link target is real)
  </read_first>
  <action>
Apply ALL FOUR README inserts in a single coherent diff per D-13:

**A. v1.2 'What's New' hero block** — inserts on the line IMMEDIATELY ABOVE `## Security` (Task 1 captured line number).

Shape (per CONTEXT § Claude's Discretion — single-paragraph format with inline anchor links; this is the simplest of the three options offered):

```markdown
## What's New in v1.2

Cronduit v1.2 — Operator Integration & Insight — adds five operator-observable features on top of the v1.1 codebase:

- **Outbound webhooks** ([§Webhooks](#webhooks)) — Standard-Webhooks-v1 payload, HMAC-SHA256 signing, full-jitter retry, SSRF-aware HTTPS posture.
- **Custom Docker labels** ([§Labels](#labels)) — `[defaults]` + per-job merge precedence with a `cronduit.*` reserved-namespace validator.
- **Failure-context panel** on run-detail — five P1 signals (time, image, config hash, duration-vs-p50, scheduler-fire-skew) on every failed or timed-out run.
- **Exit-code histogram card** on job-detail — 10-bucket last-100-runs view with a status-discriminator-wins classifier.
- **Job tagging + dashboard filter chips** ([§Tag Filter Chips](#tag-filter-chips)) — tag your jobs, get auto-rendered AND-filter chips with bookmarkable URLs.

See [MILESTONES.md](./MILESTONES.md) for the full v1.2 release log and audit. Threat model coverage for v1.2: [Webhook Outbound (SSRF)](./THREAT_MODEL.md#threat-model-5-webhook-outbound) and [Operator-supplied Docker labels](./THREAT_MODEL.md#threat-model-6-operator-supplied-docker-labels).
```

The hero block deliberately uses anchor links that depend on Task 2's §Webhooks insert (point B below) — both land in the same commit so the link is valid at HEAD.

**B. §Webhooks subsection in §Configuration** — placement per Task 1's planner decision (preferred: as a peer subsection in §Configuration, positioned to read in operator-discovery order alongside §Labels and §Tag Filter Chips). LEAN shape per PATTERNS § Plan 24-04 (~25-40 lines):

```markdown
### Webhooks

Cronduit can POST a run-finalized event to any HTTPS URL when a job run reaches a terminal state. Useful for chat notifications, on-call escalation, audit pipelines, or any system that listens for "what just ran and how did it go."

Configure webhooks per-job (or set defaults inherited by every job):

```toml
[defaults]
webhook = { secret_env = "CRONDUIT_WEBHOOK_SECRET", states = ["failed", "timeout"] }

[[jobs]]
name = "nightly-postgres-backup"
schedule = "0 2 * * *"
image = "postgres:16-alpine"
command = ["pg_dumpall"]
webhook = { url = "https://hooks.example.com/cronduit" }
```

Webhook behavior:
- **Standard-Webhooks-v1 payload.** `webhook-id` / `webhook-timestamp` / `webhook-signature` headers; body carries `payload_version: "v1"`, job name, run ID, status, exit code, duration, start/end timestamps, and tags.
- **HMAC-SHA256 signing.** Secret is read from the env var named in `secret_env` (never plaintext-in-config). Receiver examples for Python / Go / Node ship in `docs/webhooks/receivers/`.
- **State filter + coalescing.** `states = ["failed", "timeout", "stopped"]` controls which terminal states fire. Coalescing is edge-triggered: by default only the first run of a fail-streak fires (configurable per job via `fire_every`).
- **HTTPS-required + SSRF posture.** Plain HTTP is rejected for non-loopback / non-RFC1918 destinations; `userinfo` credentials are stripped from URLs before delivery.
- **Retry + drain.** Three attempts at t=0, t=30s, t=300s with full jitter. Unrecoverable failures land in the `webhook_deliveries` dead-letter table. Graceful 30s drain on shutdown.

See [docs/WEBHOOKS.md](./docs/WEBHOOKS.md) for the full payload schema, receiver examples, threat model, and operational runbook.
```

Mirror the leaner §Tag Filter Chips paragraph + TOML + bullets + forward-pointer shape verbatim — do NOT add a mermaid merge-precedence diagram (that's §Labels' deeper-shape territory, not appropriate here given docs/WEBHOOKS.md depth).

**C. §Features pointer for FCTX panel + exit-code histogram** — per Task 1's §Features decision (per checker W2):

- **If Task 1 confirmed NO `## Features` section exists** (planner-verified default at 2026-05-16): the hero block (point A) IS the §Features pointer surface — points 3 + 4 of the v1.2 hero list cover FCTX panel + exit-code histogram. NO additional edit needed under this point. Document the absorption in the commit message: `Hero block absorbs §Features pointer (README has no top-level §Features section — hero is single source of truth for v1.2 features).`
- **If Task 1 found an explicit `## Features` section** (only fires if README evolved between planning and execution): augment it with two bullets:
  - **Failure-context panel on run-detail.** Five P1 signals on every failed or timed-out run.
  - **Exit-code histogram card on job-detail.** 10-bucket distribution of the last 100 runs.

  AND keep the hero block bullets — the two surfaces are then PARALLEL pointers, not duplicates.

**D. MILESTONES cross-link** — placement per Task 1 planner decision (preferred: new `## Releases` section at the very bottom of README, above `## License` if it exists):

```markdown
## Releases

- [MILESTONES.md](./MILESTONES.md) — full v1.0 / v1.1 / v1.2 release log with shipped tags, phases, and audit pointers.
- [GitHub Releases](https://github.com/SimplicityGuy/cronduit/releases) — published binaries, source tarballs, and `git-cliff`-authored release notes.
```

If a `## License` (or similar) terminal section already exists, place `## Releases` IMMEDIATELY ABOVE it. If no terminal section, `## Releases` becomes the new last section.

Constraints across all four inserts:
- All diagrams (if any added) MUST be mermaid (project memory `feedback_diagrams_mermaid.md`). This task's prose-only inserts do NOT add diagrams.
- **No edits to existing sections beyond the four inserts above.** Specifically: **§Security (plan 24-01 owns it — see W8 section-boundary check below)**, §Labels (P17 — preserved verbatim), §Tag Filter Chips (P23 — preserved verbatim), §Monitoring / §Quickstart / §Configuration intro paragraph — all preserved.
- Anchor links MUST match GitHub-Markdown auto-derived anchors (lowercase, dash-separated, punctuation stripped). `### Webhooks` → `#webhooks`. `### Labels` → `#labels`. `### Tag Filter Chips` → `#tag-filter-chips`.

**Section-boundary diff check (resolves checker W8):**

Plan 24-01 Task 2 widens the §Security paragraph by replacing the single existing sentence pointing at THREAT_MODEL.md with a widened sentence that adds two anchor links (`#threat-model-5-webhook-outbound` + `#threat-model-6-operator-supplied-docker-labels`). Plan 24-04 must NOT touch any line inside the §Security section.

After applying the four inserts in this task, run the boundary check:

```bash
# Step 1: identify the line range of §Security in the post-edit README
SEC_START=$(grep -n "^## Security$" README.md | head -1 | cut -d: -f1)
# §Security ends at the next ## header
SEC_END=$(awk -v start="$SEC_START" 'NR > start && /^## / { print NR-1; exit }' README.md)
echo "§Security spans L${SEC_START}-${SEC_END}"

# Step 2: confirm 24-04's diff does NOT modify any line in that range
# Compare against the parent commit (the close-out PR's last 24-01 commit).
git diff HEAD~1 -- README.md | awk -v start="$SEC_START" -v end="$SEC_END" '
  /^@@/ {
    match($0, /\+([0-9]+),?([0-9]*)/, arr)
    hunk_start = arr[1] + 0
    hunk_len = (arr[2] == "" ? 1 : arr[2] + 0)
    hunk_end = hunk_start + hunk_len - 1
    in_security = (hunk_start <= end && hunk_end >= start)
  }
  in_security && /^[+-]/ && !/^[+-]{3}/ { print; found=1 }
  END { exit (found ? 1 : 0) }
'
# Exit code 0 = no §Security edits in 24-04's diff (PASS)
# Exit code 1 = §Security lines modified by 24-04 (FAIL — boundary violation)
```

If the boundary check fails, STOP — the executor has accidentally modified a line owned by plan 24-01. Revert the §Security edit and re-author the insert sites outside the §Security range.

NB: the hero block inserts on the line IMMEDIATELY ABOVE `## Security` header, which is OUTSIDE the §Security section (the `## Security` header line itself is the section start). The hero block's new `## What's New in v1.2` becomes the new section above §Security. This is allowed — the boundary check excludes the §Security header line itself (which 24-04 does not modify; it remains `## Security`).
  </action>
  <verify>
    <automated>grep -q "^## What's New in v1.2" README.md && grep -q "^### Webhooks$" README.md && grep -q "docs/WEBHOOKS.md" README.md && grep -q "^## Releases" README.md && grep -q "MILESTONES.md" README.md && grep -q "#webhooks" README.md && grep -q "#labels" README.md && grep -q "#tag-filter-chips" README.md && SEC_START=$(grep -n "^## Security$" README.md | head -1 | cut -d: -f1) && SEC_END=$(awk -v start="$SEC_START" 'NR > start && /^## / { print NR-1; exit }' README.md) && (git diff HEAD~1 -- README.md 2>/dev/null | awk -v start="$SEC_START" -v end="$SEC_END" 'BEGIN { found=0 } /^@@/ { match($0, /\+([0-9]+),?([0-9]*)/, arr); hs=arr[1]+0; hl=(arr[2]==""?1:arr[2]+0); he=hs+hl-1; in_sec=(hs<=end && he>=start) } in_sec && /^[+-]/ && !/^[+-]{3}/ { found=1 } END { exit (found ? 1 : 0) }' || (echo "FAIL — 24-04 modified §Security lines (boundary violation; §Security is owned by plan 24-01)"; exit 1)) && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## What's New in v1.2` header (exact match).
    - `README.md` contains `### Webhooks` subsection header under §Configuration (exact match).
    - Hero block bullets cite five v1.2 features: webhooks, labels, FCTX panel, exit histogram, tagging.
    - §Webhooks subsection contains: 1 intro paragraph + 1 TOML code block + 3-5 behavior bullets + forward-pointer line ending `See [docs/WEBHOOKS.md](./docs/WEBHOOKS.md) …`.
    - **§Features pointer decision documented** (per checker W2): Task 1's `grep -nE "^## Features$" README.md` result + decision path (absorbed into hero block OR augment existing §Features section) captured in the commit body / summary. Rationale: hero block is single source of truth when no §Features section exists.
    - `## Releases` section (or §Security footer cross-link, per Task 1 decision) links to `MILESTONES.md` AND `https://github.com/SimplicityGuy/cronduit/releases`.
    - All anchor links (`#webhooks`, `#labels`, `#tag-filter-chips`) follow GitHub-Markdown auto-derivation rules.
    - **§Security boundary preserved** (per checker W8): automated `git diff HEAD~1 -- README.md` boundary check confirms 24-04's commit does NOT modify any line inside §Security's line range. The §Security paragraph (its actual content lines) is owned by plan 24-01.
    - No edits to existing §Labels, §Tag Filter Chips, §Monitoring sections (diff confined to the four insert sites).
    - No mermaid diagrams added.
  </acceptance_criteria>
  <done>
    `git diff README.md` shows four additive blocks: hero block above §Security; §Webhooks subsection in §Configuration; bullets in hero covering FCTX + exit histogram (point C absorbed into hero per §Features decision); §Releases section at bottom (or §Security footer line). No deletions or modifications inside the §Security paragraph (plan 24-01 owns those edits).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — doc-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-DOC | Information Disclosure | README docs | accept | Doc-only changes. Webhook subsection's TOML example uses a placeholder env var name (`CRONDUIT_WEBHOOK_SECRET`) consistent with the locked secret-from-env posture; no real secret material exposed. |
| T-24-04-OVERLAP | Integrity | README §Security boundary | mitigate | Plans 24-01 and 24-04 both edit README.md but own DISJOINT line ranges. The automated section-boundary diff check in Task 2's `<verify>` block asserts 24-04's commit does not modify lines inside §Security; a boundary violation reddens CI and blocks merge. |
</threat_model>

<verification>
After both tasks land:
- README has v1.2 hero / §Webhooks / FCTX+exit-histogram pointers / MILESTONES cross-link.
- All four inserts in one commit.
- §Security boundary preserved (plan 24-01 owns §Security edits; plan 24-04 owns the other four insert sites).
- ROADMAP Phase 24 success criteria #1 (TM links in §Security from plan 24-01) and #3 (operator discovery of v1.2 features) jointly closed.
- rc.4 image's README (built from this PR's merge SHA) will surface v1.2 content to operators.
</verification>

<success_criteria>
- Single commit lands all four inserts coherently.
- Hero block, §Webhooks, FCTX+exit-histogram pointers, MILESTONES cross-link all present and linked.
- §Features decision documented (per W2) — hero block absorbs the pointer when no §Features section exists; otherwise both surfaces coexist.
- §Security boundary preserved (per W8) — automated boundary diff check passes.
- README leads with v1.2 content for operators reading the file fresh.
- Anchor links resolve to real subsections (no dangling links).
- Existing v1.0/v1.1 §Labels / §Tag Filter Chips preserved verbatim.
</success_criteria>

<output>
Create `.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-04-SUMMARY.md` documenting: exact insertion line ranges; planner-discretion decisions taken (§Webhooks placement, §Features pointer shape + rationale per W2, §Releases placement); anchor links added; the §Security boundary diff check result (PASS / FAIL); any TODO markers (e.g., SHIPPED date in the hero block, if any).
</output>
</content>
</invoke>