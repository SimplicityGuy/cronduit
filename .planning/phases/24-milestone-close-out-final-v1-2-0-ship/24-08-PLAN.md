---
phase: 24-milestone-close-out-final-v1-2-0-ship
plan: 08
type: execute
wave: 6
depends_on: ["24-07"]
files_modified:
  - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md
  - .planning/STATE.md
  - MILESTONES.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Maintainer opening `24-FINAL-SHIP-PREFLIGHT.md` finds an 8-section runbook covering: rc.N UAT passed / identify rc.N SHA / retag command / `:latest` hyphen-gate verification / cargo-deny ERROR-gate verification / git-cliff cumulative release body / STATE.md flip / `/gsd-complete-milestone v1.2` follow-up."
    - "Section 3 retag command uses `git tag -a -s v1.2.0 -m \"v1.2 — Operator Integration & Insight\" <rc.N-SHA>` per `docs/release-rc.md` Step 2a (signed) or 2b (unsigned fallback)."
    - "Section 4 verifies `:1.2.0` == `:1.2` == `:1` == `:latest` four-tag equality on both amd64 + arm64 (ROADMAP success criterion #4)."
    - "Section 5 verifies `cargo-deny` was the ERROR gate on the v1.2.0 tag commit's CI run (FOUND-16 fully closed)."
    - "Section 8 references `/gsd-complete-milestone v1.2` as a separate post-final-tag command (NOT part of plan 24-08 itself per CONTEXT D-12)."
    - "Sign-off table flips the `:latest` invariant from rc preflight: `:latest` MUST NOW equal `:1.2.0` digest (not `:1.1.0`)."
    - "After plan 24-08 executes, `.planning/STATE.md` milestone status reads SHIPPED with final tag + date; `MILESTONES.md` v1.2 entry's SHIPPED date is finalized (placeholder removed if plan 24-03 used one)."
    - "D-01: Final-ship SHA strategy is to cut rc.4 from the close-out PR merge SHA → run full HUMAN-UAT on rc.4 → retag the rc.4 SHA as final `v1.2.0` (bit-identical image; mirrors v1.1 P14 D-16 'what was tested is what ships' discipline). Final tag does NOT point at rc.3 SHA. If rc.4 UAT surfaces findings, fixes land in a follow-up close-out PR → cut rc.5 → retag the LAST passing-UAT rc SHA as `v1.2.0`."
  artifacts:
    - path: ".planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md"
      provides: "Maintainer-EXECUTES final-tag (v1.2.0) runbook"
      min_lines: 150
      contains: "final_tag: v1.2.0"
      also_contains: "autonomous: false"
    - path: ".planning/STATE.md"
      provides: "Milestone status flipped to SHIPPED with final tag + date (by maintainer at runbook § 7)"
      may_change: true
    - path: "MILESTONES.md"
      provides: "(maintainer-finalized) SHIPPED date in v1.2 entry resolved from placeholder to actual ship date"
      may_change: true
  key_links:
    - from: "24-FINAL-SHIP-PREFLIGHT.md § 3"
      to: "docs/release-rc.md Step 2a/2b"
      via: "verbatim citation — runbook unchanged"
      pattern: "Step 2a|Step 2b"
    - from: "24-FINAL-SHIP-PREFLIGHT.md § 4"
      to: ":1.2.0 == :1.2 == :1 == :latest digest equality"
      via: "release.yml hyphen-gate from P12 D-10"
      pattern: ":1.2.0"
    - from: "24-FINAL-SHIP-PREFLIGHT.md § 8"
      to: "/gsd-complete-milestone v1.2 (separate command — not a P24 plan)"
      via: "post-final-tag follow-up"
      pattern: "/gsd-complete-milestone v1.2"
---

<objective>
Author `24-FINAL-SHIP-PREFLIGHT.md` — the maintainer-EXECUTES final-tag runbook that retags the last-passing-UAT rc.N SHA as `v1.2.0` (bit-identical image — per D-01 and P14 D-16 "what was tested is what ships" discipline).

Per CONTEXT D-12: `/gsd-complete-milestone v1.2` is a SEPARATE post-final-tag command, NOT part of this plan. The runbook's § 8 surfaces it as the maintainer's final step AFTER the v1.2.0 tag publishes + GHCR images verify.

Per CONTEXT D-18 informational + PATTERNS § Plan 24-08: Cargo.toml stays at `1.2.0` (matches the tag prefix per `feedback_tag_release_version_match.md`).

Per project memory `feedback_uat_user_validates.md`: Claude does NOT retag. The runbook is authored; the maintainer executes locally with their GPG key.

Purpose: Satisfies ROADMAP Phase 24 success criterion #4 (`:latest` advances to `:1.2.0`; four-tag equality; cargo-deny ERROR-gate on the v1.2.0 tag's CI run).

Output: One commit `docs(24): final-ship preflight runbook (autonomous=false)` (lands in the close-out PR OR as a follow-up commit — planner discretion; per atomic-commit-per-plan, inside the same PR is acceptable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md
@.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md
@.planning/milestones/v1.1-phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md
@docs/release-rc.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 24-FINAL-SHIP-PREFLIGHT.md maintainer runbook</name>
  <files>.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md</files>
  <read_first>
    - .planning/milestones/v1.1-phases/14-bulk-enable-disable-rc-3-final-v1-1-0-ship/14-CONTEXT.md offset 92 limit 25 (D-16 retag-the-rc-SHA bit-identical discipline + D-18 + D-19 cumulative `git cliff` release body)
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md (sign-off table shape + 9-section structure — mirror for ~8 sections with the `:latest` invariant FLIPPED)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-PATTERNS.md § "Plan 24-08" (8-section structure + sign-off table — `:latest` MUST equal `:1.2.0` digest NOT `:1.1.0`)
    - .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-CONTEXT.md § D-01 (final-ship SHA strategy: retag last-passing-rc SHA) + D-12 (`/gsd-complete-milestone v1.2` is separate) + D-18 informational (Cargo.toml stays at 1.2.0)
    - docs/release-rc.md (Step 2a signed / Step 2b unsigned fallback — runbook is reused verbatim)
    - .github/workflows/release.yml (hyphen-gate from P12 D-10 — verify the gate logic is unchanged on the v1.2.0 commit)
  </read_first>
  <action>
Author `.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md` (~150-180 lines).

**Frontmatter** (mirror PATTERNS § Plan 24-08 + plan 24-06 shape):

```yaml
---
phase: 24
plan: 08
type: final-ship-preflight
autonomous: false
final_tag: v1.2.0
created: 2026-05-NN
status: pending-maintainer-execution
---
```

**Preamble** (mirror `21-RC2-PREFLIGHT.md` preamble + add the retag-the-rc-SHA invariant from P14 D-16):

```markdown
# Phase 24 — Final v1.2.0 Ship Pre-Flight

**Phase:** 24 — Milestone Close-Out — final v1.2.0 ship
**Final tag target:** `v1.2.0`
**Runbook:** [docs/release-rc.md](../../../docs/release-rc.md) Step 2a (signed) or Step 2b (unsigned fallback) — REUSED VERBATIM (D-10 / D-16 informational). Phase 24 makes NO edits to this runbook.
**Cargo.toml version:** `1.2.0` (set in Phase 15; the final tag `v1.2.0` matches per `feedback_tag_release_version_match.md` and D-18 informational).

> This artifact is a maintainer-validated checklist. Claude authored it; the maintainer retags the rc.N SHA locally per D-10 + project memory `feedback_uat_user_validates.md`.
>
> **Retag-the-rc-SHA invariant (D-01 — mirrors v1.1 P14 D-16):** `v1.2.0` MUST retag the LAST PASSING-UAT rc.N SHA (rc.4 if UAT passed first time; rc.5 / rc.6 / etc. if iterated). Bit-identical image — what the maintainer UAT-validated is what ships. NO new commits between rc.N and v1.2.0.
>
> **Per D-14 (informational):** all changes land via PR on a feature branch; the v1.2.0 tag is pushed AFTER the close-out PR has merged AND the rc.N UAT has signed off in `24-HUMAN-UAT.md`.
```

**§ 1. rc.N UAT passed**

```markdown
## 1. rc.N UAT passed (final-tag prerequisite)

- [ ] `24-HUMAN-UAT.md` Scenarios 1-6 all ticked PASSED.
- [ ] Final sign-off block in `24-HUMAN-UAT.md` filled in with maintainer name + date.
- [ ] `RC tag UAT-validated:` field in `24-HUMAN-UAT.md` records the rc tag (`v1.2.0-rc.4` or iterated `v1.2.0-rc.N`).
- [ ] If iterated (rc.5+): each iteration's UAT also recorded in `24-HUMAN-UAT.md`; only the LAST passing rc.N SHA is retagged as `v1.2.0`.

Verification:
```bash
grep -E "(Scenario [1-6] passed|Final sign-off|Maintainer name|RC tag UAT-validated)" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-HUMAN-UAT.md
```
```

**§ 2. Identify the rc.N SHA to retag**

```markdown
## 2. Identify rc.N SHA to retag

The `v1.2.0` tag points at the SAME commit SHA as the last-passing-UAT rc.N tag (bit-identical image — D-01).

```bash
RC_TAG=v1.2.0-rc.4   # or rc.5, rc.6, etc. — whichever the maintainer ran UAT against
RC_SHA=$(git rev-list -n 1 "$RC_TAG")
echo "Retagging $RC_SHA (currently $RC_TAG) as v1.2.0"
```

- [ ] `RC_SHA` captured.
- [ ] `git log -1 $RC_SHA --format=oneline` is the close-out PR merge commit (or a hotfix commit if UAT iterated).
```

**§ 3. Retag command — maintainer runs LOCALLY (Phase 12 D-13 trust anchor)**

```markdown
## 3. Retag command — maintainer runs LOCALLY

Per [`docs/release-rc.md`](../../../docs/release-rc.md) **Step 2a (signed — preferred)**:

```bash
git tag -a -s v1.2.0 -m "v1.2 — Operator Integration & Insight" "$RC_SHA"
git push origin v1.2.0
```

OR **Step 2b (unsigned fallback — only if GPG is unavailable)**:

```bash
git tag -a v1.2.0 -m "v1.2 — Operator Integration & Insight" "$RC_SHA"
git push origin v1.2.0
```

- [ ] Tag pushed to `origin`.
- [ ] `gh release view v1.2.0` returns the published release within 10-15 minutes (release.yml multi-arch build latency).
- [ ] Release body is `git cliff --tag v1.2.0` output (no hand-edit per D-15 of P23 inherits to P24).
```

**§ 4. Post-publish verification: `:latest` hyphen-gate fires + four-tag equality**

```markdown
## 4. Post-publish verification: `:latest` hyphen-gate + four-tag equality

`v1.2.0` contains NO hyphen → `release.yml`'s hyphen-gate from P12 D-10 fires → `:1.2.0` + `:1.2` + `:1` + `:latest` all publish on amd64 + arm64.

Verify each tag's digest matches the v1.2.0 multi-arch manifest:

```bash
for tag in 1.2.0 1.2 1 latest; do
  echo "== :$tag =="
  docker manifest inspect ghcr.io/simplicityguy/cronduit:$tag | jq '.manifests[] | { arch: .platform.architecture, digest: .digest }'
done
```

- [ ] amd64 digest of `:1.2.0` matches amd64 digest of `:1.2`, `:1`, AND `:latest`.
- [ ] arm64 digest of `:1.2.0` matches arm64 digest of `:1.2`, `:1`, AND `:latest`.
- [ ] Four-tag equality verified on BOTH architectures (ROADMAP Phase 24 success criterion #4).
- [ ] `:latest` digest CHANGED from the pre-tag digest (which was `:1.1.0`).
- [ ] `cronduit --version` from the published `:1.2.0` image returns `cronduit 1.2.0`.

Capture before/after digests:

```bash
# Before (record from rc.4 sign-off table in plan 24-06):
PREV_LATEST_DIGEST="<from plan 24-06 § Sign-off — the :1.1.0 digest>"

# After (run now):
NEW_LATEST_DIGEST=$(docker manifest inspect ghcr.io/simplicityguy/cronduit:latest | jq -r '.manifests[0].digest')
echo "Previous :latest digest: $PREV_LATEST_DIGEST"
echo "New :latest digest:      $NEW_LATEST_DIGEST"
test "$PREV_LATEST_DIGEST" != "$NEW_LATEST_DIGEST" && echo "ADVANCED" || echo "STUCK — investigate"
```
```

**§ 5. cargo-deny ERROR-gated on v1.2.0 tag's CI run (FOUND-16 fully closed)**

```markdown
## 5. cargo-deny ERROR-gate on v1.2.0 tag CI run

FOUND-16 promoted in plan 24-05 (cargo-deny `continue-on-error: true` → blocking). Verify the v1.2.0 tag's CI run confirms this:

```bash
gh run list --workflow ci.yml --branch <ref> --commit "$RC_SHA" --limit 5
gh run view <run-id> --log | grep -E "(cargo-deny|just deny)" -A 3
```

- [ ] CI run for the v1.2.0 tag commit (== rc.N SHA) is green.
- [ ] `cargo-deny` step (`just deny`) is REQUIRED (no `continue-on-error: true` in the log).
- [ ] No `cargo deny check` advisories / license / duplicate / ban findings (or any present are `deny.toml`-allowlisted with documented expiry from plan 24-05).
- [ ] FOUND-16 considered fully closed at this point.
```

**§ 6. git-cliff cumulative v1.2.0 release body (P14 D-19 precedent)**

```markdown
## 6. git-cliff cumulative release body

Per Phase 14 D-19 (v1.1 final ship cumulative release notes pattern), verify the v1.2.0 release body covers ALL v1.2 commits (not just the rc.4 → v1.2.0 delta):

```bash
git cliff v1.1.0..v1.2.0
```

- [ ] Output covers every commit from `v1.1.0..v1.2.0` (rc.1, rc.2, rc.3, rc.4, … final).
- [ ] Output landed verbatim as the GitHub Release body for `v1.2.0` (no hand-edit per D-15 of P23 inherits).
- [ ] Five v1.2 features visible in the release notes: webhooks / labels / FCTX / exit histogram / tags.
```

**§ 7. Update `.planning/STATE.md` + finalize MILESTONES.md SHIPPED date**

```markdown
## 7. Update STATE.md + finalize MILESTONES.md SHIPPED date

After § 4-6 verify clean:

- [ ] Open `.planning/STATE.md`; flip `milestone: v1.2` `status: planning` (or whatever pre-ship state) to `status: shipped`; set `last_updated` to NOW; bump `progress.completed_phases` to 10 and `progress.percent` to 100; under `Current Position` set `Phase: 24` `Plan: 24-08 (SHIPPED)` `Status: Milestone v1.2 SHIPPED` with the actual ship date.
- [ ] Open `MILESTONES.md`; replace the `SHIPPED 2026-05-NN` placeholder in the v1.2 entry's H2 header with the actual ship date.
- [ ] Commit both file changes as `chore(24): finalize v1.2 ship — STATE.md + MILESTONES.md ship date`.
```

**§ 8. Run `/gsd-complete-milestone v1.2` (separate command — NOT part of plan 24-08)**

```markdown
## 8. Run `/gsd-complete-milestone v1.2`

Per CONTEXT D-12, `/gsd-complete-milestone v1.2` is a SEPARATE post-final-tag command. It runs AFTER § 7's commit + tag push verifies clean. The command:

1. Archives `.planning/milestones/v1.2-ROADMAP.md` and `.planning/milestones/v1.2-REQUIREMENTS.md` (snapshots of the current `.planning/ROADMAP.md` v1.2 zone and `.planning/REQUIREMENTS.md`).
2. Rewrites the main `.planning/ROADMAP.md` with milestone groupings (mirrors the v1.0 + v1.1 archive moves).
3. Commits the archive.
4. Runs the PROJECT.md evolution review.
5. Offers to create the next milestone (v1.3) inline — maintainer decides whether to accept or defer.

- [ ] `/gsd-complete-milestone v1.2` invoked from a fresh Claude session.
- [ ] `.planning/milestones/v1.2-ROADMAP.md` + `v1.2-REQUIREMENTS.md` archive files created.
- [ ] Main `.planning/ROADMAP.md` updated with v1.2 grouped under "Milestones" section.
- [ ] PROJECT.md evolution review run (may or may not produce edits — maintainer's call).
- [ ] v1.3 milestone created OR deferred (maintainer's call).
```

**Sign-off table** (PATTERNS § Plan 24-08 — `:latest` invariant FLIPPED — `:latest` MUST equal `:1.2.0` digest, NOT `:1.1.0`):

```markdown
## Sign-off

| Field | Value |
|-------|-------|
| Maintainer signature | `__________________` |
| Date (UTC) | `__________________` |
| Final tag commit SHA (== rc.N SHA) | `__________________` |
| rc.N tag UAT-validated (== retagged SHA source) | `__________________` (e.g., `v1.2.0-rc.4`) |
| GHCR amd64 digest (`:1.2.0`) | `__________________` |
| GHCR arm64 digest (`:1.2.0`) | `__________________` |
| GHCR `:latest` digest (MUST NOW equal `:1.2.0` digest) | `__________________` |
| GHCR `:1.2` digest (MUST equal `:1.2.0` digest) | `__________________` |
| GHCR `:1` digest (MUST equal `:1.2.0` digest) | `__________________` |
| Previous `:latest` digest (was `:1.1.0`) | `__________________` |
| `/gsd-complete-milestone v1.2` completed | `__________________` (y/n) |
```

**Out-of-scope footer** (mirror plan 24-06's shape with final-tag substitution):

```markdown
## Out of scope

- `release.yml` / `cliff.toml` / `docs/release-rc.md` modifications — reused verbatim per P12 D-10..D-12 / P24 D-10 / D-16 informational.
- `Cargo.toml` version bump — stays at `1.2.0` (matches the final tag prefix per `feedback_tag_release_version_match.md`).
- New code features — Phase 24 is paperwork + final-ship.
- `/gsd-complete-milestone v1.2` itself — invoked separately AFTER this runbook completes (D-12); the archive + ROADMAP rewrite + PROJECT.md evolution review happen in that command, not here.
- v1.3 milestone kickoff — happens inside `/gsd-complete-milestone v1.2` (offer accepted/deferred at maintainer's discretion).
```

**Cross-reference footer**:

```markdown
## Cross-reference

This runbook mirrors v1.1 P14 D-16 (`v1.1.0 = retag the rc.3 SHA`) with the substitution:
- `v1.1.0` → `v1.2.0`
- `rc.3` → `last-passing-UAT rc.N` (rc.4 if UAT passed first time; rc.5+ if iterated)
- Tag message `"v1.1 — Operator Quality of Life"` → `"v1.2 — Operator Integration & Insight"`
- `:latest` invariant flipped: `:latest` now MUST equal `:1.2.0` digest (rather than staying at `:1.1.0` as it did during the rc cuts).

The sign-off table mirrors `21-RC2-PREFLIGHT.md` § Sign-off with the `:latest` invariant flipped + added rows for `:1.2` / `:1` four-tag equality verification.
```

NO `src/**` edits. NO Cargo.toml version edits. NO `release.yml` / `cliff.toml` / `docs/release-rc.md` edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "^final_tag: v1.2.0$" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "^autonomous: false$" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && for s in 1 2 3 4 5 6 7 8; do grep -qE "^## $s\..*$" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md || (echo "FAIL: § $s missing"; exit 1); done && grep -qE "^## 1\..*UAT" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -qE "^## 3\..*[Rr]etag" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -qE "^## 4\..*(four-tag|hyphen-gate|:latest)" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -qE "^## 5\..*cargo-deny" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -qE "^## 8\..*gsd-complete-milestone" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "git tag -a -s v1.2.0" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "/gsd-complete-milestone v1.2" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "MUST NOW equal" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && grep -q "Previous \`:latest\` digest (was \`:1.1.0\`)" .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md && test "$(wc -l < .planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-FINAL-SHIP-PREFLIGHT.md)" -ge 120 && echo "PASS" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `24-FINAL-SHIP-PREFLIGHT.md` exists with `final_tag: v1.2.0`, `autonomous: false`.
    - All 8 sections present (§ 1 rc.N UAT passed / § 2 identify rc.N SHA / § 3 retag command / § 4 :latest hyphen-gate + four-tag equality / § 5 cargo-deny ERROR-gate / § 6 git-cliff cumulative / § 7 STATE.md + MILESTONES.md SHIPPED date / § 8 /gsd-complete-milestone v1.2). Verify regex uses `^## $s\..*$` (per checker W4 — relaxes the trailing-anchor fragility) + per-section keyword sub-checks (`UAT`, `[Rr]etag`, `four-tag|hyphen-gate|:latest`, `cargo-deny`, `gsd-complete-milestone`) so the gate fails meaningfully if a section is renamed or content drifts.
    - § 3 retag command uses `git tag -a -s v1.2.0` per docs/release-rc.md Step 2a (signed) with Step 2b fallback.
    - § 4 verifies `:1.2.0` == `:1.2` == `:1` == `:latest` on both amd64 + arm64.
    - § 5 verifies cargo-deny was the ERROR gate (FOUND-16 closed).
    - § 8 references `/gsd-complete-milestone v1.2` as a SEPARATE post-final-tag command (NOT a P24 plan).
    - Sign-off table § "MUST NOW equal `:1.2.0` digest" present (`:latest` invariant flipped vs plan 24-06).
    - Sign-off table § "Previous `:latest` digest (was `:1.1.0`)" present (cross-check).
    - File length ≥ 120 lines.
    - NO `Cargo.toml` / `release.yml` / `cliff.toml` / `docs/release-rc.md` modifications.
  </acceptance_criteria>
  <done>
    `24-FINAL-SHIP-PREFLIGHT.md` ready for maintainer execution. Maintainer retags the rc.N SHA as `v1.2.0`; § 7 flips STATE.md and MILESTONES.md SHIPPED date; § 8 invokes `/gsd-complete-milestone v1.2`.
  </done>
</task>

</tasks>

<maintainer_steps>
After Task 1's commit lands + plan 24-07 UAT is signed off:

1. **Verify rc.N UAT passed** in `24-HUMAN-UAT.md` (all six scenarios signed-off; Final sign-off block filled).
2. **Open `24-FINAL-SHIP-PREFLIGHT.md`.**
3. **Tick § 1** (rc.N UAT passed).
4. **Run § 2** — capture `RC_SHA = $(git rev-list -n 1 <RC_TAG>)`.
5. **Run § 3 retag command** locally with GPG key — `git tag -a -s v1.2.0 -m "v1.2 — Operator Integration & Insight" "$RC_SHA"` + `git push origin v1.2.0`.
6. **Wait 10-15 min** for release.yml multi-arch build.
7. **Run § 4 digest verification** — confirm four-tag equality on amd64 + arm64 + `:latest` advanced.
8. **Run § 5** — verify cargo-deny was the ERROR gate on the v1.2.0 tag's CI run.
9. **Run § 6** — verify `git cliff v1.1.0..v1.2.0` is the release body.
10. **Run § 7** — flip STATE.md milestone status to SHIPPED; finalize MILESTONES.md v1.2 entry SHIPPED date.
11. **Run § 8** — invoke `/gsd-complete-milestone v1.2` from a fresh Claude session (archives v1.2 ROADMAP/REQUIREMENTS; rewrites main ROADMAP; PROJECT.md evolution review; offers v1.3 kickoff).
12. **Fill in the Sign-off table** with maintainer signature, date, all digest values.

Maintainer DOES NOT retag from Claude's session per `feedback_uat_user_validates.md` + Phase 12 D-13 maintainer-key trust anchor.

## Sign-off Table (mirrors runbook § Sign-off)

| Field | Value |
|-------|-------|
| Maintainer signature | (filled at execution time) |
| Date (UTC) | (filled at execution time) |
| Final tag commit SHA (== rc.N SHA) | (filled at execution time) |
| rc.N tag UAT-validated | (filled at execution time — e.g., `v1.2.0-rc.4`) |
| GHCR amd64 digest (`:1.2.0`) | (filled at execution time) |
| GHCR arm64 digest (`:1.2.0`) | (filled at execution time) |
| GHCR `:latest` digest (MUST NOW equal `:1.2.0` digest) | (filled at execution time) |
| GHCR `:1.2` digest (MUST equal `:1.2.0` digest) | (filled at execution time) |
| GHCR `:1` digest (MUST equal `:1.2.0` digest) | (filled at execution time) |
| Previous `:latest` digest (was `:1.1.0`) | (filled at execution time) |
| `/gsd-complete-milestone v1.2` completed | (filled at execution time — y/n) |
</maintainer_steps>

<threat_model>
## Trust Boundaries

No new attack surface — runbook documentation only. Documents existing release pipeline (`release.yml` + `docs/release-rc.md` reused verbatim).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08-DOC | Integrity | Final-tag runbook | accept | Doc-only change. Runbook mirrors v1.1 P14 D-16 retag-the-rc-SHA discipline. Tag signed with maintainer's GPG key per `docs/release-rc.md` Step 2a (Phase 12 D-13 trust anchor). |
| T-24-08-SC | Tampering | npm/pip/cargo installs (supply chain) | mitigate | cargo-deny ERROR-gate verified on the v1.2.0 tag's CI run (§ 5). FOUND-16 fully closed. |
</threat_model>

<verification>
After the task lands + maintainer executes the runbook:
- `v1.2.0` tag pushed and bit-identical to rc.N (last-passing-UAT) image.
- GHCR `:1.2.0` + `:1.2` + `:1` + `:latest` all advance on both archs (four-tag equality).
- `cargo-deny` was the ERROR gate on the v1.2.0 tag's CI run.
- `git cliff v1.1.0..v1.2.0` shipped as the release body.
- STATE.md milestone status SHIPPED.
- MILESTONES.md v1.2 entry SHIPPED date finalized.
- `/gsd-complete-milestone v1.2` invoked separately.
- ROADMAP Phase 24 success criterion #4 fully closed (cargo-deny ERROR-gate + four-tag equality on both archs).
- v1.2 milestone closed; v1.3 kickoff decision is the maintainer's.
</verification>

<success_criteria>
- 24-FINAL-SHIP-PREFLIGHT.md authored by Claude; executed by maintainer.
- v1.2.0 retags last-passing-UAT rc.N SHA (bit-identical image — D-01).
- `:latest` advances to `:1.2.0` on both archs; four-tag equality holds.
- cargo-deny is the ERROR gate on the v1.2.0 tag's CI run.
- Cumulative release body via `git cliff v1.1.0..v1.2.0`.
- STATE.md flipped; MILESTONES.md v1.2 entry SHIPPED date finalized.
- `/gsd-complete-milestone v1.2` invoked as separate command per D-12.
</success_criteria>

<output>
Create `.planning/phases/24-milestone-close-out-final-v1-2-0-ship/24-08-SUMMARY.md` documenting: runbook structure; the retag-the-rc-SHA discipline mirrored from P14 D-16; the four-tag equality verification commands; the `:latest` invariant flip vs plan 24-06; the separation between plan 24-08 and `/gsd-complete-milestone v1.2`.
</output>
</content>
</invoke>