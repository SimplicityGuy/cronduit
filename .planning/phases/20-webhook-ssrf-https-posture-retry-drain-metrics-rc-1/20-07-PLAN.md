---
phase: 20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1
plan: 07
type: execute
wave: 5
depends_on: [20-02, 20-04, 20-05]
files_modified:
  - docs/WEBHOOKS.md
autonomous: true
requirements: [WH-05, WH-07, WH-08, WH-10, WH-11]
tags: [webhooks, docs, operator-doc, threat-model-stub, mermaid]
must_haves:
  truths:
    - "Per D-27: `docs/WEBHOOKS.md` (existing P19 hub) is EXTENDED with 6 new sections in the order specified — retry schedule, Retry-After handling, DLQ table, drain on shutdown, HTTPS/SSRF posture, metrics family. The existing 10 P19 sections remain unchanged; Phase 20 only appends."
    - "Per WH-08 + D-27 §5 + Phase 24 ROADMAP scope: The HTTPS/SSRF section includes a forward-pointer to `THREAT_MODEL.md` Threat Model 5 (Webhook Outbound) with explicit text noting: full TM5 close-out lands in Phase 24; Phase 20 ships the implementation (HTTPS-required validator + DLQ + drain) plus this doc-pointer stub."
    - "Per CLAUDE.md / project memory `feedback_diagrams_mermaid.md`: ALL diagrams in the new sections are mermaid fenced code blocks (triple-backtick + 'mermaid' opener). NO ASCII art. Operators reading the doc see two mermaid diagrams: a 3-attempt retry chain flowchart (§ Retry schedule) and a SIGTERM → drain → drop sequence (§ Drain on shutdown)."
    - "Per D-09: The Retry-After section documents the receiver-side retry-respect contract — receivers wanting more aggressive backoff than the locked schedule should return `5xx + Retry-After` (transient + delay hint, honored within `cap_for_slot` per D-08); receivers explicitly rejecting a request should return `4xx-other` (permanent, dropped at attempt 1 per D-06). This extends the Phase 19 D-12 retry-respect table verbatim into the docs."
    - "Per D-22 + Plan 05 SUMMARY: The Metrics section explicitly documents the breaking change — P18 `_sent_total` and `_failed_total` are removed in v1.2.0-rc.1; operators must update Prometheus dashboards to use `_deliveries_total{job, status}`. The dropped-counter SEMANTIC SPLIT (P15 channel-saturation vs P20 drain-on-shutdown) is documented (D-26)."
    - "Per `<specifics>` + Plan 05 SUMMARY: The DLQ table section includes operator-facing example SQL queries (e.g., `SELECT * FROM webhook_deliveries WHERE last_attempt_at > datetime('now', '-1 hour')`) that operators can paste into `sqlite3` or `psql`."
    - "Per D-08 + RESEARCH §4.7: The Retry-After section includes the cap-math worked example table (slot 0/1/2 → cap 36s/360s/360s) so operators understand the per-slot worst-case wait."
    - "Per D-18 + Pitfall 8: The Drain section documents the worst-case shutdown ceiling = `webhook_drain_grace + 10s` (reqwest's per-attempt cap) and recommends operators tune both `[server].shutdown_grace` and `[server].webhook_drain_grace` in concert."
    - "Per Pitfall 12: The Metrics section notes that job names appear as `{job=...}` label values; operators are advised to keep names ASCII-safe for Prometheus tooling compatibility (cardinality is bounded by configured-job-count, but tooling compatibility is the surface here)."
    - "All operator commands and recipes referenced in the doc are sqlite3/psql/curl one-liners or `just <recipe>` — no ad-hoc cargo/docker invocations (project memory `feedback_uat_use_just_commands.md` extends to user-facing docs even though that memory is UAT-specific; doc consistency)."
  artifacts:
    - path: "docs/WEBHOOKS.md"
      provides: "Operator hub doc extended with 6 Phase 20 sections + 2 mermaid diagrams + TM5 forward-pointer stub"
      contains: "Retry schedule"
      min_lines: 600
  key_links:
    - from: "docs/WEBHOOKS.md § HTTPS / SSRF posture"
      to: "THREAT_MODEL.md TM5 (placeholder for Phase 24)"
      via: "Markdown link `[Threat Model 5 — full close-out in Phase 24](../THREAT_MODEL.md)`"
      pattern: "Threat Model 5"
    - from: "docs/WEBHOOKS.md § Metrics family"
      to: "src/telemetry.rs cronduit_webhook_* describes"
      via: "Operator pastes `curl localhost:8080/metrics | grep cronduit_webhook_` and sees the family"
      pattern: "cronduit_webhook_deliveries_total"
---

<objective>
Extend the operator-facing webhooks doc (`docs/WEBHOOKS.md`) with the 6 new Phase 20 sections so operators can self-serve answers to the questions Phase 20's behavior raises: "What's the retry schedule? How does Retry-After interact with it? What's in the DLQ table? What happens on SIGTERM? Why are some URLs rejected at config-load? What's in the metric stream and how did it change?" Without this doc, operators have to read source code to understand the new posture — that's a poor v1.2.0-rc.1 experience.

Purpose: Lock the operator-facing API contract for the Phase 20 webhook posture. The doc is the single place operators look BEFORE filing a GitHub issue. Phase 20 covers all 5 requirements (WH-05/07/08/10/11) here because operator-doc surface is cross-cutting; the technical implementation lives in the previous plans.
Output: Modified `docs/WEBHOOKS.md` with 6 new sections, 2 mermaid diagrams, operator-actionable example SQL, and a TM5 forward-pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RESEARCH.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-PATTERNS.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-02-SUMMARY.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-04-SUMMARY.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-05-SUMMARY.md
@docs/WEBHOOKS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append 6 new sections to docs/WEBHOOKS.md (with 2 mermaid diagrams + TM5 forward-pointer)</name>
  <files>docs/WEBHOOKS.md</files>
  <read_first>
    - docs/WEBHOOKS.md (whole file — read once; the existing 10 P19 sections set the tone, voice, and section style; new sections must match)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md § decisions D-07, D-08, D-10..D-14, D-15..D-18, D-19..D-21, D-22..D-26, D-27 (the section-by-section content lock)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RESEARCH.md § 4.4 (histogram buckets), § 4.7 (Retry-After cap math worked table), § 6.4 (describe text), § 12 (security domain)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-PATTERNS.md `docs/WEBHOOKS.md` block (D-27 section list verbatim)
  </read_first>
  <action>
    APPEND (do NOT replace) 6 new sections at the end of `docs/WEBHOOKS.md`. Use the same heading style, prose voice, and code-block formatting as the existing P19 sections. ALL diagrams are mermaid (no ASCII art per project memory).

    ---

    **Section 1 — Retry schedule**

    ```markdown
    ## Retry schedule

    Cronduit retries failed webhook deliveries up to 3 times on the locked schedule
    `t = 0, t = 30s, t = 300s`, with each delay multiplied by a uniform full-jitter
    factor in `[0.8, 1.2)`. The schedule is intentionally NOT operator-tunable —
    predictability matters more than per-job customization for v1.2 (deferred to v1.3 if
    operator demand surfaces).

    | Attempt | Base delay | Jittered range | Trigger condition |
    |---------|------------|----------------|-------------------|
    | 1       | 0s         | 0s             | First delivery    |
    | 2       | 30s        | 24s..36s       | Attempt 1 returned a transient error |
    | 3       | 300s       | 240s..360s     | Attempt 2 returned a transient error |

    Total chain wall time without `Retry-After` headers: ≈ 0 + 30s × jitter + 300s ×
    jitter + 10s (final attempt timeout) ≈ **6.7 minutes** worst case.

    ### Classification table

    | HTTP / outcome | Classification | Behavior |
    |---|---|---|
    | 200..=299 | success | counter `_deliveries_total{status="success"}` increments; no retry; no DLQ row |
    | 408 (Request Timeout) | transient | retry per schedule (treated as 5xx-class for retry purposes) |
    | 429 (Too Many Requests) | transient | retry per schedule; honors `Retry-After` (see next section) |
    | 4xx (other) | permanent | DLQ row `dlq_reason='http_4xx'`, `attempts=1`, no retry |
    | 5xx | transient | retry per schedule; honors `Retry-After` |
    | reqwest network error | transient | retry per schedule; DLQ `dlq_reason='network'` if exhausted |
    | reqwest timeout (10s per attempt — see "Drain on shutdown") | transient | retry per schedule; DLQ `dlq_reason='timeout'` if exhausted |

    ### Diagram

    ```mermaid
    flowchart TD
        START["Event arrives"] --> A1["Attempt 1<br/>(t=0)"]
        A1 -->|2xx| OK["counter:<br/>status=success<br/>no DLQ"]
        A1 -->|4xx other| PERM1["DLQ row<br/>dlq_reason=http_4xx<br/>attempts=1"]
        PERM1 --> FAIL["counter:<br/>status=failed"]
        A1 -->|408 / 429 / 5xx /<br/>network / timeout| S1{{"sleep<br/>30s × jitter<br/>(or Retry-After capped)"}}
        S1 -->|cancel fired| DRAIN1["DLQ row<br/>dlq_reason=shutdown_drain<br/>attempts=1"]
        DRAIN1 --> FAIL
        S1 -->|sleep done| A2["Attempt 2<br/>(t=30s)"]
        A2 -->|2xx| OK
        A2 -->|408 / 429 / 5xx /<br/>network / timeout| S2{{"sleep<br/>300s × jitter<br/>(or Retry-After capped)"}}
        S2 -->|cancel fired| DRAIN2["DLQ row<br/>dlq_reason=shutdown_drain<br/>attempts=2"]
        DRAIN2 --> FAIL
        S2 -->|sleep done| A3["Attempt 3<br/>(t=300s)"]
        A3 -->|2xx| OK
        A3 -->|any non-2xx| EXHAUST["DLQ row<br/>matches last classification<br/>attempts=3"]
        EXHAUST --> FAIL

        classDef success fill:#0a3d0a,stroke:#00ff7f,stroke-width:2px,color:#e0ffe0
        classDef failed fill:#3d0a0a,stroke:#ff7f7f,stroke-width:2px,color:#ffe0e0
        classDef sleep fill:#1a1a3d,stroke:#7fbfff,stroke-width:1px,color:#e0e0ff
        class OK success
        class FAIL,PERM1,EXHAUST,DRAIN1,DRAIN2 failed
        class S1,S2 sleep
    ```
    ```

    ---

    **Section 2 — Retry-After header handling**

    ```markdown
    ## Retry-After header handling

    Cronduit honors the `Retry-After` HTTP header on `429` and `5xx` responses
    (RFC 7231 §7.1.3). Only the **integer-seconds** form is supported in v1.2;
    the HTTP-date form is RFC-compliant but not implemented (logs a WARN and
    falls back to the schedule). HTTP-date support is a v1.3 candidate if a real
    receiver demands it.

    ### Cap math

    `Retry-After` is honored within a per-slot cap so a misbehaving receiver cannot
    blow past Phase 20's predictable schedule:

    ```
    delay = max(locked_schedule[next_attempt], retry_after_seconds)
    delay = min(delay, schedule[next_attempt + 1] * 1.2)   # cap
    # last attempt: cap = schedule[next_attempt] * 1.2
    ```

    Worked table (locked schedule `[0, 30s, 300s]`):

    | Pre-attempt slot | Slot delay | `Retry-After` floor | `Retry-After` cap | Worst-case wait |
    |------------------|------------|---------------------|-------------------|-----------------|
    | Before attempt 2 | 30s × jitter | 30s | `300s × 1.2 = 360s` | 360s |
    | Before attempt 3 | 300s × jitter | 300s | `300s × 1.2 = 360s` (last-slot fallback) | 360s |

    Total chain worst-case under maximally-aggressive `Retry-After`:
    `360s + 360s + 10s ≈ 12 minutes`.

    ### Receiver guidance

    - Want more aggressive backoff than the schedule? Return **5xx + `Retry-After`** (transient + delay hint).
    - Want to refuse a request permanently? Return **400-or-equivalent 4xx** (cronduit drops at attempt 1, no retry).
    - HTTP-date `Retry-After`? Cronduit logs WARN and falls back to schedule.
    ```

    ---

    **Section 3 — DLQ table (`webhook_deliveries`)**

    ```markdown
    ## Dead-letter table (`webhook_deliveries`)

    Every webhook delivery that fails to reach 2xx is recorded in the
    `webhook_deliveries` table — one row per failed delivery (regardless of
    attempt count). First-attempt successes write NO row.

    ### Schema

    ```sql
    CREATE TABLE webhook_deliveries (
        id               -- internal autoincrement
        run_id           -- FK → job_runs(id)
        job_id           -- FK → jobs(id)
        url              -- as-configured (after env-var interp)
        attempts         -- 1..=3
        last_status      -- HTTP status if any (NULL on network/timeout/shutdown)
        last_error       -- truncated reqwest error (≤500 chars)
        dlq_reason       -- closed enum: http_4xx | http_5xx | network | timeout | shutdown_drain
        first_attempt_at -- RFC3339
        last_attempt_at  -- RFC3339
    );
    CREATE INDEX idx_webhook_deliveries_last_attempt ON webhook_deliveries (last_attempt_at);
    ```

    **No payload, no headers, no signature is stored.** v1.2 prioritizes secret/PII
    hygiene over replay; receivers can re-derive the payload from `run_id` if needed.

    ### `dlq_reason` values

    | Value | When written |
    |-------|--------------|
    | `http_4xx` | Receiver returned a 4xx other than 408/429 — permanent rejection, attempt 1 only |
    | `http_5xx` | Receiver returned 5xx for all 3 attempts (or 408/429 — treated as transient) |
    | `network` | reqwest transport error for all 3 attempts (connection refused, DNS failure, etc.) |
    | `timeout` | reqwest timeout (10s per attempt, P18) for all 3 attempts |
    | `shutdown_drain` | SIGTERM mid-chain — see "Drain on shutdown" |

    ### Operator queries

    What failed in the last hour?
    ```sql
    SELECT * FROM webhook_deliveries
      WHERE last_attempt_at > datetime('now', '-1 hour')
      ORDER BY last_attempt_at DESC;
    ```
    (Postgres: replace `datetime('now', '-1 hour')` with `now() - interval '1 hour'`.)

    Which jobs are losing the most deliveries this week?
    ```sql
    SELECT job_id, dlq_reason, COUNT(*)
      FROM webhook_deliveries
      WHERE last_attempt_at > datetime('now', '-7 days')
      GROUP BY job_id, dlq_reason
      ORDER BY 3 DESC;
    ```

    Which deliveries were lost specifically to SIGTERM?
    ```sql
    SELECT * FROM webhook_deliveries
      WHERE dlq_reason = 'shutdown_drain'
      ORDER BY last_attempt_at DESC;
    ```

    ### Retention

    Rows are pruned daily by the existing retention pruner using the same
    `[server].log_retention` knob (default `90d`) — same cadence as `job_logs`
    and `job_runs`. No separate config field.
    ```

    ---

    **Section 4 — Drain on shutdown**

    ```markdown
    ## Drain on shutdown

    On SIGTERM, the webhook worker enters drain mode and continues delivering
    queued events for up to `[server].webhook_drain_grace` (default `30s`).
    In-flight HTTP requests run to completion — they are NOT cancelled mid-flight.

    ### Worst-case ceiling

    `webhook_drain_grace + 10s`. The 10s is reqwest's per-attempt timeout (P18 D-18,
    not operator-tunable in v1.2). For the default `webhook_drain_grace = "30s"`,
    that's a 40-second worst case.

    Operators with strict shutdown budgets should tune both knobs:

    ```toml
    [server]
    shutdown_grace       = "20s"   # scheduler waits for running jobs
    webhook_drain_grace  = "10s"   # worker drains webhooks
    # → worst-case shutdown = 10s + 10s = 20s
    ```

    The two budgets are sequential — `shutdown_grace` covers in-flight job execution
    (the scheduler's wait loop); `webhook_drain_grace` covers webhook queue drain
    AFTER the scheduler finishes (no overlap, D-17).

    ### Diagram

    ```mermaid
    sequenceDiagram
        participant OS as OS / operator
        participant SCH as Scheduler
        participant WK as Webhook worker
        participant DISP as RetryingDispatcher
        participant DB as DLQ table

        OS->>SCH: SIGTERM
        SCH->>WK: cancel.cancelled() fires
        Note over WK: drain_deadline = now + 30s
        WK->>DISP: deliver(event 1) [in flight]
        Note over DISP: HTTP attempt running<br/>(uncancelable, 10s cap)
        DISP-->>WK: Ok or Err
        WK->>DISP: deliver(event 2)
        Note over DISP: needs to sleep 30s before retry
        DISP->>DISP: tokio::select! sleep vs cancel
        DISP->>DB: INSERT webhook_deliveries<br/>dlq_reason=shutdown_drain
        DISP-->>WK: Err(shutdown drain)

        Note over WK: drain budget elapsed (t=30s)
        WK->>WK: while rx.try_recv() {<br/>counter +1 status=dropped<br/>}
        WK-->>SCH: worker exits
        SCH-->>OS: process exits
    ```

    ### What gets dropped vs DLQ-recorded

    - **Mid-chain (already pulled from queue, in retry loop):** Cancel-aware sleep
      bails out → DLQ row with `dlq_reason='shutdown_drain'`, `attempts=N` (actual
      count). The metric `_deliveries_total{status="failed"}` increments.
    - **Still queued (not yet pulled at deadline):** Drained-and-dropped via
      `rx.try_recv()` loop; `_deliveries_total{status="dropped"}` increments per
      event; **no DLQ row** (post-channel only).
    - **In-flight HTTP:** Allowed to outlast the budget by up to 10s (reqwest cap).
    ```

    ---

    **Section 5 — HTTPS / SSRF posture**

    ```markdown
    ## HTTPS / SSRF posture

    Cronduit gates outbound webhook URLs at config-load time. `https://` is required
    for non-loopback / non-RFC1918 destinations; `http://` is permitted ONLY for the
    locked allowlist below.

    ### Allowed `http://` destinations

    | Range | Notes |
    |-------|-------|
    | `127.0.0.0/8` | IPv4 loopback |
    | `::1` | IPv6 loopback |
    | `10.0.0.0/8` | RFC 1918 private |
    | `172.16.0.0/12` | RFC 1918 private |
    | `192.168.0.0/16` | RFC 1918 private |
    | `fd00::/8` | IPv6 ULA (cronduit accepts the broader RFC 4193 `fc00::/7` range via the stdlib `Ipv6Addr::is_unique_local()` helper) |
    | `localhost` (literal hostname) | Special-cased; case-insensitive |

    Anything else over `http://` is rejected at LOAD time:

    ```
    [[jobs]] `backup-nightly`: webhook.url `http://example.com` requires HTTPS for
    non-loopback / non-RFC1918 destinations. Use `https://` or one of the allowed
    local nets: 127/8, ::1, 10/8, 172.16/12, 192.168/16, fd00::/8.
    ```

    HTTP-allowed URLs emit a startup INFO log naming the URL + classified-net
    (`localhost`, `loopback`, `RFC1918`, `ULA`) so operators can confirm the
    validator's decision matches their intent.

    ### Accepted residual risk: hostname → public IP

    Cronduit does **NOT** resolve DNS at config-load. A hostname that is not the
    literal string `localhost` is rejected for HTTP, even if it resolves to a
    private IP via `/etc/hosts` or local DNS. Conversely, `localhost` (the literal
    hostname) is accepted regardless of how it resolves on the host. This is an
    accepted residual SSRF risk per **WH-08**; a full SSRF allow/block-list filter
    is deferred to v1.3.

    ### Threat Model

    Phase 20 ships the operational mitigations above. The full threat model
    discussion (including the operator-with-UI-access widening of the SSRF blast
    radius, the loopback-bound default mitigation, and the reverse-proxy/auth
    deployment posture) lands in
    **[Threat Model 5 — Webhook Outbound, full close-out in Phase 24](../THREAT_MODEL.md)**.
    Phase 20 is intentionally a forward-pointer; the canonical TM5 entry is part of
    the v1.2.0 milestone close-out.
    ```

    ---

    **Section 6 — Metrics family**

    ```markdown
    ## Metrics family (`cronduit_webhook_*`)

    Phase 20 introduces a labeled counter family + histogram + gauge, replacing
    the v1.2.0-rc.0 (P18) flat counters. The metrics are eagerly described and
    zero-baselined at boot, so Prometheus alerts work from the first scrape.

    ### Surface

    | Metric | Type | Labels | Notes |
    |--------|------|--------|-------|
    | `cronduit_webhook_deliveries_total` | counter | `{job, status}` | `status ∈ {success, failed, dropped}` (closed enum) |
    | `cronduit_webhook_delivery_duration_seconds` | histogram | `{job}` | Per-attempt HTTP duration; buckets `[0.05, 0.1, 0.25, 0.5, 1.0, 2.5, 5.0, 10.0]` |
    | `cronduit_webhook_queue_depth` | gauge | none | Sampled at `rx.recv()` boundary; approximate under contention |
    | `cronduit_webhook_delivery_dropped_total` | counter | none | **PRESERVED from v1.1 (P15)** — channel-saturation drops only (NOT drain drops) |

    ### Breaking change in v1.2.0-rc.1

    The P18 (v1.2.0-rc.0) flat counters are **REMOVED**:

    | Old (P18) | New (P20) |
    |-----------|-----------|
    | `cronduit_webhook_delivery_sent_total` | `cronduit_webhook_deliveries_total{status="success"}` |
    | `cronduit_webhook_delivery_failed_total` | `cronduit_webhook_deliveries_total{status="failed"}` |

    Operators with v1.2.0-rc.0 dashboards must update queries before upgrading.
    Sample migration:

    ```promql
    # Before (rc.0):
    rate(cronduit_webhook_delivery_failed_total[5m])

    # After (rc.1):
    sum(rate(cronduit_webhook_deliveries_total{status="failed"}[5m])) by (job)
    ```

    ### Dropped-counter semantic split

    There are two `dropped`-flavored counters; they measure DIFFERENT things:

    | Counter | What it measures | When it fires |
    |---------|------------------|---------------|
    | `cronduit_webhook_delivery_dropped_total` (UNLABELED, P15) | Channel-saturation drops | Scheduler's `try_send` failed because the bounded mpsc(1024) was full; the dispatcher cannot keep up |
    | `cronduit_webhook_deliveries_total{status="dropped"}` (LABELED, P20) | Drain-on-shutdown drops | SIGTERM fired; `webhook_drain_grace` elapsed; remaining queued events dropped |

    Both are normal-operation signals — set alerts on each independently:
    - `_delivery_dropped_total > 0` for sustained periods → dispatcher CANNOT keep up; investigate receiver health or increase `mpsc` capacity (v1.3 candidate).
    - `_deliveries_total{status="dropped"} > 0` after a deploy → SHUTDOWN-time loss; investigate `webhook_drain_grace` or shutdown ordering.

    ### Histogram buckets

    The duration histogram covers 50ms to 10s — chosen to match a healthy receiver
    at the bottom and reqwest's per-attempt timeout (P18 D-18) at the top:

    ```promql
    histogram_quantile(0.95,
      sum(rate(cronduit_webhook_delivery_duration_seconds_bucket[5m])) by (le, job)
    )
    ```

    ### Job names as label values

    Cronduit allows arbitrary UTF-8 in `[[jobs]].name` (no validator regex). Job
    names appear as `{job=...}` label values; for Prometheus tooling
    compatibility, operators should keep names ASCII-safe. Cardinality is bounded
    by configured-job-count × 3 (status enum), so the metric scales linearly with
    fleet size.
    ```

    ---

    Do NOT add ASCII art. Do NOT modify the existing P19 sections. Do NOT add a "Roadmap" or "Future" section that implies v1.3 work is committed (deferred items already documented in `<deferred>` of CONTEXT.md).
  </action>
  <verify>
    <automated>test -f docs/WEBHOOKS.md && wc -l docs/WEBHOOKS.md; grep -c '^## ' docs/WEBHOOKS.md; grep -c '```mermaid' docs/WEBHOOKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/WEBHOOKS.md` exists and is ≥ 600 lines (the existing P19 hub plus 6 new sections).
    - `grep -c '^## Retry schedule' docs/WEBHOOKS.md` returns 1.
    - `grep -c '^## Retry-After header handling' docs/WEBHOOKS.md` returns 1.
    - `grep -c '^## Dead-letter table' docs/WEBHOOKS.md` returns 1.
    - `grep -c '^## Drain on shutdown' docs/WEBHOOKS.md` returns 1.
    - `grep -c '^## HTTPS / SSRF posture' docs/WEBHOOKS.md` returns 1.
    - `grep -c '^## Metrics family' docs/WEBHOOKS.md` returns 1.
    - `grep -c '```mermaid' docs/WEBHOOKS.md` returns ≥ 2 (retry chain flowchart + drain sequence).
    - `grep -c 'Threat Model 5' docs/WEBHOOKS.md` returns ≥ 1 (TM5 forward-pointer for WH-08 doc-stub per security_constraint).
    - `grep -c 'webhook_drain_grace + 10s' docs/WEBHOOKS.md` returns ≥ 1 (worst-case ceiling per Pitfall 8).
    - `grep -c 'cronduit_webhook_delivery_sent_total' docs/WEBHOOKS.md` returns ≥ 1 (the breaking-change migration note must mention the old counter).
    - `grep -c 'cronduit_webhook_deliveries_total' docs/WEBHOOKS.md` returns ≥ 3 (multiple sections).
    - `grep -c 'fd00::/8' docs/WEBHOOKS.md` returns ≥ 1 (HTTPS allowlist matches D-19/D-21).
    - No ASCII-art diagrams (no lines starting with `+--` or `|`-only-art patterns inside the new sections — manual visual check; mermaid blocks only).
  </acceptance_criteria>
  <done>docs/WEBHOOKS.md has 6 new sections, 2 mermaid diagrams, TM5 forward-pointer, breaking-change migration prose, operator SQL examples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ↔ documentation | This doc is the operator-facing source of truth for webhook posture; any drift between code and doc is an operator-experience defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 (doc surface) | Information Disclosure | Operator misconfigures HTTP webhook to public destination | mitigate | The HTTPS/SSRF section documents the allowlist + the LOAD-time validator + the accepted-residual-risk for hostnames; operators see the rejection error and the doc explains why before they file an issue. The TM5 forward-pointer makes the "full close-out is Phase 24" expectation explicit. |
| T-20-04 (operator visibility) | Reliability | Operators don't know about the worst-case shutdown ceiling | mitigate | The Drain section documents `drain_grace + 10s` worst case + the recommendation to tune both `[server].shutdown_grace` and `webhook_drain_grace` together. The mermaid sequence diagram shows the in-flight HTTP outlasting the budget. |
| T-20-05 (operator dashboard) | Reliability | Operators upgrading from rc.0 see metric stream change | mitigate | The Metrics section documents the breaking change + provides PromQL migration snippets. The dropped-counter semantic split is explicitly tabulated so operators set the right alert. |
</threat_model>

<verification>
- `docs/WEBHOOKS.md` exists with all 6 new sections present.
- All diagrams in the new sections are mermaid (zero ASCII-art).
- `grep -c '\.\./THREAT_MODEL.md' docs/WEBHOOKS.md` returns ≥ 1 OR a relative path to THREAT_MODEL.md that resolves correctly (the link is for operator navigation; broken-link check is informational).
- The breaking-change migration note appears (section 6).
- The doc reads coherently after the existing 10 P19 sections (manual review).
</verification>

<success_criteria>
- [ ] 6 new sections appended to `docs/WEBHOOKS.md` per D-27 ordering.
- [ ] 2 mermaid diagrams (retry chain + drain sequence).
- [ ] TM5 forward-pointer with explicit "full close-out in Phase 24" wording.
- [ ] DLQ table section includes operator SQL examples (sqlite + postgres syntax).
- [ ] Retry-After cap-math worked table.
- [ ] Worst-case shutdown ceiling = `drain_grace + 10s` documented.
- [ ] Metrics breaking-change migration prose with PromQL before/after snippets.
- [ ] Dropped-counter semantic split (P15 vs P20) tabulated.
- [ ] No ASCII-art diagrams (project memory enforced).
</success_criteria>

<output>
After completion, create `.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-07-SUMMARY.md` documenting:
- Final line count of docs/WEBHOOKS.md
- The 6 new section headings + their line ranges
- Confirmation that 2+ mermaid blocks were added (and zero ASCII-art)
- The TM5 forward-pointer text used (verbatim) — Phase 24 reads this in advance
</output>
