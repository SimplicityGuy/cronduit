---
phase: 20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1
plan: 09
type: execute
wave: 7
depends_on: [20-01, 20-02, 20-03, 20-04, 20-05, 20-06, 20-07, 20-08]
files_modified:
  - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md
autonomous: false
requirements: []
tags: [release-engineering, rc-cut, preflight, maintainer-only, no-release-yml-edits]
must_haves:
  truths:
    - "Per D-28 + D-30 + Phase 12 P12 D-10 + Phase 12.1 GHCR hygiene: Phase 20 does NOT modify `release.yml`, `cliff.toml`, or `docs/release-rc.md`. The v1.1.0-rc.1 → v1.2.0-rc.1 path is identical (same runbook). The `:latest` GHCR tag stays at `v1.1.0` because `release.yml:134` reads `enable=${{ !contains(github.ref, '-') }}` and `v1.2.0-rc.1` contains a hyphen."
    - "Per D-29 + D-36: The rc.1 tag command is `git tag -a -s v1.2.0-rc.1 -m \"v1.2.0-rc.1 — webhook block (P15..P20)\"`. Cargo.toml is at `1.2.0` (P15 set it); the `-rc.1` suffix is tag-only per project memory `feedback_tag_release_version_match.md`."
    - "Per D-13 + Phase 12 release-rc.md precedent: The maintainer runs the tag command from a LOCAL terminal — not via CI workflow_dispatch. This plan ships a pre-flight CHECKLIST artifact (`20-RC1-PREFLIGHT.md`); the actual `git tag` invocation happens during the maintainer's runbook execution, NOT during Plan 09 task execution."
    - "Per Pitfall 9 + RESEARCH §8: Pre-flight verification confirms `release.yml:134` still reads `enable=${{ !contains(github.ref, '-') }}` and `release.yml:135` still reads `enable=${{ contains(github.ref, '-rc.') }}` — no edits required, just visual confirmation. A `git cliff --unreleased --tag v1.2.0-rc.1 | head -50` dry run shows P15..P20 commits."
    - "Per `<canonical_refs>` Phase 12 D-12 (git-cliff authoritative): GitHub Release notes will be auto-generated from conventional-commit messages by git-cliff. Phase 20 does NOT hand-edit the release body post-publish (D-31)."
    - "Per D-32 + project memory `feedback_no_direct_main_commits.md`: The rc.1 tag is cut on `main` AFTER all Phase 20 plans are merged via PR. Plan 09 itself merges via PR; the maintainer cuts the tag from `main` post-merge."
    - "Per Plan 08 prerequisite: `20-HUMAN-UAT.md` must show all maintainer checkboxes ticked BEFORE Plan 09's pre-flight checklist gates the tag cut."
    - "Per W5 split: Plan 09 has 2 tasks. Task 1 (autonomous) authors `20-RC1-PREFLIGHT.md`. Task 2 (checkpoint:human-action) is the maintainer cutting the tag locally + verifying GHCR publish + filling the sign-off block. Each task has a single, focused done-criterion."
    - "Per D-38: `cargo tree -i openssl-sys` returns empty across native + linux/amd64-musl + linux/arm64-musl targets (the rustls invariant is a hard gate)."
  artifacts:
    - path: ".planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md"
      provides: "Maintainer pre-flight checklist for the v1.2.0-rc.1 tag cut"
      contains: "v1.2.0-rc.1"
  key_links:
    - from: "20-RC1-PREFLIGHT.md § Tag command"
      to: "docs/release-rc.md (existing v1.1 runbook — REUSED VERBATIM)"
      via: "Markdown link `See [docs/release-rc.md](../../../docs/release-rc.md)`"
      pattern: "docs/release-rc\\.md"
    - from: "20-RC1-PREFLIGHT.md § GHCR `:latest` gate"
      to: ".github/workflows/release.yml:134 (P12 D-10 hyphen-gating)"
      via: "Visual-check assertion line"
      pattern: "release\\.yml"
---

<objective>
Author the rc.1 pre-flight checklist artifact that gates the v1.2.0-rc.1 tag cut. This plan does NOT cut the tag — Task 1's job is to PRODUCE the checklist; Task 2 is the maintainer running through it locally and cutting the tag from their terminal per `docs/release-rc.md` (D-13/D-29). Phase 20 does not modify any release-engineering files (release.yml, cliff.toml, release-rc.md per D-30); the entire rc.1 cut path is REUSED VERBATIM from v1.1.0-rc.1.

Purpose: Lock Success Criterion 5: "An operator pushing the `v1.2.0-rc.1` tag sees the GHCR image published at `ghcr.io/SimplicityGuy/cronduit:v1.2.0-rc.1` on both amd64 and arm64; the `:latest` tag still points at `v1.1.0` (rc tag gating from v1.1's D-10 holds — T-20-06 mitigation verified)."
Output: `20-RC1-PREFLIGHT.md` (Task 1 — Claude-authored, autonomous) + signed-off preflight after the maintainer cuts the tag (Task 2 — checkpoint:human-action). Two distinct tasks per W5 split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RESEARCH.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-08-SUMMARY.md
@docs/release-rc.md
@.github/workflows/release.yml
@cliff.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 20-RC1-PREFLIGHT.md with all 8 sections per D-13/D-29</name>
  <files>.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md</files>
  <read_first>
    - docs/release-rc.md (the v1.1 runbook — REUSED VERBATIM per D-28/D-30; the preflight references this file as the source of truth)
    - .github/workflows/release.yml (visual-confirm lines 134-135 still gate `:latest` and `:rc` correctly per P12 D-10)
    - cliff.toml (no edits expected; visual confirmation that section ordering still produces the right release-notes shape)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-CONTEXT.md § decisions D-13, D-28, D-29, D-30, D-31, D-32, D-36
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RESEARCH.md § 8 (Pitfall 9 — visual gate-logic confirmation)
  </read_first>
  <action>
    Author `.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md` with the structure below. The file is a maintainer-validated checklist; Claude authors it, the maintainer runs through it.

    Required structure (use the Write tool to create the file):

    ```markdown
    # Phase 20 — v1.2.0-rc.1 Pre-Flight Checklist

    **Phase:** 20 — Webhook SSRF/HTTPS Posture + Retry/Drain + Metrics — rc.1
    **Tag target:** `v1.2.0-rc.1`
    **Runbook:** [docs/release-rc.md](../../../docs/release-rc.md) — REUSED VERBATIM from v1.1; Phase 20 makes no edits (D-28 / D-30).
    **Cargo.toml version:** `1.2.0` (set by Phase 15; the `-rc.1` is tag-only per `feedback_tag_release_version_match.md`).

    > This artifact is a maintainer-validated checklist. Claude authored it; the maintainer runs through it and cuts the tag locally.

    ## 1. Phase 20 plans merged on `main`

    - [ ] PR for Plan 01 (DLQ migration + helpers + Wave 0 stubs) merged.
    - [ ] PR for Plan 02 (RetryingDispatcher + classification + DLQ writes) merged.
    - [ ] PR for Plan 03 (HTTPS-required validator) merged.
    - [ ] PR for Plan 04 (worker drain budget + queue_depth gauge) merged.
    - [ ] PR for Plan 05 (metrics family migration) merged.
    - [ ] PR for Plan 06 (config field + CLI wiring) merged.
    - [ ] PR for Plan 07 (docs/WEBHOOKS.md extension) merged.
    - [ ] PR for Plan 08 (UAT recipes + 20-HUMAN-UAT.md) merged.

    Verification:
    ```bash
    gh pr list --state merged --search "phase-20" --limit 30
    git log --oneline main | head -20
    ```

    ## 2. CI matrix green on `main` for the merge commit

    - [ ] `linux/amd64 × SQLite` lint+test green
    - [ ] `linux/amd64 × Postgres` lint+test green
    - [ ] `linux/arm64 × SQLite` lint+test green (cargo-zigbuild)
    - [ ] `linux/arm64 × Postgres` lint+test green
    - [ ] webhook-interop matrix (Python/Go/Node from P19) green
    - [ ] cargo-deny CI job green (non-blocking on rc.1 per D-15 of P15; promotion to blocking is Phase 24)
    - [ ] compose-smoke job green (the existing v1.1 healthcheck path still works)

    Verification:
    ```bash
    gh run list --branch main --limit 5
    gh run view <RUN_ID>   # confirm all matrix legs green
    ```

    ## 3. rustls invariant intact

    - [ ] `cargo tree -i openssl-sys` returns empty.

    Verification:
    ```bash
    cargo tree -i openssl-sys
    # EXPECT: error: package ID specification `openssl-sys` did not match any packages
    ```

    ## 4. release.yml `:latest` gate logic intact (D-30 — no edits; T-20-06 visual gate)

    - [ ] `.github/workflows/release.yml:134` still reads `enable=${{ !contains(github.ref, '-') }}` (the `:latest` skip-on-hyphen gate from P12 D-10 — this gate is what prevents `v1.2.0-rc.1` from accidentally promoting `:latest`).
    - [ ] `.github/workflows/release.yml:135` still reads `enable=${{ contains(github.ref, '-rc.') }}` (the `:rc` rolling tag gate).
    - [ ] No commits in the rc.1 PR set touch `release.yml`, `cliff.toml`, or `docs/release-rc.md` (D-30).

    Verification:
    ```bash
    grep -n "contains(github.ref" .github/workflows/release.yml
    git log --oneline --name-only main..HEAD~10 | grep -E '(release\.yml|cliff\.toml|release-rc\.md)' || echo "OK: no edits to release engineering files"
    ```

    ## 5. git-cliff release-notes preview shows P15..P20 commits

    - [ ] `git cliff --unreleased --tag v1.2.0-rc.1 | head -100` shows commits from Phases 15, 16, 17, 18, 19, 20.
    - [ ] No commit appears in the wrong section (e.g., a feat commit landing under "fixes"). If any commit is mis-categorized, hotfix the conventional-commit prefix on `main` BEFORE tagging (precedent: v1.1 P12 discipline).

    Verification:
    ```bash
    git cliff --unreleased --tag v1.2.0-rc.1 | head -100
    ```

    ## 6. 20-HUMAN-UAT.md sign-off

    - [ ] All maintainer checkboxes in `20-HUMAN-UAT.md` are ticked.
    - [ ] The Sign-off block at the bottom of `20-HUMAN-UAT.md` is filled in (maintainer name + date).

    ## 7. Tag command (maintainer runs LOCALLY)

    On a clean checkout of `main` at the merge commit:

    ```bash
    git checkout main
    git pull origin main
    git tag -a -s v1.2.0-rc.1 -m "v1.2.0-rc.1 — webhook block (P15..P20)"
    git push origin v1.2.0-rc.1
    ```

    > Per D-13 + D-29: this is a maintainer-local action. Claude does NOT execute it. The `-s` flag requires the maintainer's GPG signing key.

    ## 8. Post-publish verification (T-20-06 detection gate)

    After `release.yml` finishes (≈ 10-20 minutes for both archs):

    - [ ] `gh release view v1.2.0-rc.1` shows the auto-generated git-cliff body (D-31 — do NOT hand-edit post-publish).
    - [ ] `docker manifest inspect ghcr.io/SimplicityGuy/cronduit:v1.2.0-rc.1` returns 2 manifests (amd64 + arm64).
    - [ ] **T-20-06 detection:** capture the digest of `ghcr.io/SimplicityGuy/cronduit:latest` AND the digest of `ghcr.io/SimplicityGuy/cronduit:1.1.0`; the two digests MUST be identical. The hyphen-gate at `release.yml:134` held; the rc.1 tag did NOT promote `:latest`.
    - [ ] `docker manifest inspect ghcr.io/SimplicityGuy/cronduit:rc` digest matches `:v1.2.0-rc.1` (the `-rc.` rolling tag updates).

    Verification:
    ```bash
    gh release view v1.2.0-rc.1
    docker manifest inspect ghcr.io/SimplicityGuy/cronduit:v1.2.0-rc.1
    LATEST_DIGEST=$(docker manifest inspect ghcr.io/SimplicityGuy/cronduit:latest | sha256sum | awk '{print $1}')
    V1_1_0_DIGEST=$(docker manifest inspect ghcr.io/SimplicityGuy/cronduit:1.1.0 | sha256sum | awk '{print $1}')
    [[ "$LATEST_DIGEST" == "$V1_1_0_DIGEST" ]] && echo "OK: T-20-06 mitigation verified" || echo "FAIL: :latest was promoted to rc.1"
    docker manifest inspect ghcr.io/SimplicityGuy/cronduit:rc | grep -A2 digest
    ```

    ## Sign-off

    - [ ] All sections above ticked.
    - [ ] Tag pushed: `v1.2.0-rc.1`.
    - [ ] `:latest` STILL at v1.1.0 (verified post-publish — T-20-06 mitigation held).
    - [ ] No regressions reported in the first 24h post-publish.

    Maintainer: ____________________ Date: ____________________
    Tag commit SHA: ____________________
    GHCR amd64 digest: ____________________
    GHCR arm64 digest: ____________________
    GHCR :latest digest (must equal v1.1.0 digest): ____________________
    GHCR :1.1.0 digest (for comparison): ____________________
    ```

    Use the Write tool to create the file. After writing, verify the file exists and contains the literal `v1.2.0-rc.1` string.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md &amp;&amp; grep -c 'v1\.2\.0-rc\.1' .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md` exists.
    - File contains 8 numbered sections (`## 1.` through `## 8.`) plus the `## Sign-off` block.
    - File contains the literal `git tag -a -s v1.2.0-rc.1` command.
    - File contains the literal `release.yml:134` and `release.yml:135` references for T-20-06 visual checks.
    - Section 8 contains an explicit `LATEST_DIGEST == V1_1_0_DIGEST` comparison step (T-20-06 detection gate).
    - File contains a Sign-off block with placeholders for: Maintainer name, Date, Tag commit SHA, amd64 digest, arm64 digest, :latest digest, :1.1.0 digest.
  </acceptance_criteria>
  <done>20-RC1-PREFLIGHT.md authored with all 8 sections per D-13/D-29 and the explicit T-20-06 detection step in Section 8.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Maintainer cuts v1.2.0-rc.1 tag locally + verifies GHCR publish + signs off</name>
  <files>(no files modified by Claude in this task — checkpoint for human action)</files>
  <what-built>
    Task 1 produced `20-RC1-PREFLIGHT.md`. The maintainer:
      1. Reviews the checklist for accuracy.
      2. Runs each verification step locally (gh CLI, cargo, git cliff, etc.) and ticks boxes in Sections 1-6.
      3. Confirms `20-HUMAN-UAT.md` (Plan 08) is signed off (Section 6).
      4. Cuts the tag locally with the documented `git tag -a -s v1.2.0-rc.1 ...` command (Section 7 — manual maintainer action because the maintainer holds the GPG signing key + per project memory `feedback_uat_user_validates.md` extending to release-tag actions per D-13/D-29).
      5. Pushes the tag and monitors `release.yml` to GHCR publish on both archs.
      6. Runs Section 8's T-20-06 detection step — confirms `:latest` digest equals `:1.1.0` digest.
      7. Fills in the Sign-off block (name, date, tag SHA, both arch digests, both :latest and :1.1.0 digests).

    Why this is `checkpoint:human-action` (not `human-verify`): cutting a signed git tag requires the maintainer's GPG key + their personal identity; Claude cannot automate it.
  </what-built>
  <how-to-verify>
    1. **Sections 1-6 (pre-tag)** — maintainer ticks every checkbox; any failure aborts the cut and triggers a hotfix PR (per D-30 — runbook gaps fold into a hotfix PR before tagging; mirrors v1.1 P12 discipline).
    2. **Section 7 (tag cut)** — maintainer runs `git tag -a -s v1.2.0-rc.1 -m "v1.2.0-rc.1 — webhook block (P15..P20)"` and `git push origin v1.2.0-rc.1` from their local terminal with their GPG key.
    3. **Section 8 (post-publish T-20-06 detection)** — maintainer waits ~10-20 min for `release.yml` to finish, then runs the digest-comparison shell snippet:
       ```bash
       LATEST_DIGEST=$(docker manifest inspect ghcr.io/SimplicityGuy/cronduit:latest | sha256sum | awk '{print $1}')
       V1_1_0_DIGEST=$(docker manifest inspect ghcr.io/SimplicityGuy/cronduit:1.1.0 | sha256sum | awk '{print $1}')
       [[ "$LATEST_DIGEST" == "$V1_1_0_DIGEST" ]] && echo "OK: T-20-06 mitigation verified" || echo "FAIL"
       ```
       The shell must print "OK: T-20-06 mitigation verified". If it prints "FAIL", a hotfix is required: `release.yml`'s hyphen-gate failed and `:latest` got promoted to rc.1 incorrectly. The maintainer immediately re-tags or rolls back per D-30.
    4. **Sign-off block** — maintainer fills in name, date, tag SHA, both arch digests, both `:latest` and `:1.1.0` digests (the latter two for documented audit comparison).

    Claude does NOT execute `git tag` or `git push`. Claude does NOT run `docker manifest inspect`. The maintainer holds these credentials and runs them locally.
  </how-to-verify>
  <action>The maintainer runs through `20-RC1-PREFLIGHT.md` per the how-to-verify steps. Claude waits for the resume-signal indicating either successful tag cut + sign-off or a fixup request.</action>
  <verify>
    <automated>grep -E '^Maintainer: [^_]' .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `20-RC1-PREFLIGHT.md` Sign-off block has Maintainer name + Date filled in (no underscore placeholders remaining on those lines).
    - Tag commit SHA filled in.
    - `:latest` digest == `:1.1.0` digest captured in the Sign-off block (T-20-06 mitigation verified).
    - GHCR amd64 + arm64 digests captured for `v1.2.0-rc.1`.
  </acceptance_criteria>
  <done>Maintainer has cut the tag, GHCR shows v1.2.0-rc.1 on amd64 + arm64, the `:latest` digest still matches v1.1.0 (T-20-06 mitigation verified), and the Sign-off block in `20-RC1-PREFLIGHT.md` is filled in.</done>
  <resume-signal>Type "approved" once the tag is cut, GHCR publish verified, and `:latest` confirmed unchanged at v1.1.0. Type "fix:<description>" if any preflight section fails and Claude should address the gap (e.g., a hotfix PR before re-tagging).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer terminal ↔ origin remote | The `-s` GPG signature on the tag is verified by GitHub on push; tag-spoofing requires the maintainer's signing key |
| `release.yml` workflow ↔ GHCR registry | OIDC-authenticated push from the workflow; the `:latest` push step is gated by an `if`-condition on tag content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering / Reliability | rc.1 tag accidentally promoting `:latest` in GHCR | mitigate | `release.yml` job-level `if`-condition `!contains(github.ref, '-')` gates the `:latest` update so any tag containing `-` (e.g., `v1.2.0-rc.1`) skips the latest-tag push (P12 D-10; verified at `.github/workflows/release.yml` line ~134). Detection: Section 8 of `20-RC1-PREFLIGHT.md` (post-publish digest verification) — the operator inspects `ghcr.io/SimplicityGuy/cronduit:latest` after the rc.1 push and confirms its digest still equals `v1.1.0`'s digest, NOT `v1.2.0-rc.1`'s. Residual risk: the gate depends on the regex; if a future contributor changes `release.yml`'s `if`-condition the gate fails silently. NOT modified by Phase 20 per D-30. |
| T-20-08 | Repudiation | Unsigned tag pushed to origin | mitigate | The tag command in Section 7 of preflight uses `-s` (GPG-signed). Pushing an unsigned tag would be rejected by branch-protection on `main`. The maintainer's GPG key is the trust anchor. |
| T-20-09 | Information Disclosure | Release notes accidentally containing secrets | mitigate | git-cliff renders from conventional-commit messages on `main`; no hand-editing post-publish (D-31). Conventional-commit messages reviewed in PRs do not contain secrets (CI scans for keys would catch it). Acceptance: low likelihood given the discipline of the merge gate. |
</threat_model>

<verification>
- `20-RC1-PREFLIGHT.md` exists in the phase directory with all 8 sections + Sign-off block.
- The file references `docs/release-rc.md` (REUSED VERBATIM) and contains the literal `git tag -a -s v1.2.0-rc.1` command.
- The file references `release.yml:134` and `release.yml:135` for the gate-logic visual check (Pitfall 9 / T-20-06).
- Section 8 contains the explicit shell snippet comparing `ghcr.io/SimplicityGuy/cronduit:latest` digest to `:1.1.0` digest (T-20-06 detection).
- No release-engineering files (release.yml, cliff.toml, docs/release-rc.md) were modified in the Phase 20 plan set (D-30 — verified by `git log --oneline --name-only main..HEAD | grep -E '(release\\.yml|cliff\\.toml|release-rc\\.md)'` returning empty).
- Plan 09 has 2 distinct tasks: Task 1 (autonomous Claude authoring) + Task 2 (checkpoint:human-action maintainer cut).
</verification>

<success_criteria>
- [ ] Task 1: `20-RC1-PREFLIGHT.md` authored with all 8 sections + Sign-off block.
- [ ] Task 1: Pre-flight references the existing `docs/release-rc.md` runbook (D-28 / D-30 — no edits to release engineering files).
- [ ] Task 1: Section 4 includes Pitfall 9 / T-20-06 visual checks on `release.yml:134` and `release.yml:135`.
- [ ] Task 1: Section 8 includes the explicit `:latest` ↔ `:1.1.0` digest comparison shell snippet (T-20-06 detection).
- [ ] Task 1: Sign-off block captures: Maintainer name, Date, Tag SHA, amd64 digest, arm64 digest, :latest digest, :1.1.0 digest.
- [ ] Task 1: Tag command is documented (NOT executed by Claude).
- [ ] Task 1: No commits to `release.yml`, `cliff.toml`, or `docs/release-rc.md` from Phase 20 plans (D-30 verified).
- [ ] Task 2: Maintainer has cut the tag and signed off (resume-signal received).
- [ ] Task 2: T-20-06 mitigation verified post-publish (`:latest` digest unchanged at v1.1.0).
</success_criteria>

<output>
After completion (when the maintainer has cut the tag and ticked sign-off), create `.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-09-SUMMARY.md` documenting:
- Tag commit SHA
- GHCR amd64 digest
- GHCR arm64 digest
- `:latest` GHCR digest (must still match v1.1.0 — T-20-06 mitigation verified post-publish)
- `:1.1.0` GHCR digest (audit comparison)
- Final confirmation: no Phase 20 plan modified `release.yml`, `cliff.toml`, or `docs/release-rc.md`
</output>
</content>
</invoke>