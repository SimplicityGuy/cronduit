---
phase: 12
plan: 07
type: execute
wave: 4
depends_on: [12-02, 12-04, 12-05, 12-06]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OPS-06, OPS-07, OPS-08]
user_setup:
  - service: GitHub (maintainer-side)
    why: "Per D-13, the v1.1.0-rc.1 tag is cut locally by the maintainer (NOT by workflow_dispatch) so the trust anchor stays with the maintainer's signing key. CI / Claude must not cut the tag."
    env_vars: []
    dashboard_config:
      - task: "Run pre-flight checklist + git tag command per docs/release-rc.md"
        location: "Maintainer's local git checkout of main, AFTER the Phase 12 PR is merged"
must_haves:
  truths:
    - "OPS-06, OPS-07, and OPS-08 checkboxes in `.planning/REQUIREMENTS.md` are flipped from `[ ]` to `[x]`."
    - "The Phase 12 traceability rows for OPS-06/07/08 are flipped from `Pending` to `Done` (matching the convention used by completed v1.0 requirements in the archived milestones doc)."
    - "The maintainer (user) has cut the `v1.1.0-rc.1` tag locally per `docs/release-rc.md` AFTER the Phase 12 PR merges to main, and `release.yml` has published the multi-arch image to GHCR."
    - "Post-push verification per the runbook confirms: `:1.1.0-rc.1` and `:rc` are present and multi-arch; `:latest` is unchanged from `v1.0.1`; GitHub Release is marked prerelease."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "OPS-06/07/08 checkboxes flipped to [x]; traceability rows flipped to Done"
      contains: "[x] **OPS-06**"
  key_links:
    - from: ".planning/REQUIREMENTS.md OPS-06/07/08 checkbox state"
      to: "Phase 12 implementation across plans 01-06"
      via: "documentation-of-record that the requirements ship in v1.1.0-rc.1"
      pattern: "\\[x\\] \\*\\*OPS-0[678]\\*\\*"
---

<objective>
Phase 12 close-out — flip the OPS-06/07/08 requirement checkboxes to `[x]` AND surface the maintainer-side `v1.1.0-rc.1` tag cut as the explicit user action required to ship rc.1.

Purpose: Closes the loop on Phase 12. The checkbox flip is a small, mechanical doc edit that makes the milestone-progress dashboard accurate. The tag-cut step is the deliberately-manual maintainer action per D-13 — Claude / CI does not cut tags, and per `feedback_uat_user_validates.md` Claude does not assert UAT pass. The plan is `autonomous: false` because Task 2 IS the user action.

Output:
- `.planning/REQUIREMENTS.md` — three `[ ]` → `[x]` flips on the OPS-06/07/08 lines; three `Pending` → `Done` flips in the traceability table.
- A maintainer-action checkpoint that pauses execution and instructs the user to (a) merge the Phase 12 PR to main, (b) follow `docs/release-rc.md` to cut `v1.1.0-rc.1`, (c) verify post-push per the runbook, and (d) confirm completion to Claude.

This plan **depends on** Plans 02, 04, 05, 06 — the implementation must be complete and the supporting CI / docs in place before the requirements can legitimately be marked done. Plans 01 and 03 are transitively required (02 depends on 01; 04 depends on 03), so Wave 4 starts only when Waves 1, 2, and 3 are all green.

**Important:** This plan does NOT push the tag itself. The plan executor (Claude) edits `.planning/REQUIREMENTS.md` and then surfaces the tag-cut as a `checkpoint:human-action` task. The user runs the runbook commands locally after the PR merges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-CONTEXT.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md
@docs/release-rc.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-02-SUMMARY.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-04-SUMMARY.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-05-SUMMARY.md
@.planning/phases/12-docker-healthcheck-rc-1-cut/12-06-SUMMARY.md

<interfaces>
<!-- The exact existing REQUIREMENTS.md shapes Plan 07 patches. -->

From .planning/REQUIREMENTS.md (current OPS-06..08 entries — lines 87, 89, 91):
```markdown
- [ ] **OPS-06**: A new `cronduit health` CLI subcommand performs a local HTTP GET against `/health`, parses the JSON response, and exits 0 only if `status == "ok"`. It fails fast on connection-refused (no retry; the Docker healthcheck has its own retry policy) and reads the bind address from either a `--bind` flag or defaults to `http://127.0.0.1:8080`.

- [ ] **OPS-07**: The Dockerfile ships with a `HEALTHCHECK CMD ["/cronduit", "health"]` directive ...

- [ ] **OPS-08**: The root cause of the reported `(unhealthy)` symptom (busybox `wget --spider` in alpine:3 misparses axum's chunked responses) is reproduced ...
```

From .planning/REQUIREMENTS.md (current traceability rows — lines 170-172):
```markdown
| OPS-06   | Phase 12 | Pending |
| OPS-07   | Phase 12 | Pending |
| OPS-08   | Phase 12 | Pending |
```

The patch is purely additive — only the `[ ]` token and the `Pending` token change. Prose is preserved verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip OPS-06/07/08 checkboxes + traceability rows in .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — read lines 85-95 (the OPS-06..08 entries) AND lines 168-175 (the traceability table) so the executor sees the exact current state and avoids prose drift.
    - .planning/phases/12-docker-healthcheck-rc-1-cut/12-PATTERNS.md § "`.planning/REQUIREMENTS.md` (MODIFIED)" — for the verbatim patch description.
  </read_first>
  <behavior>
    - Three checkbox flips: `- [ ] **OPS-06**:` → `- [x] **OPS-06**:`; same for OPS-07 and OPS-08.
    - Three traceability flips: `| OPS-06   | Phase 12 | Pending |` → `| OPS-06   | Phase 12 | Done    |` (preserving column alignment); same for OPS-07 and OPS-08.
    - DO NOT alter the prose of any requirement.
    - DO NOT touch any other requirement's checkbox or traceability row (DB-09..14 etc. are owned by other phases; their state is set by their own plans).
    - DO NOT touch the `Total: 31 requirements ... All pending implementation.` summary line (which is stale in any case after Phases 10/11 closed; that line is a maintenance debt that will be addressed at milestone-completion time, not per phase).
  </behavior>
  <action>
Edit `.planning/REQUIREMENTS.md`. Two coordinated patches.

**Patch 1 — checkboxes** (around lines 87, 89, 91):

Find the line beginning `- [ ] **OPS-06**:` and replace the `- [ ]` prefix with `- [x]`. The rest of the line (prose, citations) stays verbatim.

Find the line beginning `- [ ] **OPS-07**:` and do the same.

Find the line beginning `- [ ] **OPS-08**:` and do the same.

After patching, the three lines become:
```markdown
- [x] **OPS-06**: A new `cronduit health` CLI subcommand performs a local HTTP GET against `/health`, parses the JSON response, and exits 0 only if `status == "ok"`. It fails fast on connection-refused (no retry; the Docker healthcheck has its own retry policy) and reads the bind address from either a `--bind` flag or defaults to `http://127.0.0.1:8080`.

- [x] **OPS-07**: The Dockerfile ships with a `HEALTHCHECK CMD ["/cronduit", "health"]` directive using conservative defaults (`--interval=30s --timeout=5s --start-period=60s --retries=3`), so `docker compose up` reports `healthy` out of the box without any compose-file healthcheck stanza. Operators who write their own `healthcheck:` in compose continue to work (compose overrides Dockerfile). `T-V11-HEALTH-01`, `T-V11-HEALTH-02`.

- [x] **OPS-08**: The root cause of the reported `(unhealthy)` symptom (busybox `wget --spider` in alpine:3 misparses axum's chunked responses) is reproduced in a test environment before the fix is declared complete. If the reproduction shows a different root cause, this requirement is re-scoped; the `cronduit health` subcommand fix path is correct regardless because it removes the entire busybox wget dependency from the healthcheck path.
```

**Patch 2 — traceability table** (around lines 170-172):

Find the three rows in the traceability table:
```
| OPS-06   | Phase 12 | Pending |
| OPS-07   | Phase 12 | Pending |
| OPS-08   | Phase 12 | Pending |
```

Replace `Pending` with `Done` on each, preserving column alignment (the cell width was 7 — `Pending` is 7 chars, `Done` is 4 chars; pad with 3 trailing spaces inside the cell to keep the table well-aligned). After patching:
```
| OPS-06   | Phase 12 | Done    |
| OPS-07   | Phase 12 | Done    |
| OPS-08   | Phase 12 | Done    |
```

(If the existing table uses no padding past the closest pipe, just `Done |` is acceptable — verify by re-reading lines 144-175 to see whether other phases' traceability rows have padding. Match the existing convention.)

DO NOT touch any other line in the file. Specifically:
- DO NOT update the `Total:` line at the bottom (currently says "All pending implementation" — that's a milestone-summary statement that becomes accurate when Phase 14 ships v1.1.0; correcting it per phase is more churn than it's worth).
- DO NOT touch DB-09..14, UI-16..20, OBS-*, ERG-*, or any other requirement.
- DO NOT touch the `## Future Requirements`, `## Out of Scope`, or `## Phase → requirement rollup` sections.

After editing, verify:

```bash
grep -c '^- \[x\] \*\*OPS-0[678]\*\*' .planning/REQUIREMENTS.md   # expect 3
grep -E '^\| OPS-0[678] +\| Phase 12 \| Done' .planning/REQUIREMENTS.md   # expect 3 lines
```

Both must succeed.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[x\] \*\*OPS-0[678]\*\*' .planning/REQUIREMENTS.md)" -eq 3 && test "$(grep -cE '^\| OPS-0[678] +\| Phase 12 \| Done' .planning/REQUIREMENTS.md)" -eq 3 && grep -F '- [x] **OPS-06**' .planning/REQUIREMENTS.md && grep -F '- [x] **OPS-07**' .planning/REQUIREMENTS.md && grep -F '- [x] **OPS-08**' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the literal line prefix `- [x] **OPS-06**:` (verifiable: `grep -F '- [x] **OPS-06**:' .planning/REQUIREMENTS.md` returns one match).
    - Same for OPS-07 (verifiable: `grep -F '- [x] **OPS-07**:' .planning/REQUIREMENTS.md` returns one match).
    - Same for OPS-08 (verifiable: `grep -F '- [x] **OPS-08**:' .planning/REQUIREMENTS.md` returns one match).
    - Traceability row for OPS-06 shows `Done` (verifiable: `grep -E '^\| OPS-06 +\| Phase 12 \| Done' .planning/REQUIREMENTS.md` returns one match).
    - Same for OPS-07.
    - Same for OPS-08.
    - The prose of each requirement (everything after the `**OPS-0X**:` token) is byte-identical to the pre-patch state — verifiable by diffing the patched file against the previous git revision and confirming the only changes inside the OPS-0[678] lines are the bracket character and the `Pending`→`Done` cell.
    - No other `- [ ]` checkbox in the file was touched (verifiable: `git diff .planning/REQUIREMENTS.md` shows changes confined to OPS-06/07/08 lines and their traceability rows; specifically DB-09..14, UI-16..20, OBS-*, ERG-* checkboxes still show `[ ]`).
    - The `Total: 31 requirements` summary line is unchanged (verifiable: `grep -F 'Total: 31 requirements' .planning/REQUIREMENTS.md` returns one match — same as before).
  </acceptance_criteria>
  <done>
    `.planning/REQUIREMENTS.md` reflects OPS-06/07/08 as complete with three checkbox flips and three traceability-row flips. No prose drift, no collateral edits.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Maintainer cuts v1.1.0-rc.1 tag per docs/release-rc.md (USER ACTION)</name>
  <what-built>
    Phase 12 has shipped:
    - `cronduit health` CLI subcommand (Plans 01 + 02; OPS-06).
    - Dockerfile HEALTHCHECK directive (Plan 03; OPS-07).
    - Compose-smoke CI workflow + OPS-08 reproduction fixtures (Plan 04; OPS-07 + OPS-08).
    - `release.yml` D-10 patch — `:latest` gated to skip on rc tags; `:rc` rolling tag added (Plan 05).
    - `docs/release-rc.md` maintainer runbook (Plan 06).
    - REQUIREMENTS.md OPS-06/07/08 flipped to `[x]` (Task 1 above).

    What this checkpoint does NOT include: the actual `git tag -a v1.1.0-rc.1` command. Per Phase 12 D-13, tag creation is a maintainer-side action — the trust anchor stays with the maintainer's signing key, NOT the GHA runner identity. Per `feedback_uat_user_validates.md`, Claude does not assert UAT pass on the maintainer's behalf.
  </what-built>
  <how-to-verify>
    **Pre-action gate** — confirm the Phase 12 PR has merged to main:
    1. `git checkout main && git pull --ff-only origin main`
    2. `git log -1 --oneline` — should show the Phase 12 merge commit.
    3. `gh run list --workflow=ci.yml --branch=main --limit=1` — most recent run on main is `completed/success`.
    4. `gh run list --workflow=compose-smoke.yml --branch=main --limit=1` — most recent run on main is `completed/success`.

    **Cut the tag** — open `docs/release-rc.md` and follow the runbook end-to-end. Specifically:
    1. Pre-flight checklist (all six checkboxes pass).
    2. GPG pre-flight (`git config --get user.signingkey`).
    3. Step 2a (signed) OR Step 2b (unsigned annotated) per GPG state.
    4. Push the tag: `git push origin v1.1.0-rc.1`.
    5. Watch `release.yml`: `gh run watch --exit-status`.

    **Post-push verification** — run every check in the runbook's "Post-push verification" table:
    1. `docker manifest inspect ghcr.io/simplicityguy/cronduit:1.1.0-rc.1` shows two platforms.
    2. `docker manifest inspect ghcr.io/simplicityguy/cronduit:rc` digest === `:1.1.0-rc.1` digest.
    3. `docker manifest inspect ghcr.io/simplicityguy/cronduit:latest` digest === pre-existing `v1.0.1` digest.
    4. `docker manifest inspect ghcr.io/simplicityguy/cronduit:1` and `:1.1` digests unchanged.
    5. `gh release view v1.1.0-rc.1 --json isPrerelease --jq .isPrerelease` returns `true`.
    6. Release body matches `git cliff --unreleased -o /tmp/preview.md` preview.
    7. `docker run --rm ghcr.io/simplicityguy/cronduit:1.1.0-rc.1 --version` outputs `cronduit 1.1.0`.
    8. Healthy in shipped compose stack: `docker compose ps` shows `Up N seconds (healthy)` within 90 s of `up -d`.

    If any check fails: do NOT delete-and-retag. Follow the runbook's "What if UAT fails" section — fix on main, ship `v1.1.0-rc.2`.
  </how-to-verify>
  <resume-signal>
    Reply with:
    - **`approved`** if every post-push verification check passed and the rc.1 image is published to GHCR with the correct tag set.
    - **`failed: <description>`** if any check failed; describe the divergence so we can plan an `rc.2` if needed.
    - **`deferred`** if you want to merge the Phase 12 PR but cut the rc.1 tag later (acceptable; the workflow is set up correctly and the runbook waits).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's local git environment → `git tag -a -s v1.1.0-rc.1` → tag object | The maintainer's signing key (or annotated-tag identity) is the trust anchor; D-13 keeps this off the GHA runner. |
| Pushed tag → release.yml workflow → published GHCR image + GitHub Release | The workflow trusts the tag's identity (not just its name) for the release artifact attestation. |
| Plan 07 checkpoint → user response | The user's `approved` / `failed` reply is the recorded UAT decision; per `feedback_uat_user_validates.md`, this cannot be auto-asserted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Spoofing | Tag spoofing if maintainer skips signing pre-flight | mitigate | Runbook (Plan 06) Step 2a is "preferred" with `git tag -a -s` + `git tag -v` verification; Step 2b is the explicit annotated-only fallback. The checkpoint message points the maintainer at the runbook to ensure both branches are encountered. |
| T-12-07-02 | Tampering | Plan 07 auto-asserts UAT pass without user confirmation | mitigate | Task 2 is `type="checkpoint:human-action"` with `gate="blocking"` — execution PAUSES until the user explicitly responds. Per `feedback_uat_user_validates.md`, this prevents Claude from self-validating UAT. |
| T-12-07-03 | Repudiation | Checkbox flip (Task 1) lands without verification of underlying implementation | mitigate | Plan 07 `depends_on: [12-02, 12-04, 12-05, 12-06]` — those plans must complete (and their SUMMARYs must exist) before Task 1 runs. Each upstream plan has its own automated verification (cargo test, compose-smoke green, YAML parse, runbook acceptance criteria). |
| T-12-07-04 | Information Disclosure | Maintainer's GPG key material logged | accept | The runbook only displays the configured `user.signingkey` (a key ID, not key material). Standard GitHub-recommended practice. |
| T-12-07-05 | Denial of Service | Phase 12 cannot complete because user defers the tag cut indefinitely | accept | The `deferred` resume-signal is an explicit valid response. Phase 12 implementation completion (cargo tests + compose-smoke) is decoupled from the maintainer-side rc.1 tag cut. The OPS-06/07/08 checkboxes can flip (Task 1) on PR merge; the rc.1 tag cut is the v1.1.0-rc.1 ship event, which can happen any time after merge. |
| T-12-07-06 | Elevation of Privilege | A workflow_dispatch shortcut sneaks into the runbook recommendation | mitigate | Cross-plan acceptance criterion: Plan 06 (`docs/release-rc.md`) was already verified to NOT contain `workflow_dispatch`. Plan 07 only references the runbook; it does not introduce a new tag-cut path. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` shows three OPS-0[678] checkboxes flipped to `[x]` and three traceability rows flipped to `Done`.
- No collateral edits to other requirements.
- Task 2 checkpoint pauses execution; user responds with `approved`, `failed`, or `deferred`.
- (After `approved`): GHCR has `:1.1.0-rc.1` and `:rc` tags pointing at a multi-arch manifest; `:latest` digest unchanged from `v1.0.1`; `gh release view v1.1.0-rc.1` reports prerelease=true.
</verification>

<success_criteria>
- OPS-06/07/08 are documented as complete in REQUIREMENTS.md.
- The maintainer has explicit, executable instructions to cut `v1.1.0-rc.1` per the runbook.
- The `v1.1.0-rc.1` ship is a user-validated event, not a Claude-asserted one.
- Phase 12 is closed-out with a clear hand-off to the user for the rc.1 tag cut.
</success_criteria>

<output>
After completion (after the user responds to Task 2):

Create `.planning/phases/12-docker-healthcheck-rc-1-cut/12-07-SUMMARY.md` capturing:
- The git diff of `.planning/REQUIREMENTS.md` (showing the 3 checkbox flips + 3 traceability flips).
- The user's resume-signal response (`approved` / `failed: <description>` / `deferred`).
- (If `approved`) The post-push verification command outputs the user reported (digest comparisons, `gh release view` JSON, `docker run --version` output).
- (If `failed`) The divergence the user observed; recommendation for whether Phase 12 needs an `rc.2` or whether a hotfix is needed.
- (If `deferred`) Note that Phase 12 implementation is complete; rc.1 cut is pending and tracked outside the phase plan.

Then create `.planning/phases/12-docker-healthcheck-rc-1-cut/12-PHASE-SUMMARY.md` aggregating all 7 plan SUMMARYs into a single phase-completion record (artifacts shipped, requirements closed, rc.1 ship status, lessons learned). The phase summary feeds into the next milestone-archive operation.
</output>
