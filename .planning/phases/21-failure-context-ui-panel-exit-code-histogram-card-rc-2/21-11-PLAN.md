---
phase: 21
plan: 11
type: execute
wave: 4
depends_on: [21-07, 21-08, 21-09, 21-10]
files_modified:
  - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md
  - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md
autonomous: false
requirements: [FCTX-01, FCTX-02, FCTX-03, FCTX-05, FCTX-06, EXIT-01, EXIT-02, EXIT-03, EXIT-04, EXIT-05, EXIT-06]
requirements_addressed: [FCTX-01, FCTX-02, FCTX-03, FCTX-05, FCTX-06, EXIT-01, EXIT-02, EXIT-03, EXIT-04, EXIT-05, EXIT-06]
objective: |
  Create the maintainer-validated `21-HUMAN-UAT.md` (umbrella covering
  the FCTX panel, histogram card, fire-skew, and a11y scenarios per
  D-20 / research §G) and the `21-RC2-PREFLIGHT.md` runbook for the
  v1.2.0-rc.2 tag cut (D-22..D-26 reusing `docs/release-rc.md`
  verbatim). Maintainer validates UAT manually per project memory
  `feedback_uat_user_validates.md` — Claude does NOT mark UAT passed
  from its own runs. EXIT-06 (Prometheus cardinality discipline,
  accepted-out-of-scope) is explicitly verified here as "no new
  per-job exit-code Prometheus label was added".
must_haves:
  truths:
    - "21-HUMAN-UAT.md exists with maintainer-validated scenarios for all FCTX/EXIT requirements + a11y umbrella"
    - "21-RC2-PREFLIGHT.md exists with the v1.2.0-rc.2 tag-cut runbook reusing docs/release-rc.md verbatim per D-22"
    - "EXIT-06 verification step explicitly checks no new exit_code Prometheus label was added (`grep -rn 'exit_code' src/metrics.rs` returns empty per research § Phase Requirements EXIT-06)"
    - "Tag command literal: `git tag -a -s v1.2.0-rc.2 -m \"v1.2.0-rc.2 — FCTX UI panel + exit-code histogram (P21)\"` (D-23 / D-31: in-source Cargo.toml stays at 1.2.0; -rc.2 is tag-only suffix)"
    - "Pre-flight checklist: P21 PR merged to main + green CI + green compose-smoke + git-cliff preview clean (D-23 / D-27 [informational]: PR-only workflow, no direct main commits)"
    - "NO modifications to release.yml / cliff.toml / docs/release-rc.md (D-24); rc.2 reuses runbook verbatim (D-22)"
    - "GitHub Release notes: git-cliff output is authoritative (D-25); no hand-edit of release body post-publish"
    - "No new top-level CI changes (D-21); existing linux/{amd64,arm64} × {SQLite, Postgres} matrix covers Phase 21; no new feature flag, no new lint gate"
    - "Plans frontmatter has `autonomous: false` (D-26 / D-30 [informational]: maintainer validates UAT — Claude does NOT mark UAT passed from its own runs; per project memory `feedback_uat_user_validates.md`)"
    - "All UAT steps reference existing `just` recipes (project memory `feedback_uat_use_just_commands.md`); the new recipes from plan 21-10 are referenced by name"
    - "Diagrams in HUMAN-UAT/RC2-PREFLIGHT artifacts are mermaid only, no ASCII art (D-28 [informational] / project memory `feedback_diagrams_mermaid.md`)"
  artifacts:
    - path: .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md
      provides: "maintainer UAT plan covering all FCTX/EXIT scenarios + a11y umbrella"
      contains: "21-HUMAN-UAT"
    - path: .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md
      provides: "rc.2 tag-cut runbook"
      contains: "v1.2.0-rc.2"
  key_links:
    - from: .planning/phases/.../21-HUMAN-UAT.md
      to: justfile (uat-fctx-panel, uat-exit-histogram, uat-fire-skew, uat-fctx-a11y)
      via: "step references existing just recipes only"
      pattern: "just uat-"
    - from: .planning/phases/.../21-RC2-PREFLIGHT.md
      to: docs/release-rc.md
      via: "rc.2 tag-cut runbook reuses verbatim per D-22"
      pattern: "docs/release-rc.md"
---

<objective>
Land the two maintainer-validated artifacts that close out Phase 21:
the human UAT plan and the rc.2 tag-cut pre-flight runbook. Both
deliberately have `autonomous: false` — Claude prepares the documents
and the spot-check commands; the maintainer runs them, validates the
behavior, and cuts the rc.2 tag.

Purpose: Per project memory `feedback_uat_user_validates.md`, Claude
NEVER marks UAT passed from its own test runs. This plan is the
"prepared runbook" for the human to execute.

Output:
1. `21-HUMAN-UAT.md` — umbrella maintainer-validated UAT covering
   FCTX panel, histogram card, fire-skew, and the 4-phase a11y umbrella
   (mobile/light/print/keyboard) per research §G.
2. `21-RC2-PREFLIGHT.md` — preflight checklist + literal tag command
   for v1.2.0-rc.2 cut, reusing `docs/release-rc.md` verbatim per
   D-22..D-26.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-CONTEXT.md
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RESEARCH.md
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-PATTERNS.md
@.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-UI-SPEC.md
@.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md
@docs/release-rc.md
@justfile

<interfaces>
P20 RC1-PREFLIGHT precedent (`.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md`) — this is the literal precedent Phase 21 mirrors with `rc.1` → `rc.2` and `P20` → `P21` substitutions per research § Verification Table D-22..D-26.

D-22..D-26 lock the rc.2 cut posture:
- D-22: reuse `docs/release-rc.md` verbatim
- D-23: tag command `git tag -a -s v1.2.0-rc.2 -m "v1.2.0-rc.2 — FCTX UI panel + exit-code histogram (P21)"`; pre-flight = PR merged + green CI + green compose-smoke + git-cliff preview clean
- D-24: NO modifications to release.yml / cliff.toml / docs/release-rc.md
- D-25: git-cliff output is authoritative; no hand-edit
- D-26: autonomous=false; maintainer cuts the tag locally

EXIT-06 verification (CONTEXT § Out of scope + research § Validation Architecture line 424):
"Exit codes are NOT exposed as Prometheus label" — verified by:
```
grep -rn 'exit_code' src/metrics.rs                # MUST return empty (or only doc-comment refs)
grep -rn 'cronduit_runs_total.*exit_code' src/     # MUST return empty
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Create 21-HUMAN-UAT.md (autonomous: false; maintainer-validated)</name>
  <files>.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-CONTEXT.md § Decisions D-20 (HUMAN-UAT scenarios + autonomous=false)
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RESEARCH.md § Discretion Resolutions §G (umbrella vs split decision — single uat-fctx-a11y umbrella)
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-VALIDATION.md § Manual-Only Verifications (lines 75-85; the locked manual-test list)
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md (sibling preflight precedent — ROUGHLY similar shape but covering Phase 20 rc.1)
    - justfile (Phase 21 uat-* recipes added by plan 21-10: uat-fctx-panel, uat-exit-histogram, uat-fire-skew, uat-fctx-a11y)
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-UI-SPEC.md § Interaction Contract + § Accessibility Contract (the locked maintainer-checkable behaviors)
  </read_first>
  <action>
Create `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` with the following structure. Maintainer signs off each scenario by ticking the box. Claude NEVER ticks these boxes (project memory `feedback_uat_user_validates.md`).

```markdown
---
phase: 21
plan: 11
type: human-uat
autonomous: false
created: 2026-05-02
status: pending-maintainer-validation
---

# Phase 21 — Human UAT (Maintainer-Validated)

> **Per project memory `feedback_uat_user_validates.md`:** maintainer validates each scenario manually. Claude does NOT mark UAT passed from its own test runs.
>
> **Per project memory `feedback_uat_use_just_commands.md`:** every step references an existing `just` recipe.

## Prerequisites

- [ ] Phase 21 PR is open against `main` from `phase21/ui-spec` (or successor branch)
- [ ] CI is green on the PR (full matrix: linux/{amd64,arm64} × {SQLite,Postgres})
- [ ] `just nextest` is green locally
- [ ] `just schema-diff` (if exists) shows the new `scheduled_for` column on both backends
- [ ] `cargo tree -i openssl-sys` returns empty (D-32)

## Scenario 1 — FCTX Panel Renders on Failed Run (FCTX-01, FCTX-02)

```
just uat-fctx-panel
```

Expected:
- [ ] Panel renders collapsed-by-default with summary "Failure context · 4 consecutive failures"
- [ ] Click summary → panel expands smoothly (native `<details>` toggle)
- [ ] TIME DELTAS row shows "First failure: {rel} ago • 4 consecutive failures" (no link if no prior success)
- [ ] CONFIG row shows "Config changed since last success: No" (or Yes)
- [ ] FIRE SKEW row shows "Scheduled: HH:MM:SS • Started: HH:MM:SS (+0 ms)" (manual triggers per landmine §7)
- [ ] No console errors in browser DevTools

## Scenario 2 — Panel Hidden on Success/Cancelled/Running/Stopped (FCTX-01 negative)

After Scenario 1, manually update one seeded run's status:

```
sqlite3 cronduit.dev.db "UPDATE job_runs SET status='success' WHERE id=(SELECT MAX(id) FROM job_runs);"
```

Visit the run-detail page for that run.

Expected:
- [ ] No `cd-fctx-panel` element rendered
- [ ] Page renders normally (no broken layout, no errors)

Repeat with `status='cancelled'`, `status='running'`, `status='stopped'` — each should hide the panel.

## Scenario 3 — Image Digest Row Hidden on Non-Docker Job (FCTX-03)

Edit `examples/cronduit.toml` to add a `command` job and seed a failure for it.

Expected:
- [ ] FCTX panel renders (status=failed)
- [ ] IMAGE DIGEST row is ABSENT from the panel body

## Scenario 4 — Histogram Card on Job Detail (EXIT-01..EXIT-05)

```
just uat-exit-histogram
```

Visit `/jobs/{id}` for the seeded job.

Expected:
- [ ] "Exit Code Distribution" card sibling to Duration card
- [ ] SUCCESS stat: 50% (5 successes / (10 - 1 stopped) per D-09 success-rate-excludes-stopped)
- [ ] 10 bucket columns visible with short labels: 1, 2, 3-9, 10-126, 127, 128-143, 144-254, 255, none, stopped
- [ ] BucketStopped column has count 1 with `cd-exit-bar--stopped` (slate-grey)
- [ ] Bucket128to143 column has count 1 with `cd-exit-bar--warn` (yellow) — proves EXIT-04 dual-classifier
- [ ] Recent codes table shows 1 (count 2), 137 (count 1), 127 (count 1)
- [ ] Hover BucketStopped bar → tooltip shows "NOT a crash"

## Scenario 5 — Below-Threshold Empty State (D-15)

```
just db-reset
just dev   # in another terminal
```

Visit `/jobs/{id}` for any job (which has zero runs).

Expected:
- [ ] Card renders with heading "Exit Code Distribution"
- [ ] Empty state: `—` em-dash + "Need 5+ samples; have 0" copy
- [ ] No bars, no recent-codes table, no SUCCESS stat

## Scenario 6 — Fire-Skew Row Renders on Slow-Start Container (FCTX-06)

```
just uat-fire-skew
```

Wait the prescribed time, then visit the latest run-detail page.

Expected:
- [ ] FIRE SKEW row reads "Scheduled: HH:MM:00 • Started: HH:MM:30 (+30000 ms)" (or thereabouts)
- [ ] Skew is positive (start_time after scheduled_for)

## Scenario 7 — Accessibility Umbrella (mobile, light, print, keyboard)

```
just uat-fctx-a11y
```

Walk all 4 phases interactively. For each phase, mark the relevant checkbox below.

- [ ] Phase 1 (Mobile <640px): rows STACK 1-column; histogram horizontally scrolls
- [ ] Phase 2 (Light mode): panel + card render with light tokens (no broken contrast)
- [ ] Phase 3 (Print): panel renders OPEN by default in print preview
- [ ] Phase 4 (Keyboard-only): focus rings visible on summary, link, and histogram bars; Space/Enter toggles `<details>`; tooltip appears on bar focus

## Scenario 8 — EXIT-06 Cardinality Discipline (out-of-scope verification)

Per CONTEXT § Out of Scope and research § Phase Requirements EXIT-06:

```
grep -rn 'exit_code' src/metrics.rs        # MUST return empty (no exit_code label registered)
grep -rn 'cronduit_runs_total' src/        # MUST NOT show exit_code in any label list
```

- [ ] Both greps confirm: NO new per-job exit-code Prometheus label was added
- [ ] `/metrics` endpoint scrape (during `just dev`) does NOT include any exit_code label

## Out-of-Band Spot Check — `cargo tree -i openssl-sys`

```
cargo tree -i openssl-sys
```

- [ ] Output is empty (D-32: rustls-everywhere invariant preserved; Phase 21 added zero new external crates)

## Sign-Off

- [ ] All 8 scenarios above pass
- [ ] No surprise console errors / log warnings beyond expected
- [ ] PR is ready for merge to `main`
- [ ] Maintainer signature + date: `__________________`

After sign-off, proceed to `21-RC2-PREFLIGHT.md` for the v1.2.0-rc.2 tag cut.
```

This is a maintainer-facing document. The plan EXECUTOR (Claude) writes the document. The plan VERIFIER (the maintainer) walks the scenarios and ticks the boxes during the human-validation step.
  </action>
  <what-built>
    `21-HUMAN-UAT.md` document with 8 scenario checklists referencing existing `just uat-*` recipes from plan 21-10 + EXIT-06 cardinality grep verification + `cargo tree -i openssl-sys` final-gate spot check + maintainer sign-off block.
  </what-built>
  <how-to-verify>
    1. The maintainer reads through the document and confirms all scenarios are reachable from existing recipes (no `seed-fixture-runs`, no `dev-build` references).
    2. The maintainer runs each `just uat-*` recipe in sequence and ticks the boxes.
    3. The maintainer runs the EXIT-06 grep verification.
    4. The maintainer runs `cargo tree -i openssl-sys` and confirms empty output.
    5. The maintainer signs off with name + date.

    If any scenario fails, the maintainer captures the failure (screenshot + log), reopens the PR for plan-level fixes, and re-runs after the fix lands.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md && grep -c "autonomous: false" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md && grep -cE "just (uat-fctx-panel|uat-exit-histogram|uat-fire-skew|uat-fctx-a11y)" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` exits 0
    - Frontmatter has `autonomous: false`: `grep -c "^autonomous: false$" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns 1
    - All 4 plan-21-10 recipes referenced: `grep -cE "just (uat-fctx-panel|uat-exit-histogram|uat-fire-skew|uat-fctx-a11y)" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns >= 4
    - All FCTX/EXIT requirement IDs cited: `grep -cE "FCTX-0[1-6]|EXIT-0[1-6]" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns >= 8
    - EXIT-06 explicit verification step: `grep -c "EXIT-06" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns >= 1, AND `grep -c "exit_code.*src/metrics.rs\|src/metrics.rs.*exit_code" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns >= 1
    - `cargo tree -i openssl-sys` step: `grep -c "cargo tree -i openssl-sys" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns 1
    - Sign-off block: `grep -c "Sign-Off\|Maintainer signature" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns >= 1
    - No reference to non-existent recipes: `grep -c "just seed-fixture-runs\|just dev-build" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-HUMAN-UAT.md` returns 0
  </acceptance_criteria>
  <resume-signal>Type "approved" after maintainer signs off all 8 scenarios; describe issues to surface a hotfix PR before tagging.</resume-signal>
  <done>
    21-HUMAN-UAT.md exists with the 8-scenario maintainer-validated UAT. Maintainer signs off after running all `just uat-*` recipes and verifying EXIT-06 + `cargo tree -i openssl-sys`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create 21-RC2-PREFLIGHT.md and execute the v1.2.0-rc.2 tag cut (autonomous: false; maintainer-only)</name>
  <files>.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md (the literal precedent — Phase 21 mirrors verbatim with rc.1 → rc.2 and P20 → P21 substitutions)
    - docs/release-rc.md (the runbook reused verbatim per D-22)
    - .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-CONTEXT.md § Decisions D-22..D-26 (rc.2 cut posture)
  </read_first>
  <action>
Create `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` mirroring the structure of `.planning/phases/20-webhook-ssrf-https-posture-retry-drain-metrics-rc-1/20-RC1-PREFLIGHT.md` with the following adjustments:

```markdown
---
phase: 21
plan: 11
type: rc-preflight
autonomous: false
rc_tag: v1.2.0-rc.2
created: 2026-05-02
status: pending-maintainer-execution
---

# Phase 21 — v1.2.0-rc.2 Tag Cut Preflight

> **Per D-22..D-26:** reuse `docs/release-rc.md` verbatim. NO modifications to `release.yml` / `cliff.toml` / `docs/release-rc.md` (D-24).
> **Per D-26 + project memory `feedback_uat_user_validates.md`:** maintainer cuts the tag locally; this is NOT an automated step.

## Pre-Flight Checklist (D-23)

Before running `git tag`:

- [ ] Phase 21 PR is merged to `main` (squash or merge commit per repo convention)
- [ ] CI on `main` is green (full matrix: `linux/{amd64,arm64} × {SQLite,Postgres}`)
- [ ] Compose smoke test green: `just compose-smoke` (or equivalent)
- [ ] `git-cliff --unreleased --tag v1.2.0-rc.2` preview output is clean (no orphan commits, no missing scope tags, all P21 commits grouped under "Added"/"Changed"/"Fixed" sections)
- [ ] `21-HUMAN-UAT.md` is signed off by maintainer (sign-off block ticked)
- [ ] `Cargo.toml` is at version `1.2.0` (P15 already set this; per D-31 + project memory `feedback_tag_release_version_match.md` the in-source version is unsuffixed; `-rc.2` is the tag-only suffix)
- [ ] `cargo tree -i openssl-sys` empty (D-32 invariant)
- [ ] Working tree clean: `git status --porcelain` returns empty

## Tag Cut Command (D-23)

The literal command — copy-paste verbatim:

```bash
git tag -a -s v1.2.0-rc.2 -m "v1.2.0-rc.2 — FCTX UI panel + exit-code histogram (P21)"
```

Then push the tag:

```bash
git push origin v1.2.0-rc.2
```

## Post-Tag Verification (per docs/release-rc.md)

After the GitHub Actions release workflow completes (~15-20 min for multi-arch build):

- [ ] GHCR image published at `ghcr.io/SimplicityGuy/cronduit:v1.2.0-rc.2` on both amd64 and arm64
- [ ] `:latest` GHCR tag still points at `v1.1.0` (the `release.yml` patch from P12 D-10 enforces this on tags containing `-`)
- [ ] Rolling `:rc` tag updated to `v1.2.0-rc.2`
- [ ] GitHub Release page exists with `git-cliff` body content (NOT hand-edited per D-25)
- [ ] `docker run --rm ghcr.io/SimplicityGuy/cronduit:v1.2.0-rc.2 cronduit --version` returns `1.2.0`

## Out-of-Scope (per D-24)

- DO NOT modify `.github/workflows/release.yml`
- DO NOT modify `cliff.toml`
- DO NOT modify `docs/release-rc.md`
- DO NOT hand-edit the GitHub Release body post-publish (D-25)

If any maintainer-discovered runbook gap surfaces during the rc.2 cut, that becomes a hotfix PR landed BEFORE tagging (mirroring v1.1 P12 + v1.2 P20 discipline per D-24).

## Sign-Off

- [ ] All pre-flight checklist items ticked
- [ ] Tag cut command run
- [ ] Post-tag verification complete
- [ ] Maintainer signature + date: `__________________`
- [ ] STATE.md updated to reflect rc.2 shipped (separate maintainer follow-up)
```

After writing the document, the maintainer (NOT Claude) executes the pre-flight checklist + tag cut. Claude's role ends at writing the runbook.
  </action>
  <what-built>
    `21-RC2-PREFLIGHT.md` runbook — mirrors P20 RC1-PREFLIGHT precedent with rc.1 → rc.2 / P20 → P21 substitutions; literal tag command per D-23; full pre-flight + post-tag verification + sign-off block; reuses `docs/release-rc.md` verbatim per D-22; explicit "do not modify" list per D-24.
  </what-built>
  <how-to-verify>
    1. Maintainer reads the document and confirms it matches the P20 RC1-PREFLIGHT structure with the locked rc.2 substitutions.
    2. Maintainer ticks every pre-flight checklist item.
    3. Maintainer runs the literal tag command from § Tag Cut Command.
    4. Maintainer pushes the tag and waits for the GitHub Actions release workflow.
    5. Maintainer ticks the post-tag verification items as the workflow completes.
    6. Maintainer signs off.

    If any pre-flight item fails, the maintainer DOES NOT cut the tag — instead opens a hotfix PR per D-24 and re-runs the preflight after the fix lands.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md && grep -c 'autonomous: false' .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md && grep -c 'v1.2.0-rc.2 — FCTX UI panel + exit-code histogram' .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` exits 0
    - Frontmatter has `autonomous: false`: `grep -c "^autonomous: false$" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns 1
    - Tag command literal exact (D-23): `grep -c 'git tag -a -s v1.2.0-rc.2 -m "v1.2.0-rc.2 — FCTX UI panel + exit-code histogram (P21)"' .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns 1
    - Pre-flight checklist references PR merge + green CI + compose-smoke + git-cliff preview: `grep -cE "PR is merged|green CI|compose-smoke|git-cliff" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns >= 4
    - Reuses docs/release-rc.md (D-22): `grep -c "docs/release-rc.md" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns >= 1
    - Explicit "do not modify" list (D-24): `grep -cE "DO NOT modify .github/workflows/release.yml|DO NOT modify cliff.toml|DO NOT modify docs/release-rc.md" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns >= 3
    - GHCR `:latest` gating cited: `grep -c ":latest.*v1.1.0\|tags containing '-'" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns >= 1
    - Sign-off block: `grep -c "Maintainer signature" .planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-RC2-PREFLIGHT.md` returns >= 1
  </acceptance_criteria>
  <resume-signal>Type "rc.2 cut" after the tag is pushed and post-tag verification is complete; describe issues to capture in a hotfix PR before re-attempting.</resume-signal>
  <done>
    21-RC2-PREFLIGHT.md exists with the locked tag command, pre-flight checklist, post-tag verification, and explicit "do not modify" list. Maintainer cuts the tag locally per D-26.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer → tag-cut | Maintainer-only signed git tag; no automation path. |
| Maintainer → GHCR push | The release.yml workflow handles the push on tag (per P12 D-10 gating). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11-01 | Tampering | rc.2 tag content | mitigate | `git tag -a -s` requires the maintainer's signing key (per repo convention); tag verifies provenance. |
| T-21-11-02 | Spoofing | unauthorized rc.2 push | mitigate | GitHub branch protection + tag protection (existing for v1.1 / rc.1) — no additional surface introduced. |
| T-21-11-03 | Information Disclosure | UAT scenarios | accept | UAT scenarios document expected behavior — same exposure as the PR description. |
</threat_model>

<verification>
Both files exist with locked content. Maintainer validates manually per the `<resume-signal>` blocks.
</verification>

<success_criteria>
- [ ] `21-HUMAN-UAT.md` written with 8 scenarios + EXIT-06 verification + sign-off block
- [ ] `21-RC2-PREFLIGHT.md` written with literal tag command + pre-flight checklist + "do not modify" list
- [ ] Both files have `autonomous: false` in frontmatter
- [ ] HUMAN-UAT references all 4 plan-21-10 recipes
- [ ] HUMAN-UAT covers FCTX-01..06 + EXIT-01..06 (incl. EXIT-06 cardinality grep)
- [ ] RC2-PREFLIGHT mirrors P20 RC1-PREFLIGHT structure with rc.2 substitutions
- [ ] Maintainer signs off both documents (Tasks 1 & 2 are checkpoint tasks; resume-signal is the maintainer's confirmation)
</success_criteria>

<rollback>
1. If HUMAN-UAT references a missing recipe → cross-check `just --list` and update the document.
2. If RC2-PREFLIGHT pre-flight item fails (e.g., CI red) — DO NOT cut the tag. Open hotfix PR. Re-run preflight after fix lands.
3. If post-tag GHCR push fails → maintainer follows `docs/release-rc.md` troubleshooting; if a runbook gap surfaces, hotfix PR per D-24 (do NOT modify the runbook in this phase).
4. If `:latest` accidentally got pushed (release.yml gating regression) — emergency fix on `release.yml`, force-update `:latest` back to `1.1.0`. This is a P12 D-10 invariant breach and is a separate incident.
</rollback>

<output>
After both tasks complete (maintainer ticks all boxes + cuts the tag), create `.planning/phases/21-failure-context-ui-panel-exit-code-histogram-card-rc-2/21-11-SUMMARY.md` documenting the rc.2 cut, the GHCR image digest pair, and any maintainer-discovered findings (none expected; document for traceability).
</output>
