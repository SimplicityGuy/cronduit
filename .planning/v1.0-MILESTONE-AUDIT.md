---
milestone: v1.0
audited: 2026-04-14T00:00:00Z
supersedes: 2026-04-12T23:30:00Z  # prior audit was scoped to Phases 1-6 only
status: tech_debt
scores:
  requirements: 85/86 substantively satisfied; 1 partial (OPS-05 verbally approved, checkbox not flipped)
  phases: 9/9 complete on disk (49/49 plans across Phases 1-9)
  integration: 9/9 cross-phase wiring paths confirmed
  flows: 7/7 end-to-end flows complete
  nyquist:
    compliant: 3   # Phase 1, 4, 5
    partial_or_draft: 5   # Phase 2, 3, 6, 7, 8 (VALIDATION.md present but status=draft or nyquist_compliant=false)
    missing: 1   # Phase 9 (no VALIDATION.md)
    overall: gaps
gaps:
  requirements:
    - id: "OPS-05"
      status: "partial"
      phase: "Phase 6 → Phase 8 (gap closure)"
      claimed_by_plans: ["06-07-PLAN.md", "08-01-PLAN.md", "08-02-PLAN.md", "08-04-PLAN.md", "08-05-PLAN.md"]
      completed_by_plans: ["08-05-SUMMARY.md (user verbally approved)"]
      verification_status: "human_needed — Phase 8 policy gate"
      evidence: |
        Phase 8 compose-smoke CI matrix (ci.yml:133-360) proves all four quickstart jobs (echo-timestamp, http-healthcheck,
        disk-usage, hello-world) reach status=success on both docker-compose.yml and docker-compose.secure.yml within 120s
        on a fresh clone. 08-05-SUMMARY.md records user verbal approval closing the walkthrough gate. However, project
        policy (`feedback_uat_user_validates.md`) forbids Claude from flipping `result: pending` fields on UAT files —
        03-HUMAN-UAT.md, 06-HUMAN-UAT.md, 07-UAT.md, 08-HUMAN-UAT.md per-row records remain `[pending]` on disk, and
        REQUIREMENTS.md line 126 still has OPS-05 as `- [ ]`. Substance verified; bookkeeping pending operator decision.
      resolution_paths:
        - "Orchestrator accepts verbal approval as terminal → user flips REQUIREMENTS.md OPS-05 checkbox + UAT result fields in one pass"
        - "Orchestrator requires per-row walkthrough re-run → user re-tests each item and records result: pass (or issue) with re_tested_at annotation"
  integration: []
  flows: []
  orphans: []   # No REQ-IDs present in REQUIREMENTS.md traceability that are absent from all phase VERIFICATIONs
tech_debt:
  - phase: "03-read-only-web-ui-health-endpoint"
    items:
      - "03-HUMAN-UAT.md: Tests 1-4 (UI-05, UI-06, UI-09, UI-12) still `result: pending` on disk despite Phase 8 walkthrough verbal approval"
  - phase: "05-config-reload-random-resolver"
    items:
      - "Visual checkpoint from 05-05-SUMMARY.md (13-item checklist) not individually flipped; covered wholesale by Phase 8 walkthrough but not cross-referenced back"
  - phase: "06-live-events-metrics-retention-release-engineering"
    items:
      - "06-HUMAN-UAT.md: Tests 1-2 (OPS-05, UI-14 SSE live log streaming) still `result: pending` on disk despite Phase 8 walkthrough"
      - "docker-compose.yml `ports: 8080:8080` override accepted by SimplicityGuy 2026-04-13 (OPS-04) — intentional, not a gap; covered in-place by SECURITY comment block + expose: replacement snippet"
  - phase: "07-v1-cleanup-bookkeeping"
    items:
      - "07-UAT.md: Job Detail Run History auto-refresh human-in-browser UAT still `result: pending`; Phase 8 criterion 6 made this observable after quickstart fixes landed but the row was not flipped"
  - phase: "08-v1-final-human-uat-validation"
    items:
      - "08-HUMAN-UAT.md: Final Status table still in `_fill_` placeholder state; `status: pending`"
      - "08-VERIFICATION.md status is `human_needed` because the orchestrator must still decide whether verbal approval counts as terminal (see OPS-05 partial above)"
  - phase: "09-ci-cd-improvements"
    items:
      - "09-HUMAN-UAT.md: three items require live GitHub-hosted runner execution (cleanup-cache workflow on PR close, cleanup-images workflow against real GHCR, second-push cache-restore verification) — unexercisable in static analysis"
      - "Phase 9 has no VALIDATION.md (Nyquist validation never ran); ROADMAP lists `Requirements: TBD` but no REQ-IDs were ever assigned to this phase"
  - phase: "bookkeeping-wide"
    items:
      - "SUMMARY.md `requirements_completed` YAML frontmatter is empty across Phases 1-9 except 07-04-SUMMARY.md (RELOAD-04). VERIFICATIONs explicitly map REQ-IDs so coverage is sound, but the SUMMARY frontmatter cross-check lane is non-functional"
      - "REQUIREMENTS.md lines 265/272 still describe OPS-05 as `Pending` — factual but stale given Phase 8 ran the walkthrough"
nyquist:
  compliant_phases: [01, 04, 05]
  partial_phases: [02, 03, 06, 07, 08]   # VALIDATION.md exists but status=draft and/or nyquist_compliant=false
  missing_phases: [09]
  overall: "gaps — Nyquist compliance never closed out at milestone level; phases 02/03/06/07/08 validation docs exist but are draft, Phase 09 has no validation doc"
---

# v1.0 Milestone Audit

**Milestone:** v1.0 — Single-binary Rust cron scheduler with Docker executor, terminal-green web UI, and multi-arch release engineering.
**Audited:** 2026-04-14
**Prior audit:** 2026-04-12 (scope: Phases 1-6) — superseded by this report, which adds Phases 7/8/9.
**Verdict:** `tech_debt` — all v1 requirements are substantively satisfied and every integration path is confirmed; only bookkeeping and Nyquist coverage debt remains.

## Score Summary

| Dimension | Score | Status |
|---|---|---|
| Requirements | 85/86 substantively satisfied, 1 partial | `tech_debt` |
| Phases | 9/9 complete on disk, 49/49 plans | `complete` |
| Integration | 9/9 wiring paths confirmed | `passed` |
| E2E Flows | 7/7 complete | `passed` |
| Nyquist compliance | 3 compliant / 5 draft / 1 missing | `gaps` |

## 1. Requirements Coverage — 3-Source Cross-Reference

86 v1 requirements mapped across FOUND / CONF / DB / SCHED / RAND / EXEC / DOCKER / RELOAD / UI / OPS categories. Cross-referenced against:

1. **REQUIREMENTS.md traceability table** — 85 `[x]`, 1 `[ ]` (OPS-05)
2. **Phase `VERIFICATION.md` `requirements_satisfied` blocks** — 85/86 explicitly mapped to verification evidence
3. **Phase `SUMMARY.md` `requirements_completed` frontmatter** — largely empty across Phases 1-9 (bookkeeping gap, not a substance gap)

### Satisfied (85)

All `FOUND-*`, `CONF-*`, `DB-*`, `SCHED-*`, `RAND-*`, `EXEC-*`, `DOCKER-*`, `RELOAD-*`, `UI-*`, `OPS-01..04` verified. OPS-04 `ports:` vs `expose:` deviation carries an accepted override in `06-VERIFICATION.md` (Plan 04 D-12, accepted by SimplicityGuy 2026-04-13). RAND-03 re-verification block documents PR #9 closure of the `do_reroll` stub.

### Partial (1)

**OPS-05** — *README quickstart enables a stranger to clone, `docker compose up`, and schedule a working job in under 5 minutes.*

- **Substance:** satisfied. Phase 8 landed alpine:3 runtime rebase, four runnable quickstart jobs, dual docker-compose (default + `docker-compose.secure.yml` with tecnativa/docker-socket-proxy sidecar), docker daemon pre-flight + `cronduit_docker_reachable` gauge. Phase 8 compose-smoke CI matrix (ci.yml:133-360) asserts all four jobs reach `success` within 120s on both compose axes. User ran the walkthrough and verbally approved.
- **Bookkeeping:** not flipped. Per-row `result: pending` fields in `03-HUMAN-UAT.md`, `06-HUMAN-UAT.md`, `07-UAT.md`, `08-HUMAN-UAT.md` were deliberately left alone by Claude per project policy `feedback_uat_user_validates.md`. REQUIREMENTS.md line 126 still reads `- [ ]`.
- **Orchestrator decision needed:** accept verbal approval as terminal (user flips checkboxes in one pass) OR re-run per-row walkthrough with `re_tested_at` annotation.

### Unsatisfied (0)

None.

### Orphans (0)

Every REQ-ID in REQUIREMENTS.md traceability is covered by at least one VERIFICATION.md `requirements_satisfied` or explicit phase mapping.

## 2. Phase Verifications — Status Matrix

| Phase | VERIFICATION.md Status | Score | Overrides | Notes |
|---|---|---|---|---|
| 01 Foundation, Security, Persistence | `passed` | 5/5 | 0 | Re-verified 2026-04-10 after CONF-08/CONF-09/FOUND-03 gap closure (`cronduit check` wiring). |
| 02 Scheduler Core & Exec | `passed` | 6/6 | 0 | Clean pass. |
| 03 Read-Only Web UI | `human_needed` | 15/15 code-complete | 0 | Four visual UI tests pending per-row flip — covered by Phase 8 walkthrough. |
| 04 Docker Executor | `verified` | 6/6 | 0 | `container:<name>` DOCKER-10 integration test passes under live daemon. |
| 05 Reload & `@random` | `code_complete, human_needed` | 10/13 code, 3 resolved via re-verification | 0 | 3 gaps closed by PR #9 + deferral to Phase 8 walkthrough. |
| 06 Live Events, Metrics, Retention, Release | `passed` | 5/5 | 1 | OPS-04 `ports:` override accepted by operator; documented in-place with SECURITY comment + expose: replacement snippet. |
| 07 Cleanup & Bookkeeping | `human_needed` | 5/5 code-complete | 0 | Job Detail auto-refresh browser UAT pending — covered by Phase 8 walkthrough (criterion 6). |
| 08 Final Human UAT Validation | `human_needed` | 10/12 | 0 | Orchestrator policy question: verbal approval vs per-row flip. |
| 09 CI/CD Improvements | `human_needed` | 8/9 | 0 | Three items require live GitHub runner execution. No VALIDATION.md. |

Seven of nine VERIFICATIONs carry `human_needed` markers. None contain code-level unsatisfied claims — all are visual / interactive / live-environment checks that Claude cannot complete on behalf of the operator.

## 3. Cross-Phase Integration — Wiring Paths

(Delta since prior audit. Paths marked `[new]` were added in Phase 7/8; paths marked `[carried]` were confirmed in the 2026-04-12 audit and re-confirmed here against current `main`.)

| Path | REQ-IDs | Status | Evidence |
|---|---|---|---|
| `docker_daemon::preflight_ping` → gauge → `/metrics` `[new]` | DOCKER-01, OPS-02 | ok | `src/cli/run.rs:189` calls `preflight_ping` after `connect_with_defaults()`. `src/telemetry.rs:111-114` describes + eagerly registers `cronduit_docker_reachable`. `/metrics` text output is greppable from boot (verified by `ci.yml:358`). Non-fatal WARN path exists for `None` docker handle. Test: `tests/docker_daemon_preflight.rs`. |
| `GET /partials/jobs/:job_id/runs` (conditional polling) `[new]` | UI-08 | ok | `src/web/mod.rs:59-62` routes to `handlers::job_detail::job_runs_partial` (handler at `job_detail.rs:234`). Template `templates/partials/run_history.html:9-11` emits `hx-get` with `{% if any_running %}hx-trigger="every 2s"{% endif %}` — polling stops when all runs are terminal. Handler computes `any_running` at line 278. Test: `tests/job_detail_partial.rs`. |
| `POST /api/reload` → `HX-Refresh: true` `[new]` | RELOAD-02, RELOAD-04, UI-11 | ok | `src/web/handlers/api.rs:180-181` inserts header on success. Regression test `tests/reload_api.rs::reload_response_includes_hx_refresh_header` asserts header via `tower::ServiceExt::oneshot`. |
| `POST /api/jobs/:id/run` → `HX-Refresh: true` `[new]` | UI-12, SCHED-06 | ok | `api.rs:69-72`. Exercised by compose-smoke CI via Run Now API. Broader than Phase 7 plan required (reload/reroll pattern generalized) but consistent with UI-12 "trigger recorded and visible". |
| `POST /api/jobs/:id/reroll` → conditional `HX-Refresh` `[new]` | RAND-03, UI-13 | ok | `api.rs:264-267` (only on `ReloadStatus::Ok`). |
| `/api/jobs` + `/api/jobs/:id/runs` JSON (CI test hooks) `[new]` | OPS-02 | ok | `web/mod.rs:77-78`; used by `ci.yml:346-355` for deterministic name→id resolution + run-history assertions. |
| config sync → scheduler → executor → log_pipeline → DB → web UI → SSE → metrics → retention `[carried]` | FOUND/CONF/DB/SCHED/EXEC/DOCKER/RELOAD/UI/OPS | ok | No Phase 7/8/9 edits to `src/scheduler/sync.rs`, `log_pipeline.rs`, `db/queries.rs`, SSE handler, or retention loop. Only additive wiring. Original 7/7 path count confirmed in prior audit. |
| docker-compose.yml quickstart E2E `[new — full rewrite]` | OPS-04, OPS-05, DOCKER-01, UI-12 | ok | Socket mount, `DOCKER_GID` group_add, config `:ro`, named volume `cronduit-data:/data`. Dockerfile lines 82-112 rebase to alpine:3, UID 1000, pre-owns `/data`. |
| docker-compose.secure.yml (socket-proxy) `[new]` | OPS-04, DOCKER-01 | ok | Routes bollard via `DOCKER_HOST=tcp://dockerproxy:2375`; `CONTAINERS/IMAGES/POST/DELETE=1` allowlist covers hello-world lifecycle including removal. |

Integration checker verdict: **passed** (no gaps).

## 4. End-to-End Flows

| Flow | Status |
|---|---|
| Startup docker pre-flight → gauge → `/metrics` | complete |
| Quickstart E2E (default docker-compose, 4 jobs) | complete |
| Quickstart E2E (docker-compose.secure.yml, socket-proxy) | complete |
| Settings reload → auto-refresh | complete |
| Job Detail live run list (conditional polling) | complete |
| Reroll flow | complete |
| Run Now flow | complete |

## 5. Nyquist Compliance

Per `workflow.nyquist_validation` discovery scan of `*-VALIDATION.md` frontmatter:

| Phase | VALIDATION.md | `nyquist_compliant` | `wave_0_complete` | `status` | Action |
|---|---|---|---|---|---|
| 01 | exists | true | true | partial | — |
| 02 | exists | false | false | draft | `/gsd-validate-phase 02` |
| 03 | exists | false | false | draft | `/gsd-validate-phase 03` |
| 04 | exists | true | true | complete | — |
| 05 | exists | true | true | draft | `/gsd-validate-phase 05` (flip to complete) |
| 06 | exists | false | false | draft | `/gsd-validate-phase 06` |
| 07 | exists | false | false | draft | `/gsd-validate-phase 07` |
| 08 | exists | false | false | draft | `/gsd-validate-phase 08` |
| 09 | **missing** | — | — | — | `/gsd-validate-phase 09` |

This is a WARNING, not a blocker for v1.0 archive. Nyquist validation is a verification confidence-lane that was wired in mid-milestone; most phases predate its enforcement. Worth flipping before archive but not a shipping gate.

## 6. Tech Debt Summary

**Human-facing UAT bookkeeping** — 5 UAT files with `result: pending` rows that Phase 8 walkthrough covered verbally:

- `03-HUMAN-UAT.md` (UI-05, UI-06, UI-09, UI-12)
- `06-HUMAN-UAT.md` (OPS-05, UI-14)
- `07-UAT.md` (Tests 2 and 3 — docker daemon reachability, Run Now auto-refresh)
- `08-HUMAN-UAT.md` (Final Status table `_fill_` placeholders)
- `09-HUMAN-UAT.md` (cleanup-cache, cleanup-images, second-push cache restore — these are legitimately unexercisable in static analysis and need a live PR to test)

**REQUIREMENTS.md bookkeeping:**

- OPS-05 checkbox still `[ ]`
- Lines 265/272 describe OPS-05 as "Pending" — stale

**SUMMARY frontmatter bookkeeping:**

- `requirements_completed` YAML field is empty across nearly all SUMMARYs. Non-blocking; VERIFICATIONs are the authoritative source.

**Nyquist compliance:**

- Phase 9 has no VALIDATION.md
- Phases 2/3/6/7/8 have draft VALIDATION.md with `nyquist_compliant: false` or `draft` status

**Phase 9 requirement mapping:**

- Phase 9 ROADMAP entry says `Requirements: TBD (will be enumerated at plan time)` — never enumerated. Not a blocker because Phase 9 is ops-only (no runtime code changes; ci.yml / workflows / scripts only) and grep-clean against `src/`, `Cargo.toml`, `migrations/`, `tests/`, but the REQ mapping row should either be backfilled with synthetic CI-* IDs or explicitly marked "n/a — operational hygiene phase".

---

## Verdict Routing

Because the only requirement gap is a bookkeeping/policy decision on OPS-05 (substance verified, checkboxes not flipped by policy) and all cross-phase wiring is confirmed, this audit does not produce any code-level gaps requiring `/gsd-plan-milestone-gaps`. The appropriate next step is an operator decision on:

1. **Accept verbal approval as terminal** → user flips REQUIREMENTS.md OPS-05 checkbox + UAT `result: pending` rows in a single pass, optionally backfills Nyquist for the flagged phases, then runs `/gsd-complete-milestone v1.0`.
2. **Require per-row re-test** → schedule a short UAT pass with `re_tested_at` annotations, then complete milestone.
3. **Accept debt, ship anyway** → run `/gsd-complete-milestone v1.0` and log the deferred items in the next milestone's backlog.

---

*Audited by: `/gsd-audit-milestone` orchestrator + `gsd-integration-checker` (model: sonnet)*
*Prior audit (2026-04-12) superseded — this run includes Phases 7, 8, 9 and re-validates prior integration paths against current `main`.*
